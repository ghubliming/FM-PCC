# FROM v2 → v3 · 2026-09-24 · v2.28 is ready to sync (first aggregated release)

**v2.28** applies every open → v2 row (v3.73, v3.78, v3.79, v3.90, v3.96, v4.0, v4.1), each re-checked against the
current text first. v3 inherits v2.27 (`inherited/SYNC_STATE.json`); the author is preparing the first aggregated
release, so please sync with `python3 tools/sync_v2.py merge` when asked. Changelog:
`v2/changelogs/v2.28_20260924_cross_notes_v3.99_v4.1.md`.

## What changes in v3's inherited files

| file | change |
| :-- | :-- |
| `parts/01_frontmatter.tex` (abstract) | the last two result sentences are three, one per step of the study (avoiding: the average-velocity models lead, CI-MeanFM dominates, MeanFM gives up little; aligning: MeanFM dominates, endpoint projection the better projector on the constraints; quadrotor: projection carries over, FM enough on simple data, the controller decides). No numbers. |
| `chapters/01_introduction.tex` | §1.4 items 4–6 result sentences rebuilt on v3.99's conclusions; item 6 names UAV-pillars "D3IL-avoiding flown by the quadrotor"; the outline follows v4.1 (Conclusion, then Discussion). |
| `chapters/02_background.tex` | "predictive sampling in MuJoCo MPC" (was `\ac{MJPC}`). |
| `chapters/03_related_work.tex` | §3.4: HardFlow follows the setup of DPCC, counts DPCC's per-step projection among its baselines, reports the steps to the target on its trials without collision (Sec. VII-A, Table II). |
| `chapters/04_method.tex` | "MuJoCo MPC" in §4.3.3, `tab:embodiments`, §4.3.5, §4.3.6; `\ac{PD}` at the first "PD"; §4.6.3 "a collision with a pillar … as a collision of the rod with a post does in D3IL-avoiding" (the D3IL side verified: `avoiding.py` `check_failure` → `done`). |
| `parts/99_backmatter.tex` | `\acro{MJPC}` removed, `\acro{PD}` added. |

**Merge `02_background`, `04_method` and `99_backmatter` in the same sync**: an inherited `\ac{MJPC}` without its
`\acro` prints the bare key with a warning. **No label was added, removed or renamed.** Ch 5–6 already write
"MuJoCo MPC" — nothing to change there.

## One thing v3 may want to know

The v3.78 note said §1.4's "the evaluated avoiding comparisons reduce planning time" was exact. Since R36 (24-09)
`tab:avoiding-projectors` has FM at $K=3$, where endpoint projection costs more (71.5 against 64.1 ms), so that
sentence is gone from v2; §1.4 now says endpoint projection is cheaper *for the two average-velocity models*.

Claude (Opus 5.5, Claude Code), v2 · 2026-09-24 · no v3 file touched.
