# TO v3 — v4.1: the appendix changed its index; nine Chapter 5/6 sentences point at what moved or went (2026-09-24)

**From v4.1** (`v4/changelogs/v4.1_20260924_conclusion_ch7_discussion_ch8.md`, §5). The author restructured
the appendix (points 9–11 of the v4.1 instruction). **Notification only — nothing of v3's was edited**
(author: "notify is enough, you don't write the v3 chapters"). Every reference below still *resolves*
in the v4 build, because v4 keeps alias labels; what it prints is no longer what the sentence says.

## The appendix now

| letter | chapter | sections |
| :-- | :-- | :-- |
| **A** | Supplementary Material (was *Derivations*; labels `app:derivations`, `app:supplementary`) | A.1 Training-Time Sampling Laws (`app:training-time-laws`, unchanged) · **A.2 Quadrotor Dimensions** (`app:quadrotor-dimensions`) with `fig:platform-x2-dimensions`, moved here from Reproducibility |
| **B** | Extended Results (`app:results`) | B.1 D3IL-avoiding executed paths · B.2 D3IL-aligning executed paths · B.3 UAV-corridor flights along the corridor · B.4 UAV-corridor under every selection rule · B.5 D3IL-avoiding at twenty episodes per seed (`app:avoiding-twenty`; alias `app:avoiding-twenty-episode`) |
| **C** | Compute Environment (was *Reproducibility*; labels `app:repro`, `app:compute`) | the paragraph and `tab:compute` (Table C.1) only |

**Archived (out of the thesis):** D3IL-avoiding at every evaluated budget (`app:avoiding-budgets`,
`tab:app:avoiding-raw-full`, `tab:app:avoiding-dpcc-full`), the single-candidate endpoint ladder,
UAV-s-curve under every selection rule (`app:uav-scurve-rules`, `tab:app:uav-scurve-rules`), UAV-s-curve
at the budgets on disk (`app:uav-scurve-budgets`) — `v4/withheld/20260924_v4.1_archive/`.

## Sentences of v3's chapters that now say something the appendix no longer holds

| file:line | what it says | what is true now |
| :-- | :-- | :-- |
| `05_setup.tex:54` | "the X2 dimensions used in projection are drawn in `fig:platform-x2-dimensions` in `app:repro`" | the figure is in `app:supplementary` (A.2); `app:repro` prints "Appendix C", the compute environment |
| `05_setup.tex:79` (Fig 5.1 caption) | "…drawn in `fig:platform-x2-dimensions`, in `app:repro`" | same |
| `05_setup.tex:87` (Table 5.1 caption) | "(`fig:platform-x2-dimensions` in the appendix)" | still true (any appendix) |
| `06_results.tex:107` (Table 6.1 caption) | "The budgets $\nfe=5$ and $10$ are in `tab:app:avoiding-raw-full`" | that table is archived; the alias prints "Appendix B" |
| `06_results.tex:144` | "the intermediate budgets in `tab:app:avoiding-raw-full` hold it between 14.2 and 19.1 …" | same; the numbers have no printed source any more |
| `06_results.tex:335` | "…$1.000$ in $68.7$ control steps at $321.7$ ms (`tab:app:avoiding-dpcc-full`)" | archived |
| `06_results.tex:367` (Table 6.2 caption) | "The budgets between two and twenty are in `tab:app:avoiding-dpcc-full`" | archived |
| `06_results.tex:760` | "every evaluated budget under every selection rule (`app:avoiding-budgets`)" | archived |
| `06_results.tex:2058` (a `\dataref`) | "the budgets flown before it (MeanFM 10, CI-MeanFM 5) are in `app:uav-scurve-budgets`" | archived |
| `06_results.tex:2142` (Table 6.15 caption), `:2159` (`\dataref`), `:2172` | "every rule in `app:uav-scurve-rules`" / "Under every selection rule (…) the selected configuration crosses on five flights of ten" | archived; the fact (5/10 under every rule) can stay as text without the reference |

Plus the two items of the v4.0 note (the twenty-episode references; the `app:repro` pointer at
`05_setup.tex:1091–1092`, whose target is now `sec:disc:practice`).

When these are rewritten, tell v4 and the alias block on the Extended Results heading
(`v4/chapters/09_appendix.tex`, commented) goes.

Claude (Fable 5.1, Claude Code), v4 · 2026-09-24.
