# v2.27 → v3: audit corrections for the next authorized sync

**Author:** ChatGPT (Codex) · **Date:** 2026-09-23.

V2.27 applies the agreed audit TODOs to the abstract and Chapters 1–4. The author closed the noise-scale discussion as a v2 scope decision; no further reviewer approval is required. V3 files and inheritance state were not changed. This note records the implications for the next authorized sync, not a request for new experiments.

- **Claims:** the abstract now names instantaneous-velocity and consistency-interpolated average-velocity matching on avoiding. Contributions use the available primary avoiding, visual and pillars evidence in v3.75. They make no completed-result claim for the pending corridor/s-curve comparisons.
- **Average velocity:** the definition and every linked identity/target now consistently use `(x_r, r, h)`, with `h = τ − r`, plus-sign identity and JVP tangent `(v,+1,−1)`. The one-step learned endpoint is marked as an approximation of the exact flow.
- **Consistency-interpolated loss:** explicit branch weight, zero-interval target override and zero-ratio JVP branch; the small-positive-ratio target limit is distinguished from scaled-gradient correspondence.
- **Sampling/projection:** K counts sampling steps. Endpoint NFE is K + n_guide. Endpoint transport and lookahead both query the average head at h = 0; the ordinary interval sampler queries h = 1/K. Diffusion and flow activation conventions/counts are distinct.
- **One guiding step:** both the table and reporting prose now treat it as a valid single-guidance configuration. Zero guiding steps describe terminal projection. The previous blanket exclusion of one-step guidance has been removed; check consistency with Chapter 5's reporting rule at the next sync.
- **Controller and selection:** z-up plant signs, desired versus actual thrust axes, final actuator clipping, fixed dataset timing, and the predictive-controller exception are corrected. Temporal consistency uses the H−1 overlapping unnormalised observation blocks, with first-candidate fallback.
- **Detailed numerical noise discussion stays out of v2.** The generic background is distinguished from implementation, with settings referenced to Chapter 5. The engine-specific correction in v3.73's note agrees with the audit evidence; do not revive the universal “endpoint σ = 1” statement.

## Labels and inherited material

Removed: `tab:hardflow-setup` (redundant numerical setup table). No reference to it was found in v3's owned Chapters 5, 6, 8 or appendix at this pass.

Added: `eq:method:engine:afloss`, `eq:method:hf:nfe`, `tab:method-provenance`.

Preserved: all other labels, including `eq:method:engine:fm`, now an alias on the existing FM loss rather than a duplicate displayed equation. The reproduced figure is unchanged; its citation now includes page 22 of arXiv v3.

The existing v2.26 quadrotor-scene section is byte-identical. The separate v3.73 question about the claimed manipulator post-contact termination is still open; it was not part of the agreed audit edits. V2's unowned Chapter 5–8/appendix material is also unchanged and must not replace v3's live text.

Validation: 163 labels, no duplicates or unresolved references; all 53 bibliography entries cited and all citations resolved; balanced braces/environments; no owned-text holes. Not compiled.

**Signed:** ChatGPT (Codex).
