# TO v3 — v4.0: three references to re-point, one sentence to rewrite, one pointer to move (2026-09-24)

**From v4.0** (`v4/changelogs/v4.0_20260924_init_ch7_ch8_appendix.md`). v4 started today on the v3.98
handover; nothing under `v3/` was touched. Three things in v3-owned files now point at appendix
content v4 changed.

1. **`app:avoiding-twenty-episode` is dropped; `app:avoiding-twenty` replaces it.** The retired dead
   block (v3.67) is out of v4's appendix (text in `v4/withheld/20260924_v4.0_archive/`), as the v3.98
   handover README lists under "open decisions". v4 keeps `\label{app:avoiding-twenty-episode}` as an
   alias right after `\input{chapters/app_ntrial20_feasible}`, so the three references resolve to the
   twenty-episode section today:
   - `05_setup.tex:1338` "The separate twenty-episode evaluation is reported in …" — reads correctly; re-point to `app:avoiding-twenty` when convenient.
   - `06_results.tex:761` "… and the evaluation at twenty episodes per seed (…)" — same.
   - `05_setup.tex:917–922` — **now false**: "A longer evaluation of the same configuration, at twenty
     episodes per seed, exists and is *not* used here … It is kept for the record in
     `app:avoiding-twenty-episode`." The baseline's twenty-episode run is no longer in the appendix
     (the ntrial20 section has no diffusion cell, by its own guard). Suggested: "A twenty-episode
     evaluation of the two flow-based operating points exists and agrees with this protocol
     (\autoref{app:avoiding-twenty}); the baseline's own twenty-episode run did not complete and is not
     used." — the author's wording.
   When all three point at `app:avoiding-twenty`, tell v4 and the alias label goes.
2. **`05_setup.tex:1091–1092`**: "The measurements of \autoref{sec:setup:metrics} were taken on this
   machine; how its properties bear on reading them is given in \autoref{app:repro}." The appendix no
   longer reads the timing (author, 2026-09-24: Reproducibility states the environment, nothing else);
   that reading is now Ch 8 §*Towards Deployment*, `\autoref{sec:disc:practice}` (what the time per
   action contains and what the execution adds; **amended at v4.1** — the v4.0 target `sec:disc:threats`
   no longer exists). Re-point or drop the second half of the sentence.
3. **FYI:** `tab:names` and `tab:corpora` are out of the thesis (no Ch 5–6 reference existed);
   `tab:compute` and `app:repro` stay. The corridor-side section (`app:uav-corridor-side`), which sat
   inside the dead block's `\begin{deadblock}…\end{deadblock}` in v3.98 by accident (lines 290–292),
   is a normal section in v4. Ch 7–8 write **"MuJoCo MPC"** as Ch 5–6 do (Ch 2/4 use `\ac{MJPC}`; see
   `v4/notes/AUDIT_20260924_abbreviations_v2_v3.md`).

Claude (Fable 5.1, Claude Code), v4 · 2026-09-24.
