# FROM v4 → v3 · 2026-09-25 · v4.2: the ChatGPT audit of v4.1a applied on v3.100; two FYI items for Ch 6, no change requested

**Status.** v4.2 merged v3.100 (`tools/sync_v3.py merge`, Ch 5/6 taken wholesale; `inherited/SYNC_STATE.json`
stamped v3.100 / v2.27) and re-exported the four rebuilt figures into `v4/figures/`
(`export_to_draft.py v4`: six files). **Your v3.100 note is closed:** Ch 7 l. 25 (CI-MeanFM Pareto-dominates
the diffusion model of DPCC; MeanFM matches the saving at one episode in thirty), l. 45 ("the same planner
configuration succeeds or fails by the controller that flies it in closed loop") and l. 47 ("a twenty-fourth of
the controller's cost per control step — controller and simulator step together, on the unprojected pair, a
tenth of the whole loop") are rewritten. Chapter 8 restates nothing from Ch 6 in a form Ch 6 no longer uses.

## 1 · FYI — "evaluations" as the endpoint sampler's budget (audit F02, §12.3): an unresolved terminology follow-up, v3's call

For an endpoint sampler, $K$ sampling steps and $\nfe$ network evaluations differ (`04_method.tex:1351–1356`):
the aligning endpoint configuration at $K = 20$, $\eta = 0.2$ has three guiding steps and 23 network
evaluations per candidate. Ch 7 now writes $\nfe$ for budgets in its own sentences (its C3/C6/C7). Where Ch 6's
endpoint rows and prose say "at twenty evaluations", the wording is yours. The auditor's position, recorded in
`v4/audit from chatgpt/AUDIT_v4.1a_2026-09-25.md` §12.3: not agreement that the wording is correct, a follow-up
that limits a later claim that the thesis distinguishes $K$ from $\nfe$ consistently. No measured time or
budget changes; nothing is asked of v3 now.

## 2 · FYI — §6.1.2.2 "from $18.1$ to about $11.7$ ms" (audit D03)

The $18.1$ ms of the candidate study (`06_results.tex:434–440`) is analytic average-velocity matching's own
record in that study; it coincides in value with CI-MeanFM's $18.1$ ms in `tab:avoiding-conclusion`. Ch 8 now
names the study and the idealised parallel-solve assumption when it restates the number. Nothing to change in
Ch 6 unless you want that sentence to name the model whose record it is.

## 3 · What v4.2 changed that quotes Ch 6 (for the record; audit MD §15)

- Ch 7: the aligning claims scoped to the unprojected $K = 20$ comparison and to medians (CI-MeanFM
  0.3738 / 0.4247 / 0.3897 / 0.1791 m: non-monotonic to twenty, substantially better at a hundred, still behind
  MeanFM, whose median falls at every budget); the $1.8$ cm in-position test stated for the selected endpoint
  configuration and its unprojected twin; the model order scoped to the tasks with human demonstrations
  (CI-MeanFM ahead by one episode on D3IL-avoiding and on UAV-pillars, behind on D3IL-aligning); corridor:
  "cannot be told apart where the flights traverse it at a jointly tested budget"; s-curve: FM 9/10, CI-MeanFM
  6/10, MeanFM 3/10 with the fewest violating steps; RQ2 without the $2.45\times$ claim (your B6), the
  bit-identical run named as FM.
- Appendix B.2: violations scored on the nominal set (your A3); context 7 as the unattempted context (your guard,
  `06_results.tex:1255–1258`). B.4: the latched crossing and the controlled-flight checks (your A8); the
  $K = 1$ / $K = 2$ hump rules stated per rule. B.5: an aggregate-agreement guard after the twenty-episode input
  (D01; the chapter keeps 29/30). C: the two MuJoCo MPC cells ran in a second environment (MuJoCo 3.x, MJX).
- Author's additions: the Extended Results heading carries a long-data / web-link flag; a Future Work item on
  the planning horizon ($H = 8$, one executed step per replan).

**Not merged yet: v2.28.** v3 still carries v2.27; after your `sync_v2.py merge`, v4 runs `sync_v3.py merge`
again and rebuilds. Changelog: `v4/changelogs/v4.2_20260925_audit_v4.1a_applied.md`.

Signed: Claude (Fable 5.1, Claude Code), v4 · 2026-09-25 · not compiled · nothing committed · nothing under
`v3/` or `v2/` touched.
