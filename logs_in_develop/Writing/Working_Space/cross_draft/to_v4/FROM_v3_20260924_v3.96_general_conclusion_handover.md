# TO v4 — v3.96: the author's general conclusion for Ch 8, and the state of Ch 5–6 as v3 hands over

**2026-09-24 · from v3.** Read with `Working_Space/GUIDE_20260924_results_storyline_author.md`, which now has a
"general conclusion" section, and `INSIGHTS_20260924_results_per_environment.md`. Where earlier v3 notes to v4 differ,
this note wins.

## The general conclusion (author, verbatim)

> "in all the environments with expert data, like imitation learning, the MeanFlow family wins; for hard tasks the HF is
> helping; the UAV scenes reveal that it needs expert data, and when the data is simple we don't need MeanFlow models, FM
> is enough, all the models are equivalent as expected; and only in hard tasks like avoiding/pillars and aligning model +
> projection matters; and the controller matters, in s-curve and UAV-pillars."

§6.4.3 now says this in thesis wording, each part held to its data.

## Corrections to earlier notes

- **UAV-corridor RQ2 is a trade-off, with no clear dominance** (author, v3.96). Endpoint projection over the hump at K20
  with one candidate has fewer violations at less time, but its flights are ~30 steps longer. Under the tilt at K20 it
  costs more time. The v3.88 and v3.90 wording "endpoint wins/beats over the hump" is withdrawn.
- **D3IL-aligning is not "box moved in 8/10":** 0/10 contexts in position, best 2.2 and 4.7 cm (v3.95).
- **UAV-s-curve:** "the controller matters", and our cascaded geometric controller is the better one. MuJoCo MPC is not
  (v3.93).

## State of Ch 5–6 at v3.96

- Every data cell is in; the final data check found nothing missing (v3.92).
- **Each scene has its own conclusion:** §6.1.4, §6.2.4, §6.3.1.3, §6.3.2.4, §6.3.3.4.
- **§6.4 is key points, not tables:** per environment for models and for projection; three cross-cutting findings as a
  list; the supported combinations as key points.
- **Headings are academic** ("…: Pareto dominance over the baseline", "…: projection in three dimensions and the role of
  the demonstrations", "…: the role of the tracking controller").
- **Open, the author's call, no data needed:**
  - dropping the retired twenty-episode appendix block (`app:avoiding-twenty-episode`), now replaced by
    `app:avoiding-twenty`, and re-pointing its three references;
  - the optional runs of the ledger (R43, R45b, the s-curve setpoint-bound projection, D10c).
- **Bundles** are now two variants per build: annotated `_new` and clean `_full_clean` (v3.94).

## Addendum v3.97 — the thesis in three steps (author)

> "After all the main claim and test is ending at beat DPCC/HF on the avoiding benchmark, then extended on the V_A with
> more complex task and reaffirm it, then the UAV is majorly the extended controller transfer test."

§6.4.3 now opens with it.
1. **The main claim on D3IL-avoiding:** the average-velocity models Pareto-dominate DPCC. No endpoint-projection
   configuration improves on the K1 per-step operating point, since it needs K ≥ 3. This is "beat HF" in this thesis's
   own evaluation.
2. **D3IL-aligning** extends and reaffirms it.
3. **The UAV scenes** are mainly a transfer test (plant, controller, demonstrations).

Also v3.97: the UAV-pillars text now says the K1/K2 cells are the Pareto-optimal points of D3IL-avoiding, flown again to
see what the plant changes. No FM or diffusion pillars run (author).
