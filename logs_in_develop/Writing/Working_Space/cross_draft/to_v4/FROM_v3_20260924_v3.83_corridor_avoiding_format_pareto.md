# TO v4 — v3.83: UAV-corridor in the format of D3IL-avoiding; "FM is enough" is withdrawn

**2026-09-24 · from v3.83.** Supersedes the model sentence of
[`FROM_v3_20260924_v3.82_corridor_end_of_corridor_line.md`](FROM_v3_20260924_v3.82_corridor_end_of_corridor_line.md)
("on UAV-corridor instantaneous-velocity matching is enough"). Everything else in that note stands.

- **Format.** The corridor now reads like D3IL-avoiding:
  - rates over the ten flights with their spread;
  - violating steps and steps to the finish line in the tables;
  - every selection rule in a new appendix section (`app:uav-corridor-rules`);
  - a Pareto figure before and after projection (`fig:uav-corridor-tradeoff`): control steps against time per action,
    with the avoiding band rule.
- **Before projection.** FM K1 (cheapest, 250.9 steps at 9.1 ms) and CI-MeanFM K1 (shortest, 242.4 at 9.3 ms) are
  non-dominated. CI-MeanFM K1 dominates MeanFM K1. The baseline takes the same 242 steps at 19× the time.
- **After projection, at the near-satisfaction bar (≥ 95 %).**
  - The frontier belongs to the flow models, and its cheap end to the average-velocity objectives.
  - Tilt, K1: MeanFM dominates FM, 253.2 vs 259.8 steps at 27.4 vs 27.6 ms. The margin is below FM's ±9.2 spread.
  - Hump, K3 endpoint: CI-MeanFM dominates FM, 284.6 vs 299.3 at 71.9 ms each. The margin is 3× the larger spread.
  - FM K3 per-step has the fewest steps at a higher cost.
  - The baseline is dominated (more steps, 9–24× the time).
  - At a 97 % bar the hump result holds, and the tilt frontier is FM K3 per-step alone.
- **Strict S&C.**
  - The baseline is alone in the band: tilt 0.8, hump 1.0.
  - The only flow configuration with violation-free successes is FM K20 endpoint over the hump, 0.8 at 291 ms.
- **For Ch 8.** On the corridor the average-velocity models Pareto-dominate FM at the cheapest budget on the avoiding
  frontier (steps and time at equal near-satisfaction). They tie in time alone. The strict count does not separate
  them. Report it with the bar and the spreads.
