# FROM v3 → v4 · 2026-09-25 · v3.100: the ChatGPT audit of v3.98 applied in Ch 5/6 — three Ch 7 sentences to mirror, and four figure copies to refresh

**Author's decision (2026-09-25):** the agreed change list (§13 of
`v3/audit from chatgpt/AUDIT_v3.98_2026-09-24.md`) is applied in v3.100; F02 takes the CI-MeanFM lead, F03 the
rename. Changelog: `v3/changelogs/v3.100_20260925_audit_v3.98_applied.md`. Sync as usual
(`python3 tools/sync_v3.py merge`); v3.100 still carries v2.27.

## 1 · Chapter 7 restates three claims Ch 6 no longer makes in that form (v4's call, v4's wording)

| `07_conclusion.tex` | now says | Ch 6 (v3.100) says | why |
| :-- | :-- | :-- | :-- |
| l. 25 | "the average-velocity models Pareto-dominate its diffusion model with its …" | **CI-MeanFM Pareto-dominates** the diffusion model of DPCC with its own temporal U-Net (1.000, 59.2 steps, 18.1 ms against 1.000, 70.1, 553.4); **MeanFM matches the saving at one episode in thirty** (0.967, 58.6, 18.3) — a trade-off, not a dominance; FM at one evaluation dominates too (1.000, 67.0, 17.3) | audit F02: the table prints 29/30 for MeanFM; under the thesis' own rule ("good" = Pareto-dominant at equal success, else trade-off) the plural is not supported. D01 (the seed-7 anomaly) can only change the row later, by data. |
| l. 45 | "the same plans succeed or fail by the controller that flies them" | "the same planner configuration succeeds or fails by the controller that flies it" (the evaluation re-plans from the measured state every control step, so the plans under the two controllers are different plans) | audit F06; Ch 6 §6.3.3 and the Table 6.15 title/caption now say "the same planner configuration … in closed loop" |
| l. 47 | "at a twenty-fourth of the cost" | "at a twenty-fourth of the **controller's** cost per control step" (the whole loop is 9.7× unprojected, 2.2× projected; 24.1× is the controller-plus-simulator residual on the unprojected pair) | audit F07; §6.4's s-curve item now reads so |

Chapter 8 needs nothing: its "select the same plan" (l. 88, 119) is the single-candidate case and is right; it has
no dominance sentence. The abstract (v2) already names FM and CI-MeanFM for the reliability claim.

## 2 · Figure copies in `v4/figures/` are stale — the release prefers v4's copies

`RELEASE/tools/make_release.py:584` looks in `v4/figures` first. The store changed today (audit F03 and the
author's halfspace request; `Data_Analysis/DA_in_Paper/analysis/DA_20260925_aligning_frontier_label_halfspace_push.md`):

| figure | what changed | data |
| :-- | :-- | :-- |
| `fig_aligning_tradeoff`, `fig_aligning_projected_tradeoff` | the zero-line label "box not moved" → "0 % reference" (the printed percentage is 100·(1 − median final / mean initial), not a median of per-context shares; an unmoved box prints −1 %) | every plotted value unchanged |
| `fig_expert_aligning` | subtitle "… 106 direct pushes cross the keep-out region, **none the halfspace**" (the push is now tested against both constraints) | unchanged drawing |
| `fig_uav_corridor_paths` | legend vocabulary to the v3.79 rule: *violation-free flight*, *violated a constraint*, "12/12 violation-free" (the builder had it, the stored copy did not); only if v4's appendix references it | unchanged |

**Please run** `python3.14 Data_Analysis/DA_in_Paper/plotting/export_to_draft.py v4` (v3 did not write into
`v4/figures/`; `sync_v3.py` does not carry figures).

## 3 · FYI — what else v3.100 changed that Ch 7/8 might quote

Ch 5: violations are scored on the nominal set (the projector alone sees the tightened one); the alignment
percentage is defined as the reduction of the median relative to the mean initial distance; aligning's
tightening is 0.03 m (avoiding and the quadrotor scenes 0.025 m); UAV-pillars rows scoped (planar avoiding
planner, 1 s steps, five seeds, no rotor-reach band); the s-curve reference route is feasible but the learned
plans cut the corner; "endpoint and per-step compute the same plan at K=2" holds for FM only (the 2026-08-24
parity DA: bit-identical on FM, different on MeanFM); the straight push of the alignment demonstrations crosses
the keep-out region in 106/120 contexts and the halfspace in none, the learned plans enter both (7 and 1 of
ten contexts). Ch 6: "the same plans" → "the same planner configuration" (s-curve and pillars); a flight is
latched at the finish line and ends at the goal point or the cap; K=3 endpoint: five of six points dominated,
MeanFM's not; corridor band-dominance labelled; "to within 1.6" restricted to traversing cells; guards with
editorial/provenance text cleaned. Full list: §13 of the audit MD; implementation record: §14.

Signed: Claude (Fable 5.1, Claude Code), v3 · 2026-09-25 · not compiled · nothing committed · nothing under
`v4/` touched.
