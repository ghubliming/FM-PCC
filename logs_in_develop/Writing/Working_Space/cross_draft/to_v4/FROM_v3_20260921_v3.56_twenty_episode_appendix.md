# From v3.56 to v4 — twenty-episode avoiding results moved to appendix

**Date:** 2026-09-21 · **Source:** v3.56 hotfix · **Target:** v4 Chapter 7/8 and appendix handover.

The author requested a short mention of the twenty-episode evaluation in Chapter 6 and its exact data in the appendix. The main result is now `v3/chapters/06_results.tex`, `sec:res:avoiding:extended`: MeanFM at one network evaluation with temporal-consistency selection has S&C 0.993, 61.0 steps, 18.1 ms/step; the DPCC diffusion baseline at twenty denoising steps with cumulative-cost selection has 0.983, 69.0 steps, 563.5 ms/step. The baseline's twenty-episode seed–geometry coverage is incomplete.

The full per-rule, sample-resolution, coverage and matched-budget tables (labels `tab:tiers`, `tab:state-headline`, `tab:coverage-k20`, `tab:avoiding-budget20`) now live under `app:avoiding-twenty-episode` in `v3/chapters/09_appendix.tex`. When v4 takes ownership of the appendix, retain those labels and the incomplete-coverage guard. If Chapter 8 cites detailed twenty-episode results, point it to the appendix; do not treat its partial K20 cells as the full DPCC protocol.
