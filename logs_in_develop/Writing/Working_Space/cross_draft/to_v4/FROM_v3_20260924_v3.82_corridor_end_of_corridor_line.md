# TO v4 — v3.82: UAV-corridor is now read at the end of the corridor; the corridor facts of the v3.80 note are superseded

**2026-09-24 · from v3.82.** v3 did not edit `08_conclusion.tex` or §6.4 (🔒). This note replaces §1 and §2 of
[`FROM_v3_20260924_v3.80_corridor_scurve_aligning_facts.md`](FROM_v3_20260924_v3.80_corridor_scurve_aligning_facts.md)
for the corridor. Its s-curve, aligning and avoiding facts (§3) still hold.

## What changed and why

- **The finish line.** The corridor's finish line is now **the end of the corridor, x = 2.0 m**, where the walls and both
  test-time constraints end. The evaluator scores it itself (`SCENE_CLEAR_LINE_X`); the flights are unchanged.
- **Why.** The episode budget of 396 control steps came from the level corridor's demonstrations. 41 of the baseline's
  60 projected flights had passed x = 2.0 and were still flying toward the goal point, 0.8 m further on, when the budget
  ended them. Counts at the old line are printed beside the new ones (DA
  `DA_20260924_corridor_v3_R45a_clear_line.md`).
- **The near-satisfaction bar.** A second frontier reading, for the corridor only: a flight counts if it succeeds with
  at least 95 % of its control steps violation-free. The tables stay strict.

## The corridor now (Ch 6 §6.3.2, §6.3.5)

- **Tilt, strict:**
  - Only the baseline succeeds violation-free: per-step c, 8/10 at 666 ms per action (r and t 3/10).
  - Every flow-based flight reaches the end of the corridor, and every one violates the leaned plane on a few steps
    at its end (the hand-over residue). No flow configuration has any S&C.
- **Hump, strict — a trade-off:**
  - FM K = 20, endpoint single: 8/10 at 291 ms.
  - Baseline, per-step r: 10/10 at 654 ms.
  - Per-step flow at K ≤ 2 stalls before the apex.
- **Near-satisfaction bar (≥ 95 %):**
  - Every projected configuration reaches 10/10, except per-step at K ≤ 2 over the hump.
  - The frontier is the cheapest configuration: tilt K = 1 per-step (27.4–27.9 ms), hump K = 3 endpoint (71.9–72.1 ms).
    That is 24× and 9× cheaper than the baseline at the same 10/10.
  - At 97 % the tilt's K ≤ 2 cells fall to ≤ 4/10. Below ~87 % unprojected hump flights would pass.
- **The three flow models tie** in every reading (strict, 95 %, 97 % within two flights of ten, violating steps). Where
  their cost differs, FM is the cheaper. So: **on UAV-corridor instantaneous-velocity matching is enough; the
  average-velocity objectives add nothing the scene can measure.** The author asked whether a 0.90–0.95 bar lets
  MeanFM/CI-MeanFM dominate: it does not.

## Ch 8 sentences this affects (v3's 08_conclusion.tex, yours to rewrite)

l. 33–35 · l. 47–49 · l. 55–58 · l. 61–64. Each still quotes the archived v2 corridor, and none of it holds. In
particular:
- Nothing makes the average-velocity models ahead on the corridor.
- Per-step projection is not "selected" there.
- Endpoint projection at K = 20 is the only flow projector with violation-free successes over the hump.

**§6.4 (🔒):** its corridor rows quote the v2 corridor, and its "collision-free" wording is outdated. This is the
author's call.
