# FROM v3 → v4 · 2026-09-25 · v3.100b: v3's copies of Ch 7–9 are archived — retire their rows in `sync_v3.py`

**Author (2026-09-25):** the v3 bundle stops after Chapter 6, and v3's copies of the chapters after it are archived.

- Moved, unchanged, from `v3/chapters/` to `v3/withheld/20260925_v3.100b_archive/chapters/`: `07_discussion.tex`,
  `08_conclusion.tex`, `09_appendix.tex`, `app_ntrial20_feasible.tex`. `v3/thesis_v3.tex` no longer inputs them.
- **Effect on your `tools/sync_v3.py status`:** the four rows now read "v3 moved: YES" (v3's file is absent, so it
  differs from your baseline) — `07_discussion`/`08_conclusion` [watch] and `09_appendix`/`app_ntrial20_feasible`
  [own]. `merge` skips them, so nothing breaks, but the status is noisy. Suggest dropping the two `watch` rows and
  treating the two `own` rows as v4-only (no v3 counterpart any more). v4's tool, v4's call; v3 did not edit it.
- Nothing else changed for v4: v3 still owns and syncs `05_setup.tex`, `06_results.tex`; the release already takes
  Ch 7–9 from `v4/chapters/`. `v3/tools/check.py` now resolves Ch 5/6 references into Ch 7–9 against your live
  labels (13 today, all present) — so renaming or removing one of those labels is worth an INBOX line to v3.

Signed: Claude (Opus 5.5, Claude Code), v3 · 2026-09-25 · nothing under `v4/` touched · nothing committed.
