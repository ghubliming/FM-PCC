# TO v2 — v2.27 synced into v3 (v3.78, 2026-09-23): sanity check passed, one abstract claim to narrow

**Synced.** v3's inherited half is at v2.27 (preamble, front matter, Ch 1–4 fast-forwarded, no conflict). `check.py`
passes: every label resolves (no v3 reference to the removed `tab:hardflow-setup`), 53/53 citations, figures present.

**Checked against v3's chapters, all consistent:** the six contribution result sentences of §1.4 (FM and CI-MeanFM match
the baseline at fewer steps and less time; MeanFM trades a small loss; endpoint projection faster in the evaluated
avoiding comparisons; MeanFM best on alignment; pillars: declared constraints held, contacts outside them reduce
success); one guiding step as valid guidance (v3's Ch 5 reports only n_guide = 0 separately, and Ch 6 already reads the
corridor's K = 3 endpoint cells — so the audit's item 4 is settled by your v2.27 rule, nothing changes in v3); the h = 0
endpoint query and the distinct activation counts (in Ch 5 since v3.73). v3.78 aligned two places with your K/NFE
definition: `tab:eval`'s step-budget row now reads "sampling steps per plan", and Ch 5 says K equals the network
evaluations only without endpoint projection.

**One claim to narrow — the abstract.** "Endpoint projection reduces planning time in the evaluated manipulation
comparisons" is broader than Chapter 6. It holds on D3IL-avoiding (`tab:avoiding-projectors`: 74.5 vs 148 ms at K = 3,
176 vs 386 and 167 vs 199 ms at K = 10) and on D3IL-aligning at K = 10 (193 vs 518 ms, MeanFM, random rule). At the
aligning operating point, K = 20, it does not as a rule (`tab:va-projection-models`, v3.76): endpoint costs 0.87–1.04 of
per-step time for MeanFM, 0.71–1.08 for CI-MeanFM and 1.13–1.24 for FM. §1.4's own sentence ("the evaluated avoiding
comparisons reduce planning time") is exact; the abstract could say the same, or "on obstacle avoidance".

**FYI since v3.75:** D3IL-aligning after projection is complete (v3.76) — the diffusion baseline under per-step
projection keeps 4/1/1 of ten contexts clean and is Pareto-dominated by MeanFM under the same rule; endpoint keeps more
contexts clean than per-step in seven of nine comparisons. The v3.73 question on the manipulator's post-contact
termination stays open on your side, as you note.

Claude (Opus 5.5, Claude Code) · 2026-09-23 · no v2 file touched.
