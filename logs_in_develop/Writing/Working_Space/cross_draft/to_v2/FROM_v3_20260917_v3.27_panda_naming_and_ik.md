# FROM v3 → v2 · 2026-09-17 · v3.27 · the robot's name, its end effector, and the IK abbreviation

**1. Name and end effector.** D3IL calls it a **7DoF Franka Emika Panda**, and for the task family that
includes *Avoiding* and *Aligning* the robot carries a **cylindrical rod end effector**, not a gripper
\parencite[Sec.~3.3]{jia2024towards}. Both environments load `panda_rod_invisible.xml`
(rod `cylinder size="0.01 0.15"` → radius 0.01 m, length 0.30 m). v3's `tab:platforms` said "parallel
two-finger gripper", which was wrong for these tasks; it now says *Franka Emika Panda*, seven joints,
cylindrical rod end effector, wrist-mounted RGB camera. **Check any Ch 1–4 sentence that names the robot or
its gripper.**

**2. Execution / IK.** The Cartesian setpoint is executed by
`CartPosQuatImpedenceController` (`IKControllers.py:134–270`): damped-least-squares differential inverse
kinematics with a nullspace term, whose joint targets go to the joint-space tracking controller. v3 now
writes this as *"inverse kinematics (IK) turns each Cartesian setpoint into joint commands"* and defines
the abbreviation at `tab:platforms`.

**Request:** add **IK — inverse kinematics** to the acronym list (v2 owns it), and v3 will switch to
`\ac{IK}`. Until then v3 spells it out once and uses it plainly.

**3. FYI:** v3 removed the "these are intervals of simulated time, not real-time rates" clause from
`tab:platforms` (author: it reads as self-attack in a platform table, and the manipulator row makes no such
remark). The lock-step fact is kept in the source note there, and the rate wording agreed in
`FROM_v3_20260917_uav_rates_simulated_time.md` is unaffected.
