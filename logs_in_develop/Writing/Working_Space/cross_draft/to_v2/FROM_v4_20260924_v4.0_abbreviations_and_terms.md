# TO v2 — v4.0: the abbreviation recheck, two consistency findings (2026-09-24)

**From v4.0.** The author asked v4 to recheck the abbreviations of v2/v3 (v4 init, point 5). Full audit:
`v4/notes/AUDIT_20260924_abbreviations_v2_v3.md`. **The list in `99_backmatter.tex` is complete** — every
`\ac{}` resolves, every declared acronym but TUM is used, and nothing Ch 5–9 use needs a new entry; v4
leaves the file byte-identical to v2's. Two things are v2's call:

1. **MJPC vs "MuJoCo MPC".** Ch 2 (`sec:bg:quadrotor`) and Ch 4 (`sec:method:setpoint`, Table 4.2)
   write `\ac{MJPC}`; Ch 5–6 and every results table write "MuJoCo MPC" (44 uses), and v4's Ch 7–8
   follow Ch 6. One form throughout: either v2 adopts "MuJoCo MPC" (then `MJPC` leaves the list) or v3
   writes `\ac{MJPC}` at its first Ch 5 use — a note to v3 says the same.
2. **PD is undeclared.** "joint-space PD law" first appears bare in Ch 4 §4.3.1 (line ~188) and nine
   times after. If wanted: `\acro{PD}[PD]{proportional--derivative}` plus `\ac{PD}` at that first use.

FYI, no action: Ch 1's outline sentence ("Chapter 7 interprets the findings and their limits, and
Chapter 8 concludes with the research questions and future work") matches v4's structure; Ch 8 has one
new section, *Towards Deployment* (`sec:conc:practice`), that nothing in Ch 1–4 references.

Claude (Fable 5.1, Claude Code), v4 · 2026-09-24.
