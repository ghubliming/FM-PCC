# TO v2 — v3.79: one word for each outcome (author: "keep the words the same across the thesis")

v3 now uses, in Ch 5, Ch 6, the appendix and the quadrotor figures:

- **violation** — a plan or path crossing a declared constraint: *violating control steps*, *violation-free* (was
  "collision-free" on the quadrotor scenes and "clean" in some tables and figure subtitles), *success with constraint
  satisfaction*.
- **collision** — the vehicle (or the arm) physically hitting an obstacle. On UAV-pillars a collision with a pillar ends
  the flight and is not a success; it is not a violation, because five of the six pillars are in no constraint set.
- **D3IL-avoiding / UAV-pillars** as the names of the two sides of the comparison (was "table" / "air").

Two places in v2's chapters use the old words (not edited by v3):
- `chapters/01_introduction.tex:130` — "the obstacle-avoidance benchmark itself in the air" → e.g. "D3IL-avoiding flown by
  the quadrotor".
- `chapters/04_method.tex:1627–1628` — "A contact with a pillar ends the episode as a failure, as the rod's contact with a
  post does on the table." → "A collision with a pillar ends the episode as a failure …" (the table-side clause is still the
  open v3.73 check).

v3.79 also gives §6.3.1 its own conclusion (`sec:res:uav:pillars:conclusion`): the plant swap carries the declared
constraint set exactly because the planner, plan, observation, projection and constraint rows are D3IL-avoiding's and the
vehicle tracks each mapped setpoint within the tightening margin; it does not carry freedom from collision with the five
pillars outside the constraint sets. It cites §4.6.3 and writes the similarity map inline (a reference into the collapsed
Ch 4 prints "??" in the review bundle).

Claude (Opus 5.5, Claude Code) · 2026-09-24 · no v2 file touched.
