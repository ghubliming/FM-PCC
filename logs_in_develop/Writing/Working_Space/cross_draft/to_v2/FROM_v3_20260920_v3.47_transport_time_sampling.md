# From v3.47 to v2 — define the transport-time sampling laws in Chapter 4

**Date:** 2026-09-20  
**Found while auditing:** Table 5.8 (`tab:train`)

Table 5.8 records three training-time laws:

- diffusion: uniform over its discrete denoising indices;
- FM: $\ftime\sim\operatorname{Beta}(1.5,1)$;
- MeanFM and CI-MeanFM: a logit-normal time-pair draw with location $-0.4$ and scale $1.0$.

Chapter 4 already defines transport time, the discrete diffusion schedule, and the average-velocity
time pair. It also says that the average-velocity pair is logit-normal. What is missing is the
mathematical definition of the FM Beta draw; the logit-normal statement would also benefit from one
compact formula tying the parameters in Table 5.8 to Chapter 4.

Best placement: in `sec:method:fm`, immediately after Eq. `eq:method:engine:fm`, add the sampling law
used for $\ftime$,
%
\[
  \ftime\sim\operatorname{Beta}(1.5,1),
  \qquad p(\ftime)=1.5\,\ftime^{1/2},\quad \ftime\in[0,1],
\]
%
and state plainly that it weights draws towards the data end of the interpolation. In
`sec:method:meanflow`, beside the existing logit-normal sentence, define the latent normal draw and
logistic transform using the recorded location $-0.4$ and scale $1.0$, then state how the two draws are
ordered into $(r,\ftime)$. Keep the discrete diffusion schedule in its own subsection; it is not the
same continuous transport-time draw.

This belongs in v2's Chapter 4, not v4's Discussion. v3 does not edit the inherited method chapter.
