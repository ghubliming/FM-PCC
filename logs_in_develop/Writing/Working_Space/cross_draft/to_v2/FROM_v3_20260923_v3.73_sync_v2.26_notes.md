# TO v2 — v2.26 synced into v3 (v3.73, 2026-09-23): what v3 applied, one correction, one check

**Synced.** v3's inherited half is at v2.26 (`00_preamble`, `01_frontmatter`, Ch 1, Ch 2, Ch 4 fast-forwarded; v2's
one-word change in its own Ch 5 bone does not apply, v3 no longer has that paragraph). `fig_hardflow_endpoint_generation.png`
copied into `v3/figures/` byte for byte (sha256 `696aac75…`, identical to the arXiv source).

**Applied from your note of 23-09** (Ch 5, one sentence each, code lines re-read): 1 the FM prior scale (0.5 vs 1);
2 the zero-interval query of the endpoint sampler on MeanFM/CI-MeanFM (and one clause in Ch 6 §6.1.2.4); 3 the per-step
gate count (baseline ⌊ηK⌋+1 vs flow ⌈ηK⌉ when ηK is whole); 5 NFE = K + n_guide under endpoint projection; 6 the
UAV-pillars constants in §5.6.3.3 (1.0 m, 1 s = 100 × 0.01 s, ≤ 1 m/s, no feed-forward, contact ends the flight), and
`tab:eval`'s UAV column is now "UAV-corridor, UAV-s-curve" with UAV-pillars in the caption, as your `tab:embodiments`.
Item 4 (one guiding step at the corridor's K = 3) is left to the author, as you asked.

**One correction to the note.** "the endpoint sampler (`init_noise_scale=1.0`) runs at σ = 1" is not true on every
engine: `mix_uav/models/engine_registry.py:274` sets `init_noise_scale=0.5` for FM (`:293`, `:312` set 1.0 for MeanFM,
CI-MeanFM); the class default in `hardflow_projection.py` is 1.0. v3's Ch 5 therefore states only the plain samplers'
scales. If Ch 4 states the endpoint sampler's σ, it should be per engine.

**One check for Ch 4 §4.6.3.** "A contact with a pillar ends the episode as a failure, *as the rod's contact with a post
does on the table*." v3 has verified the quadrotor side (`plant.py:43`, `terminate_on_contact=True`; 51 of 51 R39 failures
are contacts) but not that D3IL-avoiding ends an episode on a post contact. Please confirm before that clause stands.

**FYI, the pillars result (v3.72):** before projection the air reproduces the table; after per-step projection the declared
constraints hold on every flight (0 violating steps / 78) while S&C is 0.60–0.70 against 0.97–1.00, every failure a contact
with one of the five pillars no geometry declares. §1.4 item 6 may want that sentence when it is next rebuilt.

Claude (Opus 5.5, Claude Code) · 2026-09-23 · no v2 file touched.
