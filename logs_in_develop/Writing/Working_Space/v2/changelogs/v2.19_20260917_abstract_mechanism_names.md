# v2.19 — 2026-09-17 · abstract corrected to mechanism names

This pass corrects v2.18. The abstract alone was revised; no chapter prose, bibliography entry,
label or cross-draft file was changed.

## Correction

v2.18 named MeanFlow and α-Flow as if the abstract were a list of published methods. That was the
wrong reading of the writing guidance. `Auxiliary/NOTES_method_naming.md` §3.2 frames the
implementations as points on one target-construction axis, and §5.5 explicitly says that α-Flow does
not appear in the abstract. The author further confirmed that the branded model names are not the
names to use here.

The abstract now describes what is trained:

- an instantaneous-velocity objective;
- an analytic average-velocity objective; and
- an intermediate objective constructed by a finite difference from the network's own prediction.

The result sentence likewise says **analytic average-velocity objective**, not MeanFlow. The
projection strand remains mechanism-first: DPCC projects the current iterate, whereas endpoint
projection predicts and projects the final trajectory before guiding later sampling steps. Neither
the MeanFlow, α-Flow nor HardFlow brand appears in the abstract.

## Scope and claims

The comparison with DPCC is stated through the requested quantities: network evaluations,
wall-clock time and control steps on the same obstacle-avoidance benchmark, with success and
constraint satisfaction preserved. The second stage remains vision-conditioned object alignment and
control of an underactuated, open-loop unstable quadrotor.

## Checks

Abstract: one paragraph, 216 words, zero digits, no negative result. Whole-file mechanical checks:
environment balance, brace balance, math-delimiter parity, dangling references and missing cited
bibliography keys all pass. No label or citation key was added or renamed. **Not compiled** — no TeX
toolchain is available in this container.
