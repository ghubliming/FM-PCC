# v2.18 — 2026-09-17 · abstract: two method axes and the full evaluation scope

The abstract alone was revised. No chapter prose, bibliography entry, label or cross-draft file was
changed.

## What changed

- The generative-model contribution now names **flow matching, MeanFlow and α-Flow** in one compact
  phrase, giving α-Flow the requested short mention without turning the abstract into a method list.
- The objective against DPCC is explicit in the compared quantities: sampling steps, wall-clock time
  and closed-loop task performance, with success and constraint satisfaction preserved. On the shared
  obstacle-avoidance benchmark, task performance is stated as the number of control steps required to
  reach the goal.
- Constraint enforcement is now a full second method axis. The abstract contrasts DPCC's per-step
  iterate projection with endpoint projection, explains the predict–project–guide mechanism, and
  retains the condition that guidance needs a sufficient step budget.
- The extension is stated as two increasingly complex settings built for the thesis:
  vision-conditioned object alignment and control of an underactuated, open-loop unstable quadrotor.
- The qualitative result sentence now covers all supported positive findings requested here: fewer
  control steps and lower compute for MeanFlow against DPCC, reduced constraint-enforcement time from
  endpoint projection where it is active, and more precise vision-conditioned alignment. It makes no
  unsupported quadrotor result claim.

## Naming and evidence

The abstract follows the canonical naming rule: **endpoint projection** describes the method; the
HardFlow brand remains confined to Related Work. The draft-only `\srcnote` records the evidence for
the new projection-cost clause in
`cross_draft/to_v2/FROM_v3_20260917_projection_cost_resolved.md`, alongside the existing avoiding and
visual-alignment sources. The abstract contains no numerical values.

## Checks

Abstract: one paragraph, 217 words, zero digits, no negative result. Whole-file mechanical checks:
environment balance, brace balance, math-delimiter parity, dangling references and missing cited
bibliography keys all pass. No label or citation key was added or renamed. **Not compiled** — no TeX
toolchain is available in this container.
