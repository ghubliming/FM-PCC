# v2.23 — 2026-09-17 · MuJoCo is the simulator; the robot is named; IK acronym confirmed

**Date:** 2026-09-17 · **Base:** v2.22 → v2.23 · **Inputs:** the two open `cross_draft/INBOX.md` → v2
items, from v3 versions **v3.26** and **v3.27** (both 2026-09-17). Both are marked ✅ v2.23.

## KP 1 — MuJoCo is the simulator (v3.26)

Source: `cross_draft/to_v2/FROM_v3_20260917_v3.26_mujoco_is_the_simulator.md`; author rule, now §8
rule 7 of the canonical naming table. D3IL supplies task files, demonstrations and camera placements;
the simulator is MuJoCo. The prefixes *D3IL-avoiding* / *D3IL-aligning* stay.

| place | before | after |
| :-- | :-- | :-- |
| contribution 6 (quadrotor benchmark) | "built on top of D3IL and MuJoCo" | "built for this thesis in MuJoCo `\parencite{todorov2012mujoco}`, inside the environment code of D3IL" |
| §4.7.1 obstacle avoidance | — | "It is simulated in MuJoCo `\parencite{todorov2012mujoco}`" |
| §4.7.2 alignment | "Its simulation, demonstrations and vision pipeline are D3IL's" | "It runs in MuJoCo; its task files, demonstrations, camera placement and vision pipeline are D3IL's" |

The §4.7.2 `\srcnote` is left as provenance, as the note allows. §2.6 ("D3IL provides such a vision
pipeline") needed no change.

## KP 2 — the robot is named, with its end effector (v3.27)

Source: `cross_draft/to_v2/FROM_v3_20260917_v3.27_panda_naming_and_ik.md`. No sentence in Ch 1–4 named
the robot or a gripper, so nothing was wrong; the name is now stated once where the manipulator tasks
begin. §4.7.1: "a seven-joint Franka Emika Panda carries a cylindrical rod as its end effector and
moves in a plane `\parencite[Sec.~3.3]{jia2024towards}`". §4.6.2: "the joint positions of the Panda".

Verified in `d3il/environments/d3il/models/mj/robot/panda_rod_invisible.xml:78` —
`geom type="cylinder" size="0.01 0.15"`, i.e. a rod of radius 0.01 m and length 0.30 m, no gripper
geometry in the task chain.

## KP 3 — IK acronym: already present

`\acro{IK}[IK]{inverse kinematics}` has been in the acronym list since v2.14 and is used as `\ac{IK}`
in §2.7, §4.6.1 and §4.6.2. **v3 can switch to `\ac{IK}` now**; no v2 change was needed.

## KP 4 — no statistical vocabulary (v3.26, awareness item)

Checked: `thesis_v2.tex` contains no $p$-value, test name or the word "significant" (abstract and §1.2
included). Nothing to change; the rule is recorded here so later passes keep it.


## KP 5 — the two meanings of $K$ (v3.28, glossary item)

`cross_draft/to_v4/FROM_v3_20260917_v3.28_step_budget_K_naming.md` is addressed to v4 but states that
the glossary is owned by v2. `tab:notation`'s $K$ row now reads: *step budget = network evaluations per
plan: denoising steps of the reverse process for the diffusion baseline, fixed with its schedule at
training; steps of the ODE solver for the flow-based models, chosen at deployment.* The macro `\nfe`,
every equation and every other table are unchanged. The longer cell made the table overflow, so
`tab:notation` is now `tabularx{\linewidth}{lLl}`.

## Checks

Braces balanced, `$` parity even, 0 dangling references, bibliography 53/53 cited. No label added or removed; `tab:notation` converted to `tabularx`. **Not compiled.** Backup: scratchpad `thesis_v2_pre_v2.23.tex`.
