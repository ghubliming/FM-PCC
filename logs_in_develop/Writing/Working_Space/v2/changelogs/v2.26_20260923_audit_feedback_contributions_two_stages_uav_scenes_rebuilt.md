# v2.26 — 2026-09-23 · the ChatGPT audit answered; §1.4 in two stages; §4.6.3 rebuilt for the current quadrotor benchmark

**Date:** 2026-09-23 · **Base:** v2.25 (2515 lines) → v2.26 (2596 lines) · bibliography unchanged, 53 entries
**Asked for:** (0) check the audit in `audit from chatgpt/AUDIT_v2.25_against_v3.70_2026-09-23.md`, write the
feedback at its end, apply nothing from it; (1) fix §1.4 Contributions; (2) renew §4.6.3 for the changed
pillars/corridor methodology; (3) say in the feedback where 1–2 touch the audit.

---

## KP 0 — the audit, checked and answered (no thesis edit)

Every finding F01–F21 and the §5/§6 advice was checked against the code and the current v3 sources before
answering — 21 verdicts, a table of what was read for each, and a signed §8 appended to the audit file.
Outcome: **18 confirmed, 3 confirmed with a qualification (F07, F18, F19–arm paragraph, F20), none
rejected.** Two corrections to the audit itself: v3 is at v3.71 (not v3.70), and F12 stops one file short —
the **instantaneous-velocity sampler also starts from `0.5·randn` while training uses σ = 1**
(`flow_matcher_v3/models/diffusion.py:164` vs `:273–297`; the repo's own `hardflow_projection.py` fix_4
docstring calls it an out-of-distribution τ=0 state). That is a train/sample mismatch in the FM arm and a
prior-scale confound between FM and the two average-velocity models; told the author in chat, filed to v3.

Three findings need the author, not a rewrite: **F17** (v2's rule says one guiding step carries "no
attributable effect"; v3 reads the corridor's K=3 endpoint cells, which have exactly one), **F01** (which
model the abstract names on avoiding — the DPCC-protocol table has FM and CI-MeanFM at 1.000, MeanFM at
0.967), and **F19's** arm–quadrotor paragraph (author-requested content; soften the absolutes, keep the
mechanism). The v3.69 inbox row (`sec:res:fewstep` wording) sits inside F01's sentence and waits for it.

## KP 1 — §1.4 Contributions in two stages

The six items had no visible order. Now: one lead paragraph, then **On the benchmark of DPCC** (1–4: three
generative models take the diffusion model's place one after another, and a second projection method is
set beside the inherited one — all with the setup unchanged) and **Beyond it** (5–6: the models and both
projection methods carried into the camera task and the quadrotor). The second `enumerate` continues the
count with `\setcounter{enumi}{4}`; no package added. Items 1–5 are verbatim except item 1's now-redundant
"The comparison is made first on the obstacle-avoidance benchmark" sentence; their `\hole`s and result
sentences are untouched pending F01.

**Item 6 rewritten** for the benchmark as it stands (v3.68–v3.71): three scenes that ask three different
questions — UAV-pillars is the obstacle-avoidance benchmark in the air, flown by the planner of items 1–4
unchanged; UAV-corridor is the three-dimensional test whose two test-time constraints leave the horizontal
plane; UAV-s-curve is reported for what its turns do to the tracking controller. The sub-list (a–c) is
folded into the paragraph; MuJoCo MPC is "the second tracking controller, used in the controller comparison
alone"; "everything in this benchmark was built for this thesis" is now specific about what was: scenes,
constraint sets, the flown demonstrations of two scenes, the control loop.

## KP 2 — §4.6.3 rebuilt: two constructions, not three instances of one

What was there described the superseded scene set (an empty scene, six 0.12 m pillars, twelve-channel
plans and flown demonstrations on every scene). Sources for the rewrite: v3 `05_setup.tex:365–512, 572–691,
1282–1309`; `uav_avoiding_bridge/frame.py`, `plant.py`, `factory.py`, `README.md`;
`config/uav_projection.yaml:846–946`; `mix_uav_test/eval_mix_uav.py:1482–1485, 1826–1837`.

- **UAV-corridor and UAV-s-curve: the spatial plan.** The $d=12$ plan, the 0.03 s planning interval, the
  six dynamics rows, the four constraint families. The corridor's two test-time constraints are described as
  forms: a halfspace whose normal has a $z$ component (the room it leaves changes with altitude) and a roof
  of two $x$–$z$ halfspaces, each over its own span. The action bound holds on the s-curve; the corridor
  leaves the action unconstrained and binds its geometry to the commanded position as well (forward pointer
  to `sec:setup:protocol`). Dimensions stay in Chapter 5.
- **`eq:method:env:switched` restated** as what the evaluation solves: the wall set $\mathcal{W}(x_t)$ is
  selected once per replan from the vehicle's current $x$ (measured, or commanded on the corridor's stack)
  and applied to every horizon step. The former per-waypoint implication was a different, stronger program
  (audit F15).
- **Rotor reach** stated with its assumption — per-axis reach at zero yaw, lateral approaches — and
  distinguished from the tightening (audit F20).
- **UAV-pillars: the obstacle-avoidance planner flown.** New `eq:method:env:pillarsmap`
  ($X = s(y_a-y_0)$, $Y=-s(x_a-x_0)$, $Z=z_{\text{fly}}$); the factor sends the rod's radius onto the radial
  rotor reach so no offset is added; planner, plan, observation, projection and constraint rows are the
  table's; each commanded position is mapped into the arena and tracked by the cascaded geometric controller
  for one control period (reference rate-limited, no velocity feed-forward, altitude held by the vertical
  loop); the measured position is mapped back and becomes the next observation; a pillar contact ends the
  episode as a failure. "The scene has no demonstrations of its own: its planner is trained on the human
  demonstrations of D3IL-avoiding." Values (36, 1.0 m, 1 s / 100 physics steps, 1 m/s) are in the `\srcnote`
  only; Chapter 5 should print the control period (filed to v3).
- **Expert demonstrations** restricted to the corridor and the s-curve; the empty scene is gone; one closing
  sentence says the corridor's boundaries did not exist when its data was flown.
- **`tab:embodiments`**: third column now "UAV-corridor, UAV-s-curve"; caption says UAV-pillars flies the
  first column and only the plant is the quadrotor.

Vocabulary check: no "bridge", "Mode T/L", "turbo", "v2/v3" scene suffixes or `pillars_hg` in running text
(the one `bridge` is the path `uav_avoiding_bridge/` inside a `\srcnote`).

## KP 3 — how 1–2 touch the audit (written into §8.4 of the audit file)

Item 2 **applies F02** (scope), **F15** (the switched-wall equation lives in the rewritten section) and the
wording half of **F20**; item 1 applies the contribution part of F02. Everything else from the audit is
untouched and listed in §8.1 with a verdict for the debate. Contribution 4's result sentence ("in every
environment") is left as v3 supplied it, pending F01.

## Checks

Environments balanced, braces balanced (delta 0), `$` parity even, **0 dangling references, no duplicate
labels**, bibliography 53/53 cited and used, `\hole` **10** (unchanged). Labels: **one added**,
`eq:method:env:pillarsmap`; **none removed**. 2515 → 2596 lines. **Not compiled** — no TeX toolchain here.
Backup: scratchpad `thesis_v2_pre_v2.26.tex`.

## Cross-draft

- `to_v3/FROM_v2_20260923_audit_findings_for_ch5_ch6.md` — F04, F12-wide, F13, F17, F03 and the pillars
  control period, each with the file and line it rests on.
- `CROSS_STATE.json`: still "current against v3.60"; v3.69 recorded as open (held for F01).

---

**Written by:** Claude (Claude Code), 2026-09-23, FM-PCC dev container, on the author's three-item
instruction. Every file and line quoted here and in the audit's §8 was read in this session. No code
changed. **Not compiled.** Nothing committed.
