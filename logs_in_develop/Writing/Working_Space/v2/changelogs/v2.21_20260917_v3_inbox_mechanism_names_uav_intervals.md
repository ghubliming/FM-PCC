# v2.21 — 2026-09-17 · v3 inbox (v3.18, v3.19, v3.20, v3.25): mechanism names, UAV simulated-time intervals, §1.2 roadmap

**Date:** 2026-09-17 · **Base:** v2.20 (`thesis_v2.tex` 2332 lines) → v2.21 (2347 lines)
**Inputs:** the five open `cross_draft/INBOX.md` → v2 items, sent by v3 versions **v3.18**, **v3.19**,
**v3.20** and **v3.25** (all dated 2026-09-17). All five are marked ✅ v2.21 in the inbox.

| # | from v3 version | inbox item | done in v2.21 |
| :-- | :-- | :-- | :-- |
| 1 | **v3.20** · 2026-09-17 | mechanism names for the three generative models | KP 1 |
| 2 | **v3.18** · 2026-09-17 | "33 Hz / 100 Hz" are simulated-time intervals | KP 2 |
| 3 | **v3.18** · 2026-09-17 | projection cost resolved, a consistent cost sentence offered | KP 3 |
| 4 | **v3.19** · 2026-09-17 | `tabular` row ≈ 14.9 cm; DPCC protocol is 5 seeds × 2 episodes | KP 4 |
| 5 | **v3.25** · 2026-09-17 | §1.2 points forward, Ch 5 owns the phase definition; UAV demos already complete | KP 5 |

---

## KP 1 — mechanism names (v3.20, author decision 2026-09-17)

Source: `cross_draft/to_v2/FROM_v3_20260917_alphaflow_naming.md`; canonical table
`Auxiliary/Naming/TRANSLATION_20260914_dev_jargon_to_scientific.md` §2 and §8 rule 1.

Names applied outside Related Work: **instantaneous-velocity matching** (`fm`), **analytic
average-velocity matching** (`mf`), **consistency-interpolated average-velocity matching** (`af`).
*Flow matching* stays as the family name (title, keywords, RQ2, §4.3.2 heading).

| place | before | after |
| :-- | :-- | :-- |
| abstract | "instantaneous velocity, analytic average velocity and an intermediate finite-difference target" (v2.19) | the three names; result sentence "analytic average-velocity matching is as reliable as DPCC …". 220 words, no digits |
| §1.2 | "MeanFlow and its consistency-training variant belong to it" | the three models belong to the family and differ in the velocity learned |
| contribution 1 | "replaced by flow matching" | "replaced by instantaneous-velocity matching, the flow-matching objective" |
| contribution 2 | title "MeanFlow in place of flow matching" | "Analytic average-velocity matching" (+ JVP target clause) |
| contribution 3 | title "Consistency training between flow matching and MeanFlow" | "Consistency-interpolated average-velocity matching"; target described by the consistency step ratio |
| §2.4 | "MeanFlow …", "this thesis refers to it as *consistency training*" (+ `song2023consistency`) | mechanism names; the "consistency training" definition and its consistency-models attribution are removed — *Consistency Training* is a different published method |
| §3 Positioning | "flow matching, MeanFlow and consistency training" | "instantaneous-velocity matching and the two average-velocity objectives" |
| §4.3 intro, §4.3.2 | brands | names; §4.3.2 Implementation opens "Instantaneous-velocity matching trains on exactly this objective." |
| §4.3.3 title | `\subsection{MeanFlow}` | `\subsection{Analytic Average-Velocity Matching}` — label `sec:method:meanflow` **kept** |
| §4.3.3 | "The MeanFlow identity --- the target that makes it trainable", `\mathcal{L}\sidx{MF}` | "The average-velocity identity", `\mathcal{L}\sidx{avg}` |
| §4.3.4 title | `\subsection{Consistency Training}` | `\subsection{Consistency-Interpolated Average-Velocity Matching}` — label `sec:method:alphaflow` **kept** |
| §4.3.4 | "applies consistency training to the MeanFlow objective", "interpolation scalar" | target interpolated by the consistency step ratio α, annealed to a final value `α_end > 0`; "step ratio" |
| §4.3.5 `eq:method:engine:iface` | case labels "flow matching", "MeanFlow," | "instantaneous velocity", "average velocity," |
| §4.3.6 | "MeanFlow and consistency training need …", "The MeanFlow target" | "The two average-velocity objectives need …", "The analytic target" |

Unchanged on purpose: Related Work §3.2/§3.3/§3.4 (brands allowed there); `\parencite` keys; code paths
in `\srcnote`; all labels. `song2023consistency` stays cited in §3.3. The short table labels
FM / MeanFM / CI-MeanFM are defined by v3 in Ch 5; Ch 1–4 have no results table that needs them.

## KP 2 — UAV intervals are simulated time (v3.18)

Source: `cross_draft/to_v2/FROM_v3_20260917_uav_rates_simulated_time.md`; re-checked in code:
`mix_uav_test/eval_mix_uav.py:1610` (`decim = round(1/(dt·DATASET_HZ))`), `:1728` (physics loop),
`uav_expert_data_collect/dataset_writer.py:31,73` (`DATASET_HZ = 33`).

- `tab:embodiments`: rows "plan rate / inner rate" → **plan interval / inner interval**; UAV column
  "33 Hz / 100 Hz" → "0.03 s simulated / 0.01 s simulated".
- §4.6.3 `eq:method:dep:multirate`: `f_plan = 33 Hz` removed; now `n_dec Δt_phys = 0.03 s`,
  `Δt_phys = 0.01 s ⇒ n_dec = 3`. Prose: a plan every `n_dec`-th physics step (the recording interval of the
  demonstrations), the controller at every physics step, and "The simulation waits for the planner, so
  these intervals hold in simulated time for any planning time." Stale srcnote line `1454--1455` updated.
- §4.7.3: "recorded at the planning rate" → "at the planning interval".
- §4.6.3 `eq:method:dep:vdes`: the two uses of `f\sidx{plan}` → `1/(n_dec Δt_phys)` (same value, 1/0.03 s), so
  no undefined symbol remains.

## KP 3 — cost sentence in contribution 4 (v3.18)

Source: `cross_draft/to_v2/FROM_v3_20260917_projection_cost_resolved.md`. The 1.86–3.57× figure was
UAV-corridor data misattributed to avoiding (resolves the v2.17 → v3 note). Appended, as offered by v3:
"Where it has guiding steps, endpoint projection takes less time per control step than per-step
projection in every environment; on UAV-corridor it is also less often collision-free
(`sec:res:constraints`)." Srcnote names both v3 sources.

## KP 4 — table width and protocol wording (v3.19)

- The over-wide row was in the unlabelled `n_guide` table in §4.5.4 (≈ 15.3 / 14.9 cm by v3's `check.py`
  heuristic): `tabular{lll}` → `tabularx{\linewidth}{lLL}` (preamble already has `tabularx` + `L`, v2.17).
- Remaining warning: `tab:compute` row "Projection solver" (≈ 14.9 cm) is in the **appendix**, v3-owned;
  not touched.
- DPCC protocol "5 seeds × 2 episodes": grep of Ch 1–4 finds no episode count; nothing to change.

## KP 5 — §1.2 roadmap (v3.25)

Source: `cross_draft/to_v2/FROM_v3_20260917_v3.25_ch5_backrefs_and_uav_method.md`.

- §1.2 "The study proceeds in two stages …" (operational definition) → a two-sentence forward roadmap:
  first DPCC's obstacle-avoidance benchmark with its setup unchanged, then the alignment task and the
  quadrotor benchmark built for this thesis; "`\autoref{ch:setup}` describes the evaluation."
- §4.7 intro: dropped "The environments follow the two stages of `\autoref{sec:intro:problem}`." so Ch 4
  no longer points backwards either; the rest of that paragraph (prove on DPCC's benchmark, then
  extend) is unchanged.
- UAV demonstrations: v3.25 confirmed §4.7.3 complete; no change.

## Checks

Environments balanced, braces balanced, `$` count even, 0 dangling refs, bibliography 52/52 cited,
label set identical to v2.20, `\hole` 11. Abstract 220 words, zero digits. **Not compiled** — no TeX
toolchain in this container. Backup: scratchpad `thesis_v2_pre_v2.21.tex`.
