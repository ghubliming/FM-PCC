# v2.24 — 2026-09-18 · Chapter 4 reorganised, RQ1 names its axes, network capacity in Table 3.1, two v3 inbox items

**Date:** 2026-09-18 · **Base:** v2.23 (2427 lines) → v2.24 (2436 lines) · bibliography unchanged, 53 entries
**Asked for:** six items (author, 2026-09-18), items 0–5 below.

---

## KP 0 — the two open cross-draft items for v2

| from | item | done |
| :-- | :-- | :-- |
| **v3.31** · 2026-09-18 | MuJoCo is cited from §1.3 on but never introduced | §2.5 now reads "simulated in MuJoCo, a rigid-body physics engine for contact-rich robotic simulation `\parencite{todorov2012mujoco}`", which is the sentence v3 §5.1 back-references |
| **v3.32** · 2026-09-18 | §4.8 (v2: `sec:method:geometric`) defines three velocity-setpoint policies but never says which one the experiments use | the section now states the policy of record |

Velocity setpoint, added after `eq:method:dep:vdes`: every reported quadrotor result brakes to rest at
every plan step; the other two policies are implemented and were measured and are not the
configuration of record. The first policy reads the increment as a mean velocity over an assumed
constant interval between plan queries, so its commanded speed moves with the arrival time of the
plan. With $\dot{p}\sidx{des} = 0$ the outer loop moves the vehicle on its position error alone, so
the setpoint leads the vehicle by a standing lag that grows with the commanded speed; `sec:res:uav`
reports what the lag costs. The measured numbers stay in Chapter 6 — v2 states the mechanism, not the
result. Sources named in the `\srcnote`: `Gen11/Epoch8_UAV_Mjpc_thrust_control/U3_v_des_Patch/
PLAN_pid_const_v.md`, `Gen15/Study/STUDY_20260901_mf_unguided_failure_uav_pillars.md` §5.

## KP 1 — RQ1 names the axes it compares

RQ1 asked whether flow matching "improves the quality--cost frontier", which never said on what.
It now asks whether flow matching **reaches the task success and the constraint satisfaction of the
diffusion model it replaces at fewer solver steps and less planning time per control step**, and
whether the number of steps at which it does so orders the three objectives. RQ2 already named its
axes (violations, control steps, computation); RQ3 is unchanged.

## KP 2 — Table 3.1 carries network capacity

`tab:related-loops`: the column *Backbone* is now *Backbone and capacity*. None of the five sources
states a parameter count — checked in HardFlow's LaTeX source, in DPCC (hyperparameter table, p. 8 of
the local PDF) and in SafeFlowMPC — so those rows read "size not reported" and the caption says so.
The row for this thesis states **$4.0$\,M parameters for every generative model**, the figure v3
already uses in `05_setup.tex:532`. This is what makes the comparison of §3.7 a matched one.

## KP 3 — §4.3.1 states the surrogate model as fact

Removed: "**The model is deliberately crude, and that is the point.**" and the sentence arguing that a
crude model with an honest bound "is a different design … and it is the design this thesis inherits".
The bullet now reads "**The model carries no knowledge of the robot.**" and states what follows: what
$f$ omits appears as the mismatch $w_t$, whose bound $\gamma$ is estimated from unconstrained
rollouts and absorbed by the tightening; feasibility is a statement about the model and its mismatch
bound, not about the plant. In `rem:eulermodel`, "the weakest defensible model of the plant" → "models
the plant by an integrator", and the `\srcnote` self-talk ("the distinction matters and the thesis
should not overstate it") is gone.

## KP 4 — Chapter 4 follows the ordered structure

| was | now |
| :-- | :-- |
| 4.1 Problem Formulation | 4.1 Problem Formulation |
| 4.2 System Overview | 4.2 System Overview |
| 4.3 Generative Models | **4.3 Plant Dynamics and Control** |
| 4.4 Plant Dynamics | 4.4 Generative Models |
| 4.5 Constraint Projection | 4.5 Constraint Projection |
| 4.6 Low-Level Control | 4.6 Environments |
| 4.7 Environments | — |

Overview → dynamics including control → generative model → constraint projection → environments.
*Plant Dynamics* and *Low-Level Control* are merged into one section, so the chapter reads from the
plant upwards: the manipulator and its first-order model, the quadrotor, from plan to setpoint,
differential \ac{IK}, the cascaded geometric controller, sampling-based predictive control. The
merged header carries **both** labels, `sec:method:dynamics` and `sec:method:deployment`, so the three
existing references to the low-level section still resolve; **no label was added or removed in this
pass**. The two section intros are merged into one paragraph, and the chapter intro now says the
controller is described from the plant upwards.

Consequence for reading: `eq:method:dpcc:tighten`, `eq:method:proj:deriv` and `sec:method:proj` are
now referenced before they are defined, inside §4.3. They are `\eqref`/`\autoref` forward references,
which is what a numbered cross-reference is for.

## KP 5 — Table 4.3 spells out its terms

`tab:embodiments`: `differential IK` → `differential \ac{IK}`, `MJPC` → `\ac{MJPC}` (both acronyms
already declared, so each is expanded at its first use in the document), `env.\ step` →
`environment step`, `sim substeps` → `simulator substeps`. The caption now defines every remaining
short form in place: the plan width $d$ and the observation block $\vect{o}$ from
`eq:method:plan`, $t\sidx{s}$ from `eq:method:dpcc:euler`, "dynamics rows" from
`eq:method:proj:deriv`, the two intervals as simulated time from `eq:method:dep:multirate`, and a
forward pointer, "the quantities measured on these loops are defined in `\autoref{sec:setup:metrics}`".

The author's second half of this item — whether the abbreviation expansions belong in an appendix
list — is a whole-thesis decision, sent to **v4** as a note; the acronym list is shared.

## Checks

Environments balanced, braces balanced, `$` parity even, 0 dangling references, bibliography 53/53
cited and 53/53 used, label set **identical** to v2.23, `\hole` 10 (unchanged), 2427 → 2436 lines.
Table-width heuristic: the only row over 14.7 cm is `tab:compute` in the appendix, which v3 owns.
**Not compiled** — no TeX toolchain in this container. Backup: scratchpad `thesis_v2_pre_v2.24.tex`.

## Cross-draft

- `to_v3/FROM_v2_20260918_ch4_reorder_and_rq1_axes.md` — the Chapter 4 section order changed and the
  low-level section is now a subsection block under `sec:method:dynamics`; the next `sync_v2.py` run
  must take the new order wholesale. Also the reworded RQ1 (Ch 1 and the Ch 8 answers) and the
  capacity column of `tab:related-loops`.
- `to_v4/FROM_v2_20260918_acronym_list_placement.md` — the author asks whether abbreviation
  expansions should sit in an appendix list; v4 owns that decision.
- INBOX: the v3.31 and v3.32 → v2 rows are marked ✅ v2.24.
