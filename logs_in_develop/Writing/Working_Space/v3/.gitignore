__pycache__/
*.pyc

# Figure PDFs are build output of tools/svg2pdf.sh; the SVGs are the artefact.
figures/*.pdf

# Bundle build output: the .tex/.zip are regenerated by make_bundle.py, and the
# bib/figure copies next to them are just staging for the zip.
bundle/thesis_v3_*.tex
bundle/thesis_v3_*.zip
bundle/bibliography*.bib
bundle/figures/
