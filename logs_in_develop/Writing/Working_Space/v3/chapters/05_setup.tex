% =============================================================================
%  CHAPTER 5 -- EXPERIMENTAL SETUP                                     [v3 OWNS]
% -----------------------------------------------------------------------------
%  v3.7 (2026-09-14): every section is organised by the three environments, in
%  the order of the study -- obstacle avoidance (the benchmark of DPCC, taken
%  over unchanged), then vision-conditioned alignment and the quadrotor benchmark
%  (both built for this thesis). How each environment is constructed and which
%  constraint forms it uses is v2's Section 4.7 (sec:method:envs); this chapter
%  holds the experimental values, the data, the baselines, the metrics and the
%  protocol. Individual changelog: changelogs/v3.7_20260914_ch5-8_restructure.md
%
%  Vocabulary: Writing/Auxiliary/Naming/TRANSLATION_20260914_dev_jargon_to_scientific.md
% =============================================================================

\chapter{Experimental Setup}\label{ch:setup}

The evaluation has two phases. First, the generative models and projection methods are established on
the obstacle-avoidance benchmark of \ac{DPCC}, with its setup taken over unchanged so that the diffusion
baseline is met on its own benchmark. Second, they are tested further in two environments built for
this thesis: a vision-conditioned alignment task and a quadrotor benchmark with three scenes.
Throughout this chapter and the results, these environments are called \emph{D3IL-avoiding},
\emph{D3IL-aligning}, \emph{UAV-corridor}, \emph{UAV-pillars}, and \emph{UAV-s-curve}; the descriptive
subsection titles and these prefixed names refer to the same tasks. \autoref{sec:method:envs} describes how each environment is constructed; this
chapter gives, for each of the three, the task and constraint values, the data, the metrics it is scored
on, the baseline it is compared with, and the training and evaluation protocol.

% =============================================================================
\section{Simulated Platforms}\label{sec:setup:platforms}
% =============================================================================

Every environment is simulated by MuJoCo, the rigid-body physics engine used throughout this thesis
(\autoref{sec:bg:quadrotor}) \parencite{todorov2012mujoco}. The two
manipulation tasks run the seven-joint Panda manipulator in MuJoCo, with the model files, task
definitions and demonstrations of D3IL \parencite{jia2024towards}. The quadrotor benchmark runs the Skydio X2 model from MuJoCo Menagerie
\parencite{zakka2022menagerie} with the quadrotor task patch distributed by MuJoCo MPC
\parencite{howell2022predictive}. MuJoCo is the simulator in every case; D3IL and MuJoCo MPC supply the
task files and data that run in it. \autoref{fig:platforms} shows both models on their own, and
\autoref{tab:platforms} gives the properties that enter the experiments.

\begin{figure}[htpb]
  \centering
  \begin{minipage}[b]{0.30\linewidth}\centering
    \includegraphics[height=3.6cm]{fig_platform_panda}\\[0.3em]
    {\footnotesize Panda manipulator}
  \end{minipage}\hspace{1.5em}
  \begin{minipage}[b]{0.45\linewidth}\centering
    \includegraphics[height=3.6cm]{fig_platform_x2}\\[0.3em]
    {\footnotesize Skydio X2 quadrotor}
  \end{minipage}
  \caption[The two simulated platforms]{The two simulated platforms, rendered in MuJoCo from the model
  files the experiments load, without their environments. Left: the Panda manipulator, from the model files
  of D3IL, used for D3IL-avoiding and D3IL-aligning. Right: the Skydio X2 model from MuJoCo
  Menagerie with the MuJoCo MPC task patch, used in the three aerial scenes. Both are simulated by
  MuJoCo. Not to scale with each other; dimensions are in
  \autoref{tab:platforms}.}
  \label{fig:platforms}
\end{figure}

\begin{table}[htpb]
  \caption[Properties of the simulated platforms]{Properties of the simulated platforms that enter
  the experiments. The X2 footprint includes the rotor disks represented by the collision model.}
  \label{tab:platforms}
  \centering
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabularx}{\linewidth}{@{}p{2.9cm} L L@{}}
    \toprule
    Platform & Model & Experimental interface \\
    \midrule
    Franka Emika Panda
      & Seven-joint manipulator, in the model and task files of D3IL; a cylindrical rod end effector of
        radius $0.01$\,m and length $0.30$\,m; a wrist-mounted RGB camera
      & The planner commands planar or spatial end-effector increments; inverse kinematics (IK) turns
        each Cartesian setpoint into joint commands. \\
    Skydio X2
      & Quadrotor from MuJoCo Menagerie with the MuJoCo MPC task patch; four rotors;
        mass $1.325$\,kg; rotor centres $0.228$\,m from the body centre;
        $0.54\times0.62$\,m collision footprint (\autoref{fig:x2-dimensions}); thrust command
        $[0,13]$\,N per rotor
      & The planner commands a spatial position increment every third physics step, $0.03$\,s, the
        interval at which the demonstrations were recorded; the geometric controller
        (\autoref{sec:method:deployment}) acts at every $0.01$\,s physics step. \\
    \bottomrule
  \end{tabularx}
\end{table}
\begin{figure}[htpb]
  \centering
  \includegraphics[width=0.62\linewidth]{fig_platform_x2_dimensions}
  \caption[The quadrotor's dimensions]{The Skydio X2 of \autoref{tab:platforms}, drawn to scale from the
  model file the experiments load: four rotor disks of radius $0.13$\,m at $(\pm0.14,\pm0.18)$\,m, the body,
  the $0.54\times0.62$\,m collision footprint (dotted), and the rotor-centre distance of $0.228$\,m. The
  dashed circle is the $0.31$\,m radius by which the projection inflates every obstacle before the solver
  sees it (\autoref{tab:uav-scenes}), so the vehicle is treated as the disk that contains it.}
  \label{fig:x2-dimensions}
\end{figure}

\srcnote{The robot is D3IL's \enquote{7DoF Franka Emika Panda} with the cylindrical end effector it
uses for this task family \parencite[Sec.~3.3]{jia2024towards}: both environments load
\texttt{models/mj/robot/panda\_rod\_invisible.xml} (rod \texttt{cylinder size="0.01 0.15"}, camera
\texttt{rgbd} on \texttt{panda\_hand}); the gripper of the full \texttt{panda.xml} is not used by these tasks.
Execution: \texttt{cartesianPosQuatTrackingController} =
\texttt{CartPosQuatImpedenceController} (damped-least-squares differential IK with a nullspace term,
\texttt{IKControllers.py:134--270}), whose joint targets go to the joint-space tracking controller;
\autoref{sec:method:deployment}. X2 mass, rotor positions, rotor-disk size, actuator range and
$0.01$\,s physics step (\texttt{<option timestep="0.01">}): \texttt{quadrotor/quadrotor\_modified.xml:4}.
Plan interval: \texttt{decim = max(1, round(1/(dt$\cdot$DATASET\_HZ))) = 3} physics steps at
\texttt{mix\_uav\_test/eval\_mix\_uav.py:1610} with \texttt{DATASET\_HZ = 33}
(\texttt{uav\_expert\_data\_collect/dataset\_writer.py:31}), the same factor by which the demonstrations were
downsampled when they were written (\texttt{dataset\_writer.py:73}); $3\times0.01 = 0.03$\,s. Both numbers are
this thesis's own configuration, not values taken from a publication. The simulation is stepped only after
the plan returns.}

% =============================================================================
\section{Tasks and Environments}\label{sec:setup:tasks}
% =============================================================================

The three environments differ in what the planner observes, how many dimensions its plan has, and which
constraint forms the projection must satisfy. Each is given below with its task, its constraint set and the
values that enter the experiments.

\subsection{Obstacle Avoidance}\label{sec:setup:tasks:avoiding}

The obstacle-avoidance task is the benchmark of \ac{DPCC}, simulated in MuJoCo from the task files and
demonstrations of D3IL: the same environment, Panda manipulator, demonstrations, constraint geometries, projection configuration and evaluation
protocol \parencite{jia2024towards,romer2025diffusion} (\autoref{sec:method:env:avoiding}). Holding
that complete benchmark fixed provides the controlled validation of replacing DPCC's diffusion model
with the three flow-based objectives. A manipulator moves its
end effector across a field of six obstacles to a goal line (\autoref{fig:env-avoiding}). At test time
the plan is constrained by halfspaces that the demonstrations never respect, in three geometries --- \emph{top-left-hard}, \emph{top-right-hard} and \emph{both-hard}, defined in \autoref{tab:avoiding-geometries} and drawn in \autoref{fig:constraints-avoiding} --- together with the circular keep-out regions and the action box
of \autoref{sec:method:env:avoiding}. The geometries are those of the \ac{DPCC} configuration \parencite[Sec.~6.1]{romer2025diffusion}. Every
constraint also has a tightened version, shrunk by the margin of $0.025$ that \ac{DPCC} introduces and
applies to this benchmark \parencite[Sec.~5.3]{romer2025diffusion} (\autoref{sec:method:dpcc}).
\srcnote{\texttt{config/projection\_eval.yaml}: \texttt{halfspace\_constraints['avoiding-d3il']},
\texttt{enlarge\_constraints: \{'avoiding': 0.025\}}; parity with \texttt{aux\_repo/dpcc} checked in
v2, \autoref{sec:method:env:avoiding}.}

\begin{table}[htpb]
  \caption[Constraint geometries of obstacle avoidance]{The three constraint geometries of the
  obstacle-avoidance task, in metres. Each halfspace keeps the planned end-effector position below the line
  through the two points; the keep-out disk keeps it outside the circle. All three share the workspace
  $x\in[0.2,0.8]$, $y\in[-0.3,0.4]$, the six obstacles of radius $0.025$ and the goal line $y=0.35$. The
  tightened versions move each line and enlarge each disk by $0.025$. The last column counts the D3IL
  demonstrations whose measured position satisfies the constraints at every step.}
  \label{tab:avoiding-geometries}
  \centering
  \begin{tabularx}{\linewidth}{@{}l L l r@{}}
    \toprule
    Geometry & Halfspace: below the line through & Keep-out disk & Satisfied by \\
    \midrule
    top-left-hard  & $(0.8,\,-0.5)$ and $(0.4,\,0.5)$ & $(0.4,\,0.08)$, $r=0.08$ & 0 of 96 \\
    top-right-hard & $(0.2,\,-0.5)$ and $(0.6,\,0.5)$ & $(0.6,\,0.08)$, $r=0.08$ & 1 of 96 \\
    both-hard      & $(0.8,\,-0.3)$ and $(0.575,\,0.5)$; $(0.2,\,-0.3)$ and $(0.425,\,0.5)$ & $(0.5,\,-0.09)$, $r=0.08$ & 2 of 96 \\
    \bottomrule
  \end{tabularx}
\end{table}
\srcnote{\texttt{config/projection\_eval.yaml} (\texttt{halfspace\_constraints}, \texttt{obstacle\_constraints});
geometry-to-constraint mapping \texttt{scripts/eval.py:93--102}; counts by
\texttt{Data\_Analysis/DA\_in\_Paper/plotting/extract/avoiding\_scene.py}, the check of
\texttt{scripts/visualize\_data\_constraints.py}.}

\begin{figure}[htpb]
  \centering
  \begin{minipage}{0.44\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_render_avoiding}\\[0.3em]
    {\footnotesize MuJoCo simulator view}
  \end{minipage}\hfill
  \begin{minipage}{0.44\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_env_avoiding}\\[0.3em]
    {\footnotesize measured demonstrations}
  \end{minipage}
  \caption[The obstacle-avoidance task]{The obstacle-avoidance task. Left: a frame rendered by MuJoCo,
  showing the manipulator and the field of six posts. Right: the measured
  end-effector position in all 96 demonstrations, viewed from above. Every demonstration starts at
  the bottom and crosses the goal line (green) between the obstacles (red).}
  \label{fig:env-avoiding}
\end{figure}


\FloatBarrier

\subsection{Vision-Conditioned Alignment}\label{sec:setup:tasks:aligning}

The alignment task of D3IL \parencite{jia2024towards} asks the manipulator to push a box into a target
pose on a table. The task runs in MuJoCo; its task files, demonstrations and camera set-up are D3IL's. Around them this thesis
built a constrained-control environment (\autoref{sec:method:env:aligning}): a three-dimensional plan
with its dynamics rows, a constraint set on the planned end-effector position and on the action, and
the coupling of the visual encoder to the generative network. The policy observes two RGB camera
streams, a front view and an in-hand view, instead of the state (\autoref{fig:aligning-cameras}). Each
episode starts from an initial box pose and a target pose, together called a context. The tightened
constraints are shrunk by $0.03$\,m.
\srcnote{\texttt{config/visual\_aligning\_eval.yaml}: \texttt{workspace\_bounds},
\texttt{action\_bounds: 'auto'}, \texttt{enlarge\_constraints: 0.03}.}

The context is drawn at random, and this is the difference from obstacle avoidance that matters most for
what the policy has to do. In obstacle avoidance every episode begins at one fixed end-effector position
and the six posts never move, so a policy can learn a route. Here the box is placed anywhere in
$x\in[0.4,0.6]$, $y\in[-0.25,-0.10]$ at any yaw in $\pm 90^{\circ}$, and its target anywhere in
$x\in[0.4,0.6]$, $y\in[0.20,0.35]$ at any yaw in $\pm 90^{\circ}$; the demonstrations were recorded from
such draws and the evaluation replays an enumerated set of them (\autoref{sec:setup:protocol:aligning}).
Two episodes therefore ask for two different pushes. \autoref{fig:aligning-contexts} draws the ten
contexts the evaluation replays, which are the ten every result in \autoref{sec:res:visual} is averaged
over.

Where the box is can only be seen. The vector half of the observation is
$(\vect{p}\sidx{des},\vect{p})\in\R^{6}$, the commanded and the measured end-effector position
(\autoref{sec:method:env:aligning}); it carries no box pose, no target pose and no object state of any
kind. The two RGB streams are the sole source of both, so the policy must locate the box in the images
before it can move toward it --- which is why a $0.10$\,m box in a $0.20\times0.15$\,m draw region is a
harder problem than its planar geometry suggests.
\srcnote{\texttt{fm\_visual\_aligning/datasets/sequence.py}: \texttt{ParityAligningDataset.OBS\_DIM = 6},
the 9D visual trajectory used for training (\texttt{diffuser\_visual\_aligning\_test/
train\_visual\_aligning\_dpcc.py:168--170}); the 23D \texttt{StateOnlyAligningDataset} that does carry
the box and target pose is not used for any result in this thesis. Draw ranges:
\texttt{gym\_aligning/envs/aligning.py:62--67} (\texttt{box\_space}, \texttt{target\_space}).}

Obstacle avoidance is a planar task that imitation policies already solve: the best policies reported
by D3IL reach a success rate of $0.95$ on it \parencite[Table~3]{jia2024towards}, so it tests constraint
satisfaction on a plan that is easy to produce. This environment raises three things at once. The plan is
three-dimensional rather than planar; the policy sees the scene through two cameras and a visual encoder
rather than through a state vector; and the task itself is not solved --- the best image-based policy
reported by D3IL finishes roughly a third of its episodes (\autoref{sec:setup:baselines:aligning},
\autoref{tab:va-vs-d3il}). It therefore asks whether the \ac{DPCC} construction still holds where the plan
is harder to produce, and it puts the projection where only some episodes finish at all.

The active geometry is \emph{combined-5} (\autoref{tab:aligning-constraints},
\autoref{fig:constraints-aligning}): a halfspace that cuts off the far corner of the table, a circular
keep-out region between the box and its target, and a box on the action --- the same two geometric forms
D3IL-avoiding uses, on a task whose plan is three-dimensional. Neither the halfspace nor the keep-out region
is respected by the demonstrations, so satisfying them is left to the projection.
\srcnote{\texttt{config/visual\_aligning\_eval.yaml}: \texttt{active\_geo\_variants: [combined\_5]},
\texttt{combined\_5.workspace\_bounds}, \texttt{enlarge\_constraints: 0.03}, \texttt{action\_bounds: 'auto'}
(the action box is the per-channel range of the demonstrated increments,
\texttt{eval\_mix\_visual\_aligning.py:257--258}).}

\begin{table}[htpb]
  \caption[Constraint set of the alignment task]{The constraint set of the alignment task, geometry
  \emph{combined-5}, in metres, on the planned end-effector position. The tightened version moves the
  halfspace $0.03$\,m inward and grows the keep-out radius by the same margin. The action box is derived
  from the demonstrations themselves, so it carries no fixed number here.}
  \label{tab:aligning-constraints}
  \centering
  \small
  \setlength{\tabcolsep}{8pt}
  \renewcommand{\arraystretch}{1.2}
  \begin{tabularx}{\linewidth}{@{}l>{\raggedright\arraybackslash}p{3.9cm} X@{}}
    \toprule
    Constraint & Form & Values \\
    \midrule
    Halfspace & below the line through two points & $(0.65,\,0.45)$ and $(0.80,\,-0.45)$ \\
    Keep-out region & outside a circle & centre $(0.50,\,0.00)$, radius $0.06$ \\
    Tightening & both, moved inward & $0.03$ \\
    Action box & range of the commanded increment & the per-channel range of the demonstrated increments \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{figure}[htpb]
  \centering
  \includegraphics[width=0.62\linewidth]{fig_constraints_aligning}
  \caption[The constraint set of the alignment task]{The constraint set of the alignment task, drawn like
  \autoref{fig:constraints-avoiding} and \autoref{fig:constraints-uav}: shaded is the region excluded for
  the planned end-effector position --- the halfspace across the far corner and the circular keep-out
  region --- and dashed the tightened boundary. The box and its target are drawn to scale and at their
  recorded yaw: each footprint is the real $0.10$\,m square, with the outline around it the coloured rims
  that make the sides distinguishable in the in-hand view, and the cross marks the centre, which is what
  the context and the final-distance metric refer to. The box is therefore of the same order as the
  $0.06$\,m keep-out radius, not a point beside it. The three poses are those of the first held-out
  context, one draw of many (\autoref{sec:setup:tasks:aligning}).}
  \label{fig:constraints-aligning}
\end{figure}

\begin{figure}[htpb]
  \centering
  \includegraphics[width=0.62\linewidth]{fig_aligning_contexts}
  \caption[The ten evaluated alignment contexts]{The ten contexts the evaluation replays, on the plane
  of \autoref{fig:constraints-aligning}. Filled marks are the box at the start of an episode, hollow
  marks its target, and a dotted line joins the two poses of one context; the dashed rectangles are the
  regions the environment draws each pose from. Both marks are box \emph{centres}, which is what the
  context and the final-distance metric refer to. One context is additionally drawn with the real
  $0.10$\,m footprints at their recorded yaw, so the marks elsewhere are anchored to the size of the
  object they stand for --- the box is of the same order as the $0.06$\,m keep-out region it has to be
  pushed around, which a figure of marks alone would understate. The yaw of each pose is drawn only for
  that one context but is part of every draw. The ten pushes are ten different pushes: their lengths
  range from $0.41$ to $0.52$\,m and average $0.4530$\,m, the distance \autoref{sec:res:visual} reports
  as the starting one, and a policy that learned one route would satisfy none of them.}
  \label{fig:aligning-contexts}
\end{figure}

\begin{figure}[htpb]
  \centering
  \begin{minipage}{0.44\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_render_aligning}\\[0.3em]
    {\footnotesize MuJoCo simulator view}
  \end{minipage}\hfill
  \begin{minipage}{0.44\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_scene_aligning}\\[0.3em]
    {\footnotesize orthographic task-geometry view}
  \end{minipage}
  \caption[The vision-conditioned alignment task]{The vision-conditioned alignment task. Left: a frame
  rendered by MuJoCo with the manipulator, movable box and coloured target.
  Right: a geometry view of the first held-out context, reconstructed from the scene description
  and its recorded box and target poses. The transparent footprint marks the target pose. These panels
  show the task; the policy itself observes the front and in-hand RGB streams.}
  \label{fig:env-aligning}
\end{figure}

\begin{figure}[htpb]
  \centering
  \begin{minipage}{0.33\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_aligning_camera_overhead}\\[0.3em]
    {\footnotesize front view}
  \end{minipage}\hspace{0.08\linewidth}
  \begin{minipage}{0.33\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_aligning_camera_wrist}\\[0.3em]
    {\footnotesize in-hand view}
  \end{minipage}
  \caption[The two camera observations used for alignment]{The two $96\times96$ RGB observations
  consumed by the visual policy, at the same instant of a recorded expert demonstration, rendered by
  MuJoCo in the alignment environment of this thesis. The two camera placements are those of the D3IL
  task files \parencite{jia2024towards}. The front view shows the
  manipulator and the box on the table; the in-hand view supplies a close view of the box and its
  coloured sides. The instant is late in the demonstration, with the box at its target. These are the two
  image tensors paired with the proprioceptive state at every step.}
  \label{fig:aligning-cameras}
\end{figure}
\srcnote{Expert demonstration \texttt{expert\_rollout\_1.gif}, frame 163: left and right $96\times96$
tiles recorded by \texttt{bp-cam} and \texttt{inhand-cam}; camera definitions in
\texttt{gym\_aligning/envs/aligning.py} and \texttt{panda.xml}. Chosen over rollout 0, frame 60 because the
box is complete and centred in the in-hand tile there; the three recorded rollouts were ranked by the
box's distance from the tile centre and its clearance from the tile border.}

\FloatBarrier

\subsection{Quadrotor Benchmark}\label{sec:setup:tasks:uav}

The quadrotor benchmark was built for this thesis around the Skydio X2 model of MuJoCo Menagerie
\parencite{zakka2022menagerie} (\autoref{sec:method:env:uav}). The vehicle is underactuated and open-loop
unstable (\autoref{sec:bg:quadrotor}), and its plan is tracked by the controller of
\autoref{sec:method:deployment}. Three scenes are evaluated, in increasing difficulty
(\autoref{fig:env-uav}, \autoref{tab:uav-scenes}):

\begin{description}
  \item[UAV-corridor] --- the entry test. Two parallel walls leave a clear width of $1.90$\,m. A sloping
    halfspace, the slide, descends at $14^\circ$ from the upper wall at the entrance to $0.05$\,m below
    the centre line at the exit, and forces every route to descend by $0.25$--$0.49$\,m before the exit.
    The models are trained on the demonstrations of a narrower corridor and never see the slide, so it is
    a test-time constraint in the same sense as the halfspaces of D3IL-avoiding.
  \item[UAV-pillars] --- six cylindrical pillars stand in two rows across three columns, and the vehicle
    must choose a way through them. At test time the projection enforces a keep-out radius larger than
    the pillar the simulator contains, so that the demonstrated routes no longer satisfy the constraint
    set and reaching the goal cleanly requires leaving them (\autoref{sec:setup:data:uav}). The scene is
    described here because it supplies the demonstrations and the trained models, but it is being
    re-evaluated under that enlarged set and yields no result in this thesis (\autoref{sec:res:uav}).
  \item[UAV-s-curve] --- the most difficult test. Two offset corridor segments, joined by a diagonal passage,
    force two sharp changes of direction.
\end{description}

\begin{table}[!htbp]
  \caption[Quadrotor scenes]{Constraints of the quadrotor scenes, in the two forms D3IL-avoiding also
  uses: halfspaces and circular keep-out regions (\autoref{sec:method:env:uav}). In every scene each
  boundary is inflated by the vehicle radius of $0.31$\,m before it reaches the solver, and the tightened
  variants move it a further $0.025$\,m inward.}
  \label{tab:uav-scenes}
  \centering
  \small
  \setlength{\tabcolsep}{8pt}
  \renewcommand{\arraystretch}{1.25}
  \begin{tabularx}{\linewidth}{@{}l>{\raggedright\arraybackslash}p{3.5cm} X@{}}
    \toprule
    Scene & Constraint & Values \\
    \midrule
    UAV-corridor
      & two wall halfspaces & inner faces $y=\pm0.95$\,m, active over $x\in[-2,2]$; clear width $1.90$\,m \\
      & slide halfspace & from $(-2.0,\,0.95)$ to $(2.0,\,-0.05)$, a descent of $14^\circ$ \\
      & wall ends & four disks of radius $0.05$\,m at $x=\pm2$, $y=\pm1$ \\
    \addlinespace[2pt]
    UAV-pillars
      & six disks & physical pillar radius $0.12$\,m at $x\in\{-2,0,2\}$ and $y=\pm0.6$\,m; the
        projection enforces an enlarged keep-out radius of $0.35$\,m at test time \\
    \addlinespace[2pt]
    UAV-s-curve
      & four switched wall halfspaces & feasible $y\in[-1.25,-0.35]$ over $x\in[-3,-0.5]$, and $y\in[0.35,1.25]$ over $x\in[0.5,3]$ \\
      & inner corners & two disks of radius $0.05$\,m at $(\mp0.5,\,\mp0.3)$ \\
    \bottomrule
  \end{tabularx}
\end{table}
\srcnote{\texttt{config/uav\_projection.yaml}: \texttt{corridor\_v2\_slide} (scene
\texttt{scene\_corridor\_v2.xml}), \texttt{pillars\_xl}, \texttt{s\_curve\_hg};
\texttt{inflation.r\_drone: 0.31}; \texttt{enlarge\_constraints: 0.025}. The six
\texttt{sphere\_outside} rows of \texttt{pillars\_xl} carry \texttt{radius: 0.35}; the pillars of
\texttt{quadrotor\_modified.xml} stay at $0.12$\,m, so the enlargement is a constraint and not a change
of the simulated scene. \texttt{pillars\_hg}, the entry that enforced $0.12$, is kept for rollback and is
not active.}

The three scenes use the same two constraint forms as D3IL-avoiding --- halfspaces and circular
keep-out regions --- so the projection solves the same kinds of rows in the air as on the table
(\autoref{fig:constraints-uav}). What differs is that a wall acts only over the span of $x$ on which it
exists, and that every wall, pillar and corner disk is inflated by the vehicle radius of $0.31$\,m before
it reaches the solver, because the vehicle is a body and not a point. The tightened variants move each of
these boundaries a further $0.025$\,m inward, the margin of \ac{DPCC}.
\srcnote{\texttt{config/uav\_projection.yaml}: \texttt{workspace\_bounds} of \texttt{corridor\_v2\_slide},
\texttt{pillars\_hg}, \texttt{s\_curve\_hg}; \texttt{planning\_inflation.r\_drone: 0.31};
\texttt{enlarge\_constraints: 0.025}, applied to the geometric constraints of \texttt{-tightened} variants only
(\texttt{eval\_mix\_uav.py:1019}).}

\begin{figure}[!htbp]
  \centering
  \begin{minipage}{0.78\linewidth}
  \centering
  \begin{minipage}{0.30\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_render_uav_corridor}\\[-0.1em]
    \includegraphics[width=\linewidth]{fig_scene_uav_corridor}\\[0.2em]
    {\footnotesize UAV-corridor}
  \end{minipage}\hfill
  \begin{minipage}{0.30\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_render_uav_pillars}\\[-0.1em]
    \includegraphics[width=\linewidth]{fig_scene_uav_pillars}\\[0.2em]
    {\footnotesize UAV-pillars}
  \end{minipage}\hfill
  \begin{minipage}{0.30\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_render_uav_scurve}\\[-0.1em]
    \includegraphics[width=\linewidth]{fig_scene_uav_scurve}\\[0.2em]
    {\footnotesize UAV-s-curve}
  \end{minipage}
  \end{minipage}
  \caption[The quadrotor scenes]{The three quadrotor scenes, in increasing difficulty. Upper row: each
  scene rendered in MuJoCo from its scene file, with the Skydio X2 at the start of the reference path of
  the demonstrations (green), seen from behind the start. The path shown for the pillars passes the three
  pillar pairs on the left, right and left. Lower row: an orthographic view of the same scene file, which
  shows the whole arrangement at true proportions --- the corridor walls, the six pillars, and the two
  offset passages of UAV-s-curve.}
  \label{fig:env-uav}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{fig_constraints_uav}
  \caption[The constraint sets of the quadrotor scenes]{The constraint set of each quadrotor scene, seen
  from above, in the same terms as \autoref{fig:constraints-avoiding}: halfspaces and circular keep-out
  regions. All three panels are drawn alike. The obstacle itself is the solid line or the red
  disk --- a wall, the slide of UAV-corridor, or a pillar. On UAV-pillars the two are not the same size:
  the small dark core is the pillar the simulator contains, $0.12$\,m, and the pale red disk around it is
  the $0.35$\,m keep-out region the projection enforces at test time, which is what closes the centre
  channel. Grey band: what the vehicle radius of
  $0.31$\,m adds around it, which is the boundary the planned position must keep clear. Dashed: the tightened boundary, a further $0.025$\,m inward. The faint
  wash beyond a wall is the rest of the halfspace it excludes; a wall constrains only the span of $x$ over
  which it exists, which is why that wash stops short of the panel edge. What is left white in each panel
  is where the plan may go.}
  \label{fig:constraints-uav}
\end{figure}

\FloatBarrier

% =============================================================================
\section{Datasets}\label{sec:setup:data}
% =============================================================================

\subsection{Obstacle Avoidance}\label{sec:setup:data:avoiding}

The demonstrations are those of the D3IL obstacle-avoidance task \parencite{jia2024towards}, as used by \ac{DPCC}. Each step
records the commanded and the measured planar end-effector position and the planar increment between
consecutive commanded positions (\autoref{sec:method:env:avoiding}). The dataset holds 96 demonstrations (\autoref{fig:env-avoiding}). None of them satisfies the constraints of top-left-hard, one those of top-right-hard and two those of both-hard (\autoref{tab:avoiding-geometries}), so satisfying the constraints is left to the projection.

\begin{figure}[htpb]
  \centering
  \includegraphics[width=0.90\linewidth]{fig_constraints_avoiding}
  \caption[The demonstrations against the three constraint geometries]{The 96 demonstrations against the
  three constraint geometries of \autoref{tab:avoiding-geometries}, the companion of
  \autoref{fig:expert-aligning} and \autoref{fig:expert-uav}. Shaded: the region excluded by the halfspaces
  and the keep-out disk; dashed: the tightened boundary. The demonstrations (grey) cross the excluded
  regions; 0, 1 and 2 of the 96 satisfy the constraints of the three geometries, which is why satisfying
  them can only be the projection's doing.}
  \label{fig:constraints-avoiding}
\end{figure}


\subsection{Vision-Conditioned Alignment}\label{sec:setup:data:aligning}

The demonstrations are those of the D3IL alignment task \parencite{jia2024towards}: its $900$ training
demonstrations, with the two camera streams rendered by MuJoCo. For this thesis each step is laid out as the nine-dimensional plan of
\autoref{sec:method:env:aligning} --- the three-dimensional increment and the commanded and measured
end-effector positions --- and paired with the two images. Cut into plans of eight steps, they give
$168\,274$ training windows.
\srcnote{Training logs of jobs \texttt{24407}, \texttt{24345}, \texttt{24124}, \texttt{25372}:
\texttt{[ ParityAligningDataset ] 900 episodes, 168274 windows (horizon=8, traj\_dim=9)}.}

The demonstrations stand in the same relation to the constraints as the obstacle-avoidance ones.
\autoref{fig:expert-aligning} draws every recorded context --- the $60$ used for training and the $60$
held out --- as the straight push from the box to its target, which is the shortest route a
demonstration could take. That segment crosses the keep-out region in $106$ of the $120$ contexts, and in
$119$ of them once the region is tightened. The demonstrations were recorded without the region
existing, so the task they teach is precisely the one the constraint forbids; going around it is the
projection's work, not something the policy could have imitated.

\begin{figure}[htpb]
  \centering
  \includegraphics[width=0.62\linewidth]{fig_expert_aligning}
  \caption[The recorded contexts against the alignment constraints]{The $120$ recorded contexts of the
  alignment task over the constraint set of \autoref{fig:constraints-aligning}. Each line joins the
  initial box centre (square) to its target centre (circle); red where that straight push crosses the
  circular keep-out region, green where it clears it. The draw ranges of
  \autoref{sec:setup:tasks:aligning} are visible as the two bands the squares and circles fall in.}
  \label{fig:expert-aligning}
\end{figure}
\dataref{DA\_in\_Paper/data/expert\_paths.json, extract/expert\_paths.py; contexts from
d3il/environments/dataset/data/aligning/\{train,test\}\_contexts.pkl}
\hole{The demonstrated end-effector paths themselves, drawn like \autoref{fig:env-avoiding}, would show
the same thing one step finer --- where the push actually goes rather than where it could go. The state
pickles are on the cluster and not in the repository; one run of the stager brings them back.}

In all three environments, a random tenth of the training windows is held out and used only to select
the checkpoint (\autoref{tab:train}).
\srcnote{\texttt{train\_test\_split: 0.9} in every resolved \texttt{Trainer} configuration.}

\subsection{Quadrotor Benchmark}\label{sec:setup:data:uav}

The demonstrations were generated for this thesis as described in \autoref{sec:method:env:uav}. That
section gives the waypoint-and-arc construction, cosine speed profile, tracking equations, rejection
rule and recorded observation--action pair; \autoref{tab:uav-demos} records only their numerical and
scene-specific parameters. The corridor with the slide reuses the demonstrations
and trained models of the corridor, because the slide is a test-time constraint.

\begin{table}[!htbp]
  \caption[Quadrotor demonstrations]{Generation of the quadrotor demonstrations. Shared by all scenes:
  altitude drawn uniformly from $[0.90,1.30]$\,m per episode, physics step $0.01$\,s, recording every
  third physics step ($0.03$\,s), the nominal gains of the tracking controller, rejection of an episode
  that falls below $0.50$\,m or touches an obstacle on more than the stated fraction of its steps.
  Durations and offsets are drawn uniformly from the stated ranges.}
  \label{tab:uav-demos}
  \centering
  \footnotesize
  \setlength{\tabcolsep}{3pt}
  \renewcommand{\arraystretch}{0.92}
  \begin{tabularx}{\linewidth}{@{}l L L L@{}}
    \toprule
    & UAV-corridor & UAV-pillars & UAV-s-curve \\
    \midrule
    Passage classes & three lanes at $y\in\{-0.12,\,0,\,0.12\}$\,m & sides of the three pillar pairs: (L,L,L), (L,R,L), (R,L,R), (R,R,R) & one \\
    Reference path & straight, $x$ from $-2.8$ to $2.8$\,m & waypoints joined by arcs of radius $\le0.45$\,m & waypoints joined by arcs of radius $\le0.45$\,m \\
    Duration & $[6,10]$\,s & $[10,16]$\,s & $[16,22]$\,s \\
    Lateral offset & start, $[-0.03,0.03]$\,m, centre lane & --- & whole path, $[-0.04,0.04]$\,m \\
    Maximum contact fraction & $2\,\%$ & $0.1\,\%$ & $8\,\%$ \\
    Episodes generated & 500 & 500 & 500 \\
    Episodes accepted & 500 & 475 (18 too low, 7 in contact) & 500 \\
    \bottomrule
  \end{tabularx}
\end{table}
\srcnote{\texttt{uav\_expert\_data\_collect/generator.py} (\texttt{\_build\_traj\_and\_init}: altitude, durations,
offsets; \texttt{SCENE\_MAX\_CONTACT\_FRACTION}; \texttt{Z\_FLOOR\_MARGIN}); \texttt{trajectories.py}
(\texttt{BLEND\_RADIUS}, \texttt{CORRIDOR\_CHANNELS}, \texttt{corridor\_path}); \texttt{dataset\_writer.py}
(\texttt{DATASET\_HZ}); \texttt{quadrotor\_modified.xml} (timestep).}

Where these demonstrations differ from the two manipulation datasets is that they were generated against
the obstacles they fly among, and \autoref{fig:expert-uav} shows what follows from it. On UAV-s-curve the
single route satisfies the constraint set the projection later enforces, tightening included: the
generator threads a passage the scene already contains, so nothing in the data has to be corrected. The
projection is then asked to repair a plan that was never broken, which is why that scene can expose a
limit of the tracking controller but cannot order the projection methods.

UAV-corridor is built the other way, and it is the way the obstacle-avoidance benchmark is built. The
three lanes clear both walls, but every one of them crosses the slide, by $0.24$, $0.36$ and $0.47$\,m
for the left, centre and right lane --- because the slide is introduced at test time and did not exist
when the demonstrations were flown. The scene in which the constraint is new to the data is exactly the
scene in which the projection has something to do.

UAV-pillars was originally the first kind and is being rebuilt into the second. Its demonstrated channels
lie at $|y|=1.11$\,m because the generator derived them from the pillar radius, the rotor reach and a
safety margin, and the constraint set it was first evaluated against enforced the same clearance --- so
all four routes satisfied it and the projection had nothing to repair. The obstacles are therefore
enlarged at test time, from the $0.12$\,m pillar the simulator contains to a keep-out radius of
$0.35$\,m, exactly as the slide and the halfspaces are introduced after the fact in the other two
benchmarks. \autoref{fig:expert-uav} is drawn against that enlarged set: all four demonstrated routes now
cross it, each by $0.15$\,m and on more than a hundred samples of its length, and the centre channel
closes, so a plan that reaches the goal without violating has to leave the demonstrated route. The scene
is re-evaluated under that constraint and reports no result in this draft
(\autoref{sec:res:uav}).

\begin{figure}[htpb]
  \centering
  \includegraphics[width=\linewidth]{fig_expert_uav}
  \caption[The demonstrated routes against the quadrotor constraints]{The reference path of every
  demonstrated route, over the constraint set of \autoref{fig:constraints-uav}: obstacles inflated by the
  vehicle radius, the tightened boundary dashed. One path per passage class of \autoref{tab:uav-demos}
  --- three lanes on UAV-corridor, the four pillar sequences, the single route of UAV-s-curve. Green:
  the path satisfies the constraints; red: it crosses them. The pillars panel is drawn against the
  enlarged test-time keep-out radius of $0.35$\,m and not against the $0.12$\,m pillar the simulator
  contains, which is the constraint set the scene is being re-evaluated under: every demonstrated route
  crosses it, by $0.15$\,m. On each route the vehicle is drawn to scale at
  its tightest moment --- the four rotor disks and the body of \autoref{fig:x2-dimensions} inside the
  $0.31$\,m disk the projection inflates every obstacle by --- because the quadrotor collides with its body
  and not with its centre line: on UAV-corridor that body sits inside the slide, on UAV-pillars it sits
  inside the enlarged keep-out region, and on UAV-s-curve it passes clear. The corridor paths cross the slide and the pillar routes cross the enlarged keep-out regions; both
  constraints are added at test time. These are the paths the
  demonstrations track, drawn from the generator itself; altitude does not enter because every constraint
  here is in the horizontal plane.}
  \label{fig:expert-uav}
\end{figure}
\dataref{DA\_in\_Paper/data/expert\_paths.json, extract/expert\_paths.py; reference paths from
uav\_expert\_data\_collect/trajectories.py, one per class of generator.py \texttt{HOMOTOPY\_CLASSES}}
\hole{The flown demonstrations rather than the reference they track: the per-episode altitude draw and
the tracking error would widen each line into a band, and the $25$ rejected pillar episodes would show
where that band reaches a pillar. The buffers are on the cluster.}

\srcnote{Collection jobs \texttt{21475} (corridor), \texttt{21482} (pillars), \texttt{21476} (s-curve):
\texttt{n\_trials=500 gain=pid\_default}; \texttt{DONE saved=475 rejected=25 \ldots floor=18 contact=7} for pillars,
\texttt{saved=500 rejected=0} for the other two. The trained models load the same counts: buffer shapes
$(500,360,6)$, $(475,560,6)$ and $(500,750,6)$ in the training logs. Gain set \texttt{pid\_default}:
\texttt{generator.py:73}, both scales $1.0$.}

% =============================================================================
\section{Metrics}\label{sec:setup:metrics}
% =============================================================================

\subsection{Obstacle Avoidance}\label{sec:setup:metrics:avoiding}

The metrics are those of \ac{DPCC} \parencite[Sec.~6.1]{romer2025diffusion}: the number of control
steps to reach the goal (steps per episode), the success rate of reaching the goal (fraction of episodes),
the success rate of reaching the goal while satisfying the constraints at every step (fraction of
episodes), and the number of control steps on which a constraint is violated (steps per episode).

To these this thesis adds the time to compute one action (milliseconds, or seconds where a
configuration is slow enough to warrant it), the quantity \ac{DPCC} states for its own
method \parencite[Sec.~6]{romer2025diffusion}. It is measured around the planner at every control step
and covers everything the planner does to produce the action that is executed: generating the $B$
candidate plans, projecting them onto the constraint set, and selecting one. The same measurement is
used in all three environments, and
\autoref{sec:setup:protocol:compute} gives the machine every configuration was measured on.

\subsection{Vision-Conditioned Alignment}\label{sec:setup:metrics:aligning}

Each episode is scored by
\begin{itemize}
  \item the final distance between the box and its target in the table plane (metres), summarised over the
    contexts by its median and its minimum, and the share of the initial distance that was closed,
    $(d\sidx{init}-d\sidx{final})/d\sidx{init}$ in per cent, against an initial distance averaging
    $0.4530$\,m: $100\,\%$ is a box brought onto its target and $0\,\%$ one that was not moved towards it
    at all;
  \item the final orientation of the box relative to the target orientation (degrees);
  \item whether the box was moved at all (a count of contexts);
  \item the number of control steps on which a constraint is violated (steps per episode);
  \item the time to compute one action (milliseconds), as in \autoref{sec:setup:metrics:avoiding}.
\end{itemize}
When generative models are compared, the distance is measured on plans executed without projection, so
that it reflects the generative model alone; when projection methods are compared, it is measured with
projection.
\srcnote{\texttt{per\_rollout\_detail.csv}: \texttt{context\_final\_xy\_dist},
\texttt{context\_final\_box\_angle\_deg} against \texttt{context\_target\_angle\_deg}, \texttt{frozen},
\texttt{n\_violations}, \texttt{avg\_time\_ms}.}

\subsection{Quadrotor Benchmark}\label{sec:setup:metrics:uav}

Each flight is scored by
\begin{itemize}
  \item whether the goal is passed: the vehicle crosses the finish line of its scene. This is the success
    criterion of D3IL-avoiding transferred to the air --- there an episode succeeds when the end effector
    crosses the goal line (\autoref{sec:setup:metrics:avoiding}), here when the vehicle crosses the line
    that closes its route;
  \item whether it is collision-free: no control step on which the vehicle, with its body radius of
    $0.31$\,m, violates a constraint;
  \item success with constraint satisfaction: the goal passed on a collision-free flight, as on
    D3IL-avoiding;
  \item the number of control steps (steps per flight), and the final distance to the goal point of the
    route (metres), which describes where a flight that does not pass the goal ends;
  \item the time to compute one action (milliseconds, \autoref{sec:setup:metrics:avoiding}), split into
    generation and projection.
\end{itemize}

% =============================================================================
\section{Baselines}\label{sec:setup:baselines}
% =============================================================================

Three generative models are compared with the diffusion model of \ac{DPCC} (\emph{Diffusion} in tables):
\emph{instantaneous-velocity matching} (\emph{FM}) \parencite{lipman2023flow}, which learns the
instantaneous velocity of the probability path; \emph{analytic average-velocity matching}
(\emph{MeanFM}) \parencite{geng2025meanflow}, which learns the average velocity over an interval through
an analytic Jacobian--vector-product target; and \emph{consistency-interpolated average-velocity
matching} (\emph{CI-MeanFM}) \parencite{zhang2025alphaflow}, whose consistency step ratio $\alpha$
interpolates the target between the instantaneous velocity at $\alpha=1$ and a stop-gradient prediction
towards the analytic average-velocity objective as $\alpha\to0$. In this thesis $\alpha$ is annealed from
$1$ and held at a final value
$\alpha_{\mathrm{end}}$, which each result states. Two projection methods are compared: the per-step projection of
\ac{DPCC} and endpoint projection \parencite{li2025hardflow}. All generative models share the temporal
U-Net backbone of the baseline, with $4.0$\,M parameters, unless a table states otherwise. Throughout
all figures and tables, the four models are labelled \emph{Diffusion}, \emph{FM}, \emph{MeanFM}, and
\emph{CI-MeanFM}, respectively; running prose retains the full mechanism names.

\subsection{Obstacle Avoidance}\label{sec:setup:baselines:avoiding}

The baseline is \ac{DPCC} in the configuration its authors report: the diffusion model at a step budget
of $\nfe=20$, action loss weight $10$, per-step projection on tightened constraints, and selection by
cumulative projection cost over $B=4$ candidate plans \parencite[Sec.~6.1]{romer2025diffusion}.
\autoref{tab:target} places the published result next to its reproduction in this thesis.

\begin{table}[htpb]
  \caption[The DPCC baseline, published and reproduced]{The DPCC baseline on the obstacle-avoidance
  task: the published result and its reproduction in this thesis, on the metrics of
  \autoref{sec:setup:metrics:avoiding}. S\&C denotes success with constraint satisfaction; time is
  seconds per control step. The spread on the reproduced rows is the standard deviation over episodes,
  pooled over the five training seeds within a geometry and then averaged over the three geometries, so it
  carries both the seed-to-seed and the episode-to-episode variation.}
  \label{tab:target}
  \centering
  \small
  \begin{tabularx}{\linewidth}{@{}L c c c c c@{}}
    \toprule
    Source and protocol & Steps & Success & S\&C & \shortstack{Violating\\steps} & s/step \\
    \midrule
    Published \parencite[Table~1]{romer2025diffusion}
      & $69.0\pm12.9$ & $1.00$ & $0.98$ & $0.0\pm0.3$ & --- \\
    Reproduced, protocol of \ac{DPCC}
      & $70.1\pm9.0$ & $1.000$ & $1.000$ & $0.0\pm0.0$ & $0.553$ \\
    Reproduced, 20 episodes per seed
      & $69.0\pm12.0$ & $0.997$ & $0.983$ & $0.03\pm0.17$ & $0.563$ \\
    \bottomrule
  \end{tabularx}
\end{table}

\dataref{temp/2508/batch\_avoiding\_combined\_20260825\_143212; diffusion $\nfe=20$, cumulative
projection cost, tightened; mean per geometry, then across the three geometries}

The reproduction at the protocol of \ac{DPCC} uses its five training seeds and two episodes per seed and
geometry (\autoref{sec:setup:protocol:avoiding}). The evaluation with twenty episodes per seed completed the
\emph{both-hard} geometry for one training seed rather than five.

\subsection{Vision-Conditioned Alignment}\label{sec:setup:baselines:aligning}

No published result exists for the configuration evaluated here, so there is no external baseline to
reproduce, as there is on D3IL-avoiding. The task itself is D3IL's \parencite{jia2024towards}, which
evaluates imitation-learning policies on it from images: two $96\times96$ RGB views, front and in-hand,
encoded by the ResNet encoder of Diffusion Policy. An episode succeeds there when the box matches the
target pose within a tolerance and its coloured sides match those of the target.

Those reported results say how hard the task is. From images the strongest policy is BESO, a
score-based policy with a history of observations, at $0.359\pm0.031$; next are DDPM-GPT at
$0.316\pm0.026$ and DDPM-ACT, which adds action chunking, at $0.278\pm0.071$. From the state the same task
is solved far more often: VAE-ACT reaches $0.891\pm0.022$ and BESO $0.861\pm0.016$. On obstacle avoidance
from the state BESO reaches $0.950\pm0.015$ \parencite[Table~3]{jia2024towards}. Seen from images, this is a
task that the policies of D3IL finish in roughly a third of episodes, where obstacle avoidance from the
state is close to solved.

That number is the reference for how difficult the task is, not a baseline for this thesis, because
the two set-ups differ in what the network produces and in what happens to it afterwards
(\autoref{tab:va-vs-d3il}). The comparison for the flow-based models is therefore made inside the thesis:
the diffusion model of \ac{DPCC}, trained on the same demonstrations with the same visual encoder,
backbone, horizon and projection, at $\nfe=20$.

\begin{table}[htpb]
  \caption[Alignment set-up against D3IL]{The alignment task as evaluated in D3IL and in this thesis.
  The simulator, the demonstrations and the visual encoder are shared; the policy and the closed loop are
  not.}
  \label{tab:va-vs-d3il}
  \centering
  \small
  \begin{tabularx}{\linewidth}{@{}p{2.6cm} L L@{}}
    \toprule
    & D3IL \parencite{jia2024towards} & This thesis \\
    \midrule
    Network output & actions, executed directly & a plan of $H=8$ steps of action and state \\
    Network        & MLP, GPT and action-chunking transformer heads & the temporal U-Net of the state-based benchmark, $4.0$\,M parameters, conditioned on a $128$-dimensional visual latent (\autoref{sec:method:backbone}) \\
    Closed loop    & the policy's action is applied & receding horizon: the plan is projected onto the constraint set, its first action is applied, and a new plan is generated at the next control step \\
    Constraints    & none & workspace box on the end effector, action box (\autoref{sec:method:env:aligning}) \\
    Scoring        & success rate and entropy of the behaviours & final distance and orientation of the box, violations, time per control step (\autoref{sec:setup:metrics:aligning}) \\
    Reported result & from images, the three strongest policies reach $0.359\pm0.031$, $0.316\pm0.026$ and $0.278\pm0.071$; from the state, $0.891\pm0.022$. On obstacle avoidance from the state, $0.950\pm0.015$ & no success rate is defined; episodes are scored by how close the box ends to its target \\
    \bottomrule
  \end{tabularx}
\end{table}
\FloatBarrier
\srcnote{D3IL: Table~3, image data, Aligning column (BESO $0.359\pm0.031$); appendix, Table~5 and the
camera-view figure ($96\times96$ front and in-hand views, image encoder of Diffusion Policy). Thesis: \texttt{config/aligning-d3il-visual.py},
\texttt{plan\_mix\_visual\_aligning\_*} (\texttt{horizon: 8}, \texttt{mpc\_batch\_size: 4}).}

\subsection{Quadrotor Benchmark}\label{sec:setup:baselines:uav}

The quadrotor benchmark is new, and no learned planner has been evaluated on it before. Its closest
precedent is the quadrotor task distributed with MuJoCo MPC \parencite{howell2022predictive}, which flies
the same Skydio X2 model \parencite{zakka2022menagerie}. That task is described here as it is
distributed, because the benchmark of this thesis starts from its vehicle model and departs from it in
everything else.

\paragraph{The original task.} The scene is a racing course of eight square gates, each a frame of four
capsules with a $1.5$\,m opening, placed at different headings and heights around a closed loop. The
vehicle starts on the floor. A goal marker leads it around the course: it sits at one of twelve fixed
positions, between $0.75$ and $2.25$\,m above the floor, placed so that flying from one to the next passes
through a gate, and it advances to the next position whenever the vehicle comes within $0.5$\,m of it,
returning to the first after the last. The gates do not appear in the objective; passing through them is
a consequence of where the goal positions are placed.

The controller is Predictive Sampling, the sampling-based predictive controller of MuJoCo MPC, and the
simulator itself is its model. At each planning instant it perturbs its current control sequence into 32
candidate sequences of the four rotor thrusts, each parameterised by five spline points, rolls every
candidate forward in MuJoCo over a horizon of $0.5$\,s at a time step of $0.01$\,s, and keeps the
cheapest. The cost of a rollout sums four quadratic terms over the horizon: the distance of the vehicle
from the current goal position, weighted $25$; its linear velocity and its angular velocity, each
weighted $1.25$; and the deviation of each rotor thrust from the hover thrust, weighted $10^{-3}$. The
first control of the kept sequence is applied to the rotors, and planning repeats. Nothing is learned,
no demonstrations are used, and no success rate is defined: the task is an interactive demonstration of
the controller.

\paragraph{This benchmark.} The benchmark of this thesis keeps the vehicle model and the simulator and
changes the three things that make the task a test of constrained generative planning. The plan comes
from a network trained on demonstrations instead of from sampling against the true dynamics; the
environment is a set of walls, pillars and a test-time slide that the plan must not violate, instead of
gates that a goal sequence leads through; and the plan is a sequence of positions tracked by the
cascaded geometric controller (\autoref{sec:method:deployment}), instead of rotor thrusts applied
directly. Each flight is scored for success and for constraint satisfaction (\autoref{sec:setup:metrics:uav}).

\srcnote{MuJoCo MPC, \texttt{mjpc/tasks/quadrotor/task.xml} (\texttt{agent\_planner 0} = Sampling,
\texttt{agent\_horizon 0.5}, \texttt{agent\_timestep 0.01}, \texttt{sampling\_trajectories 32},
\texttt{sampling\_spline\_points 5}, \texttt{sampling\_exploration 0.3}, cost weights Position 25, Lin.~Vel.~1.25, Ang.~Vel.~1.25, Control
$10^{-3}$; twelve goal keyframes at heights $0.75$--$2.25$\,m) and \texttt{quadrotor.cc} (four residual terms, none on the gates;
goal advances within $0.5$\,m and wraps after \texttt{model->nkey}); \texttt{gates.xml}: eight gates of four capsules,
$1.5$\,m opening; X2 start \texttt{pos="0 0 0.1"} in \texttt{quadrotor\_modified.xml}. \texttt{task\_transition} lists thirteen
modes (a manual loop plus twelve stages); automatic switching cycles the twelve keyframes.
Lineage: \texttt{d3il/.../quadrotor/gates.xml} is byte-identical to MuJoCo MPC's, though none of the
three scenes includes it. The quadrotor task is part
of the MuJoCo MPC software; the paper itself does not evaluate it.}

% =============================================================================
\section{Training and Evaluation Protocol}\label{sec:setup:protocol}
% =============================================================================

Every model is trained once per training seed, and the same checkpoint is then evaluated under every step
budget, projection method and selection rule; only the evaluation parameters vary between results. This
section gives the machine everything ran on, then the training parameters, then the evaluation ones.

\subsection{Compute Environment}\label{sec:setup:protocol:compute}

All training and evaluation reported in this thesis ran on a single shared GPU node of the
institute's cluster, scheduled with SLURM. The node carries two 16-core AMD EPYC~7282 processors
(32~physical cores, 64~hardware threads) and eight NVIDIA RTX~A5000 accelerators with 24~GB of
device memory each. \textbf{Every job was allocated exactly one GPU}: no result in this thesis comes
from a distributed or multi-GPU run. The full hardware and software inventory, including package
versions, is given in \autoref{app:repro}, \autoref{tab:compute}.
\srcnote{Slurm\_Codes/sbatch/templates/2026\_04\_30\_job\_template.sh; every log header in Slurm\_Codes/logs/}

The measurements of \autoref{sec:setup:metrics} were taken on this machine; how its properties bear
on reading them is given in \autoref{app:repro}.

\subsection{Training}\label{sec:setup:protocol:training}

\autoref{tab:train} lists the training parameters and what each controls. The same table covers all three
environments; where a value differs between models it is given per model.

\begin{table}[p]
  \caption[Training parameters]{Training parameters. Values are those recorded by the training runs of the
  reported models. The effective batch is the batch size times the number of gradient accumulation
  steps. \enquote{Diffusion (\ac{DPCC})} is the baseline of \autoref{tab:target}: on D3IL-avoiding it is the
  training configuration of \ac{DPCC}, taken over unchanged, and the same configuration is trained on the
  data of the other two environments.}
  \label{tab:train}
  \centering
  \small
  \renewcommand{\arraystretch}{1.28}
  \begin{tabularx}{\linewidth}{@{}p{2.7cm} L p{1.55cm} p{1.55cm} p{1.5cm} p{1.5cm}@{}}
    \toprule
    & & \multicolumn{2}{l}{D3IL-avoiding} & D3IL-aligning & UAV, all scenes \\
    \cmidrule(lr){3-4}
    Parameter & Meaning & Diffusion (\ac{DPCC}), FM & MeanFM, CI-MeanFM & all models & all models \\
    \midrule
    Plan horizon $H$ & steps in one generated plan & 8 & 8 & 8 & 8 \\
    Plan width $d$ & action and state channels per step & 6 & 6 & 9 & 9 \\
    Training steps & optimiser updates & $10^{5}$ & $10^{5}$ & $10^{5}$ & $10^{5}$ \\
    Batch size $\times$ accumulation & plans per update & $8\times2$ & $32\times2$ & $64\times2$ & $8\times2$ \\
    Learning rate & Adam step size & $10^{-4}$ & $5\cdot10^{-4}$ & $2\cdot10^{-4}$ & $10^{-4}$ \\
    Weights evaluated & raw weights, or their exponential moving average (decay $0.995$) & raw & moving average & raw & raw \\
    Action loss weight & weight of the first action in the loss & 10 & 10 & 10 diffusion, 1 others & 1 \\
    Diffusion steps & fixed at training, diffusion only & 20 & --- & 20 & 20 \\
    Transport-time sampling & distribution of $t$ in training & Beta$(1.5,1)$ for FM & logit-normal & as on D3IL-avoiding & as on D3IL-avoiding \\
    Checkpoint & which saved weights are evaluated & \multicolumn{4}{p{6.1cm}}{lowest loss on the held-out $10\,\%$ of windows} \\
    \bottomrule
  \end{tabularx}
\end{table}
\srcnote{Resolved \texttt{Trainer} configurations in the training logs: obstacle avoidance
\texttt{19765} (diffusion), \texttt{19917} (instantaneous-velocity matching, \texttt{a1.5\_b1.0\_aw10}), \texttt{24396}
(analytic average-velocity matching, \texttt{bbunet}), \texttt{25290} (consistency-interpolated average-velocity matching, \texttt{ae0.2}); alignment
\texttt{24407}, \texttt{24345}, \texttt{24124}, \texttt{25372}; quadrotor \texttt{25773}, \texttt{24656},
\texttt{24579}, \texttt{25494} and their pillars and s-curve counterparts, plus the saved
\texttt{trainer\_config.json}. DPCC: \texttt{aux\_repo/dpcc/config/avoiding-d3il.py}, block
\texttt{diffusion}. Checkpoint rule: \texttt{diffuser/utils/training.py:144--150}. Weights evaluated:
\texttt{scripts/eval.py:128} and \texttt{eval\_flow\_matching\_v3\_ode\_selectable.py:167} (raw);
\texttt{eval\_flow\_matching\_v3\_meanflow.py:324} and \texttt{eval\_flow\_matching\_v3\_alphaflow.py:340}
(\texttt{eval\_use\_ema}, default on); \texttt{eval\_mix\_visual\_aligning.py:3058} and
\texttt{eval\_mix\_uav.py:397} (raw).}

\guard{The optimiser settings are not uniform. On D3IL-avoiding analytic average-velocity matching and consistency-interpolated average-velocity matching
were trained with four times the batch and five times the learning rate of instantaneous-velocity matching and the
diffusion baseline; on D3IL-aligning the diffusion model weights the first action ten times more heavily than
the flow-based models. Analytic average-velocity matching and consistency-interpolated average-velocity matching on D3IL-avoiding are also the only models evaluated with
the moving average of their weights. The backbone and its parameter count are matched in both environments; the
training budget per update is not.}

\subsection{Evaluation}\label{sec:setup:protocol:eval}

\autoref{tab:eval} lists the evaluation parameters, which are the quantities \autoref{ch:results} varies or
holds fixed. The subsections after it state, per environment, how many training seeds and how many episodes
a result rests on. D3IL-avoiding is evaluated over five training seeds, so a result there carries the variation across seeds;
D3IL-aligning and the UAV scenes are evaluated with one training seed, seed 6, within the storage and
compute available to this work.

\begin{table}[htpb]
  \caption[Evaluation parameters]{Evaluation parameters, and the value each takes in each environment.
  A result that departs from the default states so.}
  \label{tab:eval}
  \centering
  \small
  \begin{tabularx}{\linewidth}{@{}p{2.6cm} L p{2.0cm} p{1.5cm} p{2.0cm}@{}}
    \toprule
    Parameter & Meaning & D3IL-avoiding & D3IL-aligning & UAV, all scenes \\
    \midrule
    Step budget $\nfe$ & network evaluations per plan; set at inference for the flow-based models, fixed at 20 for diffusion & per result & per result & per result \\
    Candidate plans $B$ & plans generated in parallel at each control step & 4 & 4 & 4 \\
    Selection rule & how one of the $B$ plans is executed: random, cumulative projection cost, or temporal consistency & per result & per result & per result \\
    Projection method & none, per-step or endpoint projection (\autoref{sec:method:proj}) & per result & per result & per result \\
    Activation threshold $\eta$ & fraction of the sampling steps on which projection is active & 0.5 & 0.5 & 0.5 \\
    Tightening & margin by which constraints are shrunk & 0.025 & 0.025 & $0.025$, plus vehicle radius $0.31$\,m \\
    Replanning & a new plan every control step; its first action is executed & every step & every step & every step \\
    Episode limit & control steps before an episode ends unsuccessful & 200 & 400 & 396 / 634 / 871 \\
    Low-level control & how the executed setpoint reaches the actuators & \multicolumn{2}{p{3.5cm}}{Cartesian tracking of the manipulator} & cascaded geometric tracking \\
    \bottomrule
  \end{tabularx}
\end{table}
\srcnote{Plan blocks of \texttt{config/avoiding-d3il.py}, \texttt{config/aligning-d3il-visual.py},
\texttt{config/uav\_mix.py} (\texttt{horizon}, \texttt{batch\_size} / \texttt{mpc\_batch\_size},
\texttt{diffusion\_timestep\_threshold}, \texttt{max\_episode\_length}); \texttt{config/projection\_eval.yaml}
(\texttt{enlarge\_constraints}); \texttt{mix\_uav\_test/eval\_mix\_uav.py:195--200}
(\texttt{SCENE\_MAX\_EPISODE\_LENGTH}: corridor, pillars, s-curve, each $1.2$ times the longest
demonstration). Endpoint-projection results on obstacle avoidance at the budget floor use $\eta=1.0$
(\autoref{fig:projector-cost}).}


\begin{table}[htpb]
  \caption[Evaluation protocol per environment]{What one reported result rests on, per environment.
  D3IL-avoiding is evaluated at two episode counts: the protocol of \ac{DPCC} and an extended one with ten
  times the episodes. Its results are averaged per geometry first and then across the geometries, so a
  geometry covered by fewer seeds does not carry more than its share.}
  \label{tab:protocol}
  \centering
  \small
  \setlength{\tabcolsep}{6pt}
  \renewcommand{\arraystretch}{1.2}
  \begin{tabularx}{\linewidth}{@{}l c L X@{}}
    \toprule
    Environment & Seeds & Episodes per configuration & Averaged over \\
    \midrule
    D3IL-avoiding & 6--10
      & $5\times2 = 10$ per geometry (\ac{DPCC}); $5\times20 = 100$ per geometry (extended)
      & the three constraint geometries \\
    D3IL-aligning & 6
      & 10 contexts $\times$ 1 episode; the diffusion model 3 episodes per context
      & the ten contexts, compared context by context \\
    UAV-corridor & 6 & 12 flights, four on each of the routes L, C and R & the twelve flights \\
    UAV-s-curve & 6 & 10 flights & the ten flights \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsubsection{Obstacle Avoidance}\label{sec:setup:protocol:avoiding}

\ac{DPCC} averages over \enquote{five training seeds and ten test seeds for each constraint set
formulation} \parencite[Sec.~6.1]{romer2025diffusion}; its released evaluation runs two episodes for each of
five training seeds. This thesis keeps those five seeds and reports two episode counts, the second ten times
the first (\autoref{tab:protocol}): every comparison in \autoref{sec:res:state} is made at DPCC's ten
episodes per geometry and then repeated at one hundred.

The reason for reporting both is resolution. At ten episodes a success rate resolves to $0.1$, so a rate of
$1.00$ there is not evidence of a rate of $1.00$: the baseline's perfect rates at ten episodes fall to
$0.95$ and $0.92$ at one hundred, while its tightened configurations move by at most $0.08$
(\autoref{tab:tiers}). The five seeds also carry the variation a single number hides; how much a result
moves with the seed is reported with the results themselves (\autoref{sec:res:avoiding:models}).

\begin{table}[htpb]
  \caption[Effect of the number of episodes]{The baseline at ten and at one hundred episodes per
  geometry: same configurations, same code.}
  \label{tab:tiers}
  \centering
  \begin{tabularx}{\linewidth}{@{}L cc@{}}
    \toprule
    Success with constraint satisfaction & 10 episodes & 100 episodes \\
    \midrule
    Cumulative projection cost, tightened, top-right-hard & 1.00 & 0.95 \\
    Temporal consistency, tightened, top-right-hard       & 1.00 & 0.92 \\
    Untightened configurations                            & \multicolumn{2}{c}{change by up to 0.35} \\
    Tightened configurations                              & \multicolumn{2}{c}{change by at most 0.08} \\
    \bottomrule
  \end{tabularx}
\end{table}

\dataref{DA\_20260819 --- baseline at 10 against 100 episodes per geometry}

The instantaneous-velocity-matching results were produced by the sampler inherited from the baseline, which
draws the initial noise at half the unit scale while instantaneous-velocity matching was trained at unit
scale (\autoref{sec:method:engine}).
\provisional{The instantaneous-velocity models have not been re-evaluated at unit scale.}

\subsubsection{Vision-Conditioned Alignment}\label{sec:setup:protocol:aligning}

The evaluation resets the environment to a fixed, enumerated set of contexts --- the initial box and target
poses --- so every model and projection method starts from the same ten situations
(\autoref{tab:protocol}). Because the contexts are shared, two configurations are compared context by
context: for each context, which of the two leaves the box closer to its target. A result is the number of
contexts in which one configuration is ahead, together with the median final distance over the ten.
\srcnote{\texttt{config/visual\_aligning\_eval.yaml:44--45} (\texttt{n\_contexts},
\texttt{n\_trajectories\_per\_context}); \texttt{eval\_mix\_visual\_aligning.py:1240}; context matching by
initial box and target position, \texttt{DA\_in\_Paper/analysis/va\_results.py}.}
\provisional{D3IL evaluates alignment on sixty contexts; the evaluation on fifty contexts is pending.}

\subsubsection{Quadrotor Benchmark}\label{sec:setup:protocol:uav}

The three scenes are evaluated and reported separately, never pooled: UAV-corridor constrains the commanded
position in addition to the measured one and releases the action box, so its projection configuration is not
the one used on the other two scenes. Every flight is tracked by the cascaded geometric controller
with the brake-to-rest velocity setpoint of \eqref{eq:method:dep:vdes}; the one exception is the controller
comparison of \autoref{sec:res:uav:controller}, which replaces the controller by MuJoCo MPC and says so.
\srcnote{\texttt{Gen15/U16/DA\_20260914\_corridor\_v2\_paper\_full.md} \S1
(\texttt{-bounds\_free-pdes-tightened}, \texttt{pid\_stopgo}, $n=12$);
\texttt{Campaign\_20260907\_five\_missions/DA\_20260912\_pillars\_diffusion\_baseline\_reference.md} \S8;
\texttt{batch\_uav\_20260915\_100816/per\_rollout\_detail.csv}, column \texttt{controller}: all 960 rows on
\texttt{pillars\_hg} and 880 of the 889 rows on \texttt{s\_curve\_hg} are \texttt{pid\_stopgo}, the other 9
\texttt{mjpc}; \texttt{pid\_stopgo} sets \texttt{v\_des} $=0$ at each setpoint,
\texttt{eval\_mix\_uav.py:1598, 1716--1720}.}

\subsection{Guiding Steps of Endpoint Projection}\label{sec:setup:protocol:guiding}

One evaluation parameter decides whether a configuration can carry a statement about endpoint projection at
all, so it is settled here rather than beside the results. Endpoint projection acts on the sampling steps
its activation threshold $\eta$ selects, and of those the terminal step is not a guiding one: there the
predicted endpoint is the finished sample and no later step remains for the network to react to, so what
runs is projection applied to a finished plan. The number that remains is the guiding step count
$n\sidx{guide}(\nfe,\eta)$ of \eqref{eq:method:degen:ngen}, derived in \autoref{sec:method:degenerate}. It
depends on the step budget and the threshold together, so two configurations at the same budget can differ
in it.

Three rules follow, and they hold for every result in \autoref{ch:results}.

\begin{itemize}
  \item A configuration with $n\sidx{guide}=0$ is reported separately and never averaged with
    configurations that have guiding steps: endpoint and per-step projection compute the same plan there,
    so a difference between them at that setting would be noise and not a property of the method.
  \item The count is not assumed. The evaluation records how many programs the projection solved per plan,
    and that number is compared against the prediction before a result is reported.
  \item The threshold is stated with every projected result that departs from the default of
    \autoref{tab:eval}. The endpoint-projection results on D3IL-avoiding at the budget floor use
    $\eta=1.0$, and those on D3IL-aligning $\eta=0.2$ at $\nfe=20$ and $\eta=0.4$ at $\nfe=10$, which give
    three guiding steps at both budgets.
\end{itemize}

Which configurations this excludes, and how closely the recorded program counts follow the prediction, is
reported with the results (\autoref{sec:res:constraints:degenerate}).
