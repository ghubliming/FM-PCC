% =============================================================================
%  CHAPTER 6 -- RESULTS                                                [v3 OWNS]
% -----------------------------------------------------------------------------
%  v3.7 (2026-09-14): one section per environment, in the order of the study,
%  then a section that recaps the two comparisons across all environments:
%    6.1 Obstacle avoidance       (DPCC's benchmark; + caveat on DPCC's metrics, v3.26)
%    6.2 Vision-conditioned alignment (seed 6, 10 contexts)
%    6.3 Quadrotor benchmark      (seed 6; v3.64: built like 6.1/6.2 -- 6.3.1 models BEFORE projection
%                                  (corridor-raw, pillars-raw), 6.3.2 the same cells AFTER per-step projection
%                                  (+ frontier, paths; s-curve kept in one piece), 6.3.3 per-step vs endpoint
%                                  (corridor + pillars endpoint tables); caveat on the tracking controller, v3.26)
%                                  v3.65: every UAV-pillars block is wrapped in pillarsflawed (author: temporarily
%                                  disabled, marked flawed, nothing deleted); corridor gets fig:uav-corridor-altitude
%                                  v3.66: 6.1.2.6 rewritten on matched-candidate K3/K10 cells (tab:avoiding-projectors;
%                                  Fig 6.5 + tab:hf-ladder archived); Table 6.5 drops K=1, diffusion last; Table 6.6
%                                  gets endpoint rows + a conclusion; Table 6.8 + Fig 6.7 at K=20 only; every
%                                  UAV-corridor block blanked (pending R33, corridor v3), v2 text archived in
%                                  withheld/20260922_v3.66_archive
%                                  v3.67: ALL-TIGHTENED (author): every untightened cell withdrawn -- Table 6.6 blank
%                                  (R37), diffusion untightened sentence gone (R2), s-curve plain projected cells
%                                  blank (R38), corridor untightened arm dropped from R33; convention in Ch 5 sec 5.2.1
%                                  v3.68: scene order pillars (v2, the avoiding planner flown; R39) -> corridor (v3,
%                                  tilt + roof; R33) -> s-curve (controller caveat, pilot cells only); the pillars_hg
%                                  blocks and the s-curve grid are archived in withheld/20260922_v3.68_archive
%                                  v3.68 second round: corridor = TWO constraints read separately (tilt = the slide
%                                  leaned -60 deg; hump = the roof), tilt/hump blocks in every corridor table; all
%                                  OUTDATED banners removed from the quadrotor parts (structure final, cells pending)
%                                  v3.68d: 6.3 is one part per scene (pillars / corridor / s-curve), each with
%                                  Generative Models / Projection / Projection Methods; sub-conclusions with the
%                                  model orderings in 6.1.4, 6.2.4, 6.3.5 and (author) 6.4.3; seeds never numbered
%    6.4 Comparison across environments (recap tables only, v3.26)
%  v3.26: no p-values or test names anywhere (author rule, translation table s8 r8).
%  Individual changelog: changelogs/v3.7_20260914_ch5-8_restructure.md
%
%  Labels referenced from v2's Chapter 4 and kept: sec:res:fewstep (6.1.2),
%  sec:res:constraints:degenerate (6.1.5). Others kept for v3's own references.
%  Vocabulary: Writing/Auxiliary/Naming/TRANSLATION_20260914_dev_jargon_to_scientific.md
% =============================================================================

\chapter{Results}\label{ch:results}

The results follow the order of the study: D3IL-avoiding, the benchmark of \ac{DPCC}, first, then
the two environments built for this thesis, D3IL-aligning and the UAV scenes. \autoref{sec:res:summary} brings the three together and
answers, per environment, how the generative models and the projection methods compare. Each
environment is reported in those two parts and closes with the supported model--projection combination.
Each table states its backbone and its evaluation protocol.

% =============================================================================
\section{Obstacle Avoidance}\label{sec:res:state}
% =============================================================================

Every comparison in this section is made at the protocol of \ac{DPCC} --- five training seeds and two
episodes per seed and geometry, ten episodes per geometry (\autoref{sec:setup:protocol:avoiding}).
Results are averaged per geometry and then across the three geometries. At every control step the planner generates
$B=4$ candidate plans and executes one of them, and every model is shown under each of the three rules
that make that choice (\autoref{sec:method:selection}): random selection ($r$), cumulative projection
cost ($c$) and temporal consistency ($t$). The models are then compared at their best rule.

The generative-model comparison first reads the raw outputs with projection switched off. All results
with a projector in the loop are then reported together under projection methods, including the
comparison of the four models through the fixed per-step projector of \ac{DPCC} and the comparison
between per-step and endpoint projection. The section closes with the supported model--projection
combinations.

\subsection{Generative Models}\label{sec:res:avoiding:models}

The four models of \autoref{sec:setup:baselines} are compared, on the shared temporal U-Net: FM,
MeanFM, CI-MeanFM and the diffusion model of \ac{DPCC}. One property of the step budget decides how the
results below are read. $\nfe$ counts network evaluations in both families but is not the same quantity
in them (\autoref{tab:eval}): for the flow-based models it is the number of ODE solver steps, chosen at
inference, and for the baseline the number of denoising steps, fixed when its noise schedule is
discretised during training --- so every diffusion budget is a separately trained checkpoint and every
flow-based budget is the same checkpoint read again. This subsection reads the plans executed without
projection; projected results begin in \autoref{sec:res:avoiding:projection}.

The demonstrations teach the route to the goal. With projection switched off, each of the three
flow-based models reaches it in all thirty episodes at both reported budgets; the diffusion model
reaches it in 28 of thirty at one denoising step and 29 of thirty at twenty
(\autoref{tab:avoiding-raw-models}). Reaching the goal is therefore distinct from satisfying the
test-time constraints: no unprojected row exceeds $0.100$ success with constraint satisfaction.

\subsubsection{Raw Model Outputs}\label{sec:res:avoiding:raw}

With the projector switched off, \autoref{tab:avoiding-raw-models} measures goal arrival, constraint satisfaction and generation time on the same cells as \autoref{tab:avoiding-dpcc-protocol}.

\begin{table}[htpb]
  \caption[The four models without projection]{D3IL-avoiding with the projection switched off: the
  cells of \autoref{tab:avoiding-dpcc-protocol} at the same protocol --- five training seeds $\times$
  two episodes per geometry, averaged per geometry and then over the three; tightened constraints, four
  candidate plans, temporal U-Net of $4.0$\,M parameters. Success is reaching the goal, S\&C is reaching
  it while satisfying the constraints at every step, and violating steps are counted per episode
  (\autoref{sec:setup:metrics:avoiding}); each cell is the mean $\pm$ the standard deviation over the
  five seeds (\autoref{sec:setup:metrics:spread}). The evaluation records one unprojected arm per cell,
  so this table has no counterpart of the rule column: two of the three rules are defined on the
  projection. The intermediate budgets $\nfe=5$ and $10$, which
  exist for MeanFM and the baseline only, are in \autoref{tab:app:avoiding-raw-full}. \baselinemark\ the
  baseline of record at its own budget.}
  \label{tab:avoiding-raw-models}
  \centering
  \footnotesize
  \begin{tabularx}{\linewidth}{@{}L r rrrrr@{}}
    \toprule
    Model & $\nfe$ & Success & S\&C & \shortstack{Violating\\steps} & Steps & ms/step \\
    \midrule
    MeanFM    & 1  & $1.000 \pm 0.000$ & $0.033 \pm 0.075$ & $15.5 \pm 2.1$ & $63.4 \pm 5.6$ & $10.3 \pm 0.4$ \\
    MeanFM    & 2  & $1.000 \pm 0.000$ & $0.033 \pm 0.075$ & $15.5 \pm 0.4$ & $62.7 \pm 1.8$ & $19.3 \pm 0.2$ \\
    \midrule
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 1 & $1.000 \pm 0.000$ & $0.033 \pm 0.075$ & $15.6 \pm 0.7$ & $60.5 \pm 1.5$ & $9.8 \pm 0.2$ \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 2 & $1.000 \pm 0.000$ & $0.033 \pm 0.075$ & $15.7 \pm 0.7$ & $62.9 \pm 1.2$ & $18.7 \pm 0.3$ \\
    \midrule
    FM        & 1  & $1.000 \pm 0.000$ & $0.100 \pm 0.091$ & $16.5 \pm 3.4$ & $68.6 \pm 5.9$ & $9.0 \pm 0.0$ \\
    FM        & 2  & $1.000 \pm 0.000$ & $0.100 \pm 0.091$ & $15.6 \pm 2.6$ & $61.7 \pm 2.2$ & $17.8 \pm 0.0$ \\
    FM        & 20 & $1.000 \pm 0.000$ & $0.133 \pm 0.075$ & $16.5 \pm 2.1$ & $70.2 \pm 6.3$ & $183.5 \pm 0.7$ \\
    \midrule
    Diffusion & 1  & $0.933 \pm 0.149$ & $0.000 \pm 0.000$ & $19.0 \pm 2.9$ & $68.3 \pm 16.1$ & $9.4 \pm 0.1$ \\
    Diffusion & 2  & $1.000 \pm 0.000$ & $0.000 \pm 0.000$ & $20.4 \pm 2.4$ & $61.7 \pm 8.3$ & $18.3 \pm 0.1$ \\
    \mainconfig{\baselinemark\ Diffusion} & \mainconfig{20} & \mainconfig{$0.967 \pm 0.075$} & \mainconfig{$0.100 \pm 0.091$} & \mainconfig{$17.9 \pm 2.5$} & \mainconfig{$67.8 \pm 16.8$} & \mainconfig{$179.3 \pm 1.2$}  \\
    \bottomrule
  \end{tabularx}
\end{table}

\dataref{analysis\_results\_checkpoint/19-09-UAV-Pillars-Exclude/batch\_avoiding\_combined\_20260919\_132703,
unprojected arm (\texttt{variant = diffuser}) of the cells of \autoref{tab:avoiding-dpcc-protocol};
DA\_in\_Paper/analysis/avoiding\_unprojected\_and\_budget20.py}
Read down the success column first. Every flow-based model reaches the goal in every one of its thirty
episodes at every budget in the table, and the baseline in $0.933$ of them at one denoising step and
$0.967$ at twenty. Producing a plan that arrives is not what separates these models on this benchmark.

Read the next column and none of them arrives legally. The highest success with constraint satisfaction
anywhere in the table is $0.133$, instantaneous-velocity matching at twenty evaluations; the two
average-velocity models read $0.033$ at the budgets they are operated at, instantaneous-velocity
matching $0.100$, the baseline $0.100$ at its own budget and $0.000$ at one denoising step. Between
$15.5$ and $19.0$ control steps of an average episode violate a constraint, and raising the budget does
not work that number down: the intermediate budgets in \autoref{tab:app:avoiding-raw-full} hold it
between $14.2$ and $19.1$, and the best and the worst of those counts are both a ten-evaluation row.
The demonstrations all four models learn from cross the
same constraint sets (\autoref{tab:avoiding-geometries}), so a plan that respects them is not something
any of them could have imitated. On this benchmark constraint satisfaction is the projector's work, for
the flow-based objectives exactly as for the diffusion baseline.

The last column prices generation alone. One network evaluation of a flow-based model costs $9.0$ to
$10.3$\,ms per control step and twenty denoising steps of the baseline $179.3$\,ms. Against the projected
table the remainder is the projector: about $8$ of the $18.3$\,ms analytic average-velocity matching
takes at one network evaluation, and about $374$ of the baseline's $553.4$\,ms at its own twenty
denoising steps. The projector runs on
$\lceil \eta\nfe \rceil$ sampling steps (\autoref{sec:setup:protocol:guiding}), which is one at the
flow-based operating budget and ten at the baseline's, so the budget is paid twice --- once in the
network and again in the solver.

\FloatBarrier

Those three columns score what the models produce. \autoref{fig:raw-plans} shows it. The raw plan is to a
planning model what a generated image is to an image model: the fidelity of the sample itself, before any
use is made of it \parencite{ho2020denoising,lipman2023flow,geng2025meanflow}, and the columns above are
that fidelity scored on the task. The one thing to be clear about before reading the figure is that it is
not the executed path --- the executed path is not drawn in it
at all. At every control step the policy returns a plan tensor of shape $(N,8,6)$
(\autoref{tab:train}): $N=4$ candidate plans, each $H=8$ waypoints long, each waypoint carrying a planar
position increment together with the commanded and the measured planar position. What the figure draws,
per candidate, is the \emph{measured} planar position channels of those eight waypoints --- one
eight-point curve --- and the green dot marks the first of the eight, the state the plan is anchored at.
Drawing the fan at every control step would fill the panel with a solid block, so it is drawn at every
fourth; the stored tensor keeps all of them.

Of the eight waypoints exactly one is ever carried out. The selection rule picks one of the four
candidates (\autoref{sec:method:selection}), its first action is applied as a Cartesian increment, and the
other seven waypoints are thrown away and regenerated at the next control step, because the planner
replans every step (\autoref{tab:eval}). So the eight-step plan is never executed as drawn --- and that is
what makes it worth drawing. It is the network's output before the projector repairs it and before the
controller smooths it, the quantity that for an image generative model would be the fidelity of a sample.
Every other figure and table in this section reads the models through something downstream; this one reads
them directly.
\srcnote{Figure mechanics verified against \texttt{scripts/eval.py}, 2026-09-21. The plan tensor is
\texttt{samples.observations} from \texttt{policy(conditions=\{0: obs\}, batch\_size, horizon)}
(\texttt{:348}); the candidate count is \texttt{config/avoiding-d3il.py:71}
(\texttt{\_mpc\_batch}, default $4$; a non-default fan is tagged \texttt{mpc<N>} in the results path and
none of these nine panels carries that tag). Plans are stored at every control step
(\texttt{save\_samples\_every = 1}, \texttt{:278}) and drawn at every
$\lfloor H/2 \rfloor = 4$th (\texttt{plot\_samples\_every}, \texttt{:279}); the draw loop is
\texttt{:410--414}, over \texttt{range(min(batch\_size, 4))} and \texttt{[\_, :horizon, obs\_indices['x'
]]}/\texttt{['y']}, which are channels $2$ and $3$ --- the measured planar position ---
by \texttt{config/projection\_eval.yaml:20}. The green marker is waypoint $0$ of each candidate. The
panel vendored here is the last of the six diagnostic columns, which draws the plans and the constraints
only; the black executed path is drawn in the \emph{fifth} column and is not part of the crop.
Execution: \texttt{:348--356} applies the returned \texttt{action} alone as
\texttt{next\_pos\_des = action + obs[:2]}.}

\begin{figure}[!htbp]
  \centering
  \setlength{\tabcolsep}{2pt}
  \begin{tabular}{@{}c cccc@{}}
    & {\footnotesize\bfseries MeanFM $\downarrow$}
    & {\footnotesize\bfseries CI-MeanFM $\downarrow$}
    & {\footnotesize FM}
    & {\footnotesize Diffusion} \\[0.2em]
    {\footnotesize $\nfe=1$} &
    \begin{minipage}[c]{0.21\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_raw_plans_meanflow_K1}
    \end{minipage} &
    \begin{minipage}[c]{0.21\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_raw_plans_af_K1}
    \end{minipage} &
    \begin{minipage}[c]{0.21\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_raw_plans_fm_K1}
    \end{minipage} &
    \begin{minipage}[c]{0.21\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_raw_plans_diffusion_K1}
    \end{minipage} \\
    {\footnotesize $\nfe=2$} &
    \begin{minipage}[c]{0.21\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_raw_plans_meanflow_K2}
    \end{minipage} &
    \begin{minipage}[c]{0.21\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_raw_plans_af_K2}
    \end{minipage} &
    \begin{minipage}[c]{0.21\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_raw_plans_fm_K2}
    \end{minipage} &
    \begin{minipage}[c]{0.21\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_raw_plans_diffusion_K2}
    \end{minipage} \\
    {\footnotesize $\nfe=20$} &
    \begin{minipage}[c]{0.21\linewidth}\centering{\footnotesize ---}\end{minipage} &
    \begin{minipage}[c]{0.21\linewidth}\centering{\footnotesize ---}\end{minipage} &
    \begin{minipage}[c]{0.21\linewidth}\centering{\footnotesize ---}\end{minipage} &
    \begin{minipage}[c]{0.21\linewidth}\centering
    \includegraphics[width=\linewidth]{fig_raw_plans_diffusion_K20}
    \end{minipage} \\
  \end{tabular}
  \caption[Plans without projection, four models across the step budget]{\textbf{The raw plan tensor, not
  the executed path.} One episode with the projector switched off: each panel overlays the plans the model
  emitted, at every fourth control step, all four candidates, each drawn as the measured planar position
  channels of its eight waypoints, with green at the waypoint the plan is anchored at. The executed path
  is not drawn, and only the first waypoint of one candidate per step was ever carried out. The four
  generative models in the columns, the step budget in the rows, \emph{both-hard}, one training seed.
  One episode per panel, and the same temporal U-Net of $4.0$\,M parameters throughout. The arrows mark the two average-velocity columns,
  and the last row is the diffusion baseline at the budget it was trained for; the flow-based models are
  not run there, which the dashes record.
  \autoref{tab:avoiding-raw-models} aggregates the raw outcomes over five seeds, two episodes per
  geometry and three geometries. Columns:
  analytic average-velocity matching (MeanFM), consistency-interpolated average-velocity matching
  (CI-MeanFM), instantaneous-velocity matching (FM) and the diffusion model of \ac{DPCC} (Diffusion).}
  \label{fig:raw-plans}
\end{figure}
\guard{The four panels added to this figure were rendered by their evaluations at a different dashboard
size from the three older ones, over the same data range. They are cropped so that the plot frame lands on
the pixels the older panels already use, which is why the obstacles read as circles of one size across the
whole matrix rather than as ellipses beside circles. The crop is declared in
\texttt{DA\_in\_Paper/plotting/sources.py} and recorded per panel in \texttt{figures/MANIFEST.md}.
The $\nfe=20$ panel comes from a second evaluation campaign of the same checkpoint, scene, seed and
geometry, run with a different projection threshold; this is the unprojected arm, so no projector ran in
either campaign and the plans drawn are the model's own.}

\dataref{Report\_20260819\_MF\_UNet \S7, figures 6a--6c}

Read that way, the models separate (\autoref{fig:raw-plans}). At one network evaluation the successive
plans of analytic average-velocity matching form a narrow ribbon: consecutive fans overlap, and each of
the eight-point curves is a bounded arc towards the goal. The diffusion baseline at the same budget emits
plans that jump across the workspace from one control step to the next.

That difference does not appear in the motion, and it is worth saying why before the panels are used for
anything. Two things sit between a plan and the arm (\autoref{sec:setup:platforms}): only the
\emph{first} action of the plan is executed, and that action is a Cartesian increment reaching the joints
through inverse kinematics and a joint-space PD controller. A path assembled one first-action at a time,
through an integrator, is smooth whether or not the plans behind it were. The panels are therefore the
only place the incoherence is visible at all. The analytic average-velocity matching panel at two
evaluations shows the ribbon surviving a second evaluation, without reducing the violation count in this
episode.

The two remaining objectives, in the middle two columns, fall between those extremes and in the order
the projected results below give. Instantaneous-velocity matching throws a wide, jagged fan at both budgets, and much of its length
lies inside the forbidden halfspace on the left of the panel rather than in the free corridor.
Consistency-interpolated average-velocity matching is noisy at one evaluation and markedly tighter at two,
where its plans thread the obstacle field in a band comparable to the analytic objective's. Neither is as
tight as analytic average-velocity matching at the same budget, and all three are far from the near-scribble
of the baseline at one evaluation.

The baseline's small-budget panels are two models, one trained at each budget, and the second denoising step is
what turns the scribble into a plan: at $\nfe=2$ its plans form a single coherent bundle from start to
goal. That bundle runs along the wrong side of the constraint. The same evaluation scores the
unprojected arm at $0.00$ success with constraint satisfaction with $20.5$ violating control steps per
episode, so what the second step buys is coherence and not feasibility --- which is the argument for the
projector rather than against the model. At its own twenty steps, in the last row, the baseline draws a
single tight bundle that runs from the start state to the goal line: the budget buys coherence, and the
upper half of that bundle still lies inside the left halfspace. On this panel's own cell the baseline
violates a constraint on $18.5$ control steps of the episode at twenty denoising steps, against $28.0$ at
one --- coherent and infeasible, where the small budget was incoherent and infeasible.
\dataref{job 25966, 2026-09-19, seed 6, both-hard, unprojected}

Each panel is one episode, not an average. The panels come from three evaluation campaigns of the same
scene, seed and geometry, so they share the scene and the seed without being one run of the task. Each
flow-based column is one checkpoint read at both budgets; the three diffusion panels are three
separately trained models, because a diffusion model's step count is fixed when its noise schedule is
discretised during training. The last row is the baseline at its own budget of twenty denoising steps,
the configuration every number in this chapter is measured against. The flow-based models have no panel
there: this benchmark operates them at one and two evaluations.



On that episode's cell the same measurement reads $6.0$ violating control steps for analytic
average-velocity matching at one evaluation, $12.0$ at two, and $28.0$ for the baseline at one.
\dataref{seed 6, both-hard, unprojected arm of the corpus of record}

\subsection{Projection Methods}\label{sec:res:avoiding:projection}

The remaining results all include projection. The first comparison holds the per-step projection of
\ac{DPCC} fixed and varies the generative model. The later comparison holds the generative model fixed
and varies the projection method.

\subsubsection{At the Protocol of DPCC}\label{sec:res:avoiding:dpccprotocol}\label{sec:res:fewstep}

This pass puts the projector back and holds it fixed at the per-step projection of \ac{DPCC}, so that
the generative model is the only thing that varies; the two projection methods are compared against
each other in \autoref{sec:res:avoiding:projectors}. Every number from here to the end of the
subsection is an executed-episode result of that pipeline.

Two marks recur in this table and in the ones after it. The \textbf{operating point}, \selectedmark, is
the configuration this thesis carries forward on the benchmark --- the cheapest one that holds its
success, not the highest number in a column. The \textbf{baseline of record}, \baselinemark, is the
diffusion model of \ac{DPCC} at its own budget of twenty under its own best selection rule, and it is
what every claim of this chapter is measured against. Every row of the table carries all five training
seeds and all three geometries: the budgets are $\nfe\in\{1,2,5,10\}$ for analytic average-velocity
matching, $\{1,2\}$ for the consistency-interpolated model, $\{1,2,20\}$ for instantaneous-velocity
matching and $\{1,2,10,20\}$ for the diffusion baseline, whose step count is fixed at training.

The baseline reproduces its published result (\autoref{tab:avoiding-dpcc-protocol},
\autoref{tab:target}): under cumulative projection cost it reaches a success rate with constraint
satisfaction of $1.000$ in $70.1$ control steps, at $553.4$\,ms per control step. Temporal consistency
reaches the same rate in more steps and random selection falls short of it.

The baseline's own budget is not its own best configuration. At two denoising steps under cumulative
projection cost it also reaches $1.000$, in $61.2$ control steps at $211.4$\,ms, on five seeds and three
geometries --- the same constraint satisfaction in fewer control steps at $38\,\%$ of the time. At ten
steps under temporal
consistency it reaches $1.000$ in $68.7$ control steps at $321.7$\,ms
(\autoref{tab:app:avoiding-dpcc-full}, where every row is held to the same standard). Neither changes
what the baseline of record is, because the baseline of record is the configuration \ac{DPCC} reports
and the one every comparison here is stated against. It does mean that the $30\times$ figure quoted
below is against the published configuration and not against the cheapest diffusion configuration,
which is the two-step checkpoint, at $12\times$.

That two-step time is six times the one-step time for twice the denoising steps, and where the
difference comes from is set out in \autoref{sec:res:avoiding:caveat:noise}.

Two flow-based models clear that row at a single network evaluation, on all three of its axes at once.
Instantaneous-velocity matching reaches $1.000$ in $67.0$ control steps and consistency-interpolated
average-velocity matching $1.000$ in $59.2$, the fewest in the table, each at about $18$\,ms per control
step --- a thirtieth of the baseline's, on the same temporal U-Net of $4.0$\,M parameters. Neither gives
up success with constraint satisfaction, and both finish in fewer control steps. This is the one place
in the thesis where a flow-based model is ahead of the baseline on every reported axis at once rather
than trading one against another.

Analytic average-velocity matching, the model this benchmark is operated with, reaches $0.967$ in $58.6$
control steps at $18.3$\,ms --- against the baseline's best rule, one episode of thirty fewer reaching
the goal within the constraints, $11.5$ fewer control steps, and a thirtieth of the time. What moves that
result is the selection rule: under cumulative projection cost the same model drops to $0.933$ and stalls
at $72.4$ or $97.2$ control steps, the
behaviour described in \autoref{sec:res:ablations}. The three models differ in how exposed they are to
that choice. Instantaneous-velocity matching is the only one that holds $1.000$ under all three rules at
both budgets; consistency-interpolated average-velocity matching holds it under temporal consistency at
both, but falls to $0.900$ under random selection at two evaluations and stalls under cumulative
projection cost in the same way.

\begin{table}[htpb]
  \caption[The generative models at the protocol of DPCC]{D3IL-avoiding at the protocol of \ac{DPCC}:
  five training seeds $\times$ two episodes per geometry, averaged per geometry and then over the three;
  tightened constraints, four candidate plans, temporal U-Net of $4.0$\,M parameters. Rules: $r$ random,
  $c$ cumulative projection cost, $t$ temporal consistency. Columns as in
  \autoref{sec:setup:metrics:avoiding}, each mean $\pm$ the standard deviation over the seeds
  (\autoref{sec:setup:metrics:spread}). The $\nfe=20$ FM row and the native diffusion baseline are
  retained. The budgets between two and twenty, which exist for
  MeanFM and the baseline only, are in \autoref{tab:app:avoiding-dpcc-full}. \selectedmark\ the
  operating point, \baselinemark\ the baseline of record, bold the cells that clear it on all three
  columns. The time per action of the diffusion $\nfe=2$ rows, $191$--$226$\,ms, is six times the
  $\nfe=1$ row's for twice the denoising steps, while the unprojected time of the same cell doubles as
  expected (\autoref{tab:avoiding-raw-models}); the difference is the projection at the first of the two
  denoising steps, explained in the text.}
  \label{tab:avoiding-dpcc-protocol}
  \centering
  \footnotesize
  \begin{tabularx}{\linewidth}{@{}L r c rrr@{}}
    \toprule
    Model & $\nfe$ & Rule & S\&C & Steps & ms/step \\
    \midrule
    MeanFM     & 1 & $r$ & $0.900 \pm 0.149$ & $64.3 \pm 2.6$ & $19.2 \pm 1.6$ \\
                     &    & $c$ & $0.933 \pm 0.091$ & $72.4 \pm 8.4$ & $18.3 \pm 0.5$ \\
                     &    & \mainconfig{\selectedmark\ $t$} & \mainconfig{$0.967 \pm 0.075$} & \mainconfig{$58.6 \pm 2.2$} & \mainconfig{$18.3 \pm 0.2$}  \\
    MeanFM     & 2 & $r$ & $0.967 \pm 0.075$ & $65.1 \pm 2.6$ & $27.6 \pm 0.5$ \\
                     &    & $c$ & $0.933 \pm 0.091$ & $97.2 \pm 4.8$ & $27.1 \pm 0.2$ \\
                     &    & $t$ & $0.967 \pm 0.075$ & $59.4 \pm 2.0$ & $27.7 \pm 0.5$ \\
    \midrule
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 1 & $r$ & $1.000 \pm 0.000$ & $61.5 \pm 0.8$ & $17.9 \pm 0.2$ \\
                     &    & $c$ & $0.967 \pm 0.075$ & $68.4 \pm 2.3$ & $18.0 \pm 0.6$ \\
                     &    & \textbf{$t$} & \textbf{$1.000 \pm 0.000$} & \textbf{$59.2 \pm 1.2$} & \textbf{$18.1 \pm 0.2$} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 2 & $r$ & $0.900 \pm 0.149$ & $63.3 \pm 1.3$ & $27.0 \pm 0.3$ \\
                     &    & $c$ & $0.933 \pm 0.091$ & $89.7 \pm 8.2$ & $26.6 \pm 0.2$ \\
                     &    & $t$ & $1.000 \pm 0.000$ & $60.1 \pm 3.4$ & $27.0 \pm 0.2$ \\
    \midrule
    FM         & 1 & $r$ & $1.000 \pm 0.000$ & $67.9 \pm 1.2$ & $17.4 \pm 0.1$ \\
                     &    & \textbf{$c$} & \textbf{$1.000 \pm 0.000$} & \textbf{$67.0 \pm 0.8$} & \textbf{$17.3 \pm 0.1$} \\
                     &    & $t$ & $1.000 \pm 0.000$ & $68.2 \pm 1.7$ & $17.7 \pm 0.6$ \\
    FM         & 2 & $r$ & $1.000 \pm 0.000$ & $65.7 \pm 0.8$ & $26.2 \pm 0.9$ \\
                     &    & $c$ & $1.000 \pm 0.000$ & $67.0 \pm 3.8$ & $25.5 \pm 0.2$ \\
                     &    & $t$ & $1.000 \pm 0.000$ & $63.8 \pm 1.9$ & $25.9 \pm 0.2$ \\
    FM         & 20 & $r$ & $0.867 \pm 0.075$ & $73.3 \pm 7.9$ & $852.6 \pm 624.9$ \\
                     &    & $c$ & $1.000 \pm 0.000$ & $63.2 \pm 1.5$ & $476.7 \pm 26.0$ \\
                     &    & $t$ & $1.000 \pm 0.000$ & $63.7 \pm 2.2$ & $486.5 \pm 41.4$ \\
    \midrule
    Diffusion  & 1 & $r$ & $0.600 \pm 0.303$ & $67.7 \pm 10.1$ & $33.9 \pm 8.6$ \\
                     &    & $c$ & $0.667 \pm 0.391$ & $72.1 \pm 17.6$ & $33.3 \pm 14.5$ \\
                     &    & $t$ & $0.600 \pm 0.190$ & $73.5 \pm 14.1$ & $33.3 \pm 13.2$ \\
    Diffusion  & 2 & \textbf{$r$} & \textbf{$1.000 \pm 0.000$} & \textbf{$65.1 \pm 1.4$} & \textbf{$191.1 \pm 26.5$} \\
                     &    & \textbf{$c$} & \textbf{$1.000 \pm 0.000$} & \textbf{$61.2 \pm 1.2$} & \textbf{$211.4 \pm 18.8$} \\
                     &    & $t$ & $0.933 \pm 0.091$ & $60.9 \pm 1.5$ & $225.9 \pm 48.5$ \\
    Diffusion  & 20 & $r$ & $0.967 \pm 0.075$ & $74.7 \pm 5.8$ & $573.2 \pm 22.2$ \\
                     &    & \mainconfig{\baselinemark\ $c$} & \mainconfig{$1.000 \pm 0.000$} & \mainconfig{$70.1 \pm 8.2$} & \mainconfig{$553.4 \pm 28.0$}  \\
                     &    & $t$ & $1.000 \pm 0.000$ & $76.1 \pm 15.7$ & $563.0 \pm 25.4$ \\
    \bottomrule
  \end{tabularx}
\end{table}
\dataref{analysis\_results\_checkpoint/19-09-UAV-Pillars-Exclude/batch\_avoiding\_combined\_20260919\_132703;
DA\_in\_Paper/analysis/avoiding\_rules\_by\_protocol.py; DA\_20260919 \S1. The twelve
instantaneous-velocity and consistency-interpolated cells are new in that batch; every other number is
reproduced from the batch it was first reported in. Diffusion $\nfe=2$ (v3.69, R26 closed):
\texttt{batch\_avoiding\_combined\_20260923\_110010}, folder
\texttt{H8\_K2\_T0.5\_Dmodels.GaussianDiffusion\_msgdpccproto}, trainings 26052--26055 + the job-25965
checkpoint, evaluation 26112, 5 seeds $\times$ 3 geometries $\times$ 2 episodes;
DA\_20260923\_diffusion\_K2\_five\_seeds \S3; the batch reproduces every overlapping cell of the 19-09
corpus to the last digit (\S2). Not yet in the store's figure scripts (\autoref{fig:avoiding-tradeoff}
lacks the point). \textbf{The $\nfe=2$ time, checked (author, 23-09; ledger \S29):} threshold $0.5$ in the job log
(26112), fan 4; gate \texttt{t <= 0.5 n\_timesteps} (\texttt{diffuser/models/diffusion.py:186}) projects
$t=1$ and $t=0$; reproduced by job 25966 (19-09, one seed: 226/225/276 ms) and 26112 (five seeds:
211/191/226); \texttt{post\_processing-tightened} of the same cell (final step only) 28.1 ms; SLSQP
\texttt{maxiter 1000} (\texttt{diffuser/sampling/projection.py:142}). R42 closed as explained, not run.}

\subsubsection{Candidate Plans and Selection Rules}\label{sec:res:ablations}

With a single candidate plan the three selection rules return the same plan: success and control steps
are identical in all fifteen paired seed--geometry combinations, for the diffusion baseline, analytic average-velocity matching,
instantaneous-velocity matching and consistency-interpolated average-velocity matching. With four candidates, generation time changes by at most six per
cent, because the four plans are one batched network evaluation, while projection time grows by a factor
of $3.2$ to $4.9$, because the programs are solved one after another on the CPU. Solving them in parallel
would bring analytic average-velocity matching at $\nfe=1$ from $18.1$ to about $11.7$\,ms per control step with all four
candidates kept; this is a prediction from two measured points.
\dataref{DA\_20260827 \S2, \S6, \S10.5}

Cumulative projection cost slows analytic average-velocity matching down on this task. In
\autoref{tab:avoiding-dpcc-protocol} it takes $72.4$ control steps at $\nfe=1$ and $97.2$ at $\nfe=2$,
against $58.6$ and $59.4$ under temporal consistency, and reaches $0.933$ success with constraint
satisfaction against $0.967$: the rule occasionally selects a plan that stalls, and a stalled plan costs
steps without buying constraint satisfaction. The same happens to consistency-interpolated
average-velocity matching, which goes from $60.1$ steps under temporal consistency to $89.7$ under
cumulative projection cost at $\nfe=2$, and it does not happen to the diffusion baseline, for which
cumulative projection cost is the best of the three. Temporal consistency is therefore the selection rule
the flow-based models are operated with on this task.

\subsubsection{The Cost Frontier}\label{sec:res:avoiding:pareto}

At equal success, two costs remain: how many control steps an episode takes, and how long one action takes
to compute. They are not exchangeable --- a plan that arrives in fewer steps but costs half a second per
step is not obviously better than the reverse --- so this section does not add them into one score. It
places every configuration on both axes at once and asks which ones nothing else beats on both
(\autoref{fig:avoiding-tradeoff}).

A configuration is \emph{non-dominated} when no other configuration in the comparison is at the same time
faster per action and shorter in control steps. Only configurations that keep the task solved take part:
a point enters the comparison when its success with constraint satisfaction is within $0.05$ of the best
value in its panel, so that a fast configuration that reaches the goal less reliably cannot appear as a
favourable trade. Reading the figure, colour identifies the model, marker shape the selection rule, the
number beside a point its step budget, and a ring marks each non-dominated point; down and to the left is
better on both axes, which the boxed arrow in the legend repeats. The step axis is broken at $80$: every
configuration that solves the task lies below that line, and the few above it --- the hollow $\nfe=2$
points and one diffusion cell --- are drawn on a compressed scale so that the solved band is not squeezed
into a third of the panel. The figure and
\autoref{tab:avoiding-dpcc-protocol} draw on the same cells: every budget plotted here is a row of that
table, on five training seeds and three geometries.

\begin{figure}[htpb]
  \centering
  \includegraphics[width=\linewidth]{fig_avoiding_tradeoff}
  \caption[Control steps against planner time per control step]{Control steps against the planner time
  to compute one action (generation, projection and selection; not low-level control or simulation),
  at every step budget each model was evaluated at under the protocol of \ac{DPCC} and under
  both selection rules; tightened constraints, temporal U-Net of $4.0$\,M parameters, five training
  seeds $\times$ two episodes per geometry. The budgets are $\nfe \in \{1,2,5,10\}$ for analytic
  average-velocity matching, $\{1,2\}$ for the consistency-interpolated model, $\{1,2,20\}$ for
  instantaneous-velocity matching, and $\{1,10,20\}$ for the diffusion baseline, whose step count is
  fixed at training. Every point carries all five seeds and all three geometries.
  % v3.69: the diffusion K2 five-seed cell (DA_20260923) is in the tables but not yet in this figure --
  % the store's scripts read the 19-09 corpus; promote the 23-09 batch, then rebuild k_ladder + tradeoff.
  Three panels are the constraint geometries and the fourth, headed \emph{All three geometries},
  averages within a geometry and then across the three.
  Colour is the model, marker shape the selection rule, the number beside a point its step budget; a ring
  marks a non-dominated point and the boxed arrow in the legend the direction in which both axes improve.
  The step axis is broken at $80$, marked on both spines: above the break the scale is compressed. Legend:
  instantaneous-velocity matching (FM), analytic average-velocity matching (MeanFM),
  consistency-interpolated average-velocity matching (CI-MeanFM) and the diffusion model of \ac{DPCC}
  (Diffusion). Every model appears under \emph{both} selection rules at each budget --- circles for cumulative projection
  cost, triangles for temporal consistency --- and the frontier is taken over all of them, so the ringed
  point of a model is that model at its better rule.}
  \label{fig:avoiding-tradeoff}
\end{figure}

The horizontal axis is planner time, not the full executed-step time. Full executed-step timing is
available for the separate UAV controller comparison in \autoref{tab:uav-controller}; it was not
recorded for the matched avoiding configurations plotted here.
% DATA GAP (v3.59): an end-to-end time Pareto plot for D3IL-avoiding, and corresponding matched
% comparisons for D3IL-aligning/UAV-corridor, need controller-plus-simulator timing per evaluated cell.
% Existing avg_time records include generation/projection/selection only. See pending ledger D11.

The frontier is occupied by the flow-based models at their smallest budgets, and the three of them at
the lower left reach the baseline's control-step count at one twentieth to one thirtieth of its time per
action. Consistency-interpolated average-velocity matching at one network evaluation sits at the lower
left, $59.2$ control steps at $18.1$\,ms, with analytic average-velocity matching beside it at $58.6$
and $18.3$; no configuration of any model is both faster and shorter than either.
Instantaneous-velocity matching at one and two evaluations lies a little longer in steps at a similar
time. Every diffusion configuration lies to the right by more than an order of magnitude in time, and
the baseline's own operating point, twenty evaluations with cumulative projection cost, is neither the
fastest nor the shortest point in the figure.
What the frontier shows is therefore not a single best configuration but a region --- one or two network
evaluations of a flow-based model --- inside which the remaining choices trade a control step against a
millisecond.

\subsubsection{Caveat: What the Projector Is Handed}\label{sec:res:avoiding:caveat:noise}

One number in \autoref{tab:avoiding-dpcc-protocol} does not scale the way the others do, and it says
something about the two sampler families that the table itself cannot. The diffusion baseline's own cost
doubles from one denoising step to two ($9.4$ to $18.3$\,ms, \autoref{tab:avoiding-raw-models}), as it
should; its projected cost rises six-fold, from $33$ to $191$--$226$\,ms. The projection is the same
program, on the same four candidate plans, against the same tightened constraints.

What changes is the state the projector is handed. The baseline projects at every denoising step with
$t \le 0.5\,\nfe$, which at two steps is both of them: the final step, as at every budget, and the first
--- a plan one denoising step away from Gaussian noise. No larger budget hands the projector such a plan,
because there the gated steps begin in the middle of the schedule; and the flow-based models never do,
because their projection window is the last $\lceil 0.5\,\nfe \rceil$ solver steps, which at two steps
is the terminal one alone. Projecting the final step costs what it costs everywhere: the post-processing
variant of the same cell, which projects nothing but the final step, runs at $28$\,ms per control step,
$10$\,ms above the unprojected plan. The remaining $180$--$190$\,ms is the first-step projection,
about $45$\,ms per candidate against $2$--$6$\,ms for a plan that is nearly denoised. The baseline's
projector is a sequential quadratic program solved iteratively, and its cost is set by how far from
feasible and how rough the plan it starts from is: a plan that is still mostly noise violates every
constraint at once and the dynamics rows between every pair of steps, and the solver spends its
iterations there.

Two things follow, and one does not. First, the measurement is reproducible --- two evaluations on
different days, identical on all five seeds --- and it is a property of the baseline's projection
schedule, not of the hardware or of a mis-set threshold. Second, at equal noise level the two families
cost about the same per solve: at $\nfe=20$ the diffusion baseline projects eleven steps from the middle
of the schedule at about $8.5$\,ms per solve and instantaneous-velocity matching ten at about
$7$\,ms, so what the data attribute the cost to is the noisiness of the projected state, not the
generative family as such. What the data do not yet show is whether a flow-based model's intermediate
plans --- points on a straight path from noise to data --- would project more cheaply than a diffusion
model's at the same step of a two-step schedule. That is the author's conjecture, and it is untested,
because the flow-based window excludes the first step at $\nfe=2$; the test is the same cell with the
projection window opened to both steps on the flow-based side and closed to the final step on the
diffusion side.
\guard{The ordering of this benchmark does not rest on this cell: every flow-based row is cheaper than
every diffusion row at equal success with constraint satisfaction, and the operating points are at one
evaluation, where both families project a single, final step.}
\dataref{Job logs 26112 (22-09, five seeds; \texttt{diffusion\_timestep\_threshold = 0.5}, fan 4) and
25966 (19-09, one seed); gate \texttt{diffuser/models/diffusion.py:186}; solver
\texttt{diffuser/sampling/projection.py:135--142} (SLSQP, \texttt{maxiter 1000}); the flow window
\autoref{sec:setup:protocol:guiding}. Per-solve costs: (projected $-$ unprojected) / (gated steps
$\times$ 4 candidates), from \autoref{tab:avoiding-dpcc-protocol} and \autoref{tab:avoiding-raw-models}:
diffusion $\nfe=1$ $24/4$, $\nfe=2$ $193/8$ (final step $\approx 10/4$ from
\texttt{post\_processing-tightened} 28.1 ms), $\nfe=10$ $221/24$, $\nfe=20$ $374/44$; FM $\nfe=1$ $8/4$,
$\nfe=20$ $293/40$. The two-sided test is R43 (optional) in the ledger.}

\subsubsection{Endpoint and Per-Step Projection}\label{sec:res:avoiding:projectors}

The models above were read twice, unprojected and under the per-step projector. This subsection holds the
model fixed and varies the projector, and it is made on matched work: every comparison gives both methods
four candidate plans and reads them on the same training seed, the same three geometries and the same
episodes, so that the two projectors are priced on the same thing. It is read at two budgets. $\nfe=3$ is
the smallest budget at which endpoint projection has a guiding step
(\autoref{sec:res:constraints:degenerate}), and therefore the smallest at which the two methods differ at
all; at $\nfe=10$ both have several. \autoref{tab:avoiding-projectors} prints what exists. The cells
that do not exist are marked \emph{lacking}, and they are R36 of the run ledger.
\guard{No endpoint-projection evaluation of the diffusion baseline exists on D3IL-avoiding, so no
statement in this subsection extends to it. Recorded in
\texttt{data\_status/PENDING\_20260922\_all\_lacking\_runs.md}.}

\begin{table}[htpb]
  \caption[Endpoint against per-step projection at matched candidates]{Endpoint projection
  \parencite{li2025hardflow} against per-step projection on D3IL-avoiding, both with \textbf{four
  candidate plans}, tightened constraints, the same seed, geometries and episodes on both sides of every
  pair. Per-step projection under temporal consistency ($t$) unless marked; endpoint projection under
  temporal consistency. At $\nfe=3$ endpoint projection ran at activation threshold $1.0$ (two guiding
  steps), at $\nfe=10$ at $0.5$ (four). S\&C, control steps and the time to compute one action are means
  over three geometries $\times$ two episodes, and over the number of training seeds listed. \emph{Lacking}: not evaluated
  with four candidates on both sides (R36). Temporal U-Net of $4.0$\,M parameters.}
  \label{tab:avoiding-projectors}
  \centering
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabularx}{\linewidth}{@{}L r r L rrr@{}}
    \toprule
    Model & $\nfe$ & Seeds & Projection & S\&C & Steps & ms/step \\
    \midrule
    MeanFM    & 3  & 4 & per-step, $t$ & 0.917 & 59.3 & 148 \\
              &    &       & endpoint      & \textbf{1.000} & 58.9 & \textbf{74.5} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 3 & --- & \multicolumn{4}{l}{\emph{lacking}} \\
    FM        & 3  & ---   & \multicolumn{4}{l}{\emph{lacking}} \\
    \midrule
    MeanFM    & 10 & ---   & \multicolumn{4}{l}{\emph{lacking} (endpoint projection has a single-candidate run only)} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 10 & 1 & per-step, $t$ & 0.983 & 60.5 & 386 \\
              &    &       & endpoint      & \textbf{1.000} & 60.7 & \textbf{176} \\
    FM        & 10 & 1     & per-step, $c$ & 1.000 & 63.2 & 199 \\
              &    &       & endpoint      & 1.000 & 64.0 & \textbf{167} \\
    \bottomrule
  \end{tabularx}
\end{table}
\dataref{analysis\_results\_checkpoint/19-09-UAV-Pillars-Exclude/batch\_avoiding\_combined\_20260919\_132703,
\texttt{candidates\_multidimensional\_raw}: $\nfe=3$ from
\texttt{H8\_K3\_Meuler\_T0.5\_A1\_B4\_\ldots MeanFlowODE\_msghfmink\_A1\_mfunet\_s6}, seeds 7--10 (job 25444;
the run tag names seed 6 but the evaluation does not contain it); $\nfe=10$ from
\texttt{H8\_K10\_Meuler\_T0.5\_A0.5\_B4\_\ldots AlphaFlowODE\_msgafon02\_s6} and
\texttt{K10\_thres0.5\_mpc4\_n2\_msgthrmatch}, seed 6; variants \texttt{dpcc-t-tightened} (FM:
\texttt{dpcc-c-tightened}, the only per-step rule in that run) against \texttt{hardflow\_sls-t-tightened};
\texttt{hf\_n\_genuine} $=2$ at $\nfe=3$ and $4$ at $\nfe=10$, \texttt{hf\_batch\_size} $=4$; recounted
2026-09-22 (v3.66).}

At $\nfe=3$, on the one model that has the matched cell, endpoint projection reaches $1.000$ success with
constraint satisfaction with no violating step, in $58.9$ control steps at $74.5$\,ms per step; per-step
projection reaches $0.917$ in $59.3$ steps at $148$\,ms. The difference in success is two episodes of
twenty-four; the difference in time is a factor of two, and it is the projector: the predicted endpoint is
close to the feasible set at every step, so the single solve needs few iterations, where the per-step
projector solves one program per candidate at each active step.

That result does not move the operating point of this task, because $\nfe=3$ is not on its cost frontier.
\autoref{fig:avoiding-tradeoff} and \autoref{tab:avoiding-dpcc-protocol} place the frontier at one and
two network evaluations: per-step projection at $\nfe=1$ already reaches $0.967$ and $1.000$ for the two
average-velocity models at about $18$\,ms per control step, on five seeds. Both $\nfe=3$ points of
\autoref{tab:avoiding-projectors} sit four to eight times to the right of that at the same success, so
each is dominated by a cheaper configuration that solves the task as well. The budget of three is
therefore dropped from this benchmark's operating point, and with it the one budget at which endpoint
projection would be the better projector at a price that matters.

At $\nfe=10$, where the comparison is at the same budget on one training seed, endpoint projection is ahead or
level on the constraint and cheaper on both models that have the cell. On the consistency-interpolated
model it reaches $1.000$ at $176$\,ms against $0.983$ at $386$\,ms; on instantaneous-velocity matching both
reach $1.000$, at $167$ against $199$\,ms. The advantage is under temporal consistency: under random
selection and cumulative projection cost the consistency-interpolated model scores $0.833$ and $0.883$
with endpoint projection against $0.850$ and $0.900$ with per-step projection, level within one episode,
and the cumulative-cost rule lengthens the endpoint paths to $111$ control steps because, with four
candidates, it can select a stalled plan. The analytic average-velocity model has no four-candidate
endpoint run at this budget and is lacking. \guard{Both $\nfe=10$ rows are one training seed and six
episodes per rule; the $\nfe=3$ row is four training seeds. Nothing here is read at the protocol of \ac{DPCC}, and
nothing here changes a selection made in \autoref{sec:res:avoiding:dpccprotocol}.}

The consequence for this task is that endpoint projection is not needed here, and the reason is the budget
rather than the method. At one network evaluation the two average-velocity models already reach $0.967$
and $1.000$ at about $18$\,ms per control step with the per-step projector, and instantaneous-velocity
matching reaches $1.000$ at one and at two (\autoref{tab:avoiding-dpcc-protocol}); there is no shortfall
left for a second projection method to close. Endpoint projection needs sampling steps to guide, and at
one and two evaluations it has none or one (\autoref{sec:method:degenerate},
\autoref{sec:res:constraints:degenerate}); a run at $\nfe=2$ with both methods given the same candidate
plans produced bit-identical rollouts at $2.45$ times the cost. Where it does act, at three and at ten
evaluations, it is the better projector at the same budget, by a margin that is small on the constraint
and large on the time --- and that is the result carried to the tasks that are run at those budgets,
D3IL-aligning and the quadrotor scenes.

In one line: on D3IL-avoiding endpoint projection is of no use, not because it projects worse but because
there is nothing left for it to win --- the per-step projector at one or two evaluations already reaches
$0.967$--$1.000$ at about $18$\,ms, and the budget of three, the first at which endpoint projection can
act, buys no fewer control steps and costs four times more, so it is dominated in the sense of
\autoref{sec:res:avoiding:pareto}. On D3IL-aligning, which needs twenty evaluations, the same method
matters and is the better projector than the per-step one (\autoref{sec:res:aligning:projection}).

\subsubsection{Steps at Which Projection Guides Sampling}\label{sec:res:constraints:degenerate}

The guiding step count is defined in \autoref{sec:method:degenerate} and the convention for reporting a
configuration that has none in \autoref{sec:setup:protocol:guiding}. This section reports what the
evaluations measured against it.

The count holds. With the activation threshold at $1.0$ the evaluation ran with one guiding step at
$\nfe=2$, two at $\nfe=3$ and four at $\nfe=5$, and the number of programs it actually solved grew as
$1:1.51:2.52$ against the predicted $1:1.5:2.5$, so the configurations reported below are the ones the
prediction says they are.

Two settings in this chapter have no guiding step, and neither supports a statement about endpoint
projection. A budget of $\nfe=1$ has none at any threshold, which is why D3IL-avoiding at its operating
budget is reported with per-step projection only and why UAV-corridor has no endpoint row at that budget;
at the default threshold of $0.5$, $\nfe=2$ has none either, which is where the bit-identical rollouts
of \autoref{sec:res:avoiding:projectors} come from and why \autoref{tab:avoiding-projectors} starts at
$\nfe=3$.
\dataref{job 25444 evaluation log, 2026-09-06}

% REMOVED (author, v3.60): this subsubsection carried the twenty-episode numbers in the main text.
% The twenty-episode evaluation is supplementary only -- its K=20 cells do not cover every
% seed--geometry combination -- and now lives entirely in app:avoiding-twenty-episode, with the
% census in tab:coverage-k20. Section 6.1 is reported at the protocol of DPCC throughout. Do not
% reintroduce a twenty-episode result here unless the missing evaluations are completed; see the
% pending-run ledger.

\subsection{Conclusion}\label{sec:res:avoiding:conclusion}

The supported combinations are collected in \autoref{tab:avoiding-conclusion}. At the protocol of
\ac{DPCC}, CI-MeanFM at one network evaluation with temporal consistency and per-step projection
reaches $1.000$ success with constraint satisfaction in $59.2$ control steps and $18.1$\,ms per step,
and MeanFM at the same budget and projector $0.967$ in $58.6$ steps and $18.3$\,ms. The baseline of
record reaches $1.000$ in $70.1$ steps and $553.4$\,ms. Both flow-based rows retain the temporal U-Net
of $4.0$\,M parameters used by the diffusion baseline.

In one line: on D3IL-avoiding the three flow-based models are level with each other and with the baseline
on the outcome --- CI-MeanFM $\approx$ MeanFM $\approx$ FM $\approx$ diffusion at $0.97$--$1.00$ success
with constraint satisfaction --- and separate on the price, the flow-based models solving the task at one
or two network evaluations and about $18$\,ms per control step against the baseline's twenty and
$553$\,ms. What decides the benchmark is the budget and the selection rule, not the flow objective.

\begin{table}[htpb]
  \caption[Supported combinations on D3IL-avoiding]{The supported model--projection combinations on
  D3IL-avoiding and the baseline of record, at the protocol of \ac{DPCC}. All use tightened constraints
  and the temporal U-Net of $4.0$\,M parameters. The episodes column states the number per geometry;
  every row uses five training seeds and all three geometries.}
  \label{tab:avoiding-conclusion}
  \centering
  \footnotesize
  \begin{tabularx}{\linewidth}{@{}L r l c rrrr@{}}
    \toprule
    Model & $\nfe$ & Projection & Rule & Episodes & S\&C & Steps & ms/step \\
    \midrule
    \mainconfig{\selectedmark\ MeanFM} & \mainconfig{1} & \mainconfig{per-step} &
      \mainconfig{$t$} & \mainconfig{10} & \mainconfig{0.967} & \mainconfig{58.6} & \mainconfig{18.3} \\
    CI-MeanFM & 1 & per-step & $t$ & 10 & 1.000 & 59.2 & 18.1 \\
    \mainconfig{\baselinemark\ Diffusion} & \mainconfig{20} & \mainconfig{per-step} &
      \mainconfig{$c$} & \mainconfig{10} & \mainconfig{1.000} & \mainconfig{70.1} & \mainconfig{553.4} \\
    \bottomrule
  \end{tabularx}
\end{table}

Endpoint projection is not needed at either low-budget operating point. At one evaluation it has no
guiding step, and at two it has only one; raising the budget so that it can guide sampling gives up the
low-step advantage that decides this benchmark. D3IL-aligning and the UAV scenes below test endpoint
projection at larger budgets.

The appendix carries more of this benchmark: the executed end-effector paths of the two average-velocity
models at their operated budgets (\autoref{app:avoiding-paths}), every evaluated budget under every
selection rule (\autoref{app:avoiding-budgets}), and the evaluation at twenty episodes per seed
(\autoref{app:avoiding-twenty-episode}).

% =============================================================================
\section{Vision-Conditioned Alignment}\label{sec:res:visual}
% =============================================================================

All results in this section use one training seed and the same ten contexts, and every comparison is made
context by context (\autoref{sec:setup:protocol:aligning}). The box starts on average $0.4530$\,m
from its target. The generative models are compared on the final box-to-target distance of plans executed
without projection, on which the constraint set has no bearing --- the plan is the same plan --- so all
four models are compared at once; constraint violations and the time one action costs are compared in
\autoref{sec:res:aligning:projection}.
\guard{The ten contexts are drawn from the training contexts of the D3IL task, not from its held-out set,
so these results measure how well each model plans for initial poses of the kind it was trained on.}

\subsection{Generative Models}\label{sec:res:aligning:models}

One model moves the box to its target on this task and the other three barely move it at all
(\autoref{tab:va-models}). The median is the column to read there: the spread over the ten contexts is
of the order of the mean itself, because a context in which the box is not touched contributes the whole
starting distance and one in which it is delivered contributes almost nothing. Analytic average-velocity matching ends a median $0.0741$\,m from the target,
from a starting distance of $0.4530$\,m --- it closes $84\,\%$ of the distance --- and it is the only model
that moves the box in every context. The other three end between $0.39$ and $0.46$\,m. The two
flow-based models close $10$--$14\,\%$ of the distance, while the diffusion median is $2\,\%$ farther
away than the box started; each leaves it untouched in some contexts.

The margins behind that ordering are one-sided. Analytic average-velocity matching ends closer than the
diffusion baseline in all ten contexts, by $0.3744$\,m on average, at $0.64$ of the baseline's time per
control step, and closer than instantaneous-velocity matching in nine of ten, by $0.2333$\,m. Between
instantaneous-velocity matching and the baseline there is no ordering: their medians are $0.4085$ and
$0.4619$\,m, and each ends closer in about half the contexts. Instantaneous-velocity matching leaves the
box unmoved in four of ten contexts and the diffusion baseline in five of ten.

\begin{table}[htpb]
  \caption[Alignment: the generative models without projection]{The generative models on
  D3IL-aligning, plans executed without projection (no constraint set enters), at every step budget
  of the ladder $\nfe \in \{2, 10, 20, 100\}$ for the three flow-based models; the diffusion baseline
  at the budget it was trained with, printed last as the sanity reference of every block. Final box-to-target
  distance over the ten contexts, from an initial $0.4530$\,m; in brackets, the share of that distance
  the box was moved towards its target (\autoref{sec:setup:metrics:aligning}); \emph{unmoved}: contexts
  in which the box does not move. A row printed \emph{pending} is a model--budget cell that has not been
  evaluated; $\nfe=20$ is the budget at which all four models have one.
  Mean and time are mean $\pm$ the sample standard deviation over the
  contexts (\autoref{sec:setup:metrics:spread}). Raw cells evaluated over a thirty-context superset are
  restricted to the same ten before they are reduced. Temporal U-Net of $4.0$\,M parameters, FiLM
  conditioning, one training seed.
  \selectedmark\ the selected configuration; \baselinemark\ the diffusion model of \ac{DPCC}.}
  \label{tab:va-models}
  \centering
  \footnotesize
  \begin{tabularx}{\linewidth}{@{}L r rrr r@{}}
    \toprule
    Model & $\nfe$ & Median [m] & Mean [m] & Unmoved & ms/step \\
    \midrule
    MeanFM     & 2  & 0.2779 (39\,\%) & $0.368 \pm 0.301$ & 0/10  & $28.4 \pm 2.0$ \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 2  & 0.3738 (17\,\%) & $0.357 \pm 0.201$ & 2/10  & $28.7 \pm 5.7$ \\
    FM         & 2 & \multicolumn{4}{l}{\emph{pending}} \\
    \midrule
    MeanFM     & 10 & 0.1194 (74\,\%) & $0.173 \pm 0.153$ & 1/10 & $99.2 \pm 1.9$ \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 10 & \multicolumn{4}{l}{\emph{pending}} \\
    FM         & 10 & \multicolumn{4}{l}{\emph{pending}} \\
    \midrule
    \mainconfig{\selectedmark\ MeanFM} & \mainconfig{20} & \mainconfig{0.0741 (84\,\%)} & \mainconfig{$0.093 \pm 0.073$} & \mainconfig{0/10} & \mainconfig{$190.5 \pm 2.0$}  \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 20 & 0.3897 (14\,\%) & $0.325 \pm 0.224$ & 2/10  & $189.1 \pm 5.6$ \\
    FM         & 20 & 0.4085 (10\,\%) & $0.327 \pm 0.160$ & 4/10  & $295.8 \pm 1.9$ \\
    \midrule
    MeanFM     & 100 & 0.0673 (85\,\%) & $0.136 \pm 0.148$ & 0/10 & $892.4 \pm 1.8$ \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 100 & 0.1791 (60\,\%) & $0.327 \pm 0.323$ & 0/10 & $902.2 \pm 1.5$ \\
    FM         & 100 & 0.4247 (6\,\%) & $0.352 \pm 0.180$ & 0/10 & $1425.0 \pm 1.8$ \\
    \midrule
    \mainconfig{\baselinemark\ Diffusion} & \mainconfig{20} & \mainconfig{0.4619 ($-2$\,\%)} & \mainconfig{$0.468 \pm 0.399$} & \mainconfig{5/10} & \mainconfig{$298.3 \pm 2.0$} \\
    \bottomrule
  \end{tabularx}
\end{table}

\dataref{analysis\_results\_checkpoint/15-09/batch\_va2\_20260915\_100754, recomputed by
DA\_in\_Paper/analysis/va\_results.py, every row restricted to the ten shared contexts; agrees with
Gen14 CLOSURE\_20260907 \S1, R.1, R.3 on the paired comparisons. The three \emph{pending} rows are R16 of
the run ledger (v3.62, narrowed v3.63 and v3.66). The $\nfe=1$ rows were dropped by the author at v3.66: the
ladder starts at two. The
diffusion baseline is compared at its trained budget only; its $\nfe=100$ cell (median $0.1849$\,m,
$1521.9$\,ms) exists in the corpus and is not printed, by the author's decision.}
\guard{The ordering rests on analytic average-velocity matching's margins, which hold in nine and ten of
ten contexts; the level result for instantaneous-velocity matching and diffusion is the absence of a
difference at ten contexts, not evidence that they are equal.}

\subsubsection{Step Budget and the Cost Frontier}\label{sec:res:aligning:budget}

Analytic average-velocity matching improves with the budget: its median final distance falls from
$0.2779$\,m at $\nfe=2$ to $0.1194$\,m at $\nfe=10$, $0.0741$\,m at $\nfe=20$, and
$0.0673$\,m at $\nfe=100$ (\autoref{tab:va-models}). The last fivefold increase in evaluations reduces the median distance by
$0.0068$\,m while raising the time per control step from $190.5$ to $892.4$\,ms. Unlike D3IL-avoiding,
where one network evaluation already saturates the task (\autoref{sec:res:state}), alignment rewards
additional evaluations.

The three flow-based models are measured at $\nfe=100$ as well, and the ordering among them is the same as
at twenty: analytic average-velocity matching first ($0.0741$ and $0.0673$\,m), then the
consistency-interpolated model ($0.3897$ and $0.1791$\,m), with instantaneous-velocity matching last
($0.4085$ and $0.4247$\,m). A hundred evaluations more than halve the consistency-interpolated model's
median and leave instantaneous-velocity matching where it was. The diffusion baseline is compared at the
budget it was trained with. The ladder starts at two evaluations: the analytic model closes $39\,\%$ of
the distance there against $84\,\%$ at twenty, so a smaller budget is not an operating point on this
task.
\dataref{va\_results.py; CLOSURE\_20260907 \S1, \S5.1}

Putting the outcome and its price on the same axes gives this task its cost frontier
(\autoref{fig:aligning-tradeoff}). It is not the frontier of \autoref{fig:avoiding-tradeoff} and cannot
be: on D3IL-avoiding every eligible configuration solves the task and only the two costs differ, whereas
here nothing is solved or failed --- what a model delivers is a distance, and the control-step count is
set by the episode limit rather than by arrival (\autoref{tab:eval}). The frontier is therefore the
outcome itself against the time one action costs, and a configuration is on it when nothing else both
ends closer to the target and computes faster.

Three marks of the drawing carry a reading rule. The outcome axis is the share of the starting distance
the box was moved towards its target, the bracketed number of \autoref{tab:va-models}, so the dashed
line at $0\,\%$ is a box that was not moved and $100\,\%$ at the top is a box delivered; two points
that would print on top of each other are displaced by a few pixels, which moves the drawing and not the
data; and the boxed arrow is the direction in which both axes improve.

Four of the ten configurations are on it, and all four are analytic average-velocity matching, at
$\nfe=2$, $10$, $20$ and $100$. Every other model at every budget is dominated, and the way they are dominated
is the result: at $\nfe=20$ the consistency-interpolated objective, instantaneous-velocity matching and
the diffusion baseline all sit close to the line at which the box was not moved at all, at between $189$
and $298$\,ms per control step, while the analytic objective at $\nfe=2$ --- a seventh to a tenth of
that cost --- has already closed two fifths of the distance. MeanFM's own $\nfe=100$ point reduces its median distance
by only $0.0068$\,m at $4.7$ times its $\nfe=20$ cost.

\begin{figure}[htpb]
  \centering
  \includegraphics[width=0.92\linewidth]{fig_aligning_tradeoff}
  \caption[Alignment: outcome against cost]{D3IL-aligning, the share of the starting distance the box
  was moved towards its target, against the time to compute one action, on a log axis; every evaluated
  cell of \autoref{tab:va-models}, no projection, one training seed, median over the ten
  contexts. $0\,\%$, the dashed red line, is a box that was not moved and $100\,\%$ a box delivered;
  the axis is the bracketed share of the table. A ring marks a
  configuration nothing else beats on both axes and the dashed staircase joins them; hollow markers are
  dominated, the number beside a point is its step budget, and the boxed arrow gives the direction in
  which both axes improve. Legend: MeanFM, CI-MeanFM, FM and the diffusion model of \ac{DPCC}. The outcome axis is logarithmic in the distance \emph{left} to the target --- a point sits at
  $-\log_{10}$ of the share not closed --- so the top of the drawing is stretched and $84\,\%$ and $85\,\%$
  are two points, not one; the tick labels remain the share closed.}
  \label{fig:aligning-tradeoff}
\end{figure}
\dataref{analysis\_results\_checkpoint/15-09/batch\_va2\_20260915\_100754, unprojected
(\texttt{diffuser}) on \texttt{combined\_5}; built by
DA\_in\_Paper/plotting/builders/frontier.py. Every point reproduces its row of
\autoref{tab:va-models}; the axis value is $100\times(1 - \text{median}/0.4530)$. The CI-MeanFM and FM
cells at $\nfe=100$ were added to the figure at v3.62; they had been in the table since v3.60.}

\subsubsection{Consistency Interpolation across the Step Budget}\label{sec:res:aligning:consistency}

The consistency-interpolated objective behaves differently from the analytic one on this task: it does
not improve when it is given a larger budget. At two network evaluations the two are level --- each ends
closer in five of the ten contexts. At twenty, where the analytic objective has moved the box most of the
way to its target, the consistency-interpolated one has not moved at all from where it was at two
evaluations, and it ends further away in seven of ten contexts. Raising the budget spends time and buys
nothing, and the same holds for the more aggressive setting $\alpha_{\mathrm{end}}=0.05$.

The numbers behind that are flat where the analytic objective's fall. With
$\alpha_{\mathrm{end}}=0.2$ the median final distance moves from $0.3738$ to $0.3897$\,m between two and
twenty evaluations, and with $\alpha_{\mathrm{end}}=0.05$ from $0.3978$ to $0.4488$\,m, against the analytic
objective's $0.2779$ to $0.0741$\,m.

The mechanism is in the training target. It is built from a stop-gradient copy of the network itself, so
part of the network's own error enters the field it learns, and that part does not shrink when the solver
takes smaller steps (\autoref{sec:method:alphaflow}). A task that rewards more evaluations therefore
exposes it, as this one does; a task already saturated at one evaluation, as D3IL-avoiding is, does not.
\dataref{va\_results.py; Gen14 DA\_20260907 Gate 1; CLOSURE\_20260907 \S3, R.4}
\guard{The level result at $\nfe=2$ is in distance only. Counting the contexts in which any
configuration reaches the target, analytic average-velocity matching succeeds in seven and
consistency-interpolated average-velocity matching in two (CLOSURE\_20260907 R.4).}

\subsection{Projection Methods}\label{sec:res:aligning:projection}

D3IL-aligning is the task on which the generative model needs many network evaluations: analytic
average-velocity matching comes closest to the target at $\nfe=20$ (\autoref{sec:res:aligning:budget}),
so that is the model and the budget the projection is put on. The projected results below are all read
on it.

\paragraph{The activation threshold is compressed, because the budget is high.}
Everything about the threshold on this task follows from one fact: D3IL-aligning is the task that needs a
large step budget, and the default of \ac{DPCC}, $\eta=0.5$, was set for small ones. The projector runs
on the last $\lceil \eta\nfe \rceil$ sampling steps, so at a fixed share $\eta$ its cost grows with the
budget: every extra sampling step in the active window is another program solved, for each of the four
candidate plans, at each of up to four hundred control steps. On D3IL-avoiding at one or two evaluations
the default costs one solve; here, at $\nfe=20$, it would be ten solves per candidate per control step,
and at $\nfe=100$ fifty. At those budgets the default threshold is not a projector one can run, and on
this task it is measured rather than assumed: the same model, constraint set and selection rule at
$\nfe=100$ is run at $\eta=0.5$ and at $\eta=0.1$, and the two are compared on the time per control step
and on the median final distance. \hole{The measured pair on the tightened set, R37: time per control step
and median at $\eta=0.5$ against $\eta=0.1$ at $\nfe=100$. The earlier untightened measurement, withdrawn
at v3.67, gave a factor of $12.7$ in time for $6$\,mm in median distance.}

The threshold is therefore compressed as the budget grows, so that the projector keeps a roughly constant
\emph{number} of guiding steps instead of a constant share: $\eta=0.2$ at $\nfe=20$ and $\eta=0.4$ at
$\nfe=10$, which is three guiding steps at both (\autoref{sec:setup:protocol:guiding}), where on
D3IL-avoiding at one evaluation there were none. Every projected alignment result of this chapter is under
that compressed threshold, and \autoref{tab:va-threshold} is the ladder it was chosen on. What a lower threshold gives up is the early part of sampling, where the sample is still
far from its final shape and the projection has least to hold on to; what it buys is a projected
configuration whose time per action is of the same order as the generation it is attached to.
\dataref{analysis\_results\_checkpoint/15-09/batch\_va2\_20260915\_100754, analytic average-velocity
matching at $\nfe=100$ on \texttt{combined\_5} under random selection:
\texttt{H8\_K100\_Meuler\_T0.5\_\ldots VisualMeanFlow} against
\texttt{H8\_K100\_Meuler\_T0.1\_\ldots VisualMeanFlow}, the same ten contexts. The $\eta$ value is the
\texttt{T} token of the run directory.}

Each row of \autoref{tab:va-threshold} is the analytic average-velocity model under random selection on
the tightened set, the configuration of the $\nfe=100$ comparison, under \emph{both} projectors: the
per-step projector of \ac{DPCC}, which is what the threshold governs, and endpoint projection at the same
threshold, whose guiding steps are the same count minus the terminal one. The number of guiding steps is
the quantity held roughly constant, and the two $\nfe=100$ rows are the measured cost of the uncompressed
default against the compressed one.

\begin{table}[htpb]
  \caption[Alignment: the activation threshold across the budget ladder]{The activation threshold
  $\eta$ on D3IL-aligning, for both projectors: analytic average-velocity matching under random
  selection, tightened constraint set, ten contexts, one training seed. \emph{Guiding}: sampling steps
  the projection acts on minus the terminal one, $\lceil \eta\nfe \rceil - 1$
  (\autoref{sec:setup:protocol:guiding}); the per-step projector solves one program per active step per
  candidate per control step. \emph{ms/step}: mean $\pm$ the sample standard deviation over the contexts,
  unprojected and under each projector; \emph{median}: median final box-to-target distance from
  $0.4530$\,m; \emph{clean}: contexts free of violations. The $\nfe=100$ pair is the same model,
  constraint set and rule at two thresholds. \emph{Pending}: the tightened evaluation of this ladder is
  R37; the untightened one it replaces was withdrawn at v3.67. The chapter's projected results
  (\autoref{tab:va-projection}) are at $\nfe=20$, $\eta=0.2$.}
  \label{tab:va-threshold}
  \centering
  \footnotesize
  \setlength{\tabcolsep}{3pt}
  \begin{tabularx}{\linewidth}{@{}r r r r rrr rrr@{}}
    \toprule
    & & & & \multicolumn{3}{c}{per-step projection} & \multicolumn{3}{c}{endpoint projection} \\
    \cmidrule(lr){5-7}\cmidrule(l){8-10}
    $\nfe$ & $\eta$ & Guiding & \shortstack{ms/step\\unproj.} & ms/step & Median [m] & Clean & ms/step & Median [m] & Clean \\
    \midrule
    2   & 0.5 & 0  & \multicolumn{4}{l}{\emph{pending} (R37)} & \multicolumn{3}{l}{--- no guiding step} \\
    10  & 0.4 & 3  & \multicolumn{7}{l}{\emph{pending} (R37)} \\
    20  & 0.2 & 3  & \multicolumn{7}{l}{\emph{pending} (R37)} \\
    100 & 0.1 & 9  & \multicolumn{7}{l}{\emph{pending} (R37)} \\
    100 & 0.5 & 49 & \multicolumn{7}{l}{\emph{pending} (R37)} \\
    \bottomrule
  \end{tabularx}
\end{table}

\dataref{R37 (\texttt{PENDING\_20260922\_all\_lacking\_runs.md} \S24): \texttt{combined\_5-tightened}, variants
\texttt{diffuser}, \texttt{dpcc-r} and \texttt{hardflow\_sls-r} at the five (budget, threshold) pairs, ten shared
contexts. The untightened ladder this table carried until v3.66 is in the corpus
(\texttt{batch\_va2\_20260915\_100754}, \texttt{combined\_5}) and is no longer cited.}

\hole{Conclusion of the ladder, to be read from the tightened rows: whether the compressed threshold keeps the
projected time within a small multiple of the unprojected time under both projectors while the uncompressed
default does not, and whether the outcome --- clean contexts and median --- moves with the budget under either
projector. On the withdrawn untightened ladder the answer was: the threshold decides the price and not the
result, for both projectors alike; the tightened ladder has to say it again before the chapter does.}


\autoref{tab:va-projection} compares the two projection methods on the analytic average-velocity model,
on tightened constraints, under all three selection rules.

Every cell of that table, and of every other alignment table in this chapter, is the same ten contexts.
Some of the evaluations behind them were run over thirty contexts rather than ten; those ten are a subset
of those thirty in every case, so each cell is restricted to them. The section is then paired on one
context set throughout, as \autoref{sec:setup:protocol:aligning} says it is, rather than setting a
ten-context result beside a thirty-context one.

The $\nfe=2$ block has per-step rows only. At its activation threshold of $0.5$ a two-step budget has no
guiding step --- $\lceil \eta\nfe\rceil - 1 = 0$ (\autoref{sec:setup:protocol:guiding},
\autoref{sec:res:constraints:degenerate}) --- so endpoint projection does not exist there as a method: what
the evaluation ran under that name at $\nfe=2$ is projection after sampling, and it is not printed as an
endpoint row. The smallest budget at which the two methods can be compared is $\nfe=3$.

\begin{table}[htpb]
  \caption[Alignment: the two projection methods]{D3IL-aligning, analytic average-velocity matching
  (MeanFM) with each projection method, tightened constraints, ten contexts. \emph{Violation-free}:
  contexts with no violating control step; \emph{violating steps}: per context; \emph{moved}: contexts in
  which the box moves; \emph{median} and \emph{mean}: the final box-to-target distance over the ten
  contexts, the median carrying in brackets the share of the initial $0.4530$\,m closed, so more is
  better, the mean $\pm$ the sample standard deviation over the contexts; ms/step likewise mean $\pm$
  standard deviation (\autoref{sec:setup:metrics:spread}). Rules $r$, $c$, $t$ as in
  \autoref{tab:avoiding-dpcc-protocol}; one training seed. \selectedmark\ the selected configuration. The
  $\nfe=2$ block carries no endpoint row, for the reason set out before the table.}
  \label{tab:va-projection}
  \centering
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabularx}{\linewidth}{@{}r L c rrrrrr@{}}
    \toprule
    $\nfe$ & Projection & Rule & \shortstack{Violation-\\free} & \shortstack{Violating\\steps} & Moved & Median [m] & Mean [m] & ms/step \\
    \midrule
    20 & none      & ---  & 2/10  & 31.6 & 9/10 & 0.090 (80\,\%) & $0.143 \pm 0.134$ & $172.5 \pm 60.6$ \\
       & per-step  & $r$  & 9/10  & 2.7  & 8/10 & 0.179 (61\,\%) & $0.200 \pm 0.150$ & $265.9 \pm 101.9$ \\
       &           & $c$  & 9/10  & 4.3  & 6/10 & 0.333 (27\,\%) & $0.306 \pm 0.164$ & $325.3 \pm 165.8$ \\
       &           & $t$  & 8/10  & 1.5  & 7/10 & 0.228 (50\,\%) & $0.240 \pm 0.165$ & $323.2 \pm 175.0$ \\
       & \mainconfig{endpoint}  & \mainconfig{\selectedmark\ $r$} & \mainconfig{10/10} & \mainconfig{0.0} & \mainconfig{8/10} & \mainconfig{0.197 (57\,\%)} & \mainconfig{$0.211 \pm 0.144$} & \mainconfig{$275.3 \pm 102.4$}  \\
       &           & $c$  & 10/10 & 0.0  & 6/10 & 0.323 (29\,\%) & $0.286 \pm 0.190$ & $282.3 \pm 104.1$ \\
       &           & $t$  & 9/10  & 0.6  & 7/10 & 0.230 (49\,\%) & $0.255 \pm 0.154$ & $301.5 \pm 128.6$ \\
    \midrule
    10 & none      & ---  & 1/10  & 35.5 & 8/10 & 0.142 (69\,\%) & $0.199 \pm 0.179$ & $89.7 \pm 31.5$ \\
       & per-step  & $r$  & 7/10  & 12.3 & 8/10 & 0.434 (4\,\%) & $0.420 \pm 0.352$ & $517.5 \pm 344.3$ \\
       &           & $c$  & 6/10  & 21.3 & 6/10 & 0.411 (9\,\%) & $0.352 \pm 0.174$ & $524.3 \pm 417.2$ \\
       &           & $t$  & 7/10  & 5.0  & 4/10 & 0.408 (10\,\%) & $0.271 \pm 0.217$ & $433.5 \pm 224.6$ \\
       & endpoint  & $r$  & 10/10 & 0.0  & 8/10 & 0.242 (47\,\%) & $0.236 \pm 0.126$ & $192.5 \pm 72.3$ \\
       &           & $c$  & 8/10  & 0.6  & 4/10 & 0.425 (6\,\%) & $0.307 \pm 0.198$ & $204.2 \pm 79.4$ \\
       &           & $t$  & 8/10  & 2.3  & 6/10 & 0.255 (44\,\%) & $0.284 \pm 0.161$ & $222.1 \pm 99.0$ \\
    \midrule
    2  & none      & ---  & 5/10  & 46.1 & 8/10 & 0.320 (29\,\%) & $0.323 \pm 0.279$ & $24.6 \pm 8.6$ \\
       & per-step  & $r$  & 8/10  & 11.6 & 9/10 & 0.275 (39\,\%) & $0.286 \pm 0.210$ & $46.2 \pm 18.4$ \\
       &           & $c$  & 8/10  & 6.5  & 5/10 & 0.434 (4\,\%) & $0.329 \pm 0.186$ & $47.3 \pm 19.1$ \\
       &           & $t$  & 10/10 & 0.0  & 7/10 & 0.221 (51\,\%) & $0.250 \pm 0.177$ & $43.0 \pm 16.0$ \\
       & endpoint  & \multicolumn{7}{l}{--- none: no guiding step at $\nfe=2$ (\autoref{sec:setup:protocol:guiding})} \\
    \bottomrule
  \end{tabularx}
\end{table}
\dataref{analysis\_results\_checkpoint/15-09/batch\_va2\_20260915\_100754, \texttt{mf} cells at
\texttt{K10\_T0.4} and \texttt{K20\_T0.2}, geometry \texttt{combined\_5-tightened}, variants
\texttt{dpcc-\{r,c,t\}} and \texttt{hardflow\_sls-\{r,c,t\}}; the $\nfe=2$ block \texttt{K2\_T0.5}
\texttt{dpcc-\{r,c,t\}} only --- its \texttt{hardflow\_new-\{r,c,t\}} cells (0 guiding steps, projection
after sampling) exist in the corpus and are not printed (author, v3.68); agrees with CLOSURE\_20260907 \S2, R.2}

At $\nfe=20$, endpoint projection keeps all ten contexts free of violations with random selection and
with cumulative projection cost, and nine with temporal consistency; per-step projection keeps nine, nine
and eight. Together with endpoint projection and random selection at $\nfe=10$, these are the only
configurations on D3IL-aligning that keep every context free of violations while the box is still moved.
The time per control step is the same for both methods at this budget, $275$ against $266$\,ms with random
selection: what endpoint projection adds at $\nfe=20$ is the removal of the last violations.

At $\nfe=10$ the difference is larger. Endpoint projection with random selection again keeps all ten
contexts free of violations, where per-step projection keeps seven, and it takes $193$ against $518$\,ms
per control step, less in every context whose time differs. With the other two rules it is faster by
$320$ and $211$\,ms per control step and keeps at least as many contexts free of violations.

Of the two budgets, $\nfe=20$ is the operating point, because the generative model itself is closest to
the target there; $\nfe=10$ reaches the same constraint satisfaction with endpoint projection in
$70\,\%$ of the time.
\provisional{Repeated evaluations of configurations with projection vary by about $0.4$\,m in final
distance, so the median distances of the projected rows are not ranked against each other.}

The two-evaluation block says one thing: the budget at which this task can be run cheaply is not the
budget at which the generative model solves it. Per-step projection under temporal consistency keeps all
ten contexts free of violations there at $43$\,ms per control step, which nothing at the two larger
budgets does at that price, but the median ends $0.22$--$0.43$\,m from the target, against $0.09$\,m
unprojected at $\nfe=20$.

\subsubsection{Endpoint Projection across the Models}\label{sec:res:aligning:projection:models}

The comparison above is one generative model. \autoref{tab:va-projection-models} runs it across the
models at the operating point this section has settled on --- $\nfe=20$ and the compressed threshold
$\eta=0.2$ --- under every selection rule, and it holds in the same direction. The budget ladder below
twenty was the subject of \autoref{tab:va-projection} and \autoref{tab:va-threshold}; it is not repeated
here, because the model that solves the task does so at twenty and the threshold study has already priced
the other budgets.

That table is as complete as this evaluation allows, and it is worth saying exactly where it stops. Two
flow-based models carry \emph{both} projectors at $\nfe=20$ on the tightened set, so for them the
pre-projection comparison of \autoref{tab:va-models} and the post-projection comparison here rest on the
same cells; the consistency-interpolated model has no endpoint cell. The diffusion baseline carries
neither: it has no tightened per-step cell on this task, and endpoint projection is not defined for it. A
four-model comparison before and after projection is therefore not something this data can be reduced to,
and the two missing cells are the pending runs that would change that (R2, R35).
\guard{The diffusion baseline has no projected cell on the tightened alignment constraint set under
either projection method, so nothing in this subsection or in \autoref{tab:va-projection} compares a
projector against the baseline on this task --- the comparison is between the two projectors, on the
flow-based models. Recorded as R2 in
\texttt{data\_status/PENDING\_20260922\_all\_lacking\_runs.md}; verified against the corpus 2026-09-21.}
Six comparisons run the two methods on the same model, the same budget and the same rule. Endpoint
projection keeps more contexts free of violations in five of them and per-step projection in one, the
exception being instantaneous-velocity matching under temporal consistency, by one context.

That is the reverse of D3IL-avoiding, and the reason is the budget rather than the task. This task is
run at ten and twenty network evaluations because the generative model needs them
(\autoref{sec:res:aligning:budget}), so the endpoint solve has several guiding steps to act on, where on
D3IL-avoiding at one evaluation it had none (\autoref{sec:res:avoiding:projection}). Given steps to act
on, the method does what it is for: it removes violations from the plan before the plan is finished,
rather than correcting each step after the fact.

What it does not do is dominate. Its advantage is on the constraint and not on the task: across the same
six comparisons it moves the box in more contexts than per-step projection in one, in as many in four,
and in fewer in one, and its median final distances are level. Nor is it cheaper at this budget: on the
analytic model it costs $0.87$ to $1.03$ of per-step projection's time per control step, on
instantaneous-velocity matching $1.13$ to $1.23$ of it. The honest summary is that endpoint projection is
the better projector on this task at the budget the task is run at, by a margin that is consistent rather
than large on the constraint, and at a comparable price.

\begin{table}[htpb]
  \caption[Alignment: the two projection methods across the models]{D3IL-aligning, the two projection
  methods on the three flow-based models and the diffusion baseline at the operating point, $\nfe=20$
  with the compressed threshold $\eta=0.2$, tightened constraints, one training seed. Columns as in
  \autoref{tab:va-projection}, median and mean of the final distance both printed; the same ten contexts
  in every row. The analytic average-velocity rows
  repeat the $\nfe=20$ block of \autoref{tab:va-projection} so the comparison can be read in one place.
  \emph{Pending}: not evaluated --- endpoint projection on the consistency-interpolated model (R35) and
  the per-step projector on the diffusion baseline on the tightened set (R2); endpoint projection is
  not defined for the diffusion sampler. \selectedmark\ the selected
  configuration, as in \autoref{tab:va-projection}; \baselinemark\ the diffusion model of \ac{DPCC}.}
  \label{tab:va-projection-models}
  \centering
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabularx}{\linewidth}{@{}L r c rrrrrr@{}}
    \toprule
    Model & $\nfe$ & Rule & \shortstack{Violation-\\free} & \shortstack{Violating\\steps} & Moved & Median [m] & Mean [m] & ms/step \\
    \midrule
    \multicolumn{9}{@{}l}{\emph{per-step projection}}\\
    MeanFM    & 20 & $r$ & 9/10  & 2.7  & 8/10 & 0.179 (61\,\%) & $0.200 \pm 0.150$ & $265.9 \pm 101.9$ \\
              &    & $c$ & 9/10  & 4.3  & 6/10 & 0.333 (27\,\%) & $0.306 \pm 0.164$ & $325.3 \pm 165.8$ \\
              &    & $t$ & 8/10  & 1.5  & 7/10 & 0.228 (50\,\%) & $0.240 \pm 0.165$ & $323.2 \pm 175.0$ \\
    CI-MeanFM & 20 & $r$ & 4/10  & 44.6 & 8/10 & 0.429 (5\,\%) & $0.432 \pm 0.182$  & $415.8 \pm 268.0$ \\
              &    & $c$ & 5/10  & 10.7 & 5/10 & 0.443 (2\,\%) & $0.388 \pm 0.131$  & $311.4 \pm 130.1$ \\
              &    & $t$ & 6/10  & 23.2 & 7/10 & 0.422 (7\,\%) & $0.353 \pm 0.153$  & $320.5 \pm 153.3$ \\
    FM        & 20 & $r$ & 5/10  & 14.3 & 3/10 & 0.425 (6\,\%) & $0.409 \pm 0.112$  & $409.7 \pm 147.9$ \\
              &    & $c$ & 4/10  & 14.9 & 2/10 & 0.425 (6\,\%) & $0.409 \pm 0.111$  & $406.8 \pm 149.5$ \\
              &    & $t$ & 6/10  & 12.8 & 3/10 & 0.425 (6\,\%) & $0.411 \pm 0.111$  & $402.0 \pm 146.4$ \\
    \mainconfig{\baselinemark\ Diffusion} & \mainconfig{20} & \multicolumn{7}{l}{\mainconfig{\emph{pending} (R2)}} \\
    \midrule
    \multicolumn{9}{@{}l}{\emph{endpoint projection}}\\
    \mainconfig{\selectedmark\ MeanFM} & \mainconfig{20} & \mainconfig{$r$} & \mainconfig{10/10} & \mainconfig{0.0} & \mainconfig{8/10} & \mainconfig{0.197 (57\,\%)} & \mainconfig{$0.211 \pm 0.144$} & \mainconfig{$275.3 \pm 102.4$}  \\
              &    & $c$ & 10/10 & 0.0  & 6/10 & 0.323 (29\,\%) & $0.286 \pm 0.190$ & $282.3 \pm 104.1$ \\
              &    & $t$ & 9/10  & 0.6  & 7/10 & 0.230 (49\,\%) & $0.255 \pm 0.154$ & $301.5 \pm 128.6$ \\
    CI-MeanFM & 20 & \multicolumn{7}{l}{\emph{pending} (R35)} \\
    FM        & 20 & $r$ & 6/10  & 12.0 & 3/10 & 0.443 (2\,\%) & $0.398 \pm 0.132$  & $498.0 \pm 210.4$ \\
              &    & $c$ & 5/10  & 10.4 & 3/10 & 0.425 (6\,\%) & $0.407 \pm 0.109$  & $502.4 \pm 212.0$ \\
              &    & $t$ & 5/10  & 7.5  & 2/10 & 0.425 (6\,\%) & $0.410 \pm 0.110$  & $452.4 \pm 193.4$ \\
    \mainconfig{\baselinemark\ Diffusion} & \mainconfig{20} & \multicolumn{7}{l}{\mainconfig{--- (not defined for the diffusion sampler)}} \\
    \bottomrule
  \end{tabularx}
\end{table}

\dataref{analysis\_results\_checkpoint/15-09/batch\_va2\_20260915\_100754,
\texttt{per\_rollout\_detail}; geometry \texttt{combined\_5-tightened}; cells \texttt{mf}, \texttt{af}
and \texttt{fm} at \texttt{K20\_T0.2}, variants \texttt{dpcc-\{r,c,t\}} against
\texttt{hardflow\_sls-\{r,c,t\}}; the $\nfe=2$ and $\nfe=10$ rows of earlier versions are in
\autoref{tab:va-projection} (MeanFM) and, for CI-MeanFM at $\nfe=2$, in the corpus only (v3.66)}
\guard{Consistency-interpolated average-velocity matching and instantaneous-velocity matching do not
move the box on this task at $\nfe=20$ (\autoref{tab:va-models}), so their rows compare the two
projectors on a plan that does not solve the task. That is what makes them a clean read on the
projector --- the constraint columns are not confounded by how far the box travelled --- and it is also
why the median-distance column of those rows carries no information: every cell sits within a few
centimetres of the starting $0.4530$\,m. The analytic average-velocity rows are the opposite case and are
the ones to read for the median.}

The diffusion baseline has no projected cell on the tightened set at $\nfe=20$ (R2), so no constraint
comparison between the baseline and the flow-based models is made on this task until it does.

The medians of \autoref{tab:va-projection} also hide which contexts changed. The per-context records
show three differences that the medians cannot show.

The constraint column turns over almost completely: eight of the ten contexts carry a violating step
without a projector, one still does under per-step projection, and none does under endpoint projection.
The distances do not turn over with it --- they move around, and mostly in the wrong direction.

The cost of that turnover falls on the contexts the unprojected arm had already delivered. Context~1 ends
$0.065$\,m from the target unprojected and $0.21$ or $0.25$\,m with a projector; context~8 goes from
$0.099$ to $0.28$\,m; context~9 is pushed to within $0.029$\,m unprojected and is then not moved at all.
What the projector pays for constraint satisfaction, it pays in the contexts that were nearly solved.

The exchange is not one-sided. Context~10 improves from $0.143$ to $0.022$\,m and context~4 from
$0.262$ to $0.147$\,m under the same projectors, so what the median reports as a rise from $0.090$ to
$0.197$\,m is not a uniform degradation but a redistribution --- three contexts much worse, two clearly
better, and one, context~7, that no configuration on this task moves at all.
\dataref{analysis\_results\_checkpoint/15-09/batch\_va2\_20260915\_100754,
\texttt{per\_rollout\_detail}, geometry \texttt{combined\_5-tightened}, MeanFM at $\nfe=20$ under
\texttt{diffuser}, \texttt{dpcc-r} and \texttt{hardflow\_sls-r}; the preserved
\texttt{DA\_in\_Paper/figures/da/fig\_aligning\_outcomes.svg} draws the same ten records.}

The projected configurations of \autoref{tab:va-projection-models} are compared on the two axes of
\autoref{fig:aligning-tradeoff} in \autoref{fig:aligning-projected-tradeoff}, and what separates the two
drawings is said on the page. Every point of the second is a plan \emph{after} projection on the tightened
set, so it is dearer per control step and, for the same model and budget, further from the target than its
unprojected twin --- which is drawn as a faint grey ring for analytic average-velocity matching at the same
budget, so the price can be read directly: from $84\,\%$ of the distance closed at $190$\,ms to
$61\,\%$ at $266$\,ms. What that price buys is the constraint: a filled point keeps at least nine of ten
contexts free of violations, which is this task's counterpart of success with constraint satisfaction,
where the unprojected plan keeps one or two. That is the trade this task poses: pursuing no violation
costs completion --- the projected plan ends further from the target --- and allowing some violation
buys completion, the unprojected plan ending closest of all while crossing the constraint in eight
contexts of ten. The figure places the two ends of that trade on the same axes. Only filled points enter the cost frontier; the hollow ones
remain visible, and on this task they are every point of the two models that do not move the box. The
plotted runs use random selection at $\nfe=20$, the rows of \autoref{tab:va-projection-models}. The
diffusion model is absent because it was not evaluated on that set (\autoref{sec:res:aligning:projection}).

\begin{figure}[htpb]
  \centering
  \includegraphics[width=0.92\linewidth]{fig_aligning_projected_tradeoff}
  \caption[Alignment after projection: outcome against cost]{D3IL-aligning \emph{after projection}, on the
  axes of \autoref{fig:aligning-tradeoff}: the share of the starting distance the box was moved towards
  its target, against the mean time to compute one action, log axis, over the same ten contexts; the
  cells of \autoref{tab:va-projection-models}: $\nfe=20$, $\eta=0.2$, tightened constraints, random
  selection, one training seed. Colour identifies MeanFM, CI-MeanFM or FM; circles are per-step projection
  and squares endpoint projection. The faint dashed grey ring is the unprojected MeanFM cell of
  \autoref{fig:aligning-tradeoff} at the same budget, drawn so the cost of projecting can be read against
  it. Filled points keep at least nine contexts violation-free --- the counterpart on this task of success
  with constraint satisfaction --- hollow ones do not and cannot be on the frontier, whatever their
  distance. Rings and the dashed staircase mark the non-dominated filled points; the boxed arrow is the
  direction in which both axes improve. The horizontal dashed line at $0\,\%$ is a box not moved. One selection rule is drawn, random selection, the rule the task selects; the other two rules of every
  cell are in \autoref{tab:va-projection-models}. The outcome axis is logarithmic in the distance
  \emph{left} to the target, as in \autoref{fig:aligning-tradeoff}.}
  \label{fig:aligning-projected-tradeoff}
\end{figure}
\dataref{analysis\_results\_checkpoint/15-09/batch\_va2\_20260915\_100754,
\texttt{per\_rollout\_detail}, geometry \texttt{combined\_5-tightened}, the random-selection
\texttt{dpcc-r} and \texttt{hardflow\_sls-r} cells at $\nfe=20$ (v3.66: the $\nfe=2$ and $10$ points
are no longer drawn); built by
\texttt{DA\_in\_Paper/plotting/builders/frontier.py::fig\_aligning\_projected\_tradeoff}.}


\subsection{Conclusion}\label{sec:res:aligning:conclusion}

D3IL-aligning selects analytic average-velocity matching as the generative model: at twenty evaluations
it ends closer to the target than instantaneous-velocity matching and the diffusion baseline, whereas
consistency-interpolated average-velocity matching does not improve with the larger budget. It selects
endpoint projection as the projection method: at the large budget this task needs, endpoint projection
with random selection keeps all ten contexts free of violations and still moves the box in eight, at the
time per control step of per-step projection at $\nfe=20$ and at $37\,\%$ of it at $\nfe=10$. The
conclusion is limited to training-set contexts.

In one line: on D3IL-aligning the models separate, MeanFM $\gg$ CI-MeanFM $>$ FM $\approx$ diffusion ---
$84\,\%$ of the distance closed at twenty evaluations against $14$, $10$ and $-2\,\%$, with the
consistency-interpolated model reaching $60\,\%$ only at a hundred evaluations --- and endpoint projection is
the better projector on the constraint at the budget the task needs. The executed end-effector paths of
the three projection settings, context by context, are drawn in \autoref{app:aligning-paths}.

% =============================================================================
\section{Quadrotor Benchmark}\label{sec:res:uav}
% =============================================================================

The section is one part per scene, and each scene is read like the two manipulation benchmarks: the
generative models first on the plans they generate, executed without projection; the same cells then under
the projector of \ac{DPCC}, where the cost frontier and the flown paths are; and the two projection methods
against each other last. The scenes come in the order of what they add. UAV-pillars flies the plans of the avoiding planner itself
--- the obstacle field of D3IL-avoiding scaled into an arena of pillars, the same test-time constraints, the
quadrotor as the plant --- so it asks whether a plan the projector repairs on the table survives execution
by a vehicle. UAV-corridor is the scene whose test-time constraints leave the horizontal plane --- a slide leaned into
the vertical, and a roof --- read one at a time, as the geometries of D3IL-avoiding are, and neither of
which the demonstrations satisfy.
UAV-s-curve records the tracking-controller failure examined in \autoref{sec:res:uav:controller} and is
reported as a caveat rather than as a third benchmark (\autoref{sec:res:uav:scurve}); each scene's reading is
gathered in \autoref{sec:res:uav:conclusion}.
Cells printed \emph{pending} are runs that have not landed (R39, R33 in
\texttt{data\_status/PENDING\_20260923\_uav\_corridor\_v3\_pillars\_v2\_paper\_runs.md}).

All results use one training seed (\autoref{sec:setup:protocol:uav}). Every generative model is a temporal U-Net with $3.96$--$3.97$\,M
parameters, trained with the same optimiser settings (\autoref{tab:train}), and every flight is tracked by
the cascaded geometric controller with brake-to-rest setpoints unless stated otherwise. The diffusion model
is trained at $\nfe=20$ and is compared at the budget it was trained with.

\emph{Brake-to-rest} is the third of three velocity-setpoint policies the tracker accepts, and it is the
only one of them that a learned planner can supply honestly. The tracker wants a position setpoint, a
velocity feedforward and an acceleration feedforward; the demonstrations were flown with all three, taken
as analytic derivatives of the reference curve. A planner emits none of them --- it emits one position
increment per control step --- so the velocity feedforward has to be manufactured, and the two ways of
manufacturing it each inject an error the planner cannot control. Dividing the increment by the planning
interval makes the commanded speed a function of how long inference happened to take, so latency jitter
is fed straight into the tracker. Fixing the speed at a constant magnitude in the direction of the
increment removes the jitter but discards the magnitude the planner asked for, commanding the same speed
whether the plan moved $2$\,cm or $20$. Brake-to-rest sets the velocity feedforward to zero, which makes
the tracker a pure position regulator: the only quantity that reaches the vehicle is the setpoint the
planner produced. It is the conservative choice, and it is the one under which every number in this
section was measured. \autoref{sec:res:uav:controller} is where that choice is tested rather than assumed.
\srcnote{\texttt{mix\_uav\_test/eval\_mix\_uav.py:1591--1594} lists the four tracker options:
\texttt{pid} ($v\sidx{des} = a/\Delta t\sidx{fm}$, E7 legacy, timing-sensitive), \texttt{pid\_stopgo}
($v\sidx{des} = 0$, U2, used here), \texttt{pid\_const\_v} ($v\sidx{des} = \mathrm{unit}(a)\cdot 0.4$, U3)
and \texttt{mjpc}. The comparison of the three and the expert-versus-evaluation feedforward mismatch is
\texttt{logs\_in\_develop/Gen11/Epoch8\_UAV\_Mjpc\_thrust\_control/DESIGN\_obs\_action\_loop.md} \S3.4--3.5,
whose own table marks \texttt{pid} \enquote{No --- \texttt{dt\_fm} jitter} and \texttt{pid\_const\_v}
\enquote{No --- magnitude is wrong}. The $0.4$\,m/s default is the expert dataset's average speed.}

\subsection{UAV-pillars}\label{sec:res:uav:pillars}

This scene is D3IL-avoiding flown. The arena is the obstacle field scaled by $36$, the constraint sets are
the three geometries of the table mapped into it, and the planner is the one of \autoref{sec:res:state},
not retrained (\autoref{sec:setup:tasks:uav}). What is read here is therefore not a new benchmark but a
check of the vehicle and its tracking controller: the configurations that win on the table --- the two
average-velocity models at one and two network evaluations, the operating points of
\autoref{tab:avoiding-conclusion} --- are flown, before and after per-step projection, and their flown
outcome is set beside their table outcome at the same protocol, five training seeds $\times$ two episodes
per geometry, averaged over the three geometries. Agreement means the quadrotor carries a plan as the
manipulator does, projected or not; a difference is a result about the plant, and either is a valid
reading. Cells printed \emph{pending} are the flights that have not landed (R39).

\subsubsection{On the Table and in the Air}\label{sec:res:uav:models}
\hole{Result sentence, before projection: whether the vehicle changes the outcome of an unprojected plan ---
the table and air columns should agree within the seed spread on success and violating steps, with
contact telling where a violating plan becomes a collision in the air.}

\begin{table}[htpb]
  \caption[UAV-pillars: the winning avoiding configurations, on the table and in the air]{UAV-pillars: the
  two average-velocity models at $\nfe=1$ and $2$, \textbf{before projection} and \textbf{after per-step
  projection} under temporal consistency, on tightened constraints --- executed by the manipulator
  (\emph{table}, the cells of \autoref{tab:avoiding-raw-models} and \autoref{tab:avoiding-dpcc-protocol})
  and flown by the quadrotor in the arena (\emph{air}). The protocol of \ac{DPCC} on both sides: five
  training seeds $\times$ two episodes per geometry, averaged over the three geometries, four candidate
  plans. \emph{Success}: the goal line crossed; \emph{S\&C}: success with constraint satisfaction;
  \emph{viol.}: violating control steps per episode; \emph{contact}: flights on which the vehicle touched a
  pillar. \emph{Pending}: R39. \selectedmark\ the selected configuration of D3IL-avoiding.}
  \label{tab:uav-pillars-raw}
  \centering
  \footnotesize
  \setlength{\tabcolsep}{3pt}
  \begin{tabularx}{\linewidth}{@{}L r rrr rrrr@{}}
    \toprule
    & & \multicolumn{3}{c}{table} & \multicolumn{4}{c}{air} \\
    \cmidrule(lr){3-5}\cmidrule(l){6-9}
    Model & $\nfe$ & S\&C & viol. & steps & S\&C & viol. & steps & contact \\
    \midrule
    \multicolumn{9}{@{}l}{\emph{before projection}}\\
    MeanFM    & 1 & $0.033 \pm 0.075$ & $15.5 \pm 2.1$ & $63.4 \pm 5.6$ & \multicolumn{4}{l}{\emph{pending}} \\
    MeanFM    & 2 & $0.033 \pm 0.075$ & $15.5 \pm 0.4$ & $62.7 \pm 1.8$ & \multicolumn{4}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 1 & $0.033 \pm 0.075$ & $15.6 \pm 0.7$ & $60.5 \pm 1.5$ & \multicolumn{4}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 2 & $0.033 \pm 0.075$ & $15.7 \pm 0.7$ & $62.9 \pm 1.2$ & \multicolumn{4}{l}{\emph{pending}} \\
    \midrule
    \multicolumn{9}{@{}l}{\emph{after per-step projection, temporal consistency}}\\
    \mainconfig{\selectedmark\ MeanFM} & \mainconfig{1} & \mainconfig{$0.967 \pm 0.075$} & \mainconfig{$0.0$} & \mainconfig{$58.6 \pm 2.2$} & \multicolumn{4}{l}{\mainconfig{\emph{pending}}} \\
    MeanFM    & 2 & $0.967 \pm 0.075$ & $0.0$ & $59.4 \pm 2.0$ & \multicolumn{4}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 1 & $1.000 \pm 0.000$ & $0.0$ & $59.2 \pm 1.2$ & \multicolumn{4}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 2 & $1.000 \pm 0.000$ & $0.0$ & $60.1 \pm 3.4$ & \multicolumn{4}{l}{\emph{pending}} \\
    \bottomrule
  \end{tabularx}
\end{table}
\dataref{Table columns: \autoref{tab:avoiding-raw-models} (\texttt{diffuser}) and
\autoref{tab:avoiding-dpcc-protocol} (\texttt{dpcc-t-tightened}), 19-09 corpus; the projected
\emph{viol.} column is the constraint-satisfying share's complement and is $0.0$ where S\&C equals
success. Air columns: R39, the four cells MeanFM / CI-MeanFM $\times$ $\nfe\in\{1,2\}$, variants
\texttt{diffuser} and \texttt{dpcc-t-tightened}, five seeds $\times$ three geometries $\times$ two
episodes, the quadrotor evaluation of \texttt{PENDING\_20260923\_…} \S2 (author, 23-09: the draft reads
these four cells and their paths; T3--T5 of that file are not read). How the flights are produced is a
matter for that file and is not described here (author).}

\hole{Reading, after projection: whether the plans the per-step projector repairs on the table remain
repaired in the air --- the same four cells, flown --- and what the vehicle adds: tracking error, contacts,
and whether the two models stay level.}

\FloatBarrier
\subsubsection{Executed Paths: the Manipulator and the Quadrotor}\label{sec:res:uav:pillars:projected}

\hole{Paths paragraph: what \autoref{fig:uav-pillars-paths} shows --- the same plan, the same episode and
geometry, executed by the arm on the table and flown by the vehicle in the arena: where the unprojected
plan crosses the halfspace on both platforms, whether the projected plan keeps both out of the keep-out
region, and where the flown path departs from the executed one (the controller's lag at the turn, contact
at a pillar).}

\begin{figure}[htpb]
  \centering
  \todofigure[0.34\textwidth]{\texttt{fig\_uav\_pillars\_paths}: two rows $\times$ four columns. Columns:
  MeanFM $\nfe=1$, MeanFM $\nfe=2$, CI-MeanFM $\nfe=1$, CI-MeanFM $\nfe=2$ --- the cells of
  \autoref{fig:avoiding-paths}. Upper row: the manipulator's executed end-effector path on the table
  (\texttt{exec\_paths.json}, one seed, \emph{top-right-hard}, episode 2), unprojected in grey and
  per-step projected in colour, over the avoiding geometry. Lower row: the quadrotor's flown path in the
  arena for the same seed, geometry and episode, unprojected in grey and projected in colour, over the
  mapped geometry (scale 36; \texttt{scenes.\_uav\_constraint\_panel} with
  \texttt{sources.UAV\_PILLARS\_GEOMETRIES['top-right-hard']}); a cross where the vehicle touched a pillar.
  Source: the world-frame paths of R39 (\texttt{extract/uav\_paths.py}, new group \texttt{pillars}); the
  unprojected arm paths need the \texttt{diffuser.npz} artefacts of the same cells beside the
  \texttt{dpcc-t-tightened.npz} ones (extract if present, else the upper row is projected only).}
  \caption[UAV-pillars: the same plan executed by the manipulator and flown by the quadrotor]{The same
  four configurations executed on the table (upper row, from \autoref{fig:avoiding-paths}) and flown in
  the arena (lower row): one seed, the \emph{top-right-hard} geometry, the same episode, before projection
  in grey and under per-step projection in colour. Shading and dashed boundaries as in
  \autoref{fig:constraints-avoiding-sets} and \autoref{fig:constraints-uav}; the lower row is the upper
  one at scale $36$. Pending R39.}
  \label{fig:uav-pillars-paths}
\end{figure}
\guard{\hole{After R39: the one-line reading of the two rows --- the same shape on both platforms, or
where they differ and why; either is a valid result, and the difference is what the vehicle adds.}}

\FloatBarrier
\subsection{UAV-corridor}\label{sec:res:uav:corridor}

\subsubsection{Generative Models}\label{sec:res:uav:corridor:raw}
The two constraints are read one at a time, as the avoiding geometries are: every table of this scene has a
\emph{tilt} block and a \emph{hump} block, and nothing is averaged across them.
\hole{Result sentence, before projection: which models cross the finish line unprojected and how many
flights violate each constraint. Read from \autoref{tab:uav-corridor-raw} once the run lands.
The v3.65 reading on the v2 corpus was: every model succeeds on every flight and none is collision-free,
so the scene is a repair case.} The cells that reach the finish line here are the cells whose projected
rows in \autoref{sec:res:uav:corridor} are read; a cost frontier is drawn for this arm only if the models
do not all cross the line, because a frontier over points that share one outcome is a ranking by price
(\autoref{sec:res:aligning:budget}).

\begin{table}[htpb]
  \caption[UAV-corridor: the generative models before projection]{UAV-corridor with the test-time
  constraints, \textbf{before projection}: the plan flown as generated, ten flights per row, spread over
  the three routes, one training seed, one block per constraint (\emph{tilt}, \emph{hump}). The grid is: the two average-velocity models at
  $\nfe\in\{1,2,3\}$, instantaneous-velocity matching at $\nfe\in\{1,2,3,5,20\}$, the diffusion baseline
  at its trained budget, printed last. \emph{Success}: crosses the finish line. \emph{Collision-free}: no
  violating control step. \emph{S\&C}: success on a collision-free flight. \emph{Violating steps} and
  \emph{ms/step}: mean $\pm$ the sample standard deviation over the flights
  (\autoref{sec:setup:metrics:spread}). \emph{Pending}: run not yet landed (R33). \baselinemark\ the
  diffusion model of \ac{DPCC}.}
  \label{tab:uav-corridor-raw}
  \centering
  \footnotesize
  \setlength{\tabcolsep}{3pt}
  \begin{tabularx}{\linewidth}{@{}L r rrr rr@{}}
    \toprule
    Model & $\nfe$ & Success & \shortstack{Collision-\\free} & S\&C & \shortstack{Violating\\steps} & ms/step \\
    \midrule
    \multicolumn{7}{@{}l}{\emph{tilt}}\\
    MeanFM & 1 & \multicolumn{5}{l}{\emph{pending}} \\
    MeanFM & 2 & \multicolumn{5}{l}{\emph{pending}} \\
    MeanFM & 3 & \multicolumn{5}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 1 & \multicolumn{5}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 2 & \multicolumn{5}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 3 & \multicolumn{5}{l}{\emph{pending}} \\
    FM & 1 & \multicolumn{5}{l}{\emph{pending}} \\
    FM & 2 & \multicolumn{5}{l}{\emph{pending}} \\
    FM & 3 & \multicolumn{5}{l}{\emph{pending}} \\
    FM & 5 & \multicolumn{5}{l}{\emph{pending}} \\
    FM & 20 & \multicolumn{5}{l}{\emph{pending}} \\
    \mainconfig{\baselinemark\ Diffusion} & \mainconfig{20} & \multicolumn{5}{l}{\mainconfig{\emph{pending}}} \\
    \midrule
    \multicolumn{7}{@{}l}{\emph{hump}}\\
    MeanFM & 1 & \multicolumn{5}{l}{\emph{pending}} \\
    MeanFM & 2 & \multicolumn{5}{l}{\emph{pending}} \\
    MeanFM & 3 & \multicolumn{5}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 1 & \multicolumn{5}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 2 & \multicolumn{5}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 3 & \multicolumn{5}{l}{\emph{pending}} \\
    FM & 1 & \multicolumn{5}{l}{\emph{pending}} \\
    FM & 2 & \multicolumn{5}{l}{\emph{pending}} \\
    FM & 3 & \multicolumn{5}{l}{\emph{pending}} \\
    FM & 5 & \multicolumn{5}{l}{\emph{pending}} \\
    FM & 20 & \multicolumn{5}{l}{\emph{pending}} \\
    \mainconfig{\baselinemark\ Diffusion} & \mainconfig{20} & \multicolumn{5}{l}{\mainconfig{\emph{pending}}} \\
    \bottomrule
  \end{tabularx}
\end{table}
\dataref{corridor runs, R33 (\texttt{PENDING\_20260923\_uav\_corridor\_v3\_pillars\_v2\_paper\_runs.md}), geometries \texttt{corridor\_v3\_tilt} and \texttt{corridor\_v3\_ablation\_hump}, variant
\texttt{diffuser}: \texttt{success\_relaxed}, \texttt{collision\_free\_completed},
\texttt{n\_success\_relaxed\_and\_constraints}, \texttt{n\_violations}, \texttt{avg\_time\_ms}. The v2
corpus this table was computed from until v3.65 is archived.}

\FloatBarrier

\FloatBarrier
\subsubsection{Projection}\label{sec:res:uav:projected}
\hole{Result sentence, after projection: which models succeed collision-free at which budget under the
per-step projector of \ac{DPCC} at activation threshold $0.5$, and where endpoint projection, which has a
guiding step from $\nfe=3$, stands beside it (\autoref{tab:uav-corridor}). Read the two projectors
together and pick the best configuration of each model by the table and the frontier.} The budgets are
read as one ladder across the scene: the per-step projector is active at every budget, endpoint projection
at $\nfe\ge 3$, where it has a guiding step (\autoref{sec:res:constraints:degenerate}), so at $\nfe=1$ and
$\nfe=2$ the table carries per-step rows only.

\begin{table}[htpb]
  \caption[UAV-corridor: the generative models after projection]{UAV-corridor with its two test-time
  constraints, one block each, \textbf{after projection}: each model at each budget of the grid under the per-step
  projector of \ac{DPCC} (activation threshold $0.5$, tightened constraints) and, from $\nfe=3$, under
  endpoint projection, each at its best selection rule of \autoref{tab:uav-corridor-projection}; ten
  flights per cell, one training seed. \emph{S\&C}: success on a collision-free flight; \emph{ms/step}:
  the time to compute one action. \emph{---}: endpoint projection has no guiding step at that budget.
  \emph{Pending}: run not yet landed (R33). \baselinemark\ the diffusion model of \ac{DPCC}.}
  \label{tab:uav-corridor}
  \centering
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabularx}{\linewidth}{@{}L r rrr rrr@{}}
    \toprule
    & & \multicolumn{3}{c}{per-step projection} & \multicolumn{3}{c}{endpoint projection} \\
    \cmidrule(lr){3-5}\cmidrule(l){6-8}
    Model & $\nfe$ & S\&C & rule & ms/step & S\&C & rule & ms/step \\
    \midrule
    \multicolumn{8}{@{}l}{\emph{tilt}}\\
    MeanFM & 1 & \multicolumn{3}{l}{\emph{pending}} & --- & --- & --- \\
    MeanFM & 2 & \multicolumn{3}{l}{\emph{pending}} & --- & --- & --- \\
    MeanFM & 3 & \multicolumn{3}{l}{\emph{pending}} & \multicolumn{3}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 1 & \multicolumn{3}{l}{\emph{pending}} & --- & --- & --- \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 2 & \multicolumn{3}{l}{\emph{pending}} & --- & --- & --- \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 3 & \multicolumn{3}{l}{\emph{pending}} & \multicolumn{3}{l}{\emph{pending}} \\
    FM & 1 & \multicolumn{3}{l}{\emph{pending}} & --- & --- & --- \\
    FM & 2 & \multicolumn{3}{l}{\emph{pending}} & --- & --- & --- \\
    FM & 3 & \multicolumn{3}{l}{\emph{pending}} & \multicolumn{3}{l}{\emph{pending}} \\
    FM & 5 & \multicolumn{3}{l}{\emph{pending}} & \multicolumn{3}{l}{\emph{pending}} \\
    FM & 20 & \multicolumn{3}{l}{\emph{pending}} & \multicolumn{3}{l}{\emph{pending}} \\
    \mainconfig{\baselinemark\ Diffusion} & \mainconfig{20} & \multicolumn{3}{l}{\mainconfig{\emph{pending}}} & --- & --- & --- \\
    \midrule
    \multicolumn{8}{@{}l}{\emph{hump}}\\
    MeanFM & 1 & \multicolumn{3}{l}{\emph{pending}} & --- & --- & --- \\
    MeanFM & 2 & \multicolumn{3}{l}{\emph{pending}} & --- & --- & --- \\
    MeanFM & 3 & \multicolumn{3}{l}{\emph{pending}} & \multicolumn{3}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 1 & \multicolumn{3}{l}{\emph{pending}} & --- & --- & --- \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 2 & \multicolumn{3}{l}{\emph{pending}} & --- & --- & --- \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 3 & \multicolumn{3}{l}{\emph{pending}} & \multicolumn{3}{l}{\emph{pending}} \\
    FM & 1 & \multicolumn{3}{l}{\emph{pending}} & --- & --- & --- \\
    FM & 2 & \multicolumn{3}{l}{\emph{pending}} & --- & --- & --- \\
    FM & 3 & \multicolumn{3}{l}{\emph{pending}} & \multicolumn{3}{l}{\emph{pending}} \\
    FM & 5 & \multicolumn{3}{l}{\emph{pending}} & \multicolumn{3}{l}{\emph{pending}} \\
    FM & 20 & \multicolumn{3}{l}{\emph{pending}} & \multicolumn{3}{l}{\emph{pending}} \\
    \mainconfig{\baselinemark\ Diffusion} & \mainconfig{20} & \multicolumn{3}{l}{\mainconfig{\emph{pending}}} & --- & --- & --- \\
    \bottomrule
  \end{tabularx}
\end{table}
\dataref{corridor runs, R33, both geometries; per-step = \texttt{dpcc-\{r,c,t\}} at $\eta=0.5$, endpoint =
\texttt{hardflow\_sls\{,-r,-c,-t\}}, both on the corridor's projection stack
(\autoref{sec:setup:protocol:uav}); the best rule per cell is the one \autoref{tab:uav-corridor-projection}
prints highest. The unprojected twins are \autoref{tab:uav-corridor-raw}.}

\hole{Budget paragraph: at which budget each model stops violating the halfspace, whether that is the same
budget at which it stops reaching the finish line, and what the diffusion baseline does at its one budget.
Then the frontier reading of \autoref{fig:uav-corridor-tradeoff}: which configurations are on it, the
cheapest one, and whether its shape is a step or a slope.} The frontier is drawn as before: success with
constraint satisfaction against the time one action costs, logarithmic in the number of successful flights,
a configuration with no successful flight not eligible, costs within one per cent treated as one; both
projectors are drawn on it, per-step as circles and endpoint as squares, so that the choice of projector is
read off the same picture as the choice of budget.

\begin{figure}[htpb]
  \centering
  \todofigure[0.34\textwidth]{\texttt{fig\_uav\_corridor\_tradeoff}, one panel per constraint (tilt, hump): success with
  constraint satisfaction (log axis in successful flights of ten, floor for zero) against the time to
  compute one action, every cell of \autoref{tab:uav-corridor-projection} under both projectors, one
  training seed; rings and staircase on the frontier; builder
  \texttt{DA\_in\_Paper/plotting/builders/frontier.py::fig\_uav\_corridor\_tradeoff} extended to the
  endpoint cells. Until R33 lands the builder draws the v2 corpus, which is archived and not this figure.}
  \caption[UAV-corridor: outcome against cost]{UAV-corridor, success with constraint satisfaction against
  the time to compute one action, both projectors, every budget of the grid, one training seed; drawn as
  in \autoref{fig:avoiding-tradeoff}. Pending R33.}
  \label{fig:uav-corridor-tradeoff}
\end{figure}

\hole{Paths paragraph: what the flown paths of \autoref{fig:uav-corridor-paths} show --- the budget at
which a model stops crossing the halfspace against the budget at which it stops reaching the end of the
corridor, and where the diffusion baseline sits on that trade.}

\begin{figure}[htpb]
  \centering
  \todofigure[0.34\textwidth]{\texttt{fig\_uav\_corridor\_paths}, one row per constraint (tilt, hump): the scene from above with
  the ten flown paths per cell, one row per model across its budgets and one panel per projector at the
  best rule; colour = constraint, stroke and end mark = success, cross = crashed or lost control; extract
  \texttt{plotting/extract/uav\_paths.py} (group \texttt{corridor}) on the staged rollouts of R33.}
  \caption[UAV-corridor: flown paths per model and budget]{UAV-corridor seen from above with the executed
  path of each flight, per model, budget and projector. Pending R33.}
  \label{fig:uav-corridor-paths}
\end{figure}

\paragraph{Altitude.} Both constraints of this scene act on altitude, and that is what they are for: under
the tilt the room is below the launch altitude, so a projected flight has to descend as it moves sideways;
under the hump it has to climb. \autoref{fig:uav-corridor-altitude} shows the flights from the side so that
this is seen rather than assumed. \hole{Altitude numbers per constraint: the flown band, the paired
difference between the projected and the unprojected flight, and where along $x$ it occurs. In the pilots
the projected flights descended $0.15$--$0.29$\,m under the tilt and climbed $0.30$--$0.40$\,m over the hump,
where on the level corridor of the earlier evaluation the projection moved altitude only as a by-product
of the lateral correction.}

\begin{figure}[htpb]
  \centering
  \todofigure[0.30\textwidth]{\texttt{fig\_uav\_corridor\_altitude}, one row per constraint (tilt: the descent under the leaned plane; hump: the climb): altitude $z$ over $x$
  for every flight, one panel per model, grey unprojected and blue projected, the inflated box limits
  dashed; builder \texttt{plotting/builders/altitude.py} on the R33 rollouts (group
  \texttt{corridor\_altitude}).}
  \caption[UAV-corridor: flown altitude, without and with projection]{UAV-corridor seen from the side: the
  altitude of every flight over its position along the corridor, without and with projection, one panel
  per model. Pending R33.}
  \label{fig:uav-corridor-altitude}
\end{figure}

UAV-pillars is not drawn as a frontier: its result is the difference between a plan's outcome on the table
and the outcome of the same plan flown, which is a comparison and not a trade-off
(\autoref{sec:res:uav:models}). UAV-s-curve has data, but every
configuration it has --- projected and unprojected, at every budget flown --- scores $0.00$ success with
constraint satisfaction (\autoref{tab:uav-scurve}). A cost frontier
over points that all share one outcome is a ranking of failures by price, so what that scene gets instead
is the abort count printed beside each of its rows.

\FloatBarrier

\FloatBarrier

\FloatBarrier
\subsubsection{Projection Methods}\label{sec:res:uav:projection}
\hole{Corridor result sentence, per-step against endpoint: which projector is the method on this scene,
under which rule and at which budget, read from \autoref{tab:uav-corridor-projection}; whether endpoint
projection clears the bar this thesis set for it --- overtaking the per-step projector of \ac{DPCC} at a
lower projection cost --- and whether either projector changes with the budget.}

\begin{table}[htpb]
  \caption[UAV-corridor: the two projection methods under every selection rule]{UAV-corridor, success with
  constraint satisfaction --- success on a collision-free flight --- for both projection methods under
  every selection rule, one block per constraint (\emph{tilt}, \emph{hump}), at the budgets at which
  endpoint projection has a guiding step; ten flights
  per cell, tightened constraints, activation threshold $0.5$, one training seed. Per-step: $r$ random,
  $c$ cumulative projection cost, $t$ temporal consistency, four candidate plans. Endpoint:
  \emph{single} one candidate, then the three rules over four. The diffusion baseline has per-step rows
  only. \emph{Pending}: R33.}
  \label{tab:uav-corridor-projection}
  \centering
  \footnotesize
  \setlength{\tabcolsep}{5pt}
  \begin{tabularx}{\linewidth}{@{}L r rrr rrrr@{}}
    \toprule
    & & \multicolumn{3}{c}{Per-step} & \multicolumn{4}{c}{Endpoint} \\
    \cmidrule(lr){3-5}\cmidrule(l){6-9}
    Model & $\nfe$ & $r$ & $c$ & $t$ & single & $r$ & $c$ & $t$ \\
    \midrule
    \multicolumn{9}{@{}l}{\emph{tilt}}\\
    MeanFM & 3 & \multicolumn{7}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 3 & \multicolumn{7}{l}{\emph{pending}} \\
    FM & 3 & \multicolumn{7}{l}{\emph{pending}} \\
       & 5 & \multicolumn{7}{l}{\emph{pending}} \\
       & 20 & \multicolumn{7}{l}{\emph{pending}} \\
    \mainconfig{\baselinemark\ Diffusion} & \mainconfig{20} & \multicolumn{3}{l}{\mainconfig{\emph{pending}}} & --- & --- & --- & --- \\
    \midrule
    \multicolumn{9}{@{}l}{\emph{hump}}\\
    MeanFM & 3 & \multicolumn{7}{l}{\emph{pending}} \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 3 & \multicolumn{7}{l}{\emph{pending}} \\
    FM & 3 & \multicolumn{7}{l}{\emph{pending}} \\
       & 5 & \multicolumn{7}{l}{\emph{pending}} \\
       & 20 & \multicolumn{7}{l}{\emph{pending}} \\
    \mainconfig{\baselinemark\ Diffusion} & \mainconfig{20} & \multicolumn{3}{l}{\mainconfig{\emph{pending}}} & --- & --- & --- & --- \\
    \bottomrule
  \end{tabularx}
\end{table}
\dataref{corridor runs, R33, both geometries: variants \texttt{dpcc-\{r,c,t\}} and \texttt{hardflow\_sls\{,-r,-c,-t\}}
on the corridor projection stack; \texttt{n\_success\_relaxed\_and\_constraints}.}
\guard{Endpoint projection has no row at $\nfe=1$ or $\nfe=2$ on this scene and will not get one: at the
activation threshold of $0.5$ the only active sampling step there is the terminal one, so there is no
guiding step for the method to act on (\autoref{sec:res:constraints:degenerate}). That is a property of
the method at those budgets, not a pending evaluation. The baseline's dashes are the opposite case:
endpoint projection has never been run on the diffusion model on any scene.}

The projection configuration of this scene differs from that of the other quadrotor scene
(\autoref{sec:setup:protocol:uav}), so the scenes are not pooled.

\FloatBarrier
\subsection{UAV-s-curve}\label{sec:res:uav:scurve}

UAV-s-curve is reported as a caveat and not as a third benchmark. It was piloted with each model at the
budget that model is strongest at, and every pilot fails --- before any question about the constraint can
be asked of it. A divergence guard ends a flight when the vehicle inverts, and on the plans the models
generate, executed without projection, it ends four to ten flights of ten (\autoref{tab:uav-scurve});
success with constraint satisfaction is $0.00$ in every cell the scene has, projected and unprojected
alike. Because the unprojected plan already fails at the turns at the model's best budget, no smaller
budget was flown and the projected pilots add nothing to the reading: what fails on this scene is the
vehicle's attitude in the two sharp turns, and \autoref{sec:res:uav:controller} isolates the cause by
keeping the plan and changing the tracking controller.

\begin{table}[htpb]
  \caption[UAV-s-curve: the pilot flights]{UAV-s-curve, the pilot flights: each model at the budget it is
  strongest at, plans executed without projection, brake-to-rest tracking, ten flights per cell, one training seed. \emph{Success}: crosses the finish line, which on this scene requires the flight to be safe as
  well; \emph{distance}: mean final distance to the goal point of the route; \emph{safe}: no contact and
  no loss of altitude; \emph{aborted}: ended early by the divergence guard because the vehicle inverted.
  No flight is collision-free, so success with constraint satisfaction is $0.00$ throughout. The budgets
  differ by row on purpose and the rows are not a model comparison. \baselinemark\ the baseline of record
  at its own budget.}
  \label{tab:uav-scurve}
  \centering
  \small
  \begin{tabularx}{\linewidth}{@{}L r r r r r r@{}}
    \toprule
    Model & $\nfe$ & Success & Distance [m] & Safe & Aborted & ms/step \\
    \midrule
    MeanFM        & 10 & 0/10 & 2.809 & 0/10 & 10/10 & 88.8 \\
    CI-MeanFM, $\alpha_{\mathrm{end}}=0.2$ & 5 & 2/10 & 2.247 & 2/10 & 8/10 & 45.0 \\
    FM            & 20 & 6/10 & 1.193 & 6/10 & 4/10 & 170.4 \\
    \midrule
    \mainconfig{\baselinemark\ Diffusion} & \mainconfig{20} & \mainconfig{0/10} & \mainconfig{2.432} & \mainconfig{0/10} & \mainconfig{8/10} & \mainconfig{173.9} \\
    \bottomrule
  \end{tabularx}
\end{table}
\dataref{batch\_uav\_20260919\_111701 (tag \texttt{u18sc}: MeanFM $\nfe=10$, FM $\nfe=20$, CI-MeanFM
$\nfe=5$) and batch\_uav\_20260915\_100816 (tag \texttt{u7hg}: diffusion), \texttt{per\_rollout\_detail},
geometry \texttt{s\_curve\_hg}, variant \texttt{diffuser}; \emph{success} = \texttt{success\_relaxed},
\emph{safe} = \texttt{phys\_safe}, \emph{aborted} = \texttt{divergence\_aborted}. These four cells were
Tables 6.14 and C.x of v3.67; the lower budgets that were flown (MeanFM 2, CI-MeanFM 1 and 2, FM 2) are in the
corpus and are no longer printed (author, v3.68). Recounted 2026-09-22.}
\guard{Instantaneous-velocity matching succeeds on six flights of ten, so the controller's limit is not
absolute: a plan whose turns it can follow exists. The two average-velocity models and the baseline invert
on eight to ten flights. Nothing here ranks the models: the budgets differ by row, and what separates the
rows is how the tracker copes with each model's turn, which \autoref{sec:res:uav:controller} examines on
one model with the controller changed.}
\guard{The projected pilots of this scene, flown on the plain constraint set, were withdrawn at v3.67 and
are not re-run: they are not needed to state the caveat. The one key datum the caveat still lacks is the
same unprojected plan under the two other velocity-setpoint policies of the tracker, which is R40 of
\texttt{data\_status/PENDING\_20260923\_uav\_corridor\_v3\_pillars\_v2\_paper\_runs.md}.}

On UAV-s-curve, neither method yields a flight that succeeds within the constraints, so the scene
does not rank them (\autoref{sec:res:uav:scurve}).

\subsection{Caveat: The Tracking Controller}\label{sec:res:uav:controller}

On the manipulator, inverse kinematics and the joint-space controller lie between a generated plan and
the executed motion. The quadrotor is underactuated and
open-loop unstable (\autoref{sec:bg:quadrotor}), and a plan reaches the rotors only through the cascaded
geometric controller that tracks it. That controller needs a velocity setpoint the plan does not supply,
and every result in this chapter uses the brake-to-rest policy of \autoref{sec:method:geometric}: the setpoint
is zero, so the vehicle accelerates towards each new position and brakes to rest at it. The result of a flight is therefore a property of the planner and the controller
together, and UAV-s-curve, with its two sharp turns, is where the controller becomes the limit.

This is shown by keeping the plan and changing the controller (\autoref{tab:uav-controller}). Analytic
average-velocity matching at $\nfe=10$, executed without projection, succeeds on none of ten flights
with the brake-to-rest controller and ends on average $2.81$\,m from the goal point of its route. With
MuJoCo MPC \parencite{howell2022predictive} as the tracking controller, the same plans end $0.299$, $0.298$
and $0.294$\,m from that point, and two of the three flights cross the finish line. The brake-to-rest
failures are not wall collisions: on all ten flights the vehicle stays at altitude until it loses attitude
control and inverts, between control steps $395$ and $421$, about $12$\,s into the flight. MuJoCo MPC flies
the same turn. The unprojected plan is flyable; the brake-to-rest controller cannot fly it.

What separates the two is the velocity setpoint and not the gains. Both controllers act on the same
vehicle with the same nominal gains, which the evaluation fixes and never varies, and the brake-to-rest
policy is the one that tracks the reference most closely: its mean tracking error, $0.29$ to $0.34$\,m,
is the smallest of any configuration on this scene, and it is also the only one that never advances along
the route. A controller that follows a reference tightly while making no progress along it is not failing
to track; it is failing to move. On a scene whose reference demands sustained forward speed, a setpoint
of zero velocity at every plan step is what produces both that standstill and the loss of attitude that
ends the flight.

\begin{table}[htpb]
  \caption[UAV-s-curve: the same plan under two tracking controllers]{UAV-s-curve, analytic
  average-velocity matching at $\nfe=10$, the same plans under two tracking controllers.
  \emph{Cascaded geometric, brake-to-rest} is the controller behind every other result in this chapter;
  \emph{MuJoCo MPC} (\autoref{sec:method:mjpc}) replaces it here and nowhere else. The last column is
  the measured elapsed time per executed control step, including planner and controller. \emph{Success}: crosses
  the finish line; \emph{distance}: mean final distance to the goal point of the route; \emph{safe}: no
  contact and no loss of altitude. One training seed.}
  \label{tab:uav-controller}
  \centering
  \small
  \begin{tabularx}{\linewidth}{@{}L L r r r r r@{}}
    \toprule
    Projection & Controller & Flights & Success & Distance [m] & Safe & ms/step \\
    \midrule
    none                   & cascaded geometric, brake-to-rest & 10 & 0/10 & 2.809 & 0/10 & 94.9 \\
    none                   & MuJoCo MPC    & 3  & 2/3  & 0.297 & 2/3 & 215.6 \\
    per-step, best rule    & cascaded geometric, brake-to-rest & 10 & 0/10 & 2.446 & --- & --- \\
    per-step, random       & MuJoCo MPC    & 3  & 0/3  & 2.571 & --- & 2660.1 \\
    \bottomrule
  \end{tabularx}
\end{table}
\dataref{uav\_results.py, s-curve block; Campaign DA\_20260909\_T5\_mjpc\_vs\_pid\_s\_curve F1--F3, F7;
Report\_20260909\_MJPC\_vs\_PID\_s\_curve \S5.4: full-step times on the unprojected
arms and the MuJoCo MPC per-step random arm; the other projected controller cell has no matched
elapsed-time figure in this report}
\guard{Three flights with MuJoCo MPC. The gains are held at their nominal values in every evaluation of
this thesis and were not tuned; the two alternative settings that exist scale the position loop by
$\pm20\,\%$ and leave the attitude loop and the thrust limit untouched, so neither can be expected to
address a loss of attitude. What this section supports is that the scene is limited by the choice of
velocity setpoint, not that no tuning of this controller could fly it.}

\begin{figure}[htpb]
  \centering
  \includegraphics[width=\linewidth]{fig_uav_scurve_paths}
  \caption[UAV-s-curve: flown paths under the two tracking controllers]{UAV-s-curve seen from above,
  drawn like \autoref{fig:constraints-uav}, with the executed path of every flight of the two unprojected
  rows of \autoref{tab:uav-controller}. Both panels are the same plans under the two tracking
  controllers. Green paths are successes, red paths are not. A cross at the end of a path marks a flight
  that crashed --- contact, or loss of altitude --- or that the divergence guard ended because the vehicle
  inverted: every flight of the right panel, ended by the guard between control steps $395$ and $421$,
  and one flight of the left panel, which reached the finish line's $x$ but made contact.}
  \label{fig:uav-scurve-paths}
\end{figure}
\dataref{DA\_in\_Paper/data/uav\_paths.json, extract/uav\_paths.py; runs
Emf\_K10\_mpc4\_mjpc\_T0.5\_u7hg and its \texttt{pid\_stopgo} sibling}

In that figure a brake-to-rest path stops where the vehicle lost its attitude rather than where it ran
out of authority to track: all ten flights end at the guard, while MuJoCo MPC records no abort and
follows the second passage to the goal on two flights of three. Neither controller is collision-free
without projection, which is what the projected rows of \autoref{tab:uav-controller} address.

Changing the controller is not sufficient once projection changes the plan. With per-step projection the
plan succeeds on none of three flights under MuJoCo MPC and on none of ten under the brake-to-rest
controller, and no flight succeeds within the constraints. The unprojected experiment isolates a limit of
the controller; the projected experiment remains a failure of the planner, the projection and the
controller together. UAV-s-curve therefore supports no ordering of the generative models and no preferred
projection method, and the ordering on the two other scenes is to be read for the controller with which it
was measured.

The other reason MuJoCo MPC replaces the controller here and nowhere else is what it costs to run. The
time reported in every other table of this chapter is measured around the planner and stops there
(\autoref{sec:setup:metrics:avoiding}), so it prices no controller at all; pricing one means reading the
elapsed time of the flights themselves and dividing it by the control steps that executed
(\autoref{tab:uav-controller-cost}). On the unprojected arm, where nothing lies between the plan and the
controller, a control step takes $94.9$\,ms under the brake-to-rest controller and $215.6$\,ms under
MuJoCo MPC. The planner's share of that is the same in both, between $88$ and $90$\,ms at $\nfe=10$, so
the difference belongs to the controller: about $6$\,ms per step for the cascaded geometric law against
about $126$\,ms for MuJoCo MPC, which solves an optimisation problem at every control step where the
other evaluates an algebraic law. The controller that flies this scene is about twenty times the price
of the one that does not, and by itself it costs more per control step than generating the plan --- some
seven times what the operating point of D3IL-avoiding spends on an entire action
(\autoref{tab:avoiding-conclusion}).

\begin{table}[htpb]
  \caption[UAV-s-curve: what each tracking controller costs per control step]{UAV-s-curve, analytic
  average-velocity matching at $\nfe=10$ without projection: the time one executed control step takes,
  in milliseconds, under the two tracking controllers of \autoref{tab:uav-controller}. It is the elapsed
  time of a flight divided by the control steps that executed, which is the step the divergence guard
  stopped the flight at where it was stopped and the step of success otherwise --- a
  different quantity from the planner time reported elsewhere in this chapter, and the one
  here that contains the controller. \emph{The planner} is that planner time. \emph{The controller and
  the simulator step} is the remainder; the simulated vehicle and its step are identical in the two
  columns, so the $119$\,ms between them is the controller. Ten flights on the left, three on the right,
  one training seed.}
  \label{tab:uav-controller-cost}
  \centering
  \small
  \begin{tabularx}{\linewidth}{@{}L r r@{}}
    \toprule
    Time per executed control step [ms] & Cascaded geometric, brake-to-rest & MuJoCo MPC \\
    \midrule
    The control loop                          & $94.9$ & $215.6$ \\
    \quad the planner                         & $88.4$ & $89.8$ \\
    \quad the controller and the simulator step & $6.5$ & $125.8$ \\
    \bottomrule
  \end{tabularx}
\end{table}
\dataref{Report\_20260909\_MJPC\_vs\_PID\_s\_curve \S5.4--5.5, from the job records of Campaign
DA\_20260909\_T5\_mjpc\_vs\_pid\_s\_curve; planner time from the same batch as
\autoref{tab:uav-controller}}
\guard{This is a decomposition of recorded elapsed times and not a measurement taken around the
controller call, which the evaluation does not instrument. The remainder row therefore carries the
simulator step with the controller, and it is only their \emph{difference} that isolates the controller.
One training seed, ten flights against three.}

What that price buys is a traverse and not a result. Neither controller produces a flight that succeeds
within the constraints on this scene: MuJoCo MPC turns flights that never arrive into flights
that arrive while still violating, at about twenty times the cost per control step of the controller
used everywhere else in this thesis. It is used here to locate the limit, and it is not adopted.

\subsection{Conclusion}\label{sec:res:uav:conclusion}

Each scene closes on its own line, and the benchmark on the three together.

\paragraph{UAV-pillars.} \hole{One line (R39): whether the winning avoiding configurations --- the two
average-velocity models at one and two evaluations --- fly as they execute: the same success and
constraint satisfaction before and after projection, or a stated difference, with what the vehicle adds in
contacts and tracking error.}

\paragraph{UAV-corridor.} \hole{One line per constraint (R33): which flow-based planners fly under the tilt
and under the hump, at which budget and under which projector, against the diffusion baseline; the expected
shape, from the pilots and the earlier level corridor, is the average-velocity models ahead of
instantaneous-velocity matching on the constraint and every flow model ahead of the baseline on the goal.}

\paragraph{UAV-s-curve.} No supported combination. Its limit is the tracking controller: the pilot flights
end with the vehicle inverted rather than off course, at every model's best budget, before any question about
the constraint can be asked (\autoref{sec:res:uav:controller}). The scene ranks nothing.

\paragraph{The benchmark.} What the quadrotor benchmark supports is therefore \hole{restated from R39 and R33
once they land: the cross-embodiment result of UAV-pillars and the three-dimensional constraint of
UAV-corridor}; UAV-s-curve contributes the caveat that every quadrotor result of this chapter is read for the
controller with which it was measured.

% =============================================================================
% =============================================================================
% 🔒 DO NOT EDIT (author, 2026-09-21 / v3.60)
%
% This section summarises 6.1, 6.2 and 6.3. It is the LAST thing to be written,
% because every number in it is a restatement of a number from those three, and
% a summary written over sections that are still moving has to be rewritten each
% time one of them moves.
%
% Leave it exactly as it stands until 6.1, 6.2 and 6.3 have each been verified
% against the corpus and locked by the author. Then reconcile this section with
% them in one pass. Do not "fix" a cell here on its own -- if a number here
% disagrees with its source section, the source section is what to check.
% =============================================================================
\section{Comparison across Environments}\label{sec:res:summary}
% =============================================================================

This section recaps the preceding ones and adds no new result.

\subsection{Generative Models}\label{sec:res:summary:models}

\autoref{tab:summary-models} names, per environment, the generative model that comes out best and the
measure that decides it. Analytic average-velocity matching is best, or shares first place, in each of the
three environments that yield a result.

\begin{table}[htpb]
  \caption[Generative models across environments]{The best generative model per environment, as found in
  Sections~\ref{sec:res:state}--\ref{sec:res:uav}. Temporal U-Net, 3.96--4.0\,M parameters, throughout.}
  \label{tab:summary-models}
  \centering
  \small
  \begin{tabularx}{\linewidth}{@{}p{2.3cm} L p{2.6cm} L@{}}
    \toprule
    Environment & Deciding measure & Best & The others \\
    \midrule
    D3IL-avoiding & control steps and time at equal success & MeanFM, CI-MeanFM & FM, then Diffusion \\
    D3IL-aligning & final distance of the box & MeanFM & CI-MeanFM, FM and Diffusion level \\
    UAV-corridor  & success on a collision-free flight & MeanFM, CI-MeanFM ($12/12$) & FM succeeds but not collision-free; Diffusion collision-free but never succeeds \\
    UAV-s-curve   & --- & none & limited by the tracking controller \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsection{Projection Methods}\label{sec:res:constraints}

\autoref{tab:summary-projection} names the projection method each environment selects. Per-step
projection is selected where the budget is too small for endpoint projection to act and on UAV-corridor;
endpoint projection is selected on D3IL-aligning, the task run at a budget that gives it
guiding steps.

\begin{table}[htpb]
  \caption[Projection methods across environments]{The selected projection method per environment, as
  found in Sections~\ref{sec:res:state}--\ref{sec:res:uav}.}
  \label{tab:summary-projection}
  \centering
  \small
  \begin{tabularx}{\linewidth}{@{}p{2.3cm} r p{2.3cm} L@{}}
    \toprule
    Environment & $\nfe$ & Selected & Reason \\
    \midrule
    D3IL-avoiding & 1  & per-step & no guiding step for endpoint projection \\
    D3IL-aligning & 20 & endpoint & all ten contexts free of violations \\
    UAV-corridor  & 3  & per-step & endpoint projection succeeds collision-free on at most 7 of 12 \\
    UAV-s-curve   & ---& none     & no flight succeeds within the constraints \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsection{Conclusion}\label{sec:res:summary:conclusion}

% v3.68d (author, 2026-09-23): this paragraph is the one addition made under the 🔒 lock, at the
% author's explicit request for a sub-conclusion beside the table.
Read across the environments, the generative models order the same way where the task separates them at
all. On D3IL-avoiding they do not separate on the outcome --- CI-MeanFM $\approx$ MeanFM $\approx$ FM
$\approx$ diffusion --- and separate on the price by a factor of thirty in favour of the flow-based models.
On D3IL-aligning they separate on the outcome, MeanFM $\gg$ CI-MeanFM $>$ FM $\approx$ diffusion. On the
quadrotor scenes the reading is pending the runs of \autoref{sec:res:uav:conclusion}. The projector orders
the other way round by task: per-step projection at a small budget where the task is solved at one or two
evaluations, endpoint projection at the large budget the alignment task needs.
\autoref{tab:summary-combinations} collects the selected combination of each environment.

\begin{table}[htpb]
  \caption[Selected model--projection combinations]{The selected combination per environment.}
  \label{tab:summary-combinations}
  \centering
  \small
  \begin{tabularx}{\linewidth}{@{}p{2.3cm} L L@{}}
    \toprule
    Environment & Combination & Result \\
    \midrule
    D3IL-avoiding
      & MeanFM or CI-MeanFM, $\nfe=1$, per-step projection, temporal consistency
      & S\&C $0.967$ and $1.000$ at $18.3$ and $18.1$\,ms per step; five seeds, the protocol of \ac{DPCC} \\
    D3IL-aligning
      & MeanFM, $\nfe=20$, endpoint projection, random selection
      & $10/10$ contexts free of violations, box moved in $8/10$ \\
    UAV-corridor
      & MeanFM, $\nfe=3$, per-step projection, temporal consistency
      & success, collision-free, in $12/12$ flights \\
    UAV-s-curve
      & none
      & limited by the tracking controller \\
    \bottomrule
  \end{tabularx}
\end{table}
