# v3.68 — 2026-09-22 · KEY UPDATE: the quadrotor chapters are rebuilt around pillars v2 (the avoiding planner flown), corridor v3 (tilt + roof) and the s-curve as a controller caveat; every cell blank for the 23-09 runs

Read after [`v3.67`](v3.67_20260922_all_tightened.md). Author's four items plus the issues noticed while writing the
23-09 pending file. **No bundle built.** Nothing deleted: `withheld/20260922_v3.68_archive/` holds what left the chapters.

## 0 · "The UAVs are ready; finish the new Chapter 5/6 so the data can be filled in"

Both quadrotor chapters now carry the **final structure with every cell blank**: pillars v2 (R39) and corridor v3 (R33)
as *pending* tables and `\todofigure` specs, the s-curve with its pilot cells filled. Order everywhere: pillars →
corridor → s-curve.

## 1 · "Pillars uses the avoiding planner, so 'expert data from a robot' is imprecise; real sim-to-real would need real expert data"

Chapter 5 §5.3.3 now opens with it: two scenes have quadrotor demonstrations and one has none; on UAV-pillars the planner
is the D3IL-avoiding one, trained on human manipulator demonstrations and not retrained, and the quadrotor executes its
plans in the scaled field. A `\guard` says nothing on that scene is evidence about learning to fly from flight data, and
the text says a deployed quadrotor would need demonstrations of its own. `tab:uav-demos` has a pillars column of dashes,
the "25 rejected pillar episodes" sentence and the pillars collection job are removed from the running text, the metrics
bullet for pillars is the avoiding metrics on the flown path plus contact, and the protocol row is the avoiding protocol.

## 2 · "Pillars first, then corridor (3-D solving), then s-curve (controller)"

- **Chapter 5:** §5.2.3 items rewritten in that order (pillars = cross-embodiment test at scale 36, keep-out 2.88 m,
  pillars 0.90/1.08 m; corridor = slide **and** roof, a detour in three coordinates; s-curve = the caveat);
  `tab:uav-scenes` rows reordered with the roof halfspaces added and the pillars rows as the mapped avoiding sets;
  `fig:env-uav` panels reordered; captions of `fig:env-uav`, `fig:constraints-uav`, `fig:expert-uav` carry
  `\outdated` notes (the pillars panels show the superseded scene; the corridor panels lack the roof); §5.3.3 pillars
  paragraph rewritten as "the second kind by construction"; `tab:protocol` rows and §5.5.3.3 rewritten.
- **Chapter 6 §6.3:** intro rewritten (what each scene adds, in that order); 6.3.1 = pillars-v2 *before projection*
  (new `tab:uav-pillars-raw`: table vs air, T1–T4) then corridor-raw; 6.3.2 = pillars-v2 *after projection*
  (`tab:uav-pillars`: table vs air for the selected configurations; `fig:uav-pillars-paths` as a `\todofigure`; the
  live check confined to a guard) then corridor then s-curve; 6.3.3 = pillars-v2 endpoint-vs-per-step
  (`tab:uav-pillars-endpoint` on the matched MeanFM K3 cell, T5) then corridor; 6.3.5 conclusion as holes in the
  new order. The three `pillarsflawed` blocks (`pillars_hg`) are archived, not deleted. **The mechanism by which the
  drone flies the avoiding plans is named only in `\dataref` drafting notes, never in the running text (author).**

## 3 · "S-curve: just the pilot tests; all fail at high K; then our explanation and figure of the controller"

§6.3.2 UAV-s-curve is now one paragraph, one table and two guards. `tab:uav-scurve` prints the four pilot cells, each
model at its strongest budget, unprojected: MeanFM K10 0/10 (10 inverted), CI-MeanFM K5 2/10 (8), FM K20 6/10 (4),
diffusion K20 0/10 (8). The low-K rows and the projected grid (`tab:uav-scurve-projection`) are gone from the chapter
(archived); the reading is "the vehicle's attitude fails in the turns before any question about the constraint can be
asked", pointing at §6.3.4, which is untouched and now carries the scene's purpose. The appendix per-model records keep
an `\outdated` note. Ledger: **R31 struck, R38 optional, R40 opened** (see 4).

## 4 · "The unprojected s-curve should all pass the goal line — check; if not, maybe a bug"

**Checked against the corpus (batch_uav_20260919_111701, `s_curve_hg`, `diffuser`, `pid_stopgo`): it does not pass.**
MeanFM K10 0/10 (all inverted at control steps 395–421), CI-MeanFM K1 6/10, K2 1/10, K5 2/10, FM K2 7/10, K20 6/10,
diffusion K20 0/10. Under MuJoCo MPC the same MeanFM K10 plans end 0.30 m from the goal and cross the line on 2 of 3
(Table 6.18). So the failure sits between the plan and the rotors, not in the planner: the tracker with a zero velocity
setpoint follows the reference closely (tracking error 0.29–0.34 m, the smallest on the scene) but makes no progress in
the turns and loses attitude. That is the chapter's caveat and it is consistent, but it is not yet *proven* to be the
setpoint rather than something in the evaluation loop, because the demonstrations were flown with feed-forward and no
unprojected pilot with the other two setpoint policies exists. **R40** (23-09 file §4, ~20 min) settles it: MeanFM K10
under `pid` and `pid_const_v`, and the expert reference replayed under `pid_stopgo`. Told the author in the chat.

## Second round (23-09, same changelog; bundle held)

**Item 4 — the corridor is two constraints, read separately.** The U19 note (`cross_draft/to_v3/FROM_U19_20260923_…`)
and the author: UAV-corridor carries **tilt** (the v2 slide leaned by −60° from the vertical about the launch
altitude, one plane that pushes sideways and down; tag `p23cv3t`) and **hump** (the roof; tag `p23cv3ah`), evaluated as
two scenes and reported like the avoiding geometries — there is no combined slide + roof scene. Chapter 5 §5.2.3 item,
`tab:uav-scenes` (tilt row with the `z_lean` rule, hump row), §5.3.3 corridor paragraph (lanes cross the tilt at the
launch altitude by 0.24/0.36/0.47 m and pass under the hump), the constraint-figure caption and §5.5.3.3 say so; Chapter
6's three corridor tables now have a *tilt* block and a *hump* block, the figure specs one panel/row per constraint,
and the *Altitude* paragraph is reversed: both constraints act on altitude (pilots: descent 0.15–0.29 m, climb
0.30–0.40 m). The 23-09 pending file and runbook are corrected to the two scenes, the tags, the driver, the
`hardflow_new` input name and the C3 composition; the INBOX row is ✅.

**Item 5 — no OUTDATED banners in the quadrotor parts.** All sixteen were removed from Chapters 5 and 6 (one remains,
on the appendix's per-model s-curve records). What was stale in the figures was rebuilt in the store: `fig_scene_uav_pillars`
now draws `scene_avoiding_pillars_s36.xml`; `fig_constraints_uav` draws UAV-pillars first as the avoiding *both-hard*
geometry mapped into the arena (new `sources._pillars_v2_scene`, per-scene inflation/tightening in `_uav_constraint_panel`,
tick step 5 m), the corridor as the tilt cut at the launch altitude, the s-curve as before; `fig_expert_uav` drops the
pillars routes (two panels). The pillars MuJoCo render needs the cluster and is a `\todofigure` inside `fig:env-uav`.
The chapters call the scenes UAV-pillars and UAV-corridor only — no "v2", "v3", "avoiding" or "ablation" in the text.

## Third round (23-09, same changelog; bundle held): the keep-out disk explained, and the quadrotor renders made here

**Chapter 5, item 1 — what a keep-out disk is.** After Table 5.2 (`tab:avoiding-geometries`) a paragraph now defines
the two elements of every geometry — the halfspace (one side of a line excluded for the planned position, a linear
row) and the keep-out disk (a circle of radius 0.08 m over one post that the planned position must stay outside of, the
quadratic row, which turns the free lane into a corridor between line and disk), neither a physical object — and a
**new `fig:constraints-avoiding-sets`** draws the three sets alone, labelled, on the same panels as Fig 5.8
(`builders/avoiding.py::fig_constraints_avoiding_sets`, same `geometry_panel` code path as the Chapter 6 executed-path
figure). The intro sentence of §5.2.1 points at it; Fig 5.8 keeps the demonstrations.

**Chapter 5, item 2 — Figure 5.6 rendered here.** MuJoCo 3.13 is importable in this container and renders with
`MUJOCO_GL=osmesa` (EGL and GLFW do not), so `prep/render_mujoco_scenes.py` was run locally for the three quadrotor
scenes with the same code as before: the pillars spec now points at `scene_avoiding_pillars_s36.xml`, places the X2 at the
mapped avoiding start and draws one avoiding demonstration mapped into the arena as the path (new `avoiding_demo`
path kind, thicker tube for the 25 m arena); the corridor and s-curve renders were re-rendered unchanged. The
`\todofigure` placeholder in `fig:env-uav` is gone and the caption says what the green path is on each scene.
`--only` flag added to the render script. Export: 4 files copied.

## Fourth round (23-09, same changelog; bundle held): Chapter 5 items 1–2 checked, Chapter 6 nine items and the sub-conclusions

**Ch 5.1 — are all three avoiding geometries drawn in §5.2?** Yes: `fig:constraints-avoiding-sets` (third round) has one
panel per geometry — top-left-hard, top-right-hard, both-hard — with halfspace, keep-out disk, tightened boundary, posts
and goal line; Table 5.2 lists the same three. Nothing further to add.

**Ch 5.2 — Fig 5.7's corridor panel was outdated.** `fig_constraints_uav` now has a second row: two side-view panels
along route C — *tilt* (the leaned plane, its inflated ceiling descending from 1.46 m at x = −2 to 0.90 m at x = 2,
tightened dashed, the excluded side shaded) and *hump* (the roof, its inflated boundary 0.385 m above it, excluded
below) — with the workspace-box limits and the launch altitude for scale (`scenes._uav_side_panel`, numbers from
`config/uav_projection.yaml`). The top-view corridor panel keeps the tilt's cut at the launch altitude. Caption updated.

**Ch 6.0 — tightened stated with the baseline.** §5.5.1 now says the baseline and every configuration are compared on
the tightened set, that tightening is the margin DPCC itself introduces for feasibility under execution error
\parencite[Sec.~5.3]{romer2025diffusion}, that it is DPCC's reported setting, and that it is the safer execution.

**Ch 6.1 — the "no claim rests on it" sentence is gone.** §6.1's intro now ends with one factual sentence: a longer
evaluation of selected configurations at twenty episodes per seed is in the appendix.

**Ch 6.2 — Fig 6.1 text.** One sentence: the raw plan is to a planning model what a generated image is to an image
model, the fidelity of the sample before any use is made of it \parencite{ho2020denoising,lipman2023flow,geng2025meanflow}.

**Ch 6.3 — §6.1.2.2 opener removed.** The paragraph now starts with the single-candidate fact.

**Ch 6.4 — Fig 6.3 / 6.6 sanity.** Fig 6.3 draws every model under *both* rules (c circles, t triangles) and takes the
frontier over all points, so the ringed point is the model at its better rule — now said in the caption. Fig 6.6
draws the selected rule (random) only, with the other two in Table 6.8 — said in the caption. Fig 6.5 is unprojected
(no rule).

**Ch 6.5 — Fig 6.4 overlap.** CI-MeanFM and FM (both 1.000 at both budgets) are drawn displaced by ±0.006; the caption
names the offset as a drawing offset.

**Ch 6.6 — Fig 6.5 (and 6.6) axis.** The outcome axis is now logarithmic in the distance *left*: a point sits at
−log10(share not closed), ticks 0/50/80/90/95 %; 84 % and 85 % separate. Both frontier figures share it; captions say so.

**Ch 6.7 — Table 6.7's K = 2 endpoint rows.** Checked against the corpus: the cells exist (`hardflow_new-{r,c,t}` at
K2 T0.5, `hf_n_genuine = 0`) — the data are not wrong, they are the method with zero guiding steps, i.e. projection
after sampling. Per the thesis's own convention they are no comparison, so the K = 2 block now carries per-step rows
only and one line "none: no guiding step at K = 2" with the formula reference; the two paragraphs around the table are
rewritten; Table 6.6's K = 2 endpoint cell is a dash and R37 loses that pair (ledger).

**Ch 6.8 — seeds.** "training seed 6" → "one training seed" throughout Chapter 6 prose and captions; Table 6.3's seeds
column prints counts (4 / 1); seed numbers survive only inside `\dataref`/`\srcnote` drafting notes.

**Ch 6.9 — §6.3 is one part per scene.** 6.3.1 UAV-pillars (Generative Models / Projection / Projection Methods),
6.3.2 UAV-corridor (the same three), 6.3.3 UAV-s-curve (the pilot cells, an introduction to the caveat), 6.3.4 the
caveat, 6.3.5 conclusion with one paragraph per scene and one for the benchmark. Labels kept where other chapters cite
them (`sec:res:uav:pillars`, `sec:res:uav:corridor`, `sec:res:uav:scurve`, `sec:res:uav:projection` now on the
corridor's projection-methods part).

**Sub-conclusions with the rough ordering.** §6.1.4: CI-MeanFM ≈ MeanFM ≈ FM ≈ diffusion on the outcome, the flow
models thirty times cheaper. §6.2.4: MeanFM ≫ CI-MeanFM > FM ≈ diffusion. §6.3.5: per scene (pillars, corridor as
holes with the expected shape; s-curve ranks nothing). **§6.4.3 (🔒 locked): one paragraph added at the author's
explicit request**, marked in a comment as the single edit under the lock.

## Issues noticed while writing the 23-09 pending file

Listed in `PENDING_20260923_uav_corridor_v3_pillars_v2_paper_runs.md` §5: turbo `--engine` names unverified; `_cv3` tag
parsing in DA_UAV_v1 unverified; U18 scale is 36 not 10 (Chapter 5 written at 36); the U19 roof numbers were computed
without the slide, G0 must check both jointly; corridor v3 needs the `plane: xz` coding first; the figure store still
draws `pillars_hg` and the roofless corridor.

## Checks

`python3 tools/check.py` after the second round: all mechanical checks pass; hole 32, provisional 1, guard 20, srcnote 63,
dataref 44, todofigure 5, flawed 2 (preamble definitions only), **outdated 1** (appendix). Export: six files recopied. Not compiled.

## Bundle, fourth round (author: "build the bundle, I will review")

`bundle/output/thesis_v3_20260922_204547_new.zip`, `…204547_new_clean.zip`, `…204548_new_nonotes.zip` (9659 KB; 46 figures
render, 0 placeholders; cross-references resolve; `--verify` byte-faithful). Supersedes `195031`. The 22-09 ledger got a
**NOW** table at the top (nine open runs) and the unused runs struck (R7/R14, R9, R15, R17, R27, R28).

## Bundle, second round (author: "build the bundle, I will review")

`bundle/output/thesis_v3_20260922_195031_new.zip`, `…195032_new_clean.zip`, `…195032_new_nonotes.zip` (9272 KB; 45 figures
render, 0 placeholders; cross-references resolve; `--verify` byte-faithful). Supersedes the first-round set (`162325`).

## Bundle, first round (author: the s-curve pre-projection table carries the success rate → build)

`bundle/output/thesis_v3_20260922_162325_new.zip`, `…162326_new_clean.zip`, `…162326_new_nonotes.zip` (9264 KB; 45 figures
render, 0 placeholders; cross-references resolve; `--verify` byte-faithful). Supersedes the v3.67 set (`150138`).

## Files

`v3/chapters/05_setup.tex`, `06_results.tex`, `09_appendix.tex`; `v3/withheld/20260922_v3.68_archive/` (README + the
archived blocks); `data_status/PENDING_20260922_all_lacking_runs.md` (§0 row, R31/R38, §26),
`PENDING_20260923_uav_corridor_v3_pillars_v2_paper_runs.md` (§4 R40, §5 issues), `INDEX.md`; `cross_draft/INBOX.md`;
`v3/CHANGELOG.md`, `v3/BRIEFING_20260922_v3.60.md`.

Claude (Fable 5.1, Claude Code) · 2026-09-22 · mechanical checks only. Not compiled.
