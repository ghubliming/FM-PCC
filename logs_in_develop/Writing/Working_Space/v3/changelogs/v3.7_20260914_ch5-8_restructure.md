# v3.7 — 2026-09-14 · Chapters 5–8 restructured by environment; Discussion handed to v4

Individual changelog. Each key point (KP) of the request is answered by what changed. The pointer and
the `For v2:` lines are in [`../CHANGELOG.md`](../CHANGELOG.md).

**Before writing, v3 was synced from v2.9 to v2.15.** The request depends on v2's newest Chapter 4:
§4.7 *Environments* (`sec:method:envs`) now holds how the environments and their constraint forms are
built, and v2 asks v3 to reference it rather than restate it. Those labels did not exist in v3's copies.
The seven inherited files were merged **one by one by name** (`sync_v2.py merge <file>`), which keeps the
known `merge` bug away from v3's own chapters. The one v2 edit to a v3-owned file — its placeholder Ch 6
heading *Constraint Arms* → *Projection Methods* — is adopted (KP6) and its baseline advanced.
`status`: clean at v2.15.

---

## KP1 — "We did not choose the UAV and visual aligning; we built them. Only avoiding uses DPCC's setup 1:1, for the baseline proof. Show: first prove → then more tests."

**Changed.**
- The chapter opens with the two stages of v2's §1.2: first on DPCC's obstacle-avoidance benchmark with
  its setup unchanged, then in two environments **built for this thesis**. The word *chosen* is gone.
- §5.1.1 Obstacle Avoidance: *used exactly as DPCC uses it* (with citation). The geometries are those of
  the DPCC configuration.
- §5.1.2 Vision-Conditioned Alignment: *its simulation, demonstrations and vision pipeline are D3IL's;
  around them this thesis built a constrained-control environment*.
- §5.1.3 Quadrotor Benchmark: *built for this thesis around the Skydio X2 model*.
- **The proof is now shown, not just stated.** §5.3.1 `tab:target` sets DPCC's published row next to
  v3's reproduction on DPCC's own four metrics. Published: 69.0 ± 12.9 steps · success 1.00 · with
  constraints 0.98 · 0.0 ± 0.3 violating steps. Reproduced: 69.0 · 0.997 · 0.983 · 0.03.
  Source: DPCC Table 1, read from `aux_repo/PAPERS/in Proposual/DPCC.pdf`, and
  `batch_avoiding_combined_20260825`, recomputed this session with the per-geometry mean.

## KP2 — "Delete *Two caveats that belong to the tasks rather than to any result*. No such thing; self-destroying."

**Deleted**, and with it everything in Ch 5 written in the same spirit:
- the success-criterion caveat for alignment;
- the "corridor is constraint-trivial" caveat — also outdated, since corridor v2 now separates the models (KP4);
- the *regime taxonomy* (all-pass / discriminating / all-fail);
- the scene-clearance-versus-tracking-error paragraph and its figure `fig:constraints-uav`;
- the `Definition` of improvement — "better" is never defined globally in thesis prose;
- the *Why the third entry is a different problem* subsection, now a reference to v2's §2.7 (`sec:bg:quadrotor`);
- the *Constraint sets* subsection and `tab:constraint-families`, now v2's §4.7.

The compute-environment text was condensed. Its two explanatory claims stay; the sentences about
where they are "recorded rather than" discussed are gone.

## KP3 — "The funnel, 3 stages for V_A or UAV is jargon. Record it in the translation file."

**Changed.** No *funnel*, *stage* (in that sense), *regime*, *all-pass/all-fail*, *Entry*, *Tier* or
*held table* remains in Ch 5–8. For alignment, the comparison order is now stated plainly in §5.4.2:
distance is measured on plans executed without projection when models are compared, and with projection
when projection methods are compared. §6.2 opens the same way.

**Recorded** in the shared
[`Auxiliary/Naming/TRANSLATION_20260914_dev_jargon_to_scientific.md`](../../../Auxiliary/Naming/TRANSLATION_20260914_dev_jargon_to_scientific.md)
§6 — 12 new rows marked *(v3.7)*: funnel/stages · regime / all-pass / discriminating / all-fail ·
Entry 1/2/3 · Tier 1/2 · strict / crossed-line S&C and `cfree` · `untouched`/`frozen` · `min_xy_dist` /
median / MIN and the box angle · reproducibility floor · controller-limit case · the slide · *consistency
target* (superseded by *consistency training*) · headline / foundation / held table.

## KP4 — "UAV massively updated; corridor now works. Simple corridor → hard main pillars → most difficult s-curve."

**Changed.** §5.1.3 and §6.3 now follow that order: *Corridor — the entry test · Pillars — the main
test · S-Curve — the most difficult test*.

**New corridor results, from the latest analysis.** Source:
`Gen15/U16/DA_20260914_corridor_v2_paper_full.md`, batch `temp/1409/batch_uav_20260914_091148`.

| result | value |
|---|---|
| without projection | 0/12 flights collision-free, 42–56 violating steps |
| per-step projection at $K=3,5$ | MeanFlow, consistency training and diffusion collision-free 12/12 ($p=7\times10^{-7}$) |
| crossing the finish line with projection | flow models 12/12; diffusion 0/36 |
| success at the finish line with constraints | MeanFlow 1.00 · consistency training 1.00 · flow matching ≤ 0.17 · diffusion 0.00 |
| time per control step | flow models 86–102 ms at $K=3$; diffusion 626–651 ms |
| endpoint vs per-step projection | 1.15–2.1× faster but collision-free ≤ 0.58 vs 1.00 ($p=0.005$) |

**Scene geometry** in the new `tab:uav-scenes` is read from `config/uav_projection.yaml`
(`corridor_v2_slide`, `pillars_hg`, `s_curve_hg`), not from notes:
- corridor walls at $y=\pm0.95$ m, clear 1.90 m; the 14° slide from $(-2.0, 0.95)$ to $(2.0, -0.05)$;
- pillars: radius 0.12 m at $x\in\{-2,0,2\}$, $y=\pm0.6$;
- s-curve: switched walls;
- workspace box and body radius 0.31 m.

The slide is written as a **test-time constraint** the models never saw, the same role as the
avoiding halfspaces.

Pillars gained the **diffusion reference**: `DA_20260912_pillars_diffusion_baseline_reference.md`
rev. 2. It crosses the line 50/50, is 0/50 at the goal point, and runs at 4425 ms per step.

## KP5 — "Ch 5: organise each section by the 3 major tests. Baselines: only avoiding, others placeholders. Metrics: avoiding = DPCC's with citation; V_A only what we used (min_xy_dist, angle…); UAV same."

**Changed.** Sections 5.1–5.5 each have three numbered subsections — *Obstacle Avoidance ·
Vision-Conditioned Alignment · Quadrotor Benchmark* — plus *Compute Environment* at the end of 5.5.

| section | avoiding | alignment | quadrotor |
|---|---|---|---|
| 5.1 Tasks | DPCC's task and geometries; 2 figure specs | what is D3IL's and what was built; figure spec | three scenes in order, `tab:uav-scenes`, figure spec |
| 5.2 Datasets | D3IL demonstrations as used by DPCC | D3IL demonstrations + the two streams, laid out as the 9-D plan | generated in two layers; the recorded action is the setpoint increment; corridor v2 reuses corridor's data |
| 5.3 Baselines | **written**: DPCC configuration + `tab:target` | `\hole` placeholder | `\hole` placeholder |
| 5.4 Metrics | **DPCC's four metrics, cited** (Sec. 6.1) + wall-clock | **only what the analyses use**: final box–target distance in the table plane (median, minimum over contexts), final box orientation vs target, box moved or not, violations, time — the D3IL success criterion is not listed | collision-free flight, finish-line and goal-point success each with constraint satisfaction, steps, time split into generation and projection |
| 5.5 Protocol | DPCC's 5 training seeds × 10 test episodes (cited) vs ours × 20; `tab:tiers` | enumerated paired contexts, seed 6, exact paired tests | seed 6; corridor 12 flights (4 per route); pillars/s-curve 10; scenes not pooled; controller named |

**`min_xy_dist`, checked.** No column has that name. The per-episode columns the V_A analyses use are:
- `context_final_xy_dist` — the tables report its median and MIN over contexts;
- `context_final_box_angle_deg` against `context_target_angle_deg`;
- `frozen`.

§5.4.2 describes these by what they measure. The angle has no reported numbers yet, so §6.2.1 carries a
`\hole` to add it.

## KP6 — "Results: rebuild into 3 subchapters for the 3 entries, plus a subchapter answering af~mf > fm > diffusion and HF > PCC in each env, aggregated."

**Changed.** Ch 6 = 6.1 Obstacle Avoidance · 6.2 Vision-Conditioned Alignment · 6.3 Quadrotor Benchmark
(Corridor / Pillars / S-Curve) · **6.4 Comparison across Environments**.

**6.4.1 Generative Models** — `tab:summary-models`, one row per environment, four comparisons:

| environment | consistency training vs MeanFlow | MeanFlow vs flow matching | flow matching vs diffusion | MeanFlow vs diffusion |
|---|---|---|---|---|
| avoiding | ≈ ($p=0.231$) | > (cost, seed 6) | ≈ success, 21× faster | > |
| alignment | ≈ at $K=2$, < at $K=20$ | > | ≈ | > |
| corridor | ≈ | > | completes 12/12 vs 0/36 | > |
| pillars | < | > | above reference | above reference |

**Answer, as written:** MeanFlow is ahead of flow matching and the baseline everywhere. Consistency
training is not ahead of MeanFlow anywhere. So *af ≈ mf* holds on avoiding and the corridor and fails on
alignment and pillars. *mf > fm > diffusion* holds on avoiding (on cost), on the corridor, and on
alignment in its first step.

**6.4.2 Projection Methods** — `tab:summary-projection`, endpoint vs per-step projection:

| environment | time | constraint satisfaction |
|---|---|---|
| avoiding, $K=3$ | 0.0745 vs 0.1478 s | level (n.s.) |
| alignment, $K=10$ | −325 ms/step, $p=0.0039$ | level |
| corridor, $K=3$ | 1.15–2.1× faster | lower, $p=0.005$ |
| pillars, $K=5$ | projection 8.2–21.3× faster | higher, 0/210 vs 43/270 |

**Answer, as written:** *HF > PCC* holds on time in all four environments; on constraints it holds on
pillars, is level on avoiding and alignment, and is reversed on the corridor.

**Adopted from v2.12:** the heading *Projection Methods* on `sec:res:constraints`.

**Labels kept:**
- `sec:res:fewstep` (now 6.1.2) and `sec:res:constraints:degenerate` (now 6.1.5), which v2's Ch 4 references;
- `sec:res:state`, `sec:res:visual`, `sec:res:uav`, `sec:res:ablations`, `sec:res:summary`.

**Names:** the prose uses *consistency training* (v2.15), *generative model*, *projection method*,
*endpoint / per-step projection*. It avoids *arm*, *engine*, *projector*, *fan*, *Pareto*, *cell* and
*flagship*. Branded method names appear only inside `\parencite`.

## KP7 — "Discussion: I don't know what this is; drop it, keep the structure, it should be v4's job. Update the MD for who does what."

**Changed.** `chapters/07_discussion.tex` now holds only the chapter heading, one `\hole{Written in v4.}`
and the four section headings with their labels. The labels stay because v2's Ch 4 references
`sec:disc:interpretation` and `sec:disc:negative`.

**Nothing is lost.** The 229 lines v3 had written are copied unedited to
[`../handover/HANDOVER_20260914_discussion_prose_for_v4.tex`](../handover/HANDOVER_20260914_discussion_prose_for_v4.tex).
The copy stays inside `v3/`, since v3 never writes into `v4/`. Its header warns that the prose predates
the naming rules and the corridor v2 results.

**Ownership updated** in [`../../DRAFT_OWNERSHIP.md`](../../DRAFT_OWNERSHIP.md) and in the memory entry
`thesis-draft-ownership`:
- v3 = Ch 5, Ch 6, the appendix, a concise Ch 8;
- v4 = **Ch 7 Discussion** plus refining Ch 8 and future work;
- the overlap table gained Discussion and Conclusion rows.

## KP8 — "Conclusion: a mix between v3 and v4; write something concise, v4 will refine."

**Changed.** Ch 8 went from 126 to 56 lines:
- a two-paragraph summary: the two stages, the reproduced baseline, the main findings per environment in words;
- one short answer per research question, each pointing at its section in Ch 6;
- future work as a `\hole` for v4, pointing at `future_work/`;
- two `\hole{Refine in v4}` markers.

---

## Also done in this pass

- **Appendix** (`09_appendix.tex`):
  - `tab:names` uses the current names: *generative models*, *consistency training*, *projection
    methods*, *per-step projection (DPCC)*, *endpoint projection*, *tightened constraints*;
  - `tab:corpora` is listed by environment and gained the corridor v2 and pillars-diffusion batches;
  - the defensive sentences are gone.
- **Figures:** titles and legends use *obstacle avoidance*, *model*, *projection method*, *per-step /
  endpoint projection*; `plots/sources.py` labels consistency training as such and registers the two new
  quadrotor corpora. All figures were rebuilt: 6 generated, 4 vendored.
- **v2 holes answered from the protocol side.** Ch 4's hole asking which controller produced the UAV
  results → §5.5.3. Its hole on the flow-matching sampling prior → §5.5.1.
- **One cross-reference changed for the new-sections-only bundle.** Ch 5 referenced
  `eq:method:dpcc:tighten`, which prints `??` when Ch 4 is collapsed. It now references
  `sec:method:dpcc` (Subsection 4.5.2) and resolves in both builds.

## Shared notes touched

- `Auxiliary/Naming/TRANSLATION_20260914_dev_jargon_to_scientific.md` — 12 rows in §6, header line.
- `Working_Space/DRAFT_OWNERSHIP.md` — map, v3 rules, overlap table, open items.
- Memory `thesis-draft-ownership.md` and its `MEMORY.md` line.

## Checks

| check | result |
|---|---|
| `tools/check.py` | dangling refs 0 · duplicate labels 0 · missing bib keys 0 (50 cited of 50) · environments balanced · braces 0 · no bundler-only macros in sources |
| drafting macros | `\hole` 27 · `\provisional` 7 · `\guard` 2 · `\srcnote` 43 · `\dataref` 21 · `\todofigure` 4 |
| jargon scan of Ch 5–9 | clean — remaining *rows* mean equation or table rows; *two stages* is the author's framing |
| `sync_v2.py status` | clean at v2.15 |
| bundles | `_new`: 4 v2 chapters collapsed, all references resolve · `_full`: 3 564 lines · `--verify` byte-faithful on both |

**Not compiled.**
