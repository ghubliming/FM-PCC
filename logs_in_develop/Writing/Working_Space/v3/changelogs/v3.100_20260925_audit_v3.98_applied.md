# v3.100 — 2026-09-25 · The ChatGPT audit of v3.98 applied in Ch 5/6 (§13 of the audit MD), the alignment halfspace added to the demonstration check, four figures rebuilt, `check.py` follows nested inputs

**Asked (author, 2026-09-25):** "I agree with them" (the change list, §13 of
`audit from chatgpt/AUDIT_v3.98_2026-09-24.md`, with the defaults F02 = the CI-MeanFM lead, F03 = rename);
"A4: the V_A demo is lacking the halfspace, yes, add into it, and need sanity check elsewhere, I totally
forgot there is a halfspace in V_A"; "Shipped the changes with new changelog for v3. Go. Anything to notify
v2/v4?"

## What changed in the draft (76 passages; every replaced passage verbatim in `withheld/20260925_v3.100_archive/passages_before_v3.100.tex`, by item id)

**Chapter 5 (`05_setup.tex`), A1–A17 of §13, plus A4x**
- A1 platforms: the 0.03 s planner interval and spatial increments scoped to UAV-corridor/UAV-s-curve; UAV-pillars
  flies the planar D3IL-avoiding planner at a fixed altitude, one control step of 1 s (prose and Table 5.1).
- A2 the first D3IL obstacle has radius 0.03, the other five 0.025 (`avoiding_objects.py:14–52`).
- **A3 (F01)** violation counts are taken against the nominal constraints; the projector alone sees the tightened
  set; on the two 3-D scenes the nominal surfaces carry the rotor reach; srcnote with the five evaluators' lines.
- **A4 (F13.1) + the author's halfspace request**: "Neither is respected by the demonstrations" → the straight push
  crosses the keep-out region in 106/120 contexts (119 tightened) and **the halfspace in none** (both ends of every
  push lie on the allowed side); the recorded end-effector paths are on the cluster and were not tested; the plans
  learned from the demonstrations enter both — without projection the keep-out region in **7** of the ten
  evaluation contexts and the halfspace in **1** (tightened set; untightened 7 and 2). Figure caption and dataref
  updated; A4x: the box-around-keep-out sentence also names the far corner the halfspace excludes.
- **A5 (F13.2, §11.3)** the s-curve route is the generator's reference route and is feasible; the learned plans cut
  the second corner; the scene is read for tracking and the selected per-step configuration; no projector ranking
  because endpoint projection has no guiding step at the selected budget (not because the route is feasible).
- A6, A9 the 0.31 m rotor reach scoped to UAV-corridor/UAV-s-curve; UAV-pillars carries the extent through the
  scale (rod radius 0.01 → 0.36 m radial reach), no band added.
- **A7 (F03)** the alignment percentage defined as it is computed: the reduction of the median final distance
  relative to the mean initial distance, 100·(1 − median(d_final)/0.4530); 0 % is not a box left unmoved (median
  initial distance 0.4557 m).
- **A8 (F08, §9.5)** the finish-line crossing is latched; the flight flies on; the controlled-flight checks are taken
  over the whole flight.
- A10 spread categories: multi-seed = the DPCC-protocol avoiding tables and UAV-pillars; single-seed = the one-seed
  avoiding tables, aligning, UAV-corridor, UAV-s-curve.
- A11 Table 5.x column "UAV, all scenes" → "UAV-corridor, UAV-s-curve"; caption and prose say UAV-pillars flies the
  D3IL-avoiding models and their (N,8,6) plans unchanged.
- A12 (F12) the training recipe is part of what is compared.
- A13 (F11) δ = 0.03 m on D3IL-aligning in Table 5.y and in the prose.
- A14 the endpoint lookahead is taken from the predicted state, whose endpoint is then projected.
- A15 "one more step whenever ηK is integral" → integral and below K; at η = 1 both families project on every step.
- **A16–A17 (F05)** at K = 2, η = 0.5 endpoint projection reduces to a terminal projection; bit-identical rollouts
  were measured on instantaneous-velocity matching only (dataref to the 2026-08-24 parity DA, which records that
  MeanFM's arms differed); the h = 0 sampler qualification now covers every transport step and every budget.

**Chapter 6 (`06_results.tex`), B1–B31 of §13 with B5b, B26b, B27b, plus B14b**
- B1, B4, B13 the three guards that printed provenance or editorial text: panel-cropping note → `\dataref`; the
  PENDING-ledger path → `\dataref`; "Whether the chapter states it is the author's call" removed.
- B2 the frontier's 0.05 band stated with its consequence (29/30 admitted beside 30/30).
- B3, B8 the guiding-step counts at K = 2 and the "smallest guided budget" carry their threshold (0.5 / 1.0).
- **B5, B5b (F02, §11.2)** K = 3: five of six points dominated on all three columns by CI-MeanFM K = 1 (four seeds
  against five); the MeanFM endpoint point is not (equal success, 0.3 steps shorter, 4× the time); "similar
  control-step counts at three to eight times the planning time"; the recap says "off the cost frontier, which
  admits success within 0.05".
- **B6 (F05)** the bit-identical K = 2 run named as FM; "at 2.45 times the cost" dropped (measured before the
  2026-08-24 terminal-evaluation change; dataref says so).
- **B7, B30 (F02)** the plural "the average-velocity models Pareto-dominate" → CI-MeanFM Pareto-dominates; MeanFM
  matches the saving at one episode in thirty (avoiding conclusion, §6.4 intro and item, the two §6.4 conclusion
  sentences); §6.4 intro names who dominates where (CI-MeanFM and FM on avoiding, MeanFM on aligning).
- **B9, B14 (F04)** the untightened-set caveat; the grey ring named as the untightened unprojected cell; the price
  read against the tightened evaluation's own unprojected row (80 % at 172.5 ms → 61 % at 266 ms, the ring's 84 %
  at 190 ms in brackets); B14b: "crossing the constraint in eight contexts" → the keep-out region in seven and the
  halfspace in one.
- B10 "moves the box to its target" → most of the way.
- **B11 (F03)** Table 6.5 and 6.7 captions, the reading rule, both frontier captions, the §6.4 percentage renamed;
  "0 % is a box not moved" and "the tick labels are the share closed" removed; B12 the frozen context is context 7
  (position 8 in the evaluation's order).
- B15, B20 (F06, pillars) "the same plans" → the same planner configurations; B16–B18 (F10) "changes nothing" →
  little; "guarantees" → delivered on every one of the 120 projected flights up to the moment each ended; the
  tracking figure named as the mean over flights of the 95th-percentile gap.
- B19 (F02) corridor: "dominated" within the band of at most two violating steps, in which the baseline violates
  least; B21 (F09) "to within 1.6" wherever the flights reach the end of the corridor; B31 "any flow-based
  objective" → those tested at a budget.
- **B22–B23 (F08)** a flight ends at the goal point, the cap or the abort; the crossing is latched and does not
  end it; Table 6.13's success = crossing in controlled flight.
- **B24–B28, B27b (F06, §11.4)** "keeping the plan", "the same plans" (Table 6.15 title and caption, the caveat
  paragraph, its conclusion, the guard, §6.4) → the same planner configuration, in closed loop.
- **B26, B26b, B27 (F07, §11.4)** the stale pilot sentence → MuJoCo MPC costs 24× the cascaded controller per
  control step, controller and simulator step together, unprojected pair; "isolates the controller" removed from
  the guard; "a twenty-fourth of the cost per control step" → of the controller's cost (a tenth of the loop
  unprojected, half projected), three places.
- B29 (F13.3) "follows from the data" → read as a property of the data; "as expected" → consistent with.
- Two captions trimmed back to about 100 words after the additions (Table 5.x training parameters; Table 6.13).

## Figures (C1 required; the store, `Data_Analysis/DA_in_Paper`)
- `builders/frontier.py:252, 390` label "box not moved" → "0 % reference" (and the axis comment); `fig_aligning_tradeoff`
  and `fig_aligning_projected_tradeoff` rebuilt, every plotted value unchanged (token diff: one text node each).
- `extract/expert_paths.py` tests the push against the halfspace (nominal and tightened) → `data/expert_paths.json`;
  `builders/expert.py` subtitle "…, none the halfspace"; `fig_expert_aligning` rebuilt.
- Incidental, from the full store rebuild: `fig_uav_corridor_paths` legend vocabulary to the v3.79 rule (the
  builder had it, the store copy did not); 17 other SVGs differed from HEAD only by generated clip-path ids and
  were restored. PNGs re-rendered with `svg/preview_png.py --scale 3` (the store's scale).
- Exported to `v3/figures/` (`export_to_draft.py v3`: 6 files). **v4's copies are stale and the release prefers
  them** (`make_release.py:584`): v4 runs `export_to_draft.py v4` — in the note below.
- Analysis of record: `analysis/DA_20260925_aligning_frontier_label_halfspace_push.md` (the definition, the
  0/120, the per-context breakdown, the incidental fix); `analysis/INDEX.md` row for `fig:expert-aligning` updated.
- C2 (optional) not done: the grey ring stays the untightened cell and B14 labels it.

## Tools
- E1: `tools/check.py::files_from_master` follows `\input` inside chapter files (template inputs from `parts/`
  are not reported). Result: 15 files, 8109 lines, 293 labels, 53/53 citations, 41 figures, **all pass** — the
  v3.99 false "no \label: app:avoiding-twenty" is gone. Not compiled.

## Cross-draft
- → v4: `cross_draft/to_v4/FROM_v3_20260925_v3.100_audit_applied_ch7_wording_figures.md` + INBOX row: Ch 7 l. 25
  (plural dominance), l. 45 ("the same plans"), l. 47 ("a twenty-fourth of the cost") to mirror; re-export the
  figure copies. Ch 8 needs nothing.
- → v2: nothing. Ch 4 describes the alignment constraint set with both forms (`04_method.tex:1530–1535`); the
  abstract already names FM and CI-MeanFM for the reliability claim; Ch 1–4 print none of the changed sentences.

## Not done / open
- D01 (MeanFM seed 7 / top-right-hard, 29/30 or 30/30): a cluster download and `ls`, the author's; the text
  keeps 29/30.
- The demonstrations' recorded end-effector paths against the two alignment constraints: on the cluster; the
  thesis says they were not tested.
- Bundle rebuilt on request (author, 2026-09-25 10:20): `bundle/output/thesis_v3_20260925_102006_new.{tex,zip}`
  (annotated, Ch 1–4 collapsed at v2.27; 11 files, 5941→6101 lines) and `thesis_v3_20260925_102007_full_clean.{tex,zip}`
  (15 files, 8123→8221 lines); both `--verify` byte-faithful; the rebuilt figures travel in the zips. The release is
  still built from the live files (Ch 7–9 and the appendix from v4).

Signed: Claude (Fable 5.1, Claude Code) · 2026-09-25 · figures rebuilt with local `python3.14`, no cluster job ·
not compiled · nothing committed · nothing under `v2/` or `v4/` touched.
