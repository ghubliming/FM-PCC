# v3.95 — 2026-09-24 · §6.4: the text tables become key points; D3IL-aligning said by "in position" and distance, not "moved"

**Asked (author):**
1. "Table 6.19, '10/10 contexts free of violations, box moved in 8/10' can you phrase it better? not just moved! show
   which is in position and the dist_xy. and this thing should NOT be in a table, but in main text and maybe as key
   points or whatever is better."
2. "Table 6.17 also not suitable for a table. should use others to organise and highlight."
3. "… in Chap 6.4, some part is not suitable for a table, too much text. plz recheck them and change a way."
4. "then build the bundle after finished".

## §6.4 (`chapters/06_results.tex`; the old text in `withheld/20260924_v3.95_archive/`)

**The three tables are gone:** `tab:summary-models`, `tab:summary-projection` and `tab:summary-combinations`, all
text-heavy. They were referenced only inside §6.4 (checked).

**§6.4.1 Generative Models:** a two-sentence lead, then a `description` list, one highlighted key point per
environment:
- D3IL-avoiding: the central result;
- D3IL-aligning: the result again;
- UAV-pillars: reaffirmed on the quadrotor;
- UAV-corridor: something new;
- UAV-s-curve: the controller matters.

**§6.4.2 Projection Methods:** a one-sentence lead, then a key point per environment, each headed by its operating
budget.

**§6.4.3 Conclusion:**
- the reading paragraph, as before;
- the three cross-cutting findings as a numbered list with bold heads;
- "The combination each environment supports, and what it achieves" as a `description` list, in place of Table 6.19.

**The D3IL-aligning result is no longer "box moved in 8/10".** Per context, for MeanFM K20 endpoint random:
- 0 of 10 contexts end **in position** (within 1.8 cm);
- the best two end **2.2 and 4.7 cm** from the target;
- six more close 40–75 % of the distance and end 13–27 cm away;
- two do not move (41 and 45 cm);
- the median is 0.197 m (57 % closed);
- the unprojected plan's best two end 2.8 and 2.9 cm away, and it is also 0/10 in position.

## "In position", defined where the metrics are (Ch 5) and stated where the result is (§6.2)

- **Ch 5 `sec:setup:metrics:aligning`:** a new bullet. In position = within 0.018 m of the target in the table plane,
  the position half of D3IL-aligning's own success test (`aligning.py:198`, `pos_min_dist = 0.018`). The orientation
  half (`rot_min_dist`) is not used, for the reason Ch 5 already gives (the inconsistent angle convention).
- **§6.2 conclusion** gains the fact: no context in position; 2.2 and 4.7 cm at best with endpoint projection; 2.8 and
  2.9 cm unprojected. So §6.4 still only restates.
- **Two "solves it" become "comes closest to (solving) it"**, one in the §6.2 conclusion and one in the §6.2 body
  sentence on the two-evaluation block. With no context in position, "solves" overstated.
- **Source:** `Data_Analysis/DA_in_Paper/analysis/aligning_in_position.py` (new). It reads
  `per_rollout_detail.csv` of `batch_va2_20260915_100754`, MeanFM K20 `hardflow_sls-r` and `diffuser`, tightened
  geometry.
  - It reproduces the printed medians (0.197 and 0.090 m), means and moved counts (8 and 9).
  - D3IL's own `n_success` is 0 in every context of both.

## Checks

- `python3 tools/check.py` passes. Not compiled. Nothing committed.

## Bundle (the v3.94 method, two variants)

- `bundle/output/thesis_v3_20260924_171952_new.{tex,zip}`: annotated.
- **`bundle/output/thesis_v3_20260924_171953_full_clean.{tex,zip}`**: clean, the whole thesis, no notes.
- Both `--verify` byte-faithful throughout, and cross-references resolve.

Claude (Opus 5.5, Claude Code) · 2026-09-24 · not compiled · nothing committed.
