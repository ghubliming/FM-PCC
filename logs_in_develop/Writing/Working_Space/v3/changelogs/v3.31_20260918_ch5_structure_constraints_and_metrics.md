# v3.31 — 2026-09-18 · Chapter 5: metrics before baselines, the alignment constraint set, and a training/evaluation split

All twelve Chapter 5 points of the 2026-09-18 review, plus the cross-draft check.

## Key points

0. **Cross-draft checked first.** Nothing open for v3: the v2.22 item was closed in v3.29 and
   `sync_v2.py status` reports v3 in sync with v2.23. No merge was needed.
1. **MuJoCo.** It is cited from §1.3 on but **never introduced** anywhere in the thesis. §5.1 now
   back-references §2.5 instead of re-introducing it ("MuJoCo, the rigid-body physics engine used
   throughout this thesis"), and v2 was asked to add the missing clause at its first use, since v2 owns
   Chapters 1--4.
2. **The quadrotor rates are this thesis's own configuration, and they check out:** physics
   `timestep="0.01"` in `quadrotor_modified.xml:4`, `decim = max(1, round(1/(dt·DATASET_HZ))) = 3` at
   `eval_mix_uav.py:1610` with `DATASET_HZ = 33`, giving $3\times0.01 = 0.03$\,s, the same factor the
   demonstrations were downsampled by. The source note now states each with its line, says the numbers come
   from no publication, and the table row points at the controller section.
3. **§5.2's opening rewritten.** The paragraph explaining which names the subsections use is gone; it now
   says what distinguishes the three environments and what each entry gives.
4. **§5.2.1** attributes the $0.025$ tightening margin to \ac{DPCC} with a citation, not only to the method
   chapter.
5. **The alignment task now has its constraint set on the page**, as the other two environments do:
   `tab:aligning-constraints` (workspace box, tightened box, action box) and the new
   `fig:constraints-aligning`, drawn in the same vocabulary as `fig:constraints-avoiding` and
   `fig:constraints-uav` — shaded exclusion, dashed tightened boundary — from above and from the side, with
   the box, target and start of the first held-out context.
6. **Table 5.3 rebuilt** with a wider layout: `\small`, 8\,pt column separation, a dedicated constraint
   column and room for the values; the inflation/tightening row moved into the caption.
7. **Figure 5.7 made legible**: its own text scale (1.55), larger margins, and a two-row legend, since a
   single row of four items ran off the figure at that size.
8. **Table 5.5 now carries a spread**, as the published row does: $70.1\pm9.0$ steps at DPCC's protocol and
   $69.0\pm12.0$ at twenty episodes, with violating steps $0.0\pm0.0$ and $0.03\pm0.17$. The spread is the
   episode-level standard deviation pooled over seeds within a geometry and then averaged over geometries,
   so it carries both seed-to-seed and episode-to-episode variation; the caption says so.
9. **§5.4.2 names the policies**: BESO at $0.359\pm0.031$ from images, then DDPM-GPT $0.316\pm0.026$ and
   DDPM-ACT $0.278\pm0.071$; from the state VAE-ACT $0.891\pm0.022$ and BESO $0.861\pm0.016$; on obstacle
   avoidance BESO $0.950\pm0.015$.
10. **Metrics now precede baselines** (§5.4 Metrics, §5.5 Baselines): a baseline is a number on a metric, so
    the metric comes first. Every metric carries its unit in parentheses — steps per episode, fraction of
    episodes, metres, degrees, milliseconds.
11. **`tab:train` names the baseline**: the column reads "Diffusion (\ac{DPCC})" and the caption states it is
    the baseline of `tab:target`, taken over unchanged on D3IL-avoiding and trained on the other two
    environments' data.
12. **§5.6 split into three subsections** — Compute Environment, Training, Evaluation — in that order, with
    the three per-environment protocols now subsubsections of Evaluation. Every label is unchanged, so no
    cross-reference moved.

## Verification
- `check.py` passes: 237 labels, 53 citations, **24 figures**; the new figure renders.
- `make_figs.py` rebuilt `fig_constraints_uav` and the new `fig_constraints_aligning`; both inspected as PNG
  previews, then `export_to_draft.py v3`.
- Bundles `thesis_v3_20260918_102250_new(.tex/.zip)` and `…_new_clean`: 24/24 figures, 0 placeholders.
  **Not compiled** — no TeX toolchain in this container.
