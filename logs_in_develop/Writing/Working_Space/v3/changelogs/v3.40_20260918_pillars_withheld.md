# v3.40 — 2026-09-18 · UAV-pillars withheld: the scene tests the constraint its own demonstrations were built to satisfy

Not a writing pass. One finding, and the draft made honest about it.

---

## The finding

The author's question on `fig:expert-uav` — "is pillars actually the wrong test? in learning there is NO
violate" — is right, and the generator says so in one line:

```
uav_expert_data_collect/trajectories.py:48
    _Y_L = PILLAR_Y_A - PILLAR_RADIUS - PILLAR_ROTOR_REACH - PILLAR_SAFETY   # = -1.11
```

The demonstrated channels are *derived from* the clearance the projection later enforces. At a pillar the
plan must keep $|y| \ge 0.6 + 0.12 + 0.31 = 1.03$; the routes fly at $1.11$. The data is 8 cm inside the
feasible set before anything projects it, and every number of the scene follows from that: four of four
demonstrated routes clean, the unprojected plan already at S&C $1.00$ (FM) and $0.90$ (MeanFM), per-step
projection with random selection *destroying* a clean plan, and endpoint projection "winning" by leaving
it alone.

So the scene never asked the question the chapter uses it for. D3IL-avoiding introduces its halfspaces at
test time (0, 1 and 2 of 96 demonstrations satisfy them) and UAV-corridor gets the same from its slide
(all three lanes cross it, by $0.24$–$0.47$\,m). UAV-pillars was the one scene whose constraint the data
already respected.

## What changed in the draft

`sec:res:uav:pillars` is **withheld**. The section now states the finding and carries a `\hole` to the
new note. The withdrawn text — `tab:uav-pillars`, `tab:uav-pillars-endpoint`, `tab:uav-pillars-best` and
`fig:uav-pillars-paths` — is kept verbatim at `withheld/20260918_uav_pillars_section.tex`, with a header
saying why and how to restore it.

Everything that leaned on it was withdrawn with it rather than left dangling: the chapter roadmap, the
projection-methods comparison for that scene, the quadrotor conclusion, `tab:summary-models`,
`tab:summary-projection` and `tab:summary-combinations` all read *withheld*, and the quadrotor claim of
this draft now rests on UAV-corridor. `check.py` finds no orphaned reference; 30 → 29 figures, 4 tables
out.

## What was written instead

* [`data_status/PENDING_20260918_pillars_geometry_redesign.md`](../../data_status/PENDING_20260918_pillars_geometry_redesign.md)
  — the evidence, the geometry ladder (`pillars_xl` at radius $0.35$ primary, `pillars_xxl` at $0.55$
  optional), the three things the implementer must get right, what is withheld in the draft, the figures
  that are waiting on a **download** rather than a run, and the 18-Sep jobs this makes pointless.
* [`data_status/SLURM_RUNBOOK_20260919_pillars_enlarged.md`](../../data_status/SLURM_RUNBOOK_20260919_pillars_enlarged.md)
  — the config change, a one-cell verification to run *before* the wave, and the 10-driver job matrix
  (mf/fm/af at $K=1,2,5$; diffusion at $K=20$; eleven projection configurations; tag `u7xl`).

Ledger row **R23** opened; `data_status/INDEX.md` lists both; the 18-Sep runbook carries a header saying
its groups C, D and E are superseded (`scancel 25890 25891 25892 25893 25894 25895`, only if still
queued).

**No code was written**, as instructed: the config entry and the submission are for whoever implements
them.

## Checks

`check.py`: 13 files, 4894 lines, 241 labels, 29 figures; all mechanical checks pass. Not compiled.
Nothing committed.
