# v3.73 — 2026-09-23 · v2.26 synced; Chapter 5 brought in line with the pillars result; five audit items; bundle

Author's instruction (Opus 5.5 session): "0. the chap6 pillar been updated, see if chap5 need some update or not.
1. Sync the v2 and check cross note for v3 if need any update. build the bundle." Mid-turn: "the v2 will have more big
updates soon, no need to be too exhausted extend research" — so the v2 side was merged and checked for breakage only,
and each audit item was verified by one read of the code line it cites, no further.

## 1 · Chapter 5 after the pillars result (v3.72)

Six statements were stale or predictive once §6.3.1 had its data:

| where | was | now |
| :-- | :-- | :-- |
| `sec:setup:tasks:uav` scene list, UAV-pillars | "The scene exists to test the vehicle and its controller, not the planner … Agreement says …; a difference is a result about the plant." | the scene's procedure as fact, and: each geometry's constraint set is the table's (one or two halfspaces and one keep-out disk, **which covers one of the six pillars**); the other five pillars are obstacles in no constraint set; a flight that touches one ends there |
| demonstrations vs constraints (`sec:setup:data`) | "…and the vehicle adds only what a plant adds --- tracking error between the plan and the path." (the result says 50/51 contacts are on the commanded path) | "…against the same constraint sets, which leave five of the six pillars out" |
| `sec:setup:metrics:uav` bullet | "together with whether the vehicle made contact with a pillar" | contact ends the flight, is not a success, and the tables print the share of flights that ended so (= Ch 6's *contact* column) |
| `sec:setup:protocol:eval` seeds sentence | "D3IL-aligning and the UAV scenes are evaluated with one training seed" | D3IL-avoiding **and UAV-pillars** over five seeds; aligning, corridor, s-curve one |
| `tab:eval` | UAV column "UAV, all scenes"; episode limit "396 / 634 / 871" (634 was `pillars_hg`); 3D projection and 0.31 m inflation implied for pillars | column **"UAV-corridor, UAV-s-curve"**, limit "396 / 871"; caption: UAV-pillars takes the D3IL-avoiding column in the table's coordinates, only its low-level control is the quadrotor's; text: planar position on D3IL-avoiding and UAV-pillars; srcnote: pillars' 200-step limit, never reached in R39 |
| `sec:setup:protocol:uav` + srcnote | no plant constants; srcnote tags `p23pv2turbo/p23pv2live`, "T5 four seeds as its source cell" | one sentence (v2 item 6): fixed altitude 1.0 m, each commanded position tracked for one control step of 1 s = 100 physics steps of 0.01 s, reference ≤ 1 m/s, no velocity feed-forward, contact ends the flight (`frame.py:22, 25`, `plant.py:42–44` re-read); srcnote tag `p23uavpv2live`, jobs 26151–26154 |

Two consistency fixes in Chapter 6 that the same reading exposed: the §6.3 intro said "All results use one training
seed … Every generative model is a temporal U-Net with 3.96–3.97 M parameters" — now scoped to corridor and s-curve,
with UAV-pillars flying the D3IL-avoiding models at its five seeds; and `sec:res:uav:models`' "The pillars are not in the constraint
set a geometry declares" is now "**Five of the six pillars** are in no constraint set … one keep-out disk, which covers the
sixth" (the contacted pillars are never the covered one, DA §2).

## 2 · v2 sync (v2.24 → v2.26)

`sync_v2.py merge <file>` one at a time for `parts/00_preamble.tex`, `parts/01_frontmatter.tex`, Ch 1, Ch 2, Ch 4 (all
fast-forward, clean). **Not** `merge` without a file: that would also three-way-merge v2's Ch 5 bone into v3's owned Ch 5.
v2's only change there is one word ("wall-clock measurements" → "timing measurements") in a paragraph v3 no longer has,
so the Ch 5 baseline was advanced by hand (copied from the re-split v2) and `stamp` confirmed every baseline equals v2.
`status`: in sync at v2.26.

What came down: §1.4 in two stages (item 6 now describes pillars-v2 / corridor-v3 / s-curve-caveat, consistent with Ch 6);
a Ch 2 paragraph on what a bad plan costs each platform; Ch 4 with H (not H+1) plan steps, the transport-time paragraph,
the endpoint-generation figure from the HardFlow paper, the action bound as a named family, §4.6.3 rebuilt (pillars map
`eq:method:env:pillarsmap`, the pillars loop, `eq:method:env:switched` as per-replan wall selection,
`tab:embodiments` third column "UAV-corridor, UAV-s-curve"); the abstract's "wall-clock time" is gone.

`fig_hardflow_endpoint_generation.png` (new in Ch 4) lives in `v2/figures/` and is not in the DA store; copied into
`v3/figures/` byte for byte (sha256 `696aac7555da…`, identical to `aux_repo/HardFlow_Paper_Files/arXiv-2511.08425v3/Pic/R2_visual_d3il.png`).
It is not registered in `DA_in_Paper/plotting/sources.VENDORED` — a config edit, not made; the author can ask for it.
`export_to_draft.py` does not prune without `--prune`, so the copy is safe from the next export.

## 3 · The → v3 cross note (v2.26 audit findings, six items)

Each item's code line was re-read once:

| # | finding | verified | done |
| :-- | :-- | :-- | :-- |
| 1 | FM sampler starts at 0.5·randn, trained at σ = 1; MeanFM/CI-MeanFM at 1 | `flow_matcher_v3/models/diffusion.py:163` vs `:284`; aligning `fm_diffusion.py:164`, UAV `diffusion.py:184`; baseline `diffuser/…:168` | Ch 5 `sec:setup:protocol:avoiding`: one sentence + srcnote after "the initial noise … are the baseline's own" |
| 2 | endpoint sampler queries the average-velocity head at h = 0 | meanflow `hardflow_projection.py:861–862` (`h=torch.zeros_like(t)`), used for the transport at `:959`; alphaflow `:900, :998` | Ch 5 `sec:setup:protocol:guiding`: one paragraph (only where endpoint projection has guiding steps — the n_guide = 0 sentence is left as it was); Ch 6 `sec:res:avoiding:projectors`: one clause |
| 3 | per-step gate: baseline `t <= ηK` (⌊ηK⌋+1 steps), flow `idx >= int((1−η)K)` (⌈ηK⌉) | `diffuser/models/diffusion.py:179, 186`; `flow_matcher_v3/models/diffusion.py:190–191` | Ch 5 threshold paragraph: one sentence (eleven vs ten at K = 20, η = 0.5) + srcnote |
| 4 | one guiding step at the corridor's K = 3 — v2's "no attributable effect" vs v3's reading | — | **left to the author** (v2 asked for the author's decision); nothing changed |
| 5 | NFE = K + n_guide under endpoint projection | `hardflow_projection.py:955–998` (`policy.nfe = K + n_active − 1`) | Ch 5 step-budget paragraph: one sentence + srcnote |
| 6 | Ch 5 should print the pillars constants | `frame.py`, `plant.py` | `sec:setup:protocol:uav`, see §1 above |

**Correction sent to v2:** the note's "the endpoint sampler (`init_noise_scale=1.0`)" is not universal —
`mix_uav/models/engine_registry.py:274` sets 0.5 for FM on the quadrotor. Ch 5 states only the plain samplers' scales.
**Check asked of v2:** Ch 4 §4.6.3's "as the rod's contact with a post does on the table" is not verified by v3.

Inbox: the v2.26 → v3 row ✅ v3.73 (item 4 open for the author); new → v2 row + `to_v2/FROM_v3_20260923_v3.73_sync_v2.26_notes.md`;
new → v4 row + `to_v4/FROM_v3_20260923_v3.73_sampler_facts_for_ch7.md` (the Ch 7 reading of items 1 and 2, the refined
pillars wording). Also fixed: the v3.72 → v4 row had been filed under → v2; moved.

## 4 · Bundle (seventh round)

`bundle/output/thesis_v3_20260923_160926_new.zip` (annotated), `…160926_new_clean.zip`, `…160927_new_nonotes.zip`
(10 478 KB each; 47 figures, 47 render, 0 placeholder; 9 inlined + 4 collapsed sources, `--verify` byte-faithful on all
three; Ch 1–4 collapsed to headings at v2.26). A first build two minutes earlier warned that one Ch 6 srcnote pointed at
`eq:method:dep:vdes` inside the collapsed Ch 4 and would print "??"; the srcnote now names the label in `\texttt`, and
that first build was deleted with its three log rows. Supersedes `152138`.

## Checks

`python3.14 tools/check.py` passes: 14 files, 282 labels, 38 figures, 53/53 citations. `sync_v2.py status`: in sync at
v2.26. Not compiled — no TeX toolchain here. Nothing committed.

## Left open

- Audit item 4 (one guiding step, corridor K = 3): the author's decision.
- `fig_hardflow_endpoint_generation` is not in the DA store's vendored list.
- §6.4 🔒 still summarises the older pillars state.

Claude (Opus 5.5, Claude Code) · 2026-09-23 · not compiled · nothing committed.
