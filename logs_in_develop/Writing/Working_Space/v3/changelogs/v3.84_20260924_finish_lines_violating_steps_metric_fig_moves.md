# v3.84 — 2026-09-24 · Finish lines in Fig 5.x; UAV-corridor and UAV-s-curve read on violating steps; Fig 6.6 after projection only; warnings and moves

**Asked (author), in one pass with four follow-ups:**
1. "mark the endline of the corridor and the s_curve Fig in chap5, direct put end of the wall for both (even s_curve
   now is not, but put in Fig is like that)"
2. "no one can success the NO violation, so the mention the S&C here is no sense. plz only use the violation steps as
   metric … a pareto no more at Steps-Time, but lower violation steps-time as pareto, also need the Chap5 Corridor
   metric to update/change"
3. "Figure 6.6 is no sense, same reason as avoiding, all success, no need such plot for pre projection"
4. "so is also s_curve Table 6.15 → S&C maybe drop and only use violation steps"
5. "Fig6.8 … it need same 10 runs, plz remove or ADD warning now"
6. "Maybe move Fig 6.5 and 6.7 into Appendix B section, after B1 B2, same traj demo."

No bundle asked.

## 1 · Finish lines (Ch 5 `fig:env-uav`)

- `builders/scenes.py::_uav_scene`:
  - gains a `finish=(x, y0, y1)` argument, drawn as a green floor strip at the end of the walls: corridor x = 2.0
    across y ±0.95; s-curve x = 3.0 across y 0.35–1.25;
  - UAV-pillars' scene-file `finish_line` geom is now green as well, for one colour across the figure.
- Rebuilt, rendered, read, exported.
- The caption (95 words) says the green line is the finish line, at the end of the walls on the corridor and the
  s-curve. It also says the s-curve is scored 0.2 m further on, at its goal point (x = 3.2, checked from the
  generator: goal (3.2, ~0.8, z), heading +x).

## 2 · Violating steps as the measure on UAV-corridor (and UAV-s-curve)

- **Ch 5 `sec:setup:metrics:uav`.**
  - The near-satisfaction bullet is replaced by the new metric. On UAV-corridor and UAV-s-curve the constraint is read
    as violating control steps per flight, printed in place of S&C; fewer is better.
  - The corridor's frontier is violating steps against time, among configurations that reach the finish line on
    every flight.
  - The srcnote keeps the evaluator change (x′ = 2.0) and records the withdrawal of the near bar. S&C stays in the
    corpus (corridor baseline 0.3–1.0, FM K20 endpoint over the hump 0.4–0.8, every other corridor and every s-curve
    cell 0).
- **Ch 6 §6.3.2, rebuilt again** (the v3.83 block is archived in `withheld/…/uav_corridor_v3.83_before_v3.84.tex`).
  - **Table 6.11:** Success, violating steps, steps, ms/step (no S&C), 90 words.
  - **Table 6.12:** best rule = most successes → fewest violating steps → least time (83 words). This changes only
    MeanFM/CI-MeanFM at K2 over the hump, from r to t (their 1–2 wall-drift flights reach the end).
  - **Appendix `app:uav-corridor-rules`:** four tables without S&C and without the Near column.
  - **Text:** projection results in violating steps (flow 1.4–10.4 / 0.2–8.3 against the baseline 0.8 / 0.0 at
    666 / 654 ms). The goal-point honesty is now in success (baseline 0.000 tilt, 0.300 hump). The v3.83
    before-projection frontier paragraph is gone ("as before projection on D3IL-avoiding, every configuration
    succeeds, and no cost frontier is drawn").
  - **Frontier (new) on violating steps and time:**
    - tilt: MeanFM K1 (10.2, 27.4 ms) → FM K2/K3/K5/K20 and CI-MeanFM K3 per-step → baseline (0.8, 666.3);
    - hump: CI-MeanFM K3 endpoint (8.0, 71.9) → FM K3 ps, CI-MeanFM K3 ps, FM K5 ep → FM K20 endpoint single
      (0.2, 291.0) → baseline (0.0, 654.2).
    - Reading: "no model is ahead at every budget". Where the three flow models share a budget, the average-velocity
      objectives are at the cheap end: MeanFM K1 ties FM K1 in violating steps, 0.2 ms sooner; CI-MeanFM K3 has the
      fewest under per-step on both constraints, 5.0/4.6 against 5.8/5.6, at about a sixth more time; over the hump
      endpoint gives 8.0 against 8.3 at equal time. Each difference is about one spread of the flights.
    - The v3.83 steps–time claim is withdrawn.
  - **Projection Methods paragraph** in violating steps. The endpoint bar is met over the hump at K20 with a single
    candidate: 0.2 against 1.8 at 291 against 355 ms.
  - **§6.3.5 corridor paragraph** rewritten to match.
- **Fig 6.6 → `fig:uav-corridor-tradeoff`**, after projection only (author item 3).
  - Two panels, tilt | hump; violating steps per flight against ms per action (log).
  - Band: success within 0.05 of the best. Hollow points (stalls and drift cells over the hump) take no part.
  - Per-panel y range. Built on `best_viol` of `data/corridor_v3_frontier.json` (the extract gains the new pick).
    Rendered, read, exported. Caption 79 words.
- **UAV-s-curve (author item 4).** In Tables `tab:uav-scurve`, `tab:uav-scurve-projection` and `tab:uav-controller`,
  S&C is replaced by **violating steps** (mean ± spread, counted until the flight ends; R44a CSV `n_violations`):
  - MeanFM 18.5 / 16.4 / 13.0;
  - CI-MeanFM 23.3 / 21.3 / 16.3;
  - FM 23.0 / 24.3 / 26.1;
  - baseline 119.4 ± 199.4 (two floor crashes near 500).

  Prose: "succeeds within the constraints" → "is free of violations"; the selected FM K1 crosses "with 17 to 37
  violating steps". The appendix aborts table still has an S&C header over its *not run* rows (left for the author).

## 3 · Fig 6.8 warning (author item 5)

`fig:uav-scurve-paths` (MeanFM K10 pilot: three MPC flights against ten cascaded) carries `\outdated{}` in its
caption and below it: the panels are not comparable, and the figure is replaced by the ten-flight R44c cells.
`\outdated` is visible in all three bundle variants.

## 4 · Figures moved to Appendix B (author item 6)

- `fig:uav-pillars-paths` goes to new section **`app:uav-pillars-paths`** and `fig:uav-corridor-side` to new section
  **`app:uav-corridor-side`**. Both sit right after the D3IL-avoiding/aligning executed-path sections (B.2, B.3), so
  the trajectory figures are together (B.4, B.5).
- Ch 6 keeps the prose and points to them.

## 5 · Numbering (as built)

| where | figures | tables |
| :-- | :-- | :-- |
| Ch 6 | 6.1–6.4 avoiding/aligning; **6.5 corridor frontier**; **6.6 s-curve paths** | 6.11–6.13 corridor; 6.14–6.17 s-curve |
| Appendix B | B.4 UAV-pillars paths; B.5 corridor flights; B.7 corridor rules; B.8 s-curve budgets | — |

## Records

- v4 note [`FROM_v3_20260924_v3.84_violating_steps_metric.md`](../../cross_draft/to_v4/FROM_v3_20260924_v3.84_violating_steps_metric.md)
  + INBOX row. It supersedes the model claims of the v3.82/v3.83 notes.

## Bundle (fifteenth round, built on request after the pass)

`bundle/output/thesis_v3_20260924_125313_new.zip` (annotated), `…125314_new_clean.zip`, `…125314_new_nonotes.zip`
(10 882 KB each; 79 files: the rebuilt corridor frontier, the three scene views with their finish lines, and the two
path figures now in Appendix B are all included; 9 inlined + 4 collapsed sources at v2.27; `--verify` byte-faithful on
all three; cross-references all resolve; the `\outdated` warnings on the s-curve pilot figure are present in the
no-notes build). Supersedes `20260924_123356`.

## Checks

`python3.14 tools/check.py` passes; the table-width scan flags nothing; captions ≤ 95 words. Not compiled. Bundle built afterwards on request (above). Nothing committed.

Claude (Opus 5.5, Claude Code) · 2026-09-24 · not compiled · nothing committed.
