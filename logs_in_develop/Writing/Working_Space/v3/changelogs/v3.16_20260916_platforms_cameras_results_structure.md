# v3.16 — 2026-09-16 · platforms, alignment camera inputs, and two-part result structure

## Chapter 5
- Added a platform introduction with authentic D3IL Panda and MuJoCo Skydio X2 views. `tab:platforms`
  records the experiment-facing specifications read from the model XML: manipulator joints/control
  chain; X2 mass, rotor geometry, footprint, actuator range and simulation/control rates.
- Added the two actual $96\times96$ observations beside the alignment task: fixed overhead camera and
  wrist camera at the same instant of an expert demonstration. They are frame 60 of
  `expert_rollout_0.gif`, not recreated views.
- Registered both crops in `sources.ENV_RENDER_FRAMES` and `VENDORED`; prepared PNGs, provenance notes,
  manifest and v3 exports now reproduce them.

## Chapter 6
- Every environment now follows **Generative Models → Projection Methods → Conclusion**. Existing labels
  were retained; budget, raw-plan and per-scene material became nested subsections.
- Obstacle avoidance concludes with the supported low-budget operating point: MeanFlow at one network
  evaluation with DPCC per-step projection. Endpoint projection is not selected at one/two evaluations,
  where it has zero or at most one guiding step.
- Alignment concludes with MeanFlow and the best observed tightened combination, while retaining the
  missing tightened-diffusion and single-seed/training-context limits.
- UAV results are separated into model evidence and projection evidence. The conclusion selects per-step
  projection on the corridor, endpoint projection with MeanFlow on pillars, and no combination on the
  unresolved s-curve.
- The s-curve controller comparison now fixes the model/configuration, reports the three matched initial
  conditions, distinguishes airborne inversion from wall contact, and separates the flyable unprojected
  path from the still-unsolved projected stack.
- Added a final operating-point table across all tasks; corrected Chapter 8 so it no longer says endpoint
  projection is faster in every environment.

## Supporting records and checks
- Updated the figure-pipeline README/source notebook and the shared UAV model note; the latter closes its
  X2-parameter hole from `quadrotor_modified.xml`.
- Figure pipeline: 11 generated + 11 vendored; 20 figures referenced/exported. Both camera crops visually
  inspected. `extract_env_frames.py --check` reports 4/4 current; `crop_vendored.py --check` 3/3 current.
- `tools/check.py` passes with the single inherited Chapter 4 width warning. The five `todofigure` boxes
  are the already-registered raw-plan panels that require cluster output. No TeX toolchain is installed,
  so the draft was not compiled locally.

