# v3.30 — 2026-09-17 · the Results chapter traversed against the pending-data ledger

No thesis prose changed in this pass. Every drafting marker and every `\guard` in `06_results.tex` was
matched to a row of `data_status/PENDING_20260916_missing_data_and_analyses.md`; six gaps had no row and
were added, one row was closed, and the locations were rewritten as labels.

## What was traversed

`chapters/06_results.tex`: 7 `\hole`, 6 `\todofigure`, 3 `\provisional`, 9 `\guard`, plus two gaps that
carry no marker at all (a table cell reading *not evaluated*, and a budget ladder that stops short).
`05_setup.tex` (3 `\provisional`) and `09_appendix.tex` (2 `\hole`) were checked as well. The four holes in
Ch 7 and Ch 8 are v4 ownership, not data.

## Rows added

| row | gap | found via |
| :-- | :-- | :-- |
| **R12** 🟠 | Diffusion baseline on UAV-pillars in the six configurations it was never run in (per-step random, and all four endpoint cells); it has 5 of 11 | the *---: not evaluated* cells of `tab:uav-pillars` — no marker, so it was not in the ledger |
| **R13** 🟡 | MeanFM and FM at $\nfe=1$ on UAV-pillars; the scene has them at $\nfe=2,5$ only, while CI-MeanFM is quoted from $\nfe=1$ | enumerating the batch's (engine, $\nfe$) cells per scene |
| **R14** 🟡 | Candidate-matched endpoint-projection ladder on D3IL-avoiding (endpoint ran at $B=1$, per-step at $B=4$) | the `\guard` under `tab:hf-ladder` |
| **R15** 🟡 | MuJoCo MPC on UAV-s-curve beyond three flights | the `\guard` under `tab:uav-controller` |
| **R16** ⚪ | Budget ladder for FM and CI-MeanFM on D3IL-aligning ($\nfe=2,10$) — the task has MeanFM at 2/10/20/100, FM at 20/100 only | enumerating the alignment batch |
| **R17** ⚪ | Repeat evaluations to measure the ≈0.4 m run-to-run variation on projected alignment rows instead of citing it | the `\provisional` in `sec:res:aligning:projection` |

## Rows changed

- **D3 closed ❌**: the ≈119 ms/step MuJoCo MPC overhead is not verifiable against the committed timing
  (89.8 ms MPC against 88.4 ms brake-to-rest, unprojected), and v3.28 removed the claim from the draft
  rather than leaving it to be checked.
- **D10** moved after D9 and marked *(needs the cluster)* — it is a download, not a local analysis.
- **R2, R3, R7, R9, R10, R11, D1, D2** now cite labels (`sec:res:aligning:projection`, `tab:hf-ladder`, …)
  instead of section numbers, which had drifted by one or two levels since v3.26.

## New section in the ledger

**§4 Coverage of the Results chapter** — a 26-row table mapping every marker and guard in the draft to the
row that owns it, so the next traversal is a diff rather than a re-read. The header records that the
traversal happened and that locations are labels.

## Verification
- `check.py` passes; bundles rebuilt, 23/23 figures. Not compiled — no TeX toolchain here.
- Marker counts in the ledger's coverage table match `check.py`'s totals for the Results chapter.
