# v3.47 — 2026-09-20 · raw versus projected results, locked budgets, and a complete pending ledger

This pass reorganises §6.1 around the distinction the experiments actually make: raw generative output
first, then every result obtained with projection. It also removes two tables that repeated comparisons
already expressed by the protocol tables and frontier.

## Chapter 5

- Removed the standalone alignment-constraint figure (formerly Fig. 5.4). The ten-context figure already
  draws those contexts on the same halfspace and keep-out geometry, so it now carries the relevant
  references and becomes the single figure for that plane.
- Removed the plan-smoothness `\hole`. No smoothness analysis is planned.
- Audited Table 5.8's transport-time row against Chapter 4. Transport time and the average-velocity time
  pair are present, but FM's Beta law is not defined mathematically. This belongs to v2's Method chapter,
  not v4; a dated v3.47 note was added to `cross_draft/to_v2` and `cross_draft/INBOX.md` with
  the exact placement and formula.

## Chapter 6

- Renamed §6.1.1.1 from *The Plans Themselves* to **Raw Model Outputs**.
- §6.1.1 now contains only results with projection switched off: the eight-panel raw-plan figure and the
  twenty-episode raw-output table.
- All results with a projector active now sit in §6.1.2 **Projection Methods**: the fixed-per-step
  comparison across generative models, protocol extension, cost frontier, budget ladder, and the
  endpoint-versus-per-step study.
- Deleted the prose subsection *Why the Projected Numbers Separate the Models So Little*. Its
  meta-explanatory framing and unmeasured smoothness argument are no longer in the thesis.
- Deleted the former seed-spread table and its nearby prose. It mixed the extended protocol with a
  ten-times-smaller CI-MeanFM row and did not add a decision beyond the protocol tables.
- Deleted the former single-seed equal-constraint-satisfaction table and the per-geometry cheapest-cell
  table, together with the search commentary around them. The five-seed protocol tables, frontier and
  conclusion carry the result without a second selected-budget narrative.
- Locked the main DPCC-protocol table to $\nfe\in\{1,2\}$ for all four models, plus the diffusion
  baseline at its native $\nfe=20$. Diffusion $\nfe=2$ is printed directly as pending because only seed
  6 exists. The unmatched diffusion $\nfe=10$ rows moved to a new Appendix table.
- The extended-protocol table now prints CI-MeanFM at $\nfe=1,2$ directly as pending instead of silently
  omitting the model.
- §6.1.3 gains a compact final-combination table: the extended MeanFM operating point, the CI-MeanFM
  result at DPCC's protocol, and the diffusion baseline of record, each with model, budget, projection,
  selection rule, protocol size, S&C, steps and time.
- Corrected one residual phrase `consistency training` to the approved mechanism name.

## Figures

- Fig. 6.6 is rebuilt as a larger, cleaner single panel: no internal title, subtitle or explanatory
  prose; only the three treatment labels remain in the plot. Marker semantics and protocol stay in the
  caption.
- Every frontier's preferred-direction arrow now uses the thesis dark slate colour instead of red.
- Rebuilt the three frontier figures and Fig. 6.6 in
  `Data_Analysis/DA_in_Paper`, regenerated their PNG previews, visually inspected the outputs,
  and exported them to v3. The draft now references 35 figures.

## Pending work and cross-draft record

- Replaced `PENDING_20260920_all_lacking_runs.md` with a complete current checklist rather than a
  list of only newly discovered gaps.
- Added R25: CI-MeanFM at the extended obstacle-avoidance protocol, $\nfe=1,2$, all rules, five existing
  checkpoints and twenty episodes per seed and geometry; evaluation only.
- Added R26: diffusion $\nfe=2$ for seeds 7--10; four trainings, because the denoising budget is fixed
  at training.
- Added R27: optional diffusion-under-endpoint evaluation on UAV-corridor.
- Retired the smoothness analysis R11/D1 and closed the already completed raw-plan-panel item R24 in the
  historical ledger.

## Verification

`python3 tools/check.py` passes: 13 source files, 5,503 lines, 249 labels, 53 citations and
35 referenced figures; no dangling references, missing citations, missing figures, unbalanced
environments or brace errors. `tools/sync_v2.py status` reports the inherited half clean at v2.24.
The four changed figures were regenerated as SVG and PNG and exported without stale-preview warnings.
The v3-only annotated and clean review bundles were built as
`thesis_v3_20260920_132552_new` and
`thesis_v3_20260920_132552_new_clean`; bundle verification reports all eight inlined and
four collapsed sources byte-faithful. The expected two cross-references into collapsed Chapter 4 print
as `??` in the v3-only review bundle; they resolve in a full bundle.
**Not compiled locally; no TeX toolchain is installed.**
