# v3.85 — 2026-09-24 · Corridor: a steps Pareto within violation groups, the budget trade stated; UAV-pillars paths back in the main text

**Asked (author):**
1. "maybe B3 should back to Fig6.5 it better put in main text… it has own special reason to be here. B4 in appendix,
   just a demo"
2. "also add a Pareto for steps like the post projection pareto like avoiding. Combine with the Fig6.5, lets see UNDER
   SAME GROUP OF VIOLATION RANGES in Fig6.5 (there is clear cluster) who is better in steps for cross the goalline/success."
3. Mid-pass: "also from in the Fig5.1 you can observe there is tradeoff btw K ←→ violation steps, should mention in the
   text". Read as the corridor frontier figure, whose (a, b) carry the budgets along the staircase.

No bundle asked.

## 1 · Figures

- **B.3 → back to Ch 6.** `fig:uav-pillars-paths` is again Fig 6.5 in `sec:res:uav:pillars:projected`, and section
  `app:uav-pillars-paths` is removed. `fig:uav-corridor-side` stays in the appendix as **B.3** (the "just a demo" one).
- **Fig 6.6 `fig:uav-corridor-tradeoff` is now 2×2** (store, `builders/frontier.py`).
  - **(a, b):** the v3.84 violating-steps frontier, with violation groups shaded as horizontal bands I–III.
  - **(c, d):** control steps to the finish line against time, **D3IL-avoiding's frontier drawn once per violation
    group**. Groups are vertical bands (alternating shades, split at the geometric mean where two would touch); each
    group has its own dashed staircase and rings.
  - **Groups:** a new group starts where the sorted violating steps of the eligible points jump by more than
    **2 violating steps** (`VIOL_GAP`). This reproduces the clusters the author saw:

    | constraint | I | II | III |
    | :-- | :-- | :-- | :-- |
    | tilt | 9.8–10.4 (K1–2) | 4.4–7.6 (K3–5) | 0.8–2.1 (K20, baseline) |
    | hump | 8.0–8.3 (K3 endpoint) | 3.9–5.6 (K3 per-step, K5) | 0.0–1.8 (K20, baseline) |

  - The groups also occupy disjoint time ranges. Rendered, read, fixed (band overlap on the hump), exported.
    Caption 99 words.

## 2 · Text (Ch 6 §6.3.2, §6.3.5; Ch 5)

- **Budget trade, explicit** (author item 3). "The budget is the trade between the two axes: every step up in network
  evaluations buys fewer violating steps with more time per action":
  - tilt: ~10 violating steps at K1–2 (27–36 ms), 4–8 at K3–5 (72–117 ms), 1–2 at K20 (360–417 ms);
  - hump: ~8 (K3 endpoint) and 4–6 (K3 per-step) at 72–91 ms, ~4 at K5 (112–114 ms), <2 at K20 (291–355 ms).
- **New paragraph, steps within groups** (author item 2):

  | constraint | group | result (steps to the line, ms per action) |
  | :-- | :-- | :-- |
  | tilt | I | MeanFM K1 253.2 at 27.4 against FM K1 259.8 at 27.6, so **MeanFM dominates FM at K1**; CI-MeanFM K1/K2 fewest steps (252.0 / 251.9) |
  | tilt | II | FM K3 per-step shortest (247.2 at 79.2); FM/CI-MeanFM K3 endpoint about 7 ms cheaper at 253–258 steps |
  | tilt | III | FM K20 per-step (252.6 at 360) dominates FM K20 endpoint (269.8 at 417) and the baseline (290.3 at 666) |
  | hump | I | **CI-MeanFM K3 endpoint dominates** (284.6 against MeanFM 287.7 and FM 299.3 at 72 ms) |
  | hump | II | FM K3 per-step dominates (277.0 at 77.1) |
  | hump | III | FM K20 endpoint single (316.2 at 291) and per-step (285.1 at 355) on the frontier; baseline dominated (320.8 at 654) |

  Most differences are about one spread of the flights; the largest (hump endpoint, CI-MeanFM vs FM) is three.
- **§6.3.5 UAV-corridor.** The budget trade, plus the within-group sentence: the average-velocity objectives reach the
  line in the fewest steps at the smallest budgets, FM at K3 and K20, and the baseline is dominated in its group.
- **Ch 5 metric bullet.** "within groups of configurations that violate about equally [the frontier] is drawn as on
  D3IL-avoiding, on control steps to the finish line against time".

## Numbering (as built)

| where | figures |
| :-- | :-- |
| Ch 6 | 6.5 UAV-pillars paths; 6.6 corridor frontier (2×2); 6.7 s-curve pilot (with its OUTDATED warning) |
| Appendix B | B.1 avoiding paths, B.2 aligning paths, B.3 corridor side view |

Tables are unchanged from v3.84.

## Records

→ v4 note [`FROM_v3_20260924_v3.85_corridor_steps_within_groups.md`](../../cross_draft/to_v4/FROM_v3_20260924_v3.85_corridor_steps_within_groups.md) + INBOX row.

## Bundle (sixteenth round, built on request after the pass)

`bundle/output/thesis_v3_20260924_130644_new.zip` (annotated), `…130644_new_clean.zip`, `…130645_new_nonotes.zip`
(≈ 11 000 KB each; 9 inlined + 4 collapsed sources at v2.27; `--verify` byte-faithful on all three; cross-references all
resolve; the packed `fig_uav_corridor_tradeoff.png` is byte-identical to the store's 2×2 figure; `fig_uav_pillars_paths`
sits in Chapter 6 and `app:uav-pillars-paths` is gone). Supersedes `20260924_125313`.

## Checks

`python3.14 tools/check.py` passes. Not compiled. Bundle built afterwards on request (above). Nothing committed.

Claude (Opus 5.5, Claude Code) · 2026-09-24 · not compiled · nothing committed.
