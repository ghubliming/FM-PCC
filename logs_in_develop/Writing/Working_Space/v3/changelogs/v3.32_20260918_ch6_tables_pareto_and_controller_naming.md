# v3.32 — 2026-09-18 · Chapter 6: the tables filled, the cost frontier explained, the pillars result read flight by flight

All eleven Chapter 6 points of the 2026-09-18 review. Three of them changed what the chapter reports.

## Key points

1. **Table 6.1's mark and its jargon.** The caption now says what the bold triangle marks — the
   configuration this thesis carries forward, *the cheapest configuration that holds its success*, not the
   largest number in a column — and spells out S\&C as success with constraint satisfaction, pointing at
   `sec:setup:metrics:avoiding` for all three columns.
2. **Table 6.1 filled.** The baseline now appears at $\nfe=1,5,10,20$ under all three rules at DPCC's own
   protocol: it collapses to $0.600$--$0.667$ at one evaluation and reaches $1.000$ from five on. The two
   genuinely missing rows are marked `\hole` **(must have)** and the two acceptable ones `\guard`
   **(acceptable gap)**, as requested.
3. **§6.1.1.2 rewritten.** It now states the finding first — with ten times the episodes, the selection
   rule matters more than the model — then the evidence per model, then the two consequences. No
   number-first paragraphs, no hedging.
4. **Table 6.2 filled.** It was missing cells that exist in the batch: **FM at $\nfe=5$ and $\nfe=20$**, and
   **MeanFM at $\nfe=5,10,20$**, all at five seeds × twenty episodes and all now in the table. What remains
   missing is marked: the baseline below its training budget at this protocol and CI-MeanFM (`\hole`, must
   have; ledger **R18**, **R19**), FM at $\nfe=10$ and MeanFM's two-geometry $\nfe=20$ row (`\guard`).
5. **The cost frontier has its own subsection** (`sec:res:avoiding:pareto`): what is plotted, what
   *non-dominated* means, why configurations are only admitted within $0.05$ of the best success in the
   panel, and what the frontier then shows — a region of one or two evaluations rather than one best point.
6. **Figure 6.2 gains CI-MeanFM**: dotted, hollow triangles, seed 6, drawn as its own series because it
   exists on this backbone for one seed. It is flat in $\nfe$ like the other flow-based models
   ($0.95$--$1.00$ from $\nfe=1$ to $20$). The legend area was widened so the label is not clipped.
7. **Table 6.4** names the rules as Table 6.1 writes them: temporal consistency is $t$, random $r$,
   cumulative projection cost $c$.
8. **The alignment distances carry a percentage** of the initial $0.4530$\,m in brackets, in both
   `tab:va-models` and `tab:va-projection`, and §5.4.2 defines the percentage: $100\,\%$ is a box not moved
   towards its target, $0\,\%$ one that reaches it. MeanFM at $\nfe=20$ reads $0.0741$ (16\,\%) against the
   baseline's $0.4140$ (91\,\%).
9. **§6.2.1.2 rewritten** the same way as §6.1.1.2: the behaviour first (the objective does not improve with
   budget), then the flat numbers against the analytic objective's falling ones, then the mechanism.
10. **The pillars result was checked flight by flight, and it holds with a qualification now in the text.**
    FM's $1.00$ is real, but six of its ten flights never settle and run to the $634$-step limit, ending
    $0.36$--$0.39$\,m out; MeanFM's $0.90$ is nine flights of about $420$ steps ending within $0.30$\,m plus
    one flight that loses the field entirely. The section, the conclusion and the recap row now say both.
11. **Brake-to-rest is named as what it is**: the same cascaded geometric controller with the zero velocity
    setpoint of `eq:method:dep:vdes`, not a different controller. `tab:uav-controller` reads "cascaded
    geometric, brake-to-rest" against "MuJoCo MPC", and `sec:res:uav:controller` states once that every
    quadrotor result uses that policy. The repo does discuss why the timing-sensitive default policy is not
    used (`U3_v_des_Patch/PLAN_pid_const_v.md`) and what brake-to-rest costs in tracking lag
    (`STUDY_20260901_mf_unguided_failure_uav_pillars.md` §5); v2 defines the three policies but says neither,
    so that was sent to v2.

## Verification
- `check.py` passes: 238 labels, 53 citations, 24 figures.
- `fig_avoiding_k_ladder` rebuilt and inspected, then exported to v3.
- Bundles rebuilt, 24/24 figures, 0 placeholders. **Not compiled** — no TeX toolchain here.
