# v3.21 — 2026-09-17 · compact UAV floats and result-table model labels

## Asked for

1. Reduce the excessive footprint and blank space around Table 5.3, Figure 5.6 and Table 5.4.
2. Keep the full scientific mechanism names in prose but introduce concise labels for tables.
3. Repair the overlapping columns in Tables 6.8 and 6.10, and the crowded model names exemplified by
   Table 6.7.

## Naming decision

Section 5.3 now defines the four compact, table-only labels at the same place as the full names:

| full prose name | compact table label |
|---|---|
| the diffusion model of DPCC | Diffusion |
| instantaneous-velocity matching | FM |
| analytic average-velocity matching | MeanFM |
| consistency-interpolated average-velocity matching | CI-MeanFM |

`MeanFM` describes the analytic average-velocity target rather than restoring the published brand.
Likewise, `CI-MeanFM` is a mechanism abbreviation and does not restore the prohibited brand name.
The canonical translation table records the distinction between the full prose names and these
space-constrained labels.

All v3 result tables now use the compact labels in model cells. Captions and running prose retain the
full mechanism names. The long comparison headers in Table 6.10 are expressed as compact model pairs.

## Layout repair

- Table 5.3 uses `\footnotesize`, reduced column padding, a slightly tighter row height and permissive
  `[!htbp]` placement.
- Figure 5.6 keeps the same six source images and caption but scales their combined panel to 78% of the
  text width. The section-level `\FloatBarrier` remains, so the UAV figures cannot leak into the dataset
  section.
- Table 5.4 uses `\footnotesize`, reduced column padding and row height, and `[!htbp]` placement.
- Table 6.7 uses the compact model labels, `\footnotesize`, reduced padding, and two-line headers for
  “Collision-free” and “Violating steps”; these headers no longer compete in non-wrapping columns.
- Table 6.8 uses compact column headings plus reduced padding and type size.
- Table 6.10 uses compact comparison headings plus reduced padding and type size.

## Verification

- `python3 tools/check.py`: passes; 14 files, 230 labels, 52 citations and 22 figures. The two existing
  inherited Chapter 4 width warnings remain and belong to v2.
- New-sections-only bundle: `thesis_v3_20260917_104554_new.tex/.zip`; 22/22 figures rendered, zero
  placeholders. `--verify` reports nine inlined v3 sources and four collapsed inherited v2 sources
  byte-faithful.
- Not compiled because the container has no TeX toolchain; final pagination and float placement still
  require the user's Overleaf build.
