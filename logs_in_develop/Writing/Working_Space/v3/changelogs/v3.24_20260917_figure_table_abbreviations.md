# v3.24 — 2026-09-17 · consistent model labels in every figure and table

## Decision

All labels inside figures and table bodies use one vocabulary, regardless of available space:

| visual label | full scientific name retained in prose and descriptions |
|---|---|
| Diffusion | the diffusion model of DPCC |
| FM | instantaneous-velocity matching |
| MeanFM | analytic average-velocity matching |
| CI-MeanFM | consistency-interpolated average-velocity matching |

Section 5.3 defines these mappings at first mention and now states explicitly that they apply to every
figure and table. Running prose keeps the full mechanism names. The affected captions spell out the
mapping so each visual remains interpretable when read away from Section 5.3.

## Changes

- Figure 6.3's four column headings and pending-panel labels now use the short labels. Its caption maps
  all four labels back to their full mechanism names.
- The legends/labels in `fig_avoiding_tradeoff`, `fig_avoiding_k_ladder`,
  `fig_avoiding_raw_models`, and `fig_avoiding_projector_cost` now use the same vocabulary. Their
  captions provide the full-name mapping where applicable.
- Appendix tables now use the visual labels in their thesis-name and corpus columns; artefact tokens
  such as `MeanFlowODE` and `AlphaFlowODE` remain only in the explicit provenance column.
- The canonical translation table now requires the abbreviations in all figures and tables, rather
  than only where space is constrained.
- `Data_Analysis/DA_in_Paper/plotting/sources.py` is the canonical visual vocabulary;
  `builders/avoiding.py` uses `MeanFM` in its projector-cost subtitle.

## Figure rebuild and verification

- Rebuilt all four affected official charts as SVG, generated PNG previews under Python 3.14, visually
  inspected all four, and exported the current figure store to v3. No rendered v3 SVG contains a long
  mechanism name or a published model brand.
- `python3 tools/check.py` passes: 14 files, 230 labels, 52 citations and 22 figures. The two inherited
  Chapter 4 width warnings are unchanged.
- New-sections-only bundle: `thesis_v3_20260917_110958_new.tex/.zip`; 22/22 figures rendered, zero
  placeholders, and all nine inlined plus four collapsed sources verify byte-faithful.
- Not compiled because this container has no TeX toolchain.
