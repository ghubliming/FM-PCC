# v3.72 — 2026-09-23 · UAV-pillars landed: §6.3.1 read from the R39 corpus

Scope (author): Chapter 6 pillars section only. Input: `cross_draft/to_v3/FROM_DA_20260923_pillars_v2_live_result.md`
and the analysis of record `Data_Analysis/DA_in_Paper/analysis/DA_20260923_pillars_v2_live.md` (`pillars_v2_live.py`),
corpus `analysis_results_checkpoint/23-09-UAV-Pillars-v2/live_p23uavpv2live` (jobs 26151–26154, tag `p23uavpv2live`,
240 flights, no cap end, no abort). Nothing else in the draft touched except the two conclusion paragraphs and the
§6.3 intro's pending list.

## What §6.3.1 now says

- **Intro:** the pending sentence replaced by "every flight ended by crossing the goal line or by touching a pillar;
  none reached the episode limit and none was aborted."
- **§6.3.1.1 On the Table and in the Air.** Before projection the vehicle changes nothing (S&C 0.033 → 0.033–0.100,
  viol. 15–20, ~60 steps, planner time equal to 0.9 ms; 9 of 120 unprojected flights end on a pillar). After
  projection the declared constraints hold in the air (0 violating steps on all 78 flights that reached the line)
  and S&C still falls to 0.600–0.700: 42 of 120 projected flights end on a pillar with no declared violation up to
  the contact; loss on *both-hard* (2–4 of 10 vs 7–9); the two models level. Third paragraph: the contacts are a
  property of the plans and the scene (pillars not in the declared set; rod radius → rotor reach; 50 of 51
  commanded paths within the reach; 29 of 42 projected contacts on both-hard's third row). Closing line: "What
  transfers … is exactly the declared constraint set. What does not transfer is the safety the table never measured."
- **`tab:uav-pillars-raw`** air columns filled (mean ± seed spread, `stdev` over the five seeds of each seed's
  three-geometry mean — the table side's convention, `avoiding_seed_spread.py`):

  | model | nfe | S&C air | viol. | steps | contact |
  | :-- | --: | --: | --: | --: | --: |
  | MeanFM | 1 | 0.100 ± 0.091 | 19.9 ± 0.6 | 62.7 ± 3.3 | 0.00 |
  | MeanFM | 2 | 0.100 ± 0.091 | 19.1 ± 1.8 | 60.9 ± 3.2 | 0.20 ± 0.27 |
  | CI-MeanFM | 1 | 0.100 ± 0.149 | 18.1 ± 3.2 | 59.7 ± 0.7 | 0.00 |
  | CI-MeanFM | 2 | 0.033 ± 0.075 | 19.8 ± 1.7 | 62.3 ± 4.2 | 0.10 ± 0.22 |
  | MeanFM ▶ (proj.) | 1 | 0.600 ± 0.190 | 0.0 | 61.4 ± 2.3 | 0.40 ± 0.19 |
  | MeanFM (proj.) | 2 | 0.667 ± 0.236 | 0.0 | 60.2 ± 1.6 | 0.33 ± 0.24 |
  | CI-MeanFM (proj.) | 1 | 0.633 ± 0.217 | 0.0 | 60.7 ± 1.6 | 0.37 ± 0.22 |
  | CI-MeanFM (proj.) | 2 | 0.700 ± 0.139 | 0.0 | 60.6 ± 0.6 | 0.30 ± 0.14 |

  *steps* is the per-seed geometry mean over successful flights, so it differs from the DA's pooled mean by ≤ 0.2
  (61.4 vs 61.2 etc.); the dataref says so. Time per action in the air is dataref-only (equal to the table's).
- **`tab:uav-pillars-geo`** (new): the projected rows per geometry — S&C table / air and contacts of ten (DA §1,
  second table).
- **§6.3.1.2 Executed Paths** written from `fig_uav_pillars_paths` (MeanFM nfe 1: before/after × three
  geometries): unprojected flights cross the halfspace or the disk on their way; projected paths hug the tightened
  boundary and end on the pillars that stand at it (1 / 3 / 8 contacts). The `\todofigure` placeholder is replaced
  by the store figure; `fig_uav_pillars_paths_cimf` (CI-MeanFM nfe 1: 8 / 9 / 2) stays in the store, unreferenced,
  named in the dataref.
- **Conclusion §6.3.5:** the UAV-pillars paragraph written (repaired plans stay repaired; the vehicle adds contact
  with the pillars the declared set does not contain; models level); "The benchmark" paragraph: the pillars half
  filled, the corridor half stays a `\hole` (R33).

## Figure store

Panel subtitles of both pillars figures were clipped on the right ("10/10 cl") and carried a run-tag name
("after projection (dpcc-t)") → renamed to "projected" in `data/uav_paths.json` (6 strings) and
`plotting/extract/pillars_v2_paths.py`; both SVGs rebuilt, PNGs at 3×, `fig_uav_pillars_paths` exported to v3
(`export_to_draft.py v3`). Read after the rebuild: the subtitles fit.

## Records

`data_status/PENDING_20260923 §2`: R39 ✅ landed, cost row struck. `cross_draft/INBOX.md`: the DA → v3 row ✅ v3.72;
new row → v4 with `to_v4/FROM_v3_20260923_v3.72_pillars_landed.md` (Ch 7: declared constraints transfer, physical safety
does not; Ch 8 may quote pillars again; the v3.69 "one seed" line superseded). Caption checklist addendum (92 / 32 / 63
words).

## Checks

`python3.14 tools/check.py` passes: 14 files, 280 labels, 37 figures; holes 28 → 23, todofigure 4 → 3, dataref 44 → 46.
Not compiled — no TeX toolchain here. No bundle built (not asked). Nothing committed.

## Left open

- §6.4 🔒 still summarises the older pillars state; reconcile when the author unlocks it.
- The CI-MeanFM paths figure is built but not printed — the author's call whether it goes to the appendix.
- Ch 5 §5.3.1's scene list and §5.6.3.3 already describe pillars at five seeds × two episodes; unchanged.

Claude (Fable 5.1, Claude Code) · 2026-09-23 · not compiled · nothing committed.
