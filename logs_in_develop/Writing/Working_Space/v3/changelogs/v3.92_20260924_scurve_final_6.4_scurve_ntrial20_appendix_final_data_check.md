# v3.92 — 2026-09-24 · UAV-s-curve final (R44 complete), §6.4 with the s-curve, the ntrial20 appendix added as it is, final data check (nothing missing)

**Asked (author):**
1. "final parts are done and processed by the DA agent, use the finalize the scurve of thesis. and the later Chap6
   conlusion part, I will review. build the bundle at task ending."
2. "Final Check if any datas are missing and need to run for the Chap6. if yes, write a new PENDING2409 md for it, if
   no, do nth."
3. "the ntrial 20 appdix is been curated by other agent, use as it is, add into v3 version and changlog. dont change
   anything, use it. put it end of the Appendix."

**Sources:**
- `Data_Analysis/DA_in_Paper/analysis/DA_20260924_scurve_R44bc_projection_controller.md` (complete; C1 job 26204,
  md5-verified);
- the notes `cross_draft/to_v3/FROM_DA_20260924_scurve_R44c_complete.md` and `…_scurve_finish_line_wall_end.md`;
- `Working_Space/ntrial20_appendix_20260924/`.

## 1 · UAV-s-curve finalized (`chapters/06_results.tex`, `chapters/05_setup.tex`; old texts in `withheld/20260924_v3.92_archive/`)

**Figure 6.9 `fig:uav-scurve-paths`.**
- Exported by `plotting/export_to_draft.py v3`; the draft copy is identical to the store's.
- Now the 2×2 on FM at K1: unprojected and per-step rows, cascaded geometric and MuJoCo MPC columns, ten flights per
  panel.
- New caption; the dataref names the extractor, the runs and the panel counts.
- The pilot (MeanFM K10, 10 against 3) is retired from the text: its paragraph, the two `\outdated` and the "three
  against ten" guard. The pilot is kept only in the datarefs.

**Table 6.16 `tab:uav-controller` is complete.**
- New row, per-step under MuJoCo MPC: 0/10, 105.5 ± 46.0, 1.626 m, 7/10 aborted.
- "Pending" is removed from the caption. The dataref adds C1's facts: seven inverted at 15.7–20.7 s against the outer
  wall; 654 of 1055 violating steps on the walls; cross-track 12.1 cm.

**The controller caveat (§6.3.3.3).**
- The comparison of record comes first: MPC removes the inversion, and every flight ends within 0.3 m of the goal.
  Instability is the controller's limit; the violations are not.
- "Changing the controller is not the whole answer…" now stands at ten against ten. Under projection the cascaded
  controller brings 5/10 across and MPC 0/10; the controller that rescues the unprojected plans loses every projected
  one; no flight of the forty is violation-free.
- The cost paragraph drops the pilot split (now only in the dataref), and the dataref adds the projected-plan loop cost
  (159.4 against 348.6 ms) and why it is not split.
- New closing sentence.

**The reading after Table 6.14** gains the late crossing: the plans cross the gap ~0.2 m late, at x = 0.21–0.27 m,
against the demonstrations' Z-route at x = 0 (DA §8b).

**The conclusion (§6.3.3.4) is final, no longer `\provisional`.** It opens with the storyline, the controller on an
extreme trajectory, and gives the DA verdict's three limits:
1. stability at the second turn is the controller's (MPC keeps every flight upright, at 24× the cost per step);
2. the rescue depends on the plan (under projection MPC loses all ten, cascaded brings 5/10 across);
3. the corner cut is the plan's (0.2 m late; neither MPC nor per-step projection, as configured, removes it).

Then the simple-data paragraph, with the half-scale-noise `\guard`.

**The finish line at the end of the walls (DA note, code change U19 coding3).**
- Ch 5 `fig:env-uav` caption: the "(UAV-s-curve is scored 0.2 m further on …)" parenthetical is gone.
- The success item names both lines (x = 2.0 and 3.0 m); the corridor's goal-point reason stays corridor-only.
- The srcnote gives `SCENE_CLEAR_LINE_X = {'corridor': 2.0, 's_curve': 3.0}`, the coding3 changelog, and why no count
  moves (0 of 160).
- The notes to Tables 6.14–6.16 say "identical under the end-of-wall line".

**Status texts.** The §6.3 intro's "Cells printed pending…" sentence is removed. The Ch 5 R44 srcnote now says "all
landed 24-09".

## 2 · §6.4 with UAV-s-curve (for the author's review)

- The `\hole` is removed; the 🔒 banner records v3.92.
- §6.4.1 prose: "UAV-s-curve takes the plans onto a route of sharp turns, where the tracking controller, not the model,
  sets the outcome."
- `tab:summary-models` row: the controller on an extreme trajectory; the average-velocity objectives are not ahead at
  K1, an order read for the controller.
- `tab:summary-projection` row, K1, per-step only: it binds the measured position, leaves the corner cut in place,
  costs four of nine crossings, at 126–155 ms.
- `tab:summary-combinations` row: FM K1 without projection, cascaded; 9/10 cross, none violation-free; MPC keeps all
  ten upright at 24×.
- The "UAV-s-curve is pending" caption notes are removed.
- §6.4.3: one sentence (stability is the controller's limit, which MPC lifts at 24× and loses again under projection;
  constraint satisfaction is the plan's).

## 3 · The ntrial20 appendix, as it is

- `Working_Space/ntrial20_appendix_20260924/app_ntrial20_feasible.tex` is copied **byte-identical** to
  `chapters/app_ntrial20_feasible.tex` (SHA-256 `ddde881204c5…` on both).
- It is `\input` in `chapters/09_appendix.tex` as the **last section of Appendix B**, immediately before
  `\chapter{Reproducibility}`, which is where its own header asks for it. That makes it the end of the results
  appendix without raising `\section` to `\chapter`, the one alternative its header offers, which would change the file.
- It adds `app:avoiding-twenty`, `tab:app:twenty-projected` and `tab:app:twenty-raw`.
- **Not done, because it changes more than adding the section:**
  - retiring the dead block `app:avoiding-twenty-episode`;
  - re-pointing its three references (`05_setup.tex` two, `06_results.tex` one);
  - the Ch 5 edit to the dropped DPCC twenty-episode row, which the header lists for when the dead block goes.

  Both sections now sit in the build under different labels, as the header says they can. This is the author's call.
- check.py resolves every reference of the new section (`sec:setup:metrics:spread`, `tab:avoiding-dpcc-protocol`,
  `tab:avoiding-raw-models`, `sec:res:ablations`).

## 4 · Final data check for Chapter 6: nothing is missing, so no PENDING file (as asked)

Every *pending* / *lacking* / `\hole` / `\provisional` / `\outdated` / `\flawed` in Ch 5, Ch 6 and the appendix was
checked against `data_status/PENDING_20260922_all_lacking_runs.md` (its NOW table said the only open data cells were
R44b and R44c; both are now written).

| what remains | kind | a run needed? |
| :-- | :-- | :-- |
| appendix `\hole` ×2 (compute total; job ids in `tab:corpora`) | bookkeeping in Reproducibility | no, log arithmetic |
| dead block `app:avoiding-twenty-episode` | superseded by the new `app:avoiding-twenty` | no |
| `\outdated`, "UAV-s-curve at the budgets on disk" | kept for reference; "no tightened re-run is planned (R38 superseded by R44)" | no |
| `\provisional`, D3IL-aligning final-distance variance | a caveat; its repeat run R17 is struck in the ledger | no (it could become a `\guard`; the author's wording call) |
| D10c (box pose for the aligning path figure) | the appendix figure draws the end effector, stated in a `\guard` | optional |
| R43, R45b, the s-curve setpoint-bound per-step run (DA §7), CI-MeanFM K10 of `tab:avoiding-projectors` at two episodes | optional, the author's call | optional |

The ledger's NOW section and the s-curve ledger carry this status line; no `PENDING_2409` file was written.

## Records

- INBOX: both DA s-curve items are ✅ v3.92, and there is a → v4 row.
- `INSIGHTS_20260924_results_per_environment.md`: s-curve marked final; S4 gains the late crossing; S6 added (the
  rescue depends on the plan).

## Checks

- `python3 tools/check.py` passes (hole 6, provisional 1, outdated 1, flawed 2).
- Not compiled. Nothing committed.

## Bundle (author: "build the bundle at task ending")

- `bundle/output/thesis_v3_20260924_155404_new.zip` (annotated), `…_155405_new_clean.zip`, `…_155405_new_nonotes.zip`.
- 11 sources inlined (the ntrial20 section among them), cross-references all resolve, 48 figures render (the rebuilt
  `fig_uav_scurve_paths` included).
- **`--verify` reports one MISMATCH, `chapters/09_appendix.tex`, and it is the verifier, not the content.**
  - `cmd_verify` compares the whole parent block, including the nested `app_ntrial20_feasible` block, against
    `09_appendix.tex`, which holds only its `\input` line there.
  - Checked by hand with the tool's own helpers (script in the session scratchpad, the tool unchanged): fold the nested
    block back to its `\input` line, dropping the two blank lines the flatten adds around it. Then `09_appendix.tex` is
    byte-faithful in all three bundles, and the nested block equals `app_ntrial20_feasible.tex`, which the tool itself
    also reports OK.
  - Teaching `cmd_verify` about nested inputs is a small change to `bundle/make_bundle.py`; it is not made without the
    author's go-ahead.

Claude (Opus 5.5, Claude Code) · 2026-09-24 · not compiled · nothing committed.
