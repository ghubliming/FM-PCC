# v3.64 — 2026-09-22 · The quadrotor section is rebuilt in the order of the manipulation ones: before projection, after projection, then the two projectors; the alignment threshold is said to be compressed because the budget is high

Read after [`v3.63`](v3.63_20260922_pillars_fallback_hg_u17_abandoned.md). Two author items. **No bundle
built** — the author said not to.

---

## 1 · "The V_A threshold discussion is due to HIGH K; the DPCC 0.5 threshold is unrealisable, so the projection threshold is compressed"

The paragraph of §6.2.2 is re-headed **"The activation threshold is compressed, because the budget is high"** and
now opens with that fact: D3IL-aligning is the task that needs a large step budget, and the default of DPCC,
$\eta=0.5$, was set for small ones — one solve on D3IL-avoiding at one or two evaluations, ten per candidate per
control step at $\nfe=20$, fifty at $\nfe=100$, "not a projector one can run". The measured pair at $\nfe=100$
(15,218 ms against 1,195 ms for 6 mm) stays as the proof, the rule is stated as *compressing the threshold as the
budget grows* so that the number of guiding steps stays roughly constant, and the sentence before `tab:va-threshold`
calls the two $\nfe=100$ rows "the measured cost of the uncompressed default against the compressed one". Every
projected alignment result is stated to be under the compressed threshold. No number changed.

## 2 · "Rebuild the UAV chapters — corridor/pillars — like Avoiding/V_A: first the pre-projection table (pareto if needed), then the post-projection table/pareto, HF together in the post-projection chapter or as representative comparisons; leave s-curve"

§6.3 is now three parts, in the order of §6.1 and §6.2, with the labels the other chapters point at kept on the
subsections that carry the meaning:

| part | corridor | pillars | s-curve |
| :-- | :-- | :-- | :-- |
| **6.3.1 Generative Models** — before projection (`sec:res:uav:models`) | **new `tab:uav-corridor-raw`** (10 cells: success 12/12, collision-free 0/12, 43–56 violating steps, 9–45 ms; diffusion 181 ms) + a paragraph; no frontier, and the text says why (S&C 0.00 everywhere) | the caveat paragraph (unchanged, `sec:res:uav:pillars` stays here for Chapter 5's references) + **new `tab:uav-pillars-raw`** (8 cells + 2 pending; success, collision-free, S&C, goal, violating steps, ms) | — |
| **6.3.2 Projection** — after per-step projection (`sec:res:uav:projected`) | `tab:uav-corridor` back to its **projected rows only** (caption "after projection"), the budget-ladder paragraphs, Fig 6.9, Fig 6.10; `sec:res:uav:corridor` moves here, which is what Chapter 8 cites | `tab:uav-pillars` (per-step block), `tab:uav-pillars-best`, the flight-by-flight and *goal* paragraphs, `fig:uav-pillars-paths`, step budget, CI paragraph (`sec:res:uav:pillars:projected`) | **kept in one piece, unchanged**, as the last subsubsection of 6.3.2 |
| **6.3.3 Projection Methods** — per-step against endpoint (`sec:res:uav:projection`) | corridor prose, `tab:uav-corridor-projection`, its guard, the tightening result | **`tab:uav-pillars-endpoint` moved here** (with its *goal* block) in front of the pillars paragraph; the "two blocks" paragraph in 6.3.2 points forward to it | the one-sentence note, as before |

So the HF (endpoint) material is **together in the projection-method part**, per the author's second option, and the
per-scene parts keep a one-line pointer to it. The section intro says the order. The pillars "two blocks" paragraph
and the moved table's caption were re-worded for their new places; the pillars paragraph of 6.3.3 no longer repeats
the not-pooled clause, which the corridor paragraph before it already carries. The chapter's header comment lists
the new layout.

**New numbers, all from the corpora, recounted today:** `tab:uav-corridor-raw` is the `none` half of the v3.63
pair table (unchanged values); `tab:uav-pillars-raw` is the `diffuser` variant of the eight `pillars_hg` cells
(15-09 batch, `u7hg`/`EPlatest_u7hg`), whose S&C column reproduces the *no proj.* column of `tab:uav-pillars`
exactly (0.30, 0.90, 0.00, 0.20, 0.00, 0.30, 1.00, 0.00) and whose *goal* column adds FM at $\nfe=5$ = 9/10
(collision-free on all ten, settled on nine). No run, no cell changed.

## Checks

`python3.14 tools/check.py`: 14 files, 6 927 lines, 278 labels, 53 citations, 40 figures — **all mechanical checks
pass**; drafting macros: 16 `\hole`, 3 `\provisional`, 20 `\guard`, 64 `\srcnote`, 55 `\dataref`, 0 `\todofigure`.
No figure changed. **Not compiled** — no TeX toolchain here. **No bundle built** (author's instruction); the v3.63
`105444/105445` set predates this restructure.

## Files

- `v3/chapters/06_results.tex` — §6.2.2 threshold paragraph; §6.3 intro; new §6.3.1 (`tab:uav-corridor-raw`,
  `tab:uav-pillars-raw`), §6.3.2 (`sec:res:uav:projected`, `sec:res:uav:corridor`, `sec:res:uav:pillars:projected`),
  §6.3.3 (endpoint table moved, order corridor → pillars → s-curve); header comment.
- `v3/CHANGELOG.md`, `v3/BRIEFING_20260922_v3.60.md`, `data_status/PENDING_20260922_all_lacking_runs.md` (§0 addendum row).

Claude (Fable 5.1, Claude Code) · 2026-09-22 · mechanical checks only. Not compiled. No bundle.
