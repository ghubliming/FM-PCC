# v3.75 — 2026-09-23 · UAV-s-curve: the grid at K = 1, 2, 20; projection and controller pending until the raw grid is read (R44)

**Asked (author, review of the v3.74 bundle):** item 5 of the v3.71 round asked for Table 6.13 (now 6.14) as MeanFM /
CI-MeanFM / FM at K = 1, 2, 20 and diffusion at K = 20, ten flights, missing cells pending — "why now … contain the weird
K10,5 runs? any special reason (in changelog maybe)? if not rebuild in my way and I will run the slurm". "Table 6.15 feels
so random. why K2, the HF is even not on in K2. we may choose first the raw NN output then decide on which model run the
projection. So, now keep the template, and fix them … leave pending, then build a NEW s_curve pending md, first run the raw
NN then after decide run the projection." Mid-turn: "if any live s_curve runs in the 2209 pending md, delete them and point
to your new md". And "keep update and rebuild bundle".

**Was there a reason for K = 10 and 5?** No author decision. At v3.71 the MeanFM K = 10 and CI-MeanFM K = 5 cells were
printed because they were on disk (the v3.67 pilot budgets, "each model at the budget it is strongest at"; MeanFM K = 10
is also the MuJoCo MPC pilot's budget). The v3.71 changelog records the table, not a reason. And the author is right about
K = 2: at the default threshold 0.5 endpoint projection has no guiding step there and the evaluation drops it
(`eval_k_sweep.sh` header, `HF_DEGENERATE_SKIPPED.txt`), so the FM K = 2 projection table could not compare the methods.

## Chapter 6 (§6.3.3–6.3.5)

- **`tab:uav-scurve` (Table 6.14)** — rows MeanFM, CI-MeanFM, FM × K ∈ {1, 2, 20} + diffusion K = 20; **MeanFM K = 10 and
  CI-MeanFM K = 5 removed** (to `app:uav-scurve-budgets`); the ▶ selection mark removed. Filled (ten flights each, as
  before): MeanFM 2, CI-MeanFM 1 and 2, FM 2 and 20, diffusion 20. Pending (R44a): MeanFM 1 and 20, CI-MeanFM 20, FM 1.
  Caption 80 words.
- The reading after it states the filled cells only; the choice of configuration is a `\hole` until the grid is complete.
- **§6.3.3.2 Projection** — one factual paragraph (endpoint guides only on steps before the terminal one the threshold
  selects: nine at K = 20, none at K = 1–2); **`tab:uav-scurve-projection` (Table 6.15) all pending (R44b)** for "the
  configuration selected in Table 6.14"; the FM K = 2 per-step numbers of v3.71 moved to the dataref; the reading is a `\hole`.
  Caption 51 words.
- **`tab:uav-controller` (Table 6.16)** — for the selected configuration, all four cells pending (R44a/b for the cascaded
  geometric rows, R44c for MuJoCo MPC). The pilot (MeanFM K = 10, Fig 6.9, Table 6.17) is unchanged. Caption
  71 words.
- §6.3 intro's pending sentence and the s-curve conclusion rewritten; no s-curve "R42" remains in the draft (the one R42 left,
  §6.1.2, is the older ledger's diffusion-K2 item, closed).

## Chapter 5 and the appendix

- §5.6.3.3: UAV-s-curve is evaluated with the flow-based models at 1, 2 and 20 and the baseline at 20 without projection,
  the selected configuration under both methods, its plans under both controllers; srcnote points to R44.
- `app:uav-scurve-budgets`: banner updated; the "outside the grid" table now holds MeanFM K = 10 and **CI-MeanFM K = 5**
  (FM K = 20 moved into the grid; caption 37 words); the plain-set projected cells
  that were "pending (R38)" are now "not run" — R38 is deleted, projection is flown only on the selected configuration.

## Runs

- **New file `data_status/PENDING_20260923_uav_scurve_R44_raw_first.md` (R44)** — the only live s-curve list.
  **A (now):** the raw grid; required A1 MeanFM 1, A3 MeanFM 20, A6 CI-MeanFM 20, A7 FM 1; recommended: re-fly the other six
  cells under the same tag `p23scgrid` (one tag for the whole table; settles the CI-MeanFM checkpoint question). Commands,
  log checks and the DA columns are in the file (~1 GPU-h required, ~2 full). **Decision point:** the author picks the
  configuration; endpoint needs K ≥ 3, so in this grid only K = 20 compares both methods — if the winner is at K = 1–2 the
  author chooses K = 20 for that model or per-step only. **B:** projection (`dpcc-r/c/t-tightened`, `hardflow_new-r/c/t-tightened`
  only at K ≥ 3), tag `p23scproj`. **C:** MuJoCo MPC on the selected plans, tag `p23scmjpc`. Nothing submitted.
- **22-09 ledger:** R40 row in the NOW table replaced by an R44 row; R38 and the old R15 🟡 row deleted into pointers; R31's
  pointer and the struck list updated; a banner at the top. **22-09 runbook:** banner — groups D, E, G struck, see R44.
  **23-09 ledger §4 and runbook §3b:** the v3.71 R42 s-curve spec (never submitted) replaced by pointers.
- The ID R42 had been used twice (22-09: diffusion-K2 re-time; 23-09: the v3.71 s-curve); the s-curve runs are now R44,
  which no file used before.

## Bundle (ninth round)

`bundle/output/thesis_v3_20260923_164500_new.zip`, `…164500_new_clean.zip`, `…164501_new_nonotes.zip` (10 478 KB;
47/47 figures render; `--verify` byte-faithful on all three). Supersedes `163243`.

## Checks

`python3.14 tools/check.py` passes. Not compiled. Nothing committed.

Claude (Opus 5.5, Claude Code) · 2026-09-23 · not compiled · nothing committed.
