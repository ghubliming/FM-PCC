# v3.99 — 2026-09-24 · The v4.0 and v4.1 cross notes applied: Ch 5/6 no longer point at appendix content v4 moved or archived; bundle

**Asked (author):** "check the inbox cross note, update the v3, only job is to check cross note from v2/4, nth else,
update and build bundle (i am prepare for 1st aggregated release)".

## The inbox

| from | note | state before | action |
| :-- | :-- | :-- | :-- |
| v4.0 | `cross_draft/to_v3/FROM_v4_20260924_v4.0_twenty_episode_refs_app_repro_pointer.md` | ⏳ | applied, items 1–2; item 3 is FYI (no Ch 5/6 reference to `tab:names`/`tab:corpora`; Ch 5–6 already write "MuJoCo MPC") |
| v4.1 | `cross_draft/to_v3/FROM_v4_20260924_v4.1_appendix_index_changes.md` | ⏳ | applied, every row of its table |
| v2 | — | no open → v3 row (v2.27 was absorbed at v3.78) | none |

## What changed (17 passages; the old text of each is in `withheld/20260924_v3.99_archive/refs_before_v3.99.tex`; line numbers are after the edit)

**Ch 5 (`05_setup.tex`)**
- l. 53–54 and the Fig 5.1 caption (l. 78–79): the X2 dimensions figure is "in the appendix", no longer "in
  `app:repro`" (v4 moved it to A.2, Supplementary Material; `app:repro` now prints the Compute Environment).
- l. 918–920 (reproduction of DPCC): the sentence that kept the baseline's incomplete twenty-episode run "for the
  record" in the appendix was false once v4.0 dropped that section. Now: "A twenty-episode evaluation of two of the
  flow-based models at the budgets they are operated at exists and agrees with this protocol
  (`app:avoiding-twenty`); the baseline's own twenty-episode run did not complete and is not used."
  - **Adapted from the author's wording** in the v4.0 note ("the two flow-based operating points"): Ch 6 marks only
    one operating point (▶, MeanFM K1 t, Table 6.2), and the twenty-episode section evaluates two models,
    analytic average-velocity and instantaneous-velocity matching, at K = 1 and 2.
- l. 1089–1090 (compute environment): "how its properties bear on reading them is given in `app:repro`" became
  "what the measured times contain, and what they leave out, is discussed in `ch:discussion`".
  - v4's target `sec:disc:practice` (Ch 8 Towards Deployment) exists only in v4's build, so v3's build would print "??".
  - `ch:discussion` resolves in both builds: in v4's it is Chapter 8, which holds that section.
- l. 1335–1336 (avoiding protocol): `app:avoiding-twenty-episode` → `app:avoiding-twenty`.

**Ch 6 (`06_results.tex`)**
- **Table 6.1 caption (l. 106–107) and Table 6.2 caption (l. 369–370):** "The budgets K = 5 and 10 are in `tab:app:avoiding-raw-full` /
  The budgets between two and twenty are in `tab:app:avoiding-dpcc-full`" became "The budgets K = 5 and 10 were
  evaluated as well and are not printed." (v4.1 archived both appendix tables.)
- **The unprojected reading (l. 147–148):** "the intermediate budgets of five and ten, which the table does not
  print, hold it between 14.2 and 19.1, and the best and the worst of those counts both come at a budget of ten".
  - Checked against the archived table: MeanFM 5 is 16.4, MeanFM 10 is 14.2, diffusion 10 is 19.1.
- **The baseline at ten steps (l. 338):** the parenthetical reference is gone; 68.7 steps and 321.7 ms stay as text.
- **Tables 6.1 and 6.2 `\dataref`:** each now names the cells quoted in the text as cells of the same batch
  (`batch_avoiding_combined_20260919_132703`, confirmed from the archived table's own dataref). They also say
  where v4 archived the tables (`v4/withheld/20260924_v4.1_archive/`, data `app_long/avoiding_budgets.tex`).
- **l. 764–766, what the appendix carries for D3IL-avoiding:** "every evaluated budget under every selection rule"
  is gone. The twenty-episode pointer now reads `app:avoiding-twenty`.
- **l. 696–697:** a LaTeX comment named the dropped label; it now names `app:avoiding-twenty`. Not printed.
- **Table 6.13 `\dataref` (l. 2062–2064):** the budgets flown before the grid (MeanFM 10, CI-MeanFM 5) "were in the appendix
  section archived by v4.1", with the path.
- **UAV-s-curve projection:**
  - Table 6.14 caption (`tab:uav-scurve-projection`, l. 2146–2147; v4's note calls it Table 6.15): "; every rule in `app:uav-scurve-rules`" is gone. The fact
    stays: the three rules cross on five flights each, and random selection is carried by the tie-break.
  - Its `\dataref` names the archive path.
  - The reading (l. 2179): "Under every selection rule the selected configuration crosses on five flights of ten",
    without the reference, as v4 suggested.

## Checks

- **Every reference of Ch 5 + Ch 6 resolves in both builds** (a scratch scan of 366 references; nothing written to
  the repo):
  - v3's own build;
  - v4's build, which is v3's Ch 1–6 plus v4's Ch 7–9.
- **None of them targets a v4 alias label** (`app:avoiding-budgets`, `tab:app:avoiding-raw-full`,
  `tab:app:avoiding-dpcc-full`, `app:uav-scurve-rules`, `app:uav-scurve-budgets`, `app:avoiding-twenty-episode`),
  so v4 can remove its alias block.
- **Captions touched stay under 100 words:** Fig 5.1 ~95, Table 6.1 ~92, Table 6.2 ~85, Table 6.14 ~85.
- **`python3 tools/check.py` reports one FAIL, a false positive:** "reference with no \label: app:avoiding-twenty".
  - The label is defined in `chapters/app_ntrial20_feasible.tex`, which `09_appendix.tex:900` inputs.
  - check.py reads only the master's own `\input` list, not nested ones, so it never sees that file. Until v3.99 no
    chapter referenced a label in it.
  - LaTeX resolves the label, and the bundle's cross-reference check passes, since it inlines nested inputs.
  - A fix to check.py (follow nested `\input`) is proposed to the author and **not applied**, per the rule on
    unrequested code edits.
- Not compiled. Nothing committed.

## Bundle (two variants; includes v3.99)

- `bundle/output/thesis_v3_20260924_214628_new.{tex,zip}`: annotated; Ch 1–4 collapsed (v2.27).
- **`bundle/output/thesis_v3_20260924_214628_full_clean.{tex,zip}`**: clean, the whole thesis.
- Both verified byte-faithful with `--verify`, and no dangling reference in either (135 and 211 distinct targets).

## For the aggregated release

- v3's build still carries v3's own copies of Ch 7, Ch 8 and the appendix, as they were handed over at v3.98 (the
  pre-v4 Discussion and Conclusion; the appendix with the dead twenty-episode block and the four sections v4 archived).
- The release's Ch 7, Ch 8 and appendix are v4's. v4 picks up these Ch 5/6 edits with `python3 tools/sync_v3.py merge`
  (v4's tool, not run from here).
- Told v4: `cross_draft/to_v4/FROM_v3_20260924_v3.99_appendix_references_repointed.md`.

Claude (Opus 5.5, Claude Code) · 2026-09-24 · not compiled · nothing committed.
