# v3.59 — 2026-09-21 · Platform figures, timing scope and result markers

## Changes

- Reverted the v3.57 Figure 5.1 consolidation: the Panda and X2 MuJoCo renders remain Figure 5.1. The X2 dimensions and projection footprint were first restored as a separate Figure 5.2, then moved to the Reproducibility appendix in the same-day continuation, as the author requested. The main Chapter 5 prose and Table 5.1 point to the appendix figure. The figure asset and label `fig:platform-x2-dimensions` are unchanged; a v4 handover note says to keep it there.
- Verified Table 5.1's UAV interval against the Gen15 evaluation loop. The planner updates the X2 position setpoint once per three MuJoCo steps at $0.01$ s each, while the cascaded geometric controller computes rotor thrusts at every step. The table and prose now distinguish those rates from the manipulator's IK route. The $0.03$ s is **simulated time**, not a wall-clock planning deadline.
- Verified the corridor halfspace: its configured line runs from $(-2,0.95)$ to $(2,-0.05)$ in the horizontal $x$--$y$ plane, giving $\arctan(1/4)=14.04^\circ$. The prose now says *lateral* rather than potentially suggesting an altitude descent. UAV-s-curve is described as a tracking-controller stress test, not ranked “most difficult”; the scene figure caption no longer ranks the scenes. The constraint-figure introduction says what is shown without calling it a redraw.
- Removed the metacommentary about confidence intervals, tests and significance at the end of §5.4. The preceding bullets still define exactly what each printed spread measures.
- Table 5.8 now contains the actual model input/output plan shapes $(N,8,6)$, $(N,8,9)$ and $(N,8,12)$ and the state input shapes. Its caption gives the exact per-camera RGB tensor $(N,1,3,96,96)$ and the combined $(N,128)$ visual feature. The v3.58 v2 Method handover already requests the corresponding shape notation in Chapter 4.
- Figure 6.3's caption now names its horizontal axis as **planner time**: generation, projection and selection, excluding low-level control and MuJoCo stepping. Full executed-step time exists for the limited UAV-s-curve controller comparison in Table 6.15, not for the matched avoiding cells needed for a total-time Pareto plot. New pending item D11 specifies the required matched instrumentation for avoiding, with alignment/corridor extensions.
- §6.1.2.8 states the measured twenty-episode result directly and points to the detailed appendix; its incomplete $\nfe=20$ seed–geometry coverage is retained as a source comment and in the appendix coverage table, not an apologetic main-text sentence.
- Table 6.7 repeats the selected MeanFM endpoint-projection row with the triangle marker and adds a dotted diffusion-baseline row explicitly marked *not evaluated on tightened constraints*. No result was imputed for the missing baseline cell (R2).
- **Continuation:** `fig:env-avoiding` dropped the 96-demonstration trajectory panel and every description of it. It first used only the authentic MuJoCo frame with the Panda end effector past the green goal line: frame 200, first 320×180 tile, of `d3il/figures/github_readme.gif`; that frame and surrounding frames 180–210 were visually checked. Because moving the X2 figure to the appendix changes figure numbering, this is the object formerly called Figure 5.3 and now numbered Figure 5.2 by LaTeX. The stored `fig_env_avoiding` plot remains in the official DA store but is not included in the thesis. D12 records the print-resolution improvement, which requires MuJoCo on the cluster; neither local Python has `mujoco`.
- **Continuation:** §5.4.4 is titled *Aggregation and Variability*, which names its actual subject: means, seed-to-seed spread, episode-to-episode spread and counts. Figure 6.8 now draws one matched executed path per model–budget cell: episode 2 from the same seed and geometry in all four panels. START and END mark the recorded coordinates directly; the unused failure legend and overlaid second path are gone. The left-to-right paths have measured planar lengths $0.708$, $0.700$, $0.699$ and $0.695$ m, respectively, giving a legible sequence without inventing a trajectory. The caption and nearby text distinguish the four displayed paths from the eight evaluated episodes; the latter were all goal-reaching and violation-free.
- **Hotfix:** Figure 5.2 now pairs the original MuJoCo starting pose on the left (frame 0, first tile) with the goal-passed pose on the right (frame 200, same tile). Both are extracted from the tracked montage through `sources.ENV_RENDER_FRAMES`; their two official DA assets were exported to v3. The caption describes only those simulator views. D12 now covers print-resolution replacements for both 320×180 stills.

## Evidence and limits

- UAV loop: `mix_uav_test/eval_mix_uav.py:1609--1613,1710--1736`; simulation timestep `d3il/environments/d3il/models/mj/robot/quadrotor/quadrotor_modified.xml:4`. Dataset sampling: `uav_expert_data_collect/dataset_writer.py:31,73`. The cited evaluations use `pid_stopgo`, i.e. a cascaded tracker with zero desired velocity at each setpoint.
- Halfspace: `config/uav_projection.yaml:812--838`; the line changes $y$ by $-1.0$ m over $+4.0$ m in $x$. Its angle is a geometric calculation, not a fitted measurement.
- Planner-time boundary: `scripts/eval.py:348--350` times plan generation/selection and `Data_Analysis/DA_Result_Curated_MD/Report_20260909_MJPC_vs_PID_s_curve/README.md` §5.1 verifies `avg_time_ms = fm_ms + proj_ms` and excludes the tracker. `tab:uav-controller` uses separate full elapsed-step timing.
- Table 6.7 repeats the selected row of `tab:va-projection`; tightened projected diffusion is missing as R2 in `data_status/PENDING_20260920_all_lacking_runs.md`. The note in §6.1.2.8 follows the exact K20 coverage in `tab:coverage-k20`.
- The avoiding simulator views are `d3il/figures/github_readme.gif`, frames 0 and 200, first MuJoCo tile, extracted by `Data_Analysis/DA_in_Paper/plotting/prep/extract_env_frames.py` to `data/prepared/fig_render_avoiding_start.png` and `fig_render_avoiding.png`. The latter visibly places the end effector beyond the goal line; `fig_env_avoiding` is an orthographic demonstration plot and is no longer part of the thesis figure. The X2 footprint asset is `Data_Analysis/DA_in_Paper/figures/env/fig_platform_x2_dimensions.{svg,png}`.
- Figure 6.8 reads `Data_Analysis/DA_in_Paper/data/exec_paths.json` through `plotting/builders/exec_paths.py`. Episode index 1 is the second of the two protocol episodes in each cell; its common start and the four ending positions are recorded in that JSON. Planar lengths above are sums of successive Euclidean distances in `xy`, used only to check the figure order, not as a thesis metric. The official figure SVG and PNG were regenerated and exported to v3.

## Checks

- The first v3.59 bundles (`153652_new`, `153656_new_clean`, `153700_new_nonotes`) are superseded by this continuation. The figure exporter finds 38 referenced figures, all current. `python3 tools/check.py` passes: 14 files, 5999 lines, 265 labels, 53/53 cited bibliography entries and 38 referenced figures. Replacement **v3-only** bundles: `thesis_v3_20260921_154736_new.zip` (annotated), `thesis_v3_20260921_154739_new_clean.zip` (source notes hidden) and `thesis_v3_20260921_154745_new_nonotes.zip` (source, data and guard notes hidden). All report resolved cross-references and 42/42 packaged figures renderable, with no placeholders. `make_bundle.py --verify` reports 9 inlined and 4 collapsed sources byte-faithful. No TeX toolchain is available; **not compiled**.
- The earlier v3.59 bundles through `154745_new_nonotes` are superseded by this second continuation. The figure store and v3 export are current. `python3 tools/check.py` passes: 14 files, 5997 lines, 265 labels, 53/53 cited bibliography entries and 38 referenced figures. Final **v3-only** bundles: `thesis_v3_20260921_155342_new.zip` (annotated), `thesis_v3_20260921_155342_new_clean.zip` (source notes hidden) and `thesis_v3_20260921_155343_new_nonotes.zip` (source, data and guard notes hidden). Each reports resolved cross-references and 42/42 packaged figures renderable with no placeholder; `make_bundle.py --verify` reports 9 inlined and 4 collapsed sources byte-faithful. No TeX toolchain is available; **not compiled**.
- The bundles through `155343_new_nonotes` are superseded by the Figure 5.2 hotfix. `python3 tools/check.py` passes: 14 files, 6004 lines, 265 labels, 53/53 cited bibliography entries and 39 referenced figures. The final **v3-only** bundles are `thesis_v3_20260921_192520_new.zip` (annotated), `thesis_v3_20260921_192520_new_clean.zip` (source notes hidden) and `thesis_v3_20260921_192520_new_nonotes.zip` (source, data and guard notes hidden). All have resolved cross-references, 43/43 packaged figures renderable and no placeholder. `make_bundle.py --verify` reports 9 inlined and 4 collapsed sources byte-faithful. No TeX toolchain is available; **not compiled**.

**Written by:** Codex GPT-6 Astra · 2026-09-21. **Not compiled.**
