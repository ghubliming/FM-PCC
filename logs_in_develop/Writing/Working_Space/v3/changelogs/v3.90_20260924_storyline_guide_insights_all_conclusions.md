# v3.90 — 2026-09-24 · The author's storyline written down; every sub-conclusion and §6.4 rewritten to it, with the insights of each section

**Asked (author), in three messages:**
1. "you know we spend so much effort prove in the avoiding the mf/af donimte and then proved plus the HF in the alining
   this is already done. the UAV - pillars is just a again reaffirm of the avoiding and it success and we also point the
   UAV controller in the pillars vs avoiding; in the corridor we proved something new that the expertdata issue and the
   whole DPCC concept works on our new env; in hte s_Curve we deep discuss the controller in extreme trajs. this is the
   GUIDE/PRINCIPLE from me, maybe you put it somewhere to reference with todays date."
2. "keep refine each sub consluion and hte final 6.4"
3. "also inside each secen we have speical insigt/consluion like the difu K2 time discuss in the d3il-avoiding … plz
   gather our insights! and really apply into the conclusions."

## Written down

- **`Working_Space/GUIDE_20260924_results_storyline_author.md`:** the author's words verbatim; the five lines (what each
  environment's conclusion must say first); how to apply them. For all drafts.
- **`Working_Space/INSIGHTS_20260924_results_per_environment.md`:** 27 insights gathered from §6.1–§6.3, each with its
  source label and where it is now used (23 applied, 4 not, with the reason).
- **Memory:** `thesis-results-storyline-guide` (index line in `MEMORY.md`).

## Ch 6 (`chapters/06_results.tex`; the v3.89 texts in `withheld/20260924_v3.90_archive/`)

**§6.1.4 D3IL-avoiding: opens with the central claim.**
- MF/AF Pareto-dominate DPCC's diffusion model at its protocol, with its own 4.0 M U-Net. CI-MeanFM: 1.000 in 59.2
  steps at 18.1 ms, against 70.1 and 553.4. MeanFM: one episode in thirty.
- Then the insights:
  - FM at K1 dominates the baseline too (the flow sampler), and MF/AF add the fewest steps;
  - **the K2 baseline** (1.000, 61.2 steps, 211.4 ms): the saving is 12×, not 30×;
  - its K2 time is 6× its K1 time because its projector is handed a plan one step from noise (45 against 2–6 ms per
    candidate);
  - the frontier is a region, not a point;
  - temporal consistency is the flows' rule, and cumulative cost stalls them.
- The endpoint paragraph gains: at K3 endpoint projection halves the average-velocity models' time, because the
  predicted endpoint is near the feasible set. This is the same law as the K2 baseline.
- The old sentence "the step budget and the selection rule set the ordering, not the flow objective" is gone.

**§6.2.4 D3IL-aligning: the result again, plus endpoint projection.**
- MeanFM dominates the baseline's best projected row (9 against 4, 0.179 against 0.462 m, 266 against 809 ms).
- Every non-dominated unprojected configuration is MeanFM; at K2 it has closed two fifths at 1/7–1/10 of the cost.
- CI-MeanFM's stop-gradient target: exposed by a task that rewards evaluations, hidden on avoiding.
- Endpoint projection is the better projector *on the constraint*. It is the reverse of avoiding because of the budget,
  not the task. It does not dominate: a comparable price.
- The task's trade: zero violation costs completion, and the cheap budget (K2, 43 ms) is not the solving budget.

**§6.3.1.3 UAV-pillars: reaffirmation, then the plant and its controller.**
- The declared constraints transfer; the models keep their order and gap; per-step projection again repairs every plan.
- Then what the quadrotor changes: closer tracking (0.007 against 0.02–0.04), and collisions with undeclared pillars.
  The skim at the rod's radius becomes the rotor reach (50/51 on the commanded path); a collision is catastrophic and
  ends the flight.
- "Not flown" is now a `\guard`, not part of the conclusion.

**§6.3.2.4 UAV-corridor: something new, in two parts, then RQ2.**
1. The DPCC concept works in 3-D. The flights descend 540/560 and climb 560/560; violating steps go from 29–99 to
   0–10. The residue under the tilt is **the hand-over**: the vehicle catches up with a plan already released 0.5 m
   ahead.
2. The expert data: on straight lines the model does not matter. The one shortfall lies at the edge of the data: plans
   ≤ 1.30 m against the 1.48 m apex. Against the baseline it is a trade-off, and the baseline flies wider (0.37 against
   0.19 m) and longer (290/321 steps).
3. RQ2: endpoint wins over the hump at K20 with one candidate. Under the tilt it is a trade-off; at K3–5 per-step is
   ahead.

**§6.3.3.4 UAV-s-curve (still `\provisional`): the controller first.**
- Inversions come 11.6–15.9 s in. The tracking error (0.30–0.33 m) is more than 2× the clearance (0.121 m).
- MuJoCo MPC arrives, still violating, at about 126 against 6 ms per control step: more than planning, about 7× avoiding's
  entire action.
- Then the simple-data point: MF/AF not ahead at K1, read for the controller.

**§6.4.**
- §6.4.1 prose: the study's lines.
- `tab:summary-models` rows:
  - avoiding: the central result, including FM at K1 and the K2 twelvefold;
  - aligning: every non-dominated configuration is MeanFM;
  - pillars: reaffirmation plus the plant;
  - corridor: new — DPCC in 3-D and the expert data.
- §6.4.3 reading: the thesis rests on the two D3IL tasks, and the quadrotor carries the result to a new plant. It adds
  **three cross-cutting findings**:
  - the price of a projection is set by what the projector is handed;
  - a task saturated at one evaluation hides what one that rewards evaluations exposes;
  - the demonstrations bound what a model can show.

**Checked while writing:**
- Every number restates its source section (list in the insights file).
- On D3IL-aligning, "endpoint wins" is kept to "better on the constraint", as §6.2 says ("What it does not do is
  dominate").
- Every section label cited in the datarefs exists (check.py).

## Checks

`python3 tools/check.py` passes. Not compiled. No bundle (not asked). Nothing committed.

Claude (Opus 5.5, Claude Code) · 2026-09-24 · not compiled · nothing committed.
