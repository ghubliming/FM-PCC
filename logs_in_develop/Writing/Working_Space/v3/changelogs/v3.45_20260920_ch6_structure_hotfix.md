# v3.45 — 2026-09-20 · hotfix: §6.1 gets the same shape as §6.2 and §6.3, and the tables get their models back

Five author items, all about **structure and coverage** rather than numbers. Nothing here changes a
result; two things correct what a table was allowed to search over.

`python3.14 tools/check.py`: all mechanical checks pass. **Not compiled.**

---

## 4 · 🔧 §6.1 is now **Generative Models → Projection Methods → Conclusion**

The author's complaint, and it was right: v3.43 left §6.1 with **four** subsections in an order no other
section uses, with a block of results sitting *after* the projection discussion.

| v3.44 | v3.45 |
| :-- | :-- |
| 6.1.1 The Generative Models (unprojected) | **6.1.1 Generative Models** |
| 6.1.2 Projection Methods | 6.1.1.1 Smoothness of the Plans |
| 6.1.3 The Models under Per-Step Projection | 6.1.1.2 The Plans Themselves |
| 6.1.4 Conclusion | 6.1.1.3 At the Protocol of DPCC |
| | 6.1.1.4 Extended Evaluation |
| | 6.1.1.5 The Cost Frontier |
| | 6.1.1.6 Step Budget |
| | **6.1.2 Projection Methods** |
| | **6.1.3 Conclusion** |

This is exactly §6.2 (Generative Models · Projection Methods · Conclusion) and §6.3 (the same, plus the
controller caveat). The generative-model subsection reads the **same four models twice** — once
unprojected, once under the per-step projector — which is the "same data in two phases" the author asked
for, and the subsection now says so in its opening paragraph.

**Labels.** `sec:res:avoiding:models` moves to the merged subsection, so the reference from
`05_setup.tex` still lands on the model results. `sec:res:avoiding:plans` is retired and its two
references re-pointed at `sec:res:avoiding:raw` and at the text below them. Every other label travelled
with its block. No reference broke.

---

## 0 · Table 6.8 is locked to the section's budget set

`tab:state-lowk` searched **every** budget for the cheapest configuration at S\&C $=1.00$, which is how a
$\nfe=5$ MeanFM cell got into a section whose budget set is $\{1,2\}$ for the average-velocity models.
Recomputed over the locked set (`avoiding_af_unet`, seed 6, 20 episodes, six rules). **Every other cell
of the table reproduces unchanged**; the one that does not is MeanFM on *top-right-hard*, which does not
reach $1.00$ within $\{1,2\}$ at all — its best is **0.95 at $\nfe=1$**, one episode of twenty short.

The cell is now a dash carrying that value, the caption states the search space, and the paragraph under
the table was rewritten: CI-MeanFM reaches full success on all three geometries, MeanFM on two of three,
and the place they part is one episode wide. The `\guard` beneath it was aligned to match.

## 1 · No $\nfe=100$ anywhere in the alignment results

Removed from `tab:va-models`, from the budget paragraph, from the frontier paragraph and from
`fig:aligning-tradeoff`, which was rebuilt. The figure's frontier is **unchanged** — MeanFM at
$\nfe=2$, $10$, $20$ — because the point that left was dominated by MeanFM at $\nfe=10$ anyway.

⚠️ **What this costs, stated so it is not lost.** That checkpoint is `visual_aligning_dpcc`'s own
configured chain length (`config/aligning-d3il-visual.py:454`, `n_diffusion_steps: 100`) and it is the
**only** diffusion configuration that moves the box on this task ($0.1849$\,m, against $0.4140$\,m at
$\nfe=20$). Removing it makes the flow-based result look better, so the draft does not simply drop it: a
`\guard` under the budget paragraph records that the checkpoint exists, what it costs
($1526.6$\,ms per control step) and why it is outside the reported set. `plotting/sources.py` keeps the
cell registered and filters it through a new `ALIGNING_REPORTED` set, so the figure and the tables cannot
drift apart about which budgets are shown.

## 2 · The `2$^{\ast}$` marker is gone

It was a footnote marker in a data column. The asterisk is deleted, the row reads `2`, and its three
caveats are three plain sentences at the end of the caption.

## 3 · The projection phase gets its missing models

**`tab:va-projection-models` had CI-MeanFM and FM and no MeanFM at all.** It now carries all three
flow-based models — MeanFM at $\nfe=2$, $10$ and $20$ repeated from `tab:va-projection` so the cross-model
reading is in one place. The counting sentence is unchanged at **fifteen of eighteen** because those
eighteen comparisons always included the MeanFM ones; what changed is that the reader can now see them.
The "moves the box" sentence was recomputed over the same eighteen: **more in four, as many in seven,
fewer in seven** — which sharpens rather than softens the existing claim that endpoint projection's
advantage is on the constraint and not on the task.

**The same audit was run over the whole chapter**, and three real coverage gaps came out of it. All three
are now stated in the draft where they occur and recorded in
[`../../data_status/PENDING_20260920_all_lacking_runs.md`](../../data_status/PENDING_20260920_all_lacking_runs.md) §3b:

* **D3IL-avoiding** projection phase is MeanFM alone — endpoint projection was **never run** on FM or on
  the diffusion baseline there. §6.1.2 now opens by saying so and points at the aligning table for the
  cross-model version.
* **UAV-corridor** — the baseline's per-step row is now printed in `tab:uav-corridor-projection` so both
  phases cover the same four models, with four dashes where endpoint projection was never run on it.
* **UAV-s-curve** — the projected re-run covers the three flow models only; the `\guard` says so.

🔴 **The one gap worth a run:** *endpoint projection has never been run on the diffusion baseline on any
scene in any environment.* Every endpoint-versus-per-step statement in the thesis is therefore made on
flow-based models. It is an evaluation, not a training. Cheapest close: UAV-corridor at $\nfe=20$, four
cells, twelve flights each.

---

## Left open

* The diffusion-baseline endpoint-projection evaluation above.
* The two flown-path figures (v3.44 §9), still a download.
