# v3.62 — 2026-09-22 · Fig 6.3 gets a broken step axis, Table 6.5 prints the whole ladder, the alignment frontiers show the share closed, Fig 6.8 draws nine contexts

Read together with [`v3.61`](v3.61_20260922_tables_concise_frontiers_percent_scurve_grid.md) and
[`v3.60`](v3.60_20260921_ch5_margins_success_and_params.md): this pass revises two v3.61 outcomes on the
author's review (the trimmed Table 6.5, the log-percent axis) and two figures, then a second round of three
quadrotor items. The bundle was built at the end, on the author's word (see *Checks*). **This closes v3.62.**

---

## 1 · Fig 6.3 — "the y axis is too narrow … try log or other scale"

**Tried log first, as asked; it changed nothing.** The step range is 56–100, under a factor of two, so a log
axis moves the solved cluster from 41 % to 47 % of the panel height. Rendered, inspected, rejected.

**What actually squeezed the cluster** was structural: a 26 % headroom band reserved for the boxed direction
key inside every panel, plus the two hollow $\nfe=2$ points at 90–100 steps stretching the range.

**Done instead — a broken axis.** `_tradeoff_panel` maps steps through a piecewise-linear function: below
$80$ steps the axis is linear and takes 74 % of the height; above it the axis is linear again but compressed.
Every configuration that solves the task lies below 80; only the hollow $\nfe=2$ points and one diffusion cell
(top-right-hard, temporal rule, $\nfe=20$) lie above. The break is marked by the conventional slanted
strokes on both spines and a faint dashed line across the panel; tick labels are real step counts (55…80,
then 90, 100). The boxed *better* key is drawn **once, in the legend strip**, instead of inside each panel.
Prose and caption say so (`06_results.tex`, the paragraph before the figure and the caption).

Fallback, if the author prefers the old drawing: set `Y_BREAK = 10_000` in `builders/avoiding.py` and the
map is the identity throughout; the legend key stays.

## 2 · Table 6.5 — "we need K1/2/10 … build THE TABLE and mark lack"

`tab:va-models` now prints the ladder $\nfe \in \{1, 2, 10, 20, 100\}$ for all four models, five blocks, with
*pending* in every unevaluated cell — the s-curve convention of v3.61. Eleven of twenty cells are pending:

| $\nfe$ | MeanFM | CI-MeanFM | FM | Diffusion |
| :-- | :-- | :-- | :-- | :-- |
| 1 | pending | pending | pending | pending |
| 2 | 0.2779 | 0.3738 | pending | pending |
| 10 | 0.1194 | pending | pending | pending |
| 20 | ✅ | ✅ | ✅ | ✅ |
| 100 | ✅ | ✅ | ✅ | ✅ |

The $\nfe=1$ row was not named by the author ("1,2,10 or pick whatever you think we need"); it is printed
because one evaluation is the regime the thesis argues for on the other two scenes, and **no model has such a
cell on this task**. With the main table complete, the v3.61 appendix copy is redundant and is removed
(`app:aligning-budgets`, `tab:app:va-models-full`, 49 lines); the five cross-references in §6.2 now point at
`tab:va-models`. The §6.2.1.1 sentence on the incomplete ladder is rewritten ("Below twenty the ladder is
incomplete: …"). **R16 is widened** in the ledger (§20) to the eleven cells: eight flow-model evaluations
and three diffusion trainings (chain lengths 1, 2, 10), ~9 h + ~3 h.

## 3 · Figs 6.6 and 6.7 — "the 100 % is better? … inverted???"

Not mechanically inverted — but the v3.61 axis (final distance as % of the start, log) put $100\,\%$ at the
**bad** end, which is what reads as inverted. Both frontiers now plot the **share of the starting distance
closed**, $100\times(1-\text{median}/0.4530)$, on a **linear** axis, $0$ to $100$: $0\,\%$ is the box not
moved (dashed line), $100\,\%$ a box delivered, and the axis is exactly the bracketed number of Table 6.5.
The frontier is computed on the distances before the conversion and is unchanged. The *better* key points
up-left and sits top-left, which is empty on both figures.

🔴 **Found while doing it: Fig 6.6 was missing two rows of its own table.** The CI-MeanFM and FM cells at
$\nfe=100$, added to `tab:va-models` at v3.60, were never added to `sources.ALIGNING_CELLS/REPORTED`, so the
caption's "every unprojected cell of the table" was false by two points. Both reproduce the table from the
corpus (0.1791 m / 902.2 ms; 0.4247 m / 1425.0 ms) and are drawn now; both are dominated, so the frontier
and the prose count change only from "nine configurations" to "eleven". The dataref says so.

Three collisions fixed in the rebuild and verified on the rendered PNGs: the budget-label rule flipped
(diffusion's label now below its marker, the others above, because the baseline is the lower of the pair on
this axis); the *box not moved* label of Fig 6.7 centred under the line (hollow points sit on the line at
both ends); the $\nfe=20$ per-step label of Fig 6.7 moved up-left of its ring, off the endpoint square.

## 4 · Fig 6.8 — "show more traj, and ensure the violation in the middle one"

The figure drew two of ten contexts (positions 3 and 6), and the middle panel's **only violating path is
position 2**, which that choice skipped. All nine moving contexts are drawn now (`ALIGNING_SHOWN = (0–7, 9)`),
at a thinner stroke; position 8 (context 7 of the prose, id 6 in the JSON) is left out of every panel because
its logged path has no extent — the arm never leaves the start. Panel subtitles now read 1 / 8 / 9 of 9
violation-free, which agree with the ten-context counts of `tab:va-projection` (2 / 9 / 10) once the unmoved
context is added back; the prose says so. The middle panel's red path is visible: it rounds the disk on the
right and runs off the bottom-left corner of the plotted extent.

---

# Second round, same changelog (author, 2026-09-22) — Fig 6.9 on a log axis, Table 6.8 before and after projection, *success* everywhere on the quadrotor, crosses on Fig 6.12

## 5 · Fig 6.9 — "log the y axis"

The success axis is now **logarithmic in the number of successful flights** (ticks 1/12, 2/12, 4/12, 6/12,
12/12), so 2/12 against 6/12 is as visible as 6/12 against 12/12. Zero cannot be drawn on a log axis, so the
three configurations that never succeed (FM at $\nfe=1$ and $3$, diffusion) sit on a **floor band under an
axis break**, the same break marks as Fig 6.3.

🔴 **A frontier defect went with it.** The old drawing ringed FM at $\nfe=1$ — zero successes — as
"non-dominated" for being the cheapest point, and CI-MeanFM at $\nfe=1$ (4/12) for being 0.1 ms cheaper than
MeanFM at $\nfe=1$ (6/12); the prose never counted either. Two rules now, both stated in the caption and the
paragraph before the figure: a configuration with no successful flight is not eligible, and two costs within
one per cent are one cost. Ties in success still do not dominate, so the four at 12/12 all stay. The rings
are now MeanFM 1, MeanFM 3, CI-MeanFM 3, MeanFM 5, CI-MeanFM 5 — exactly the five points the prose describes.

## 6 · Table 6.8 — "need pareto for pre projection" + "simply say success"

Read as: the table must show the **pre-projection arm** of every cell beside the projected one. It now has a
*Projection* column and prints every model–budget cell twice, *none* and *per-step*; the ten unprojected rows
are recounted from the corpus (`diffuser` variant, same twelve flights):

| model, K | success | collision-free | violating steps | ms/step |
| :-- | :-- | :-- | :-- | :-- |
| MeanFM 1 / 3 / 5 | 12/12 | 0/12 | 54.2 / 54.8 / 54.6 | 9.5 / 27.2 / 45.2 |
| CI-MeanFM 1 / 3 / 5 | 12/12 | 0/12 | 54.8 / 56.1 / 56.2 | 9.4 / 26.9 / 44.6 |
| FM 1 / 3 / 5 | 12/12 | 0/12 | 42.6 / 46.1 / 47.6 | 9.1 / 26.7 / 43.7 |
| Diffusion 20 | 12/12 | 0/12 | 49.8 | 180.6 |

The prose sentence that used to *be* the unprojected arm ("the unprojected arm is the sentence above and
nothing else") is rewritten to point at the pairs. Nothing projected changed.

**The metric is called *success* now, on every quadrotor scene.** Chapter 5 defines it once
(`sec:setup:metrics:uav`: "success: the vehicle crosses the finish line of its scene … every quadrotor table
and figure of the results chapter calls a flight that does so a *success*"), and the results chapter and
appendix use only that word: the *Passed* column of Tables 6.8, 6.9, 6.10, 6.11, 6.12, 6.14 and the two
appendix s-curve tables is *Success*; every "passed the goal / passes the goal / pass the goal" in the UAV
prose, captions, the conclusion and the three summary tables is "succeeds / success"; the S&C definition is
"success on a collision-free flight". The path figures' legends and subtitles say the same
(`builders/paths.py`: "success: crossed the finish line", "no success", "n/12 success · n/12 clean"). 47
occurrences; the only "pass" left in the UAV section is "the second passage to the goal", which is geometry.
The D3IL-avoiding render caption "Goal passed" in Chapter 5 is not a quadrotor metric and is untouched.

## 7 · Fig 6.12 — "if crashed or lost control, mark the end with an X"

Done, from data rather than by eye. The extract (`plotting/extract/uav_paths.py`) now records two per-flight
flags from each run's npz — `aborted` (`divergence_aborted`) and `safe` (`phys_safe`), plus the guard's
`reason` — and `data/uav_paths.json` was regenerated from the staged 2026-09-18 drop (`temp/18-09-2026/`):
byte-identical apart from the new fields and the date. `builders/paths.py` draws a **cross** at the end of a
flight that is aborted or unsafe, in any of the three path figures, and adds the legend entry only when such a
flight exists. On Fig 6.12 that is **every flight of the brake-to-rest panel** (all ten ended by the guard,
reason *inverted*, between control steps 395 and 421) and **one of three under MuJoCo MPC** (not aborted, but
`phys_safe = 0`: it reached the finish line's $x$ and made contact). Corridor and pillars have no such flight,
so their drawings are unchanged apart from the legend wording. The caption says what a cross is and names both
counts.

## 8 · Safety check on the v3 sources (author's mid-round note)

The author flagged a "potential unsafe v3 .tex change from outside". Checked before touching anything:

- `git log`: v3.61 was committed at 21:27 UTC on 2026-09-21 (`999152f1 Docs Update 21-Sep-II`), so **the v3.61
  edits are in HEAD** (markers re-grepped: Table 5.9 at 0.31 m, `tab:uav-scurve-projection`, the appendix
  full tables — all present).
- `git diff` of the chapters against HEAD at 09:35 UTC was **exactly the v3.62 edit list** of §1–§4 (97 lines in
  `06_results.tex`, the 49-line appendix removal) and nothing else; `05_setup.tex` was untouched since the
  commit. No foreign edit was found.
- Line endings: `git ls-files --eol` reports **LF in the index and LF on disk for every draft file**, and the
  HEAD blobs are LF, so my text-mode writes changed content only. The warning git prints ("LF will be replaced
  by CRLF the next time Git touches it") comes from `core.autocrlf = true` in this Linux checkout: a future
  checkout or reset would rewrite the working copy to CRLF, which is the one "outside" change that could
  touch every line. Not changed here — it is the author's git configuration; `git config core.autocrlf
  false` (or `input`) would stop it.
- A copy of `chapters/`, `parts/` and `thesis_v3.tex` as of 09:34 UTC is in the session scratchpad
  (`v3_backup_093449/`), before this round's edits.

## Checks

`python3.14 tools/check.py` (after the second round): 14 files, 6 611 lines, 271 labels, 53 citations — **all mechanical checks pass**;
drafting macros: 18 `\hole`, 3 `\provisional`, 19 `\guard`, 64 `\srcnote`, 45 `\dataref`, 1 `\todofigure`.
Figures: `make_figs.py` for `fig_avoiding_tradeoff`, `fig_aligning` (5 figures), `fig_aligning_paths`, and in the second round `fig_uav_corridor_tradeoff` and the three `fig_uav_*_paths`; every
rebuilt PNG inspected at scale 3; `export_to_draft.py v3` copied 9 files (four figures × svg+png, plus
`EXPORTED.md`). `fig_aligning_contexts.svg` was re-emitted by the rebuild with only a clip-path id changed;
both copies were restored from git. **Not compiled** — no TeX toolchain here. `tools/sync_v2.py`: v2.25 still
not absorbed.

**Bundle** (built 2026-09-22 09:43 UTC, on the author's word, closing the update): `export_to_draft.py v3` reported
0 copied, 59 current. `thesis_v3_20260922_094334_new.zip` (annotated), `thesis_v3_20260922_094335_new_clean.zip`
(source notes hidden), `thesis_v3_20260922_094335_new_nonotes.zip` (source, data and guard notes hidden), 8812 KB
each; Chapters 1–4 collapsed at v2.24, cross-references all resolve, 43/43 packaged figures renderable, 0
placeholders. `make_bundle.py --verify`: 9 inlined + 4 collapsed sources byte-faithful. These supersede the v3.61
`085914/085915` bundles.

## Files (second round adds)

- `v3/chapters/05_setup.tex` — `sec:setup:metrics:uav`: the metric is *success*.
- `v3/chapters/06_results.tex` — Table 6.8 rebuilt (before/after projection) + dataref; corridor prose; Fig 6.9
  prose + caption; Fig 6.10 and 6.12 captions; *Success* headers in Tables 6.9–6.12, 6.14; the wording sweep.
- `v3/chapters/09_appendix.tex` — *Success* headers and caption of the two s-curve tables.
- `DA_in_Paper/plotting/builders/frontier.py` — corridor frontier: log success axis, floor, eligibility, 1 % tie.
- `DA_in_Paper/plotting/builders/paths.py` — `_failed`, `_xmark`, legend item, *success* wording.
- `DA_in_Paper/plotting/extract/uav_paths.py` — `aborted`, `safe`, `reason` per flight; `data/uav_paths.json` regenerated.
- `DA_in_Paper/figures/da/` and `v3/figures/` — `fig_uav_corridor_tradeoff`, `fig_uav_scurve_paths`,
  `fig_uav_pillars_paths`, `fig_uav_corridor_paths`.

## Files (first round)

- `v3/chapters/06_results.tex` — Fig 6.3 prose + caption; `tab:va-models` (full ladder) + dataref; §6.2.1.1
  ladder paragraph; Fig 6.6 prose, caption, dataref; Fig 6.7 caption; Fig 6.8 prose, caption, dataref.
- `v3/chapters/09_appendix.tex` — `app:aligning-budgets` removed.
- `DA_in_Paper/plotting/builders/avoiding.py` — `Y_BREAK`, `Y_LOWER_SHARE`, `_broken`, `_break_marks`; panel
  rewritten on the broken axis; legend strip carries the key; legend advances retuned.
- `DA_in_Paper/plotting/builders/frontier.py` — `PCT_*`, `_pct_closed`; both alignment frontiers; label rule.
- `DA_in_Paper/plotting/builders/exec_paths.py` — `ALIGNING_SHOWN`, stroke width.
- `DA_in_Paper/plotting/sources.py` — `('af', 100)`, `('fm', 100)` in `ALIGNING_CELLS` and `ALIGNING_REPORTED`.
- `DA_in_Paper/figures/da/` and `v3/figures/` — `fig_avoiding_tradeoff`, `fig_aligning_tradeoff`,
  `fig_aligning_projected_tradeoff`, `fig_aligning_paths` (svg + png); `MANIFEST.md`, `EXPORTED.md`.
- `data_status/PENDING_20260922_all_lacking_runs.md` — R16 rows (§0, §1), §14 census, new §20;
  `data_status/INDEX.md` hook; `v3/CHANGELOG.md`; `v3/BRIEFING_20260922_v3.60.md` addendum.

Claude (Fable 5.1, Claude Code) · 2026-09-22 · mechanical checks only. Not compiled. Bundled 09:43 UTC.
