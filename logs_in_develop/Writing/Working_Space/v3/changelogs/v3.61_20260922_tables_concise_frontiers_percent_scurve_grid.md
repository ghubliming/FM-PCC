# v3.61 — 2026-09-22 · The concise tables come back, the alignment frontiers go to a log-percent axis, and UAV-s-curve becomes a matched grid

Read together with [`v3.60`](v3.60_20260921_ch5_margins_success_and_params.md): this pass revises three
of its outcomes (the body-inflation number, the K=5/K=10 rows, the ragged s-curve tables) on the author's
review, and it is written so the two changelogs are understood as one update. The bundle was built
afterwards, on the author's word (see *Checks*).

---

## 0 · Cross-draft: one note for v3, and it was right

`cross_draft/INBOX.md` had one open (⏳) item for v3, from v2.25: *"v3.60's own note is wrong on one
number — the planner is offset by 0.31, not 0.31 + 0.02."* Verified in the repository, not taken from
the note:

| what | where | reads |
| :-- | :-- | :-- |
| file-level default | `config/uav_projection.yaml:175` | `inflation: {r_drone: 0.31, margin_base: 0.02}` |
| every evaluated scene entry | `:360, :376, :438, :513` (and the later scenes) | `planning_inflation: {r_drone: 0.31, margin_base: 0.0}` |
| what the planner reads | `mix_uav_test/eval_mix_uav.py:924, :1017, :1279` | `config.get('planning_inflation') or config.get('inflation')` — the override first |
| what the scorer reads | `:793–806` | `r_drone` of `inflation` alone |

So planner and scorer are **both** offset by $0.31$ m; the $0.02$ m pad is a file default the scenes
switch off. **Changed:** `tab:eval` Body-inflation cell `$0.31 + 0.02$` → `$0.31$`; the paragraph under
the table says the pad exists and is switched off; the `\srcnote` cites the override lines and records the
correction. INBOX row → ✅ v3.61. A correction banner was prepended to v3's own note
`to_v2/FROM_v3_20260921_v3.60_rotor_reach_not_radius.md` so v2 does not re-apply the wrong number.
Items 2–5 of the v2 note (H transitions, transport-time pointer to `sec:setup:protocol`, v2's new
`figures/` folder, action bound) need nothing from v3 now; the figure folder is a merge-time item.
`tools/sync_v2.py status` reports v2 at v2.25 with four inherited files moved — **not absorbed in this
pass**, which was not asked.

## 1 · Read with v3.60

The v3.60 changelog is the record for Chapter 5 items 1–6 and Chapter 6 items 1–20. This file amends:
its §1 (the inflation number, above), its §12 (the K=5/K=10 promotion, §3 below) and its §27 (the
s-curve tables, §6 below). Everything else in v3.60 stands.

## 2 · Fig 5.1 caption — the appendix pointer

*"dimensions are in Table 5.1 and Figure C.1 in the appendix"* said what without saying what of what.
Now: *"The dimensions of both platforms are in `tab:platforms`; the quadrotor's rotor layout and the
footprint the projection uses for it are drawn in `fig:platform-x2-dimensions`, in `app:repro`."*

## 3 · 🔴 Tables 6.1, 6.2 and 6.5 are concise again; the full tables are in the appendix

**Author:** *"I kinda regret on the Table 6.2 update. the inconsistent K make reader confused … the
pre-3.60 table is concise … such table should try to maximum aligned K across models, and in appendix
provide full table."* Applied as a general rule for the chapter:

| table | main text now | appendix |
| :-- | :-- | :-- |
| `tab:avoiding-raw-models` (6.1) | the pre-v3.60 rows: $\nfe\in\{1,2\}$ for all, FM 20, diffusion 1/2/20 | `tab:app:avoiding-raw-full` — adds MeanFM 5, 10 and diffusion 10 |
| `tab:avoiding-dpcc-protocol` (6.2) | the pre-v3.60 rows | `tab:app:avoiding-dpcc-full` — adds MeanFM 5, 10 and diffusion 10 (its bold $t$ row) |
| `tab:va-models` (6.5) | $\nfe=20$ and $100$ only — the two budgets all four models carry | `tab:app:va-models-full` — adds MeanFM 2, CI-MeanFM 2, MeanFM 10 and the three *not evaluated* rows |

The main-text rows were restored **from `git show HEAD`** (commit `a3545da7`, which predates v3.60's table
edit), not retyped. Each main caption carries one sentence pointing to its full table. Prose repointed:
§6.1 after Table 6.1 (the *0.167 at ten evaluations* and *both a ten-evaluation row* sentences now read
off the concise table and send the intermediate budgets to the appendix; the highest S\&C in the concise
table is FM's $0.133$ at twenty), §6.1.1.3's diffusion-at-ten paragraph (cites the appendix table instead
of *"marked in bold accordingly"*), §6.2's ladder paragraph and its *not evaluated* sentence, and the
Fig 6.6 `\dataref`. New appendix sections `app:avoiding-budgets` and `app:aligning-budgets` say why the
rows are there. UAV-corridor was already matched ($\{1,3,5\}$ + diffusion 20); UAV-s-curve is §6.

## 4 · Fig 6.6 — the direction key is back

`builders/frontier.py` had dropped `_dirarrow` when K=100 was added (*"the old direction key covers both
right-hand data points"*). On the new axis pair (§5) the bottom-left corner is empty, so the boxed
*better* arrow sits there, pointing into the corner — the same placement in Fig 6.7.

## 5 · Fig 6.7 — the frontier explained, and both alignment frontiers on a log-percent axis

**Author:** *"what is that frontier. and don't use meter, use log-percentage! so is other pareto
figures in V_A."* Two changes to both `fig_aligning_tradeoff` and `fig_aligning_projected_tradeoff`:

- **Axis.** The outcome axis is now the median final distance **as a percentage of the $0.4530$ m
  starting distance, on a log scale** (ticks 10, 20, 50, 100; the *box not moved* line at 100 %).
  Down-left remains better, as before. The bracketed share in `tab:va-models` is the complement of
  this axis (16 % here is the table's 84 %); both captions say so. *Read as: "log axis of the
  percentage". If "percentage closed on a linear axis" was meant, it is a four-line change in
  `_pct_of_start` and `PCT_*`.*
- **The frontier of Fig 6.7 was never wrong, it was unexplained.** It runs through three MeanFM cells
  (endpoint 2, endpoint 10, per-step 20) because those are the only configurations with ≥ 9/10
  violation-free contexts; the visibly best-and-cheapest point, MeanFM per-step at $\nfe=2$, is hollow
  because it keeps only 8/10 and is excluded by rule. The legend now carries *"< 9/10 clean"* for the
  hollow marker and the caption names that point as the reason it is hollow.

Rebuilt with `make_figs.py fig_aligning`, PNG previews refreshed at scale 3, `export_to_draft.py v3` run
(4 files copied). `fig_aligning_paths.svg` and `fig_aligning_contexts.svg` were regenerated by the same
match and differ from `HEAD` by one line each (a timestamp), as they already did before this pass.

## 6 · 🔴 UAV-s-curve: a matched grid, mostly pending (R31)

**Author:** *"the S_curve feels still ridiculous, we are lacking much of the proper runs right? If agree
build the table first then I will fill in slowly, mark the missing lines."* Agreed. Census of both UAV
corpora (`s_curve_hg`, seed 6, `per_rollout_detail.csv`, recounted 2026-09-22) and the result:

- **`tab:uav-scurve`** (unprojected) is now a $\{1,2,3,5\}$ × {MeanFM, CI-MeanFM, FM} grid + diffusion 20,
  columns Passed / Distance / Safe / **Aborted** / ms per step. **Filled:** MeanFM 2, CI-MeanFM 1, 2, 5,
  FM 2, diffusion 20. **Pending (7):** MeanFM 1, 3, 5; CI-MeanFM 3; FM 1, 3, 5. $\nfe=2$ is kept beside
  the corridor ladder because it is where the filled cells are; the `\guard` says it goes if the scene
  is to mirror the corridor exactly.
- **`tab:uav-scurve-projection`** now has the corridor's shape at $\nfe\in\{3,5\}$: per-step $t$ +
  endpoint single/$r$/$c$/$t$, post-correction cells only. **Filled:** CI-MeanFM 5 (the `u18sc` block),
  diffusion 20 per-step $t$ (`u7hg`; per-step is not what the correction touched). **Pending (5 cells ×
  5 variants):** MeanFM 3, 5; CI-MeanFM 3; FM 3, 5. Pre-correction endpoint cells (`u7hg`,
  `u6unet_ae02`) are used nowhere.
- **`tab:uav-scurve-aborts` and the per-variant listing moved to the appendix** as
  `tab:app:uav-scurve-aborts` and `tab:app:uav-scurve-variants`, under `app:uav-scurve-budgets`, with a
  small `tab:app:uav-scurve-unprojected-extra` for the two unprojected cells off the ladder (MeanFM 10,
  FM 20). The section prose was rewritten around the grid; the abort mechanism, the 1361–3610 ms
  per-step cost and the controller pointer are kept. The corridor-frontier paragraph that cited the two
  moved tables now cites the grid.
- **Ledger:** §18 of `PENDING_20260922_all_lacking_runs.md` — the census, **R31** with the exact cell
  list, a rough cost (~1 h unprojected, ~30 h projected, ±50 %), and one open choice: the per-step arm
  in `u18sc` is plain `dpcc-t`, the corridor's is `-tightened`; the grid follows `u18sc`. **R29 is
  superseded by R31** (§0, §17).

Numbers verified against the draft's previous cells: MeanFM 2 (0/10, 2.515), CI-MeanFM 2 (1/10, 2.082),
FM 2 (7/10, 1.110), CI-MeanFM 1 (6/10, 1.306), diffusion 20 (0/10, 2.432) all reproduce; the CI-MeanFM
5 rollouts are byte-identical across `u7hg`, `u6unet_ae02` and `u18sc` (2/10, 2.247) with the time per
step differing by a millisecond, and the grid takes `u18sc`.

## Checks

`python3.14 tools/check.py`: 14 files, 6 606 lines, 273 labels, 53 citations — **all mechanical checks
pass**; drafting macros: 18 `\hole`, 3 `\provisional`, 19 `\guard`, 64 `\srcnote`, 46 `\dataref`,
1 `\todofigure` (the pillars figure). `tools/sync_v2.py status`: v2 at v2.25, four inherited files moved,
**not absorbed**. **Not compiled** — no TeX toolchain here.

**Bundle** (built 2026-09-22 09:00 UTC, on the author's word, after the six items above): `export_to_draft.py v3`
reported 0 copied, 59 current, so the log-percent frontiers in `figures/` are the ones the bundle packs.
`thesis_v3_20260922_085914_new.zip` (annotated), `thesis_v3_20260922_085915_new_clean.zip` (source notes
hidden), `thesis_v3_20260922_085915_new_nonotes.zip` (source, data and guard notes hidden), 8664 KB each;
10 sources inlined, 4 601 → 4 758 lines, Chapters 1–4 collapsed at v2.24, cross-references all resolve,
43/43 packaged figures renderable, 0 placeholders. `make_bundle.py --verify`: 9 inlined + 4 collapsed
sources byte-faithful. These supersede the v3.60 `205646/205651` bundles.

## Files

`v3/chapters/05_setup.tex`, `06_results.tex`, `09_appendix.tex`; `Data_Analysis/DA_in_Paper/plotting/builders/frontier.py`,
`figures/da/fig_aligning_{tradeoff,projected_tradeoff}.{svg,png}`, `figures/MANIFEST.md`; `v3/figures/` (export);
`cross_draft/INBOX.md`, `cross_draft/to_v2/FROM_v3_20260921_v3.60_rotor_reach_not_radius.md`;
`data_status/PENDING_20260922_all_lacking_runs.md` (§0, §17, new §18); `v3/BRIEFING_20260922_v3.60.md` (addendum).

**Written by:** Claude (Fable 5.1, Claude Code) · 2026-09-22 · FM-PCC dev container. Every number above was
recounted from the named corpus or read at the named line in this session. **Not compiled.**
