# v3.26 — 2026-09-17 · MuJoCo wording, Ch 5 holes filled, no statistics, two caveats, UAV restructure, recap tables

Pre-check: read v3.20–v3.25 (other agents: mechanism names, FM/MeanFM/CI-MeanFM labels, monochrome emphasis,
clean bundle). Built on top of them; nothing reverted.

## Key points

1. **MuJoCo is the simulator** (Fig 5.5 and all of Ch 5). Labels "(D3IL)" / "D3IL simulator view" → "MuJoCo
   simulator view", captions "rendered by MuJoCo"; §5.1, §5.2.1, §5.2.2 say D3IL supplies model/task files,
   demonstrations and camera placements. Translation table §8 **rule 7** added. v2 note sent
   (`cross_draft/to_v2/FROM_v3_20260917_v3.26_mujoco_is_the_simulator.md`).
2. **Hole §5.2.3 filled.** The boxes are *not* identical: shared x∈[-4,4] m and altitude [0.30,1.80] m; lateral
   y ±2.5 m (pillars), ±1.8 m (s-curve), walls (corridor). Tightening 0.025 m on top of the always-on 0.31 m
   inflation. `tab:uav-scenes` "All" row extended. Source: `config/uav_projection.yaml`, `eval_mix_uav.py:1019`.
3. **§5.3 citations.** D3IL was already cited in §5.1/§5.2; one `\parencite{jia2024towards}` added at each
   D3IL dataset subsection (data source cited where the data is used).
4. **Hole in `tab:uav-demos` filled.** Episodes generated/accepted 500/500, 500/475 (18 too low, 7 in contact),
   500/500; nominal gains (`pid_default`) for all. Alignment: 900 demonstrations, 168,274 windows. 90/10 window
   split in all environments. Sources: collect jobs 21475/21482/21476, training-log buffer shapes, trainer configs.
5. **§5.4.3 rewritten**: the original MuJoCo MPC quadrotor task described in full (8 gates, 12 goal positions,
   0.5 m switch, Predictive Sampling 32×5 splines, 0.5 s/0.01 s, cost weights 25/1.25/1.25/1e-3, rotor-thrust
   controls, no learning, no success metric), then "This benchmark". Corrected: twelve goal positions, not thirteen.
6. **§5.6.2 without statistics**: protocol told like avoiding (fixed contexts, three rules, both constraint
   sets, context-by-context comparison). **All p-values, test names and "significant" removed from Ch 5, Ch 6 and
   the Ch 8 draft**; counts over contexts/episodes/flights instead. Translation table **rule 8**; v4 note sent.
7. **Hole §5.6.3 filled**: brake-to-rest cascaded controller on all pillars_hg rows and 880/889 s_curve_hg rows;
   the 9 others are the MuJoCo MPC comparison (checkpoint CSV `controller` column).
8. **New `sec:res:avoiding:caveat`** "Caveat: What the Metrics of DPCC Do Not Measure", before Plans before
   Projection (Fig 6.3): stack tolerance (DPCC Table 2), then smoothness, quoting DPCC Sec. 5.4 and Fig. 3
   ("a smoother behavior and shorter time to reach the goal"); links to the UAV controller caveat.
9. **§6.2.2 rewritten, new `tab:va-projection`**: endpoint projection (translation table name for HF-SLSQP) is
   the method on D3IL-aligning. MeanFM K20 tightened: 10/10, 10/10, 9/10 contexts violation-free (r/c/t) vs
   9/9/8 per-step, same time (275 vs 266 ms). With K10 r (10/10, 193 vs 518 ms) these are the only
   configurations keeping every context violation-free while moving the box. §6.2.3 now selects endpoint projection.
10. **§6.3 restructured**: Generative Models per scene: UAV-corridor (four models incl. K1, new rows);
    UAV-pillars as the main test (four-model table incl. Diffusion; unprojected plans; enforced constraints;
    new `tab:uav-pillars-best` (best projected: MeanFM 0.90, CI-MeanFM 0.70, FM 0.50, Diffusion 0.00);
    step budget over the 7 common configurations 0.429→0.571 MeanFM, FM 0.343→0.257, CI 0.300→0.271);
    UAV-s-curve (unprojected listing). Projection Methods short closure. **New UAV-only
    `\subsection{Caveat: The Tracking Controller}`** (`sec:res:uav:controller`, `tab:uav-controller`).
    Label `sec:res:uav:consistency` removed (unreferenced). The unverified "MPC adds 119 ms" claim was dropped.
11. **§6.4 = recap only**: three plain tables — best model per environment, selected projection per environment,
    selected combination. No p-values. Ch 8 draft aligned (aligning constraint result now "higher"; RQ4 → controller caveat).

## Evidence and tooling
- `DA_in_Paper/analysis/va_results.py`: new *projection methods* block (tab:va-projection).
  `uav_results.py`: best projected configuration per model and 7-configuration budget means. Both re-run; every
  new table value reproduces (one rounding fix, 0.409 → 0.408). `analysis/INDEX.md` rows updated.
- `data_status/PENDING_…`: D4 ✅ v3.25, D5/D6/D7 ✅ v3.26. INBOX: one → v2 row, one → v4 row.
- Memory `thesis-prose-style`: no statistics in the thesis; MuJoCo = simulator; caveats as story sections.
- `tools/check.py` passes (234 labels, 52 citations, 22 figures; the two Ch 4 width warnings remain, owned by
  v2). Bundles `thesis_v3_20260917_161355_new(.tex/.zip)` and `…_new_clean`, 22/22 figures, references resolve.
  **Not compiled** (no TeX toolchain).

## Left open
- Smoothness metric (D1), alignment orientation (D2), DPCC-protocol rows (R8), tightened diffusion on
  D3IL-aligning: unchanged.
