# v3.29 — 2026-09-17 · v2.23 absorbed, three RQs, captions cleaned, smoothness section, trajectory figures flagged

## Key points

0. **v2 sync first.** `sync_v2.py merge` absorbed **v2.23** (frontmatter, backmatter, Ch 1–3, Ch 4,
   bibliography; baseline stamped in `inherited/SYNC_STATE.json`). v2.23 is v2's own application of the
   MuJoCo/Panda note sent in v3.27, so the two drafts now say the same thing about the simulator and the
   robot. The open v2 → v3 item is closed with it:
   - **three research questions, not four** — the budget RQ was deleted by author decision and folded into
     RQ1, and the remaining two renumbered. `08_conclusion.tex` now answers RQ1 (generative model, with the
     budget evidence inside it), RQ2 (constraints) and RQ3 (transfer).
   - `rem:pidname` and the Chapter 4 "Deviation" blocks are gone from the inherited copy; the new bib key
     `mellinger2011minimum` and the new labels arrived with the merge. `check.py`: 53 citations, 236 labels,
     no dangling references.
1. **Table 6.1 caption trimmed.** The sentence about excluded transformer-backbone runs is removed; the
   caption states the backbone as a fact ("Temporal U-Net, 4.0 M parameters") and nothing about what was not
   used. The backbone detail lives where it belongs, in the analysis script and the data-status record.
2. **The remaining gap is recorded** in a dated, scope-limited file:
   `data_status/PENDING_20260917_dpcc_protocol_rows_table61.md` — what Table 6.1 still lacks (FM at K=1/K=2;
   CI-MeanFM at K=1/K=2, which needs four trainings first), why the table matters, the `Folder_Name`
   backbone trap that hid the MeanFM rows, and why the five-seed α-Flow cells there do not count. It states
   that it does **not** supersede the general ledger; `data_status/INDEX.md` lists it and its naming rule now
   allows a scope-limited PENDING. `REQUEST_20260917_avoiding_dpcc_protocol.md` corrected: the MeanFlow U-Net
   runs are marked found, and their submission step struck out.
3. **Table 6.4 reordered** so every geometry block runs MeanFM, CI-MeanFM ($\alpha_{\mathrm{end}}=0.2$, then
   $0.05$), FM, Diffusion, like the other tables. The marked rows are unchanged.
4. **§6.1.1.4 is now "Smoothness of the Plans"** (`sec:res:avoiding:smoothness`). It states what the section
   does — compare the plans themselves, where the models differ — instead of listing what the metrics fail to
   do. DPCC's Table 2 is read as the measured tolerance of the stack and its Fig. 3 as the value it places on
   smoothness; the same facts, without the framing. The one cross-reference was updated.
5. **Trajectory figures flagged as holes with a download request.** `fig:uav-scurve-paths` is placed after
   Table 6.12 as a `\todofigure` with its caption written, plus a `\hole` naming the two cluster run folders.
   Holes added for UAV-pillars (per-model flown paths at the configurations of Table 6.10), UAV-corridor
   (paths through the slide) and D3IL-aligning (box paths over the ten contexts).
   `DA_in_Paper/plotting/REQUEST_20260917_trajectory_figures.md` lists, per figure, the exact run folders on
   the cluster, what each artefact must contain, and the local steps afterwards; ledger row **D10** added.
   The committed batches hold per-rollout scalars only, which is why none of these can be built here.

## Verification
- `check.py` passes: 14 files, 236 labels, 53 citations, 23 figures; drafting macros 22 holes / 6 provisional
  / 10 guards / 6 todofigures, all intentional.
- Bundles rebuilt (annotated and `--clean-notes`), 23/23 figures rendered. **Not compiled** — no TeX here.
