# v3.56 — 2026-09-21 · Alignment Pareto alternative and UAV-pillars result scaffold

## What changed

- `chapters/06_results.tex` §6.1.1 now distinguishes learning the route from satisfying the test-time constraints. In the unprojected DPCC-protocol table, FM, MeanFM and CI-MeanFM each reach the goal in 30/30 episodes at both reported budgets; diffusion reaches it in 28/30 at one denoising step and 29/30 at twenty. Thus the requested blanket “all four at 100%” would contradict `tab:avoiding-raw-models` and was not written.
- The Figure 6.7 slot now contains `fig_aligning_projected_tradeoff`: projected D3IL-aligning final distance against time per control step, using random selection and the ten shared contexts on `combined_5-tightened`. It plots every complete flow-model cell at the chapter's budgets. Filled points have at least 9/10 violation-free contexts; the non-dominated filled points carry rings and the staircase. The former context-by-context `fig_aligning_outcomes` remains in the DA figure store and in the draft's exported figure folder as an alternative; its thesis references were replaced by the table and a data reference, not deleted.
- `plotting/builders/frontier.py` builds that figure from `analysis_results_checkpoint/15-09/batch_va2_20260915_100754/per_rollout_detail.csv` using the existing exact cell prefixes and suffixes in `plotting/sources.py`. The figure was exported to v3. The new figure and its provenance are registered in `DA_in_Paper/figures/MANIFEST.md` and `analysis/INDEX.md`.
- The UAV-corridor caption and Chapter 5 terminology now name the **sloping test-time halfspace**, rather than “the slide”; the corridor's marked comparison row is **the diffusion model of DPCC**, not a UAV “baseline of record.” The canonical translation table records both distinctions. Artefact tokens such as `corridor_v2_slide` remain unchanged.
- `chapters/06_results.tex` restores `sec:res:uav:pillars` between corridor and s-curve. It includes the established enlarged-geometry fact and the completed one-cell verification, plus separate generative-model and projection table templates and a figure placeholder. No `pillars_hg` result is imported. The complete `pillars_xl` campaign is R23 in `data_status/PENDING_20260920_all_lacking_runs.md`; its intended path figure is registered as planned in `plotting/sources.py`. Chapter 5 points to this pending section instead of claiming pillars is permanently absent.

## Hotfix continued — 2026-09-21

- Figure 5.3 now uses frame 200 of the tracked MuJoCo montage, showing the Panda end effector across the green goal line, and its caption describes only that render and the 96 top-down routes. The unsupported link to Figure 5.9 was removed. `plotting/sources.py`, the prepared frame, the figure store and v3 export were updated together.
- §6.1.1 now presents the unprojected model table and its discussion before the raw-plan montage and its discussion. A float barrier holds the table before the montage. The montage caption now gives the correct five-seed, two-episode, three-geometry aggregation, rather than “twenty episodes.”
- The yellow-alert paragraph beginning “None of this is evidence…” was deleted. §6.1.2.8 now gives one short twenty-episode comparison and points to `app:avoiding-twenty-episode`; the detailed per-rule, sample-resolution, coverage and matched-budget tables moved intact to Appendix A's Extended Results chapter. The surrounding appendix prose was shortened, and a v4 handover note records the move.
- Figure 6.7 now distinguishes MeanFM in dark blue from CI-MeanFM in dark plum; FM remains orange. The projected Pareto SVG and PNG were rebuilt and inspected; the former context plot remains in the store.

**Evidence:** `d3il/figures/github_readme.gif` frame 200; `tab:avoiding-raw-models`; the existing `19-09-UAV-Pillars-Exclude/batch_avoiding_combined_20260919_132703` analysis for the moved tables; `15-09/batch_va2_20260915_100754` for Figure 6.7. No new result number was inferred.

**Hotfix checks:** `python3 tools/check.py` passes (14 files, 5925 lines, 264 labels, 39 figures). Figure export to v3 passes. The final three v3-only bundles are `104909_new`, `104912_new_clean` and `104916_new_nonotes`; the last verified byte-faithful, with the same two Chapter 4 references unresolved in collapsed mode. No TeX compiler exists here; not compiled.

## Evidence and limits

- Avoiding counts: `tab:avoiding-raw-models`, computed by `DA_in_Paper/analysis/avoiding_unprojected_and_budget20.py` from `19-09-UAV-Pillars-Exclude/batch_avoiding_combined_20260919_132703` (five seeds, two episodes, three geometries).
- Alignment points: the same ten-context `15-09/batch_va2_20260915_100754` cells as `tab:va-projection-models`; diffusion has no tightened projected cell and is absent from the chart.
- Pillars verification: `19-09-UAV-Pillars-Exclude/batch_uav_20260919_111701`, `pillars_xl`, `u7xlchk`, unprojected. The full ten-cell, eleven-variant campaign and its executed-path artefacts remain missing; both tables and the path figure are visibly pending.

## Checks

- `python3 tools/check.py`: 14 files, 5959 lines, 263 labels, 39 referenced figures; all mechanical checks pass. There are 18 holes and one planned figure, including the new pillars scaffold.
- `python3 tools/sync_v2.py status`: inherited chapters still at v2.24; no upstream move or sync in this pass.
- Figure regenerated in DA, raster preview inspected, copied into v3. The previous alignment outcome figure was retained. No TeX compiler exists in this container; **not compiled**.
- The pre-hotfix v3-only bundles were `103056_new`, `103056_new_clean` and `103057_new_nonotes`; all three passed `make_bundle.py --verify` (9 inlined + 4 collapsed sources, byte-faithful). The v3-only collapse leaves two inherited Chapter 4 equation references unresolved (`eq:method:degen:ngen`, `eq:method:dep:vdes`); the full-document mode retains those equations. The one pillars `\todofigure` remains visible by design. The hotfix bundles supersede these outputs.

**Written by:** Codex GPT-5 · 2026-09-21. **Not compiled.**
