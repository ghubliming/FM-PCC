# v3.13 — environment renders and figure explanations

**Date:** 2026-09-15

## Changed

- Replaced the alignment and quadrotor `\todofigure` boxes in Chapter 5. Obstacle avoidance and
  alignment now include authentic D3IL simulator frames from `d3il/figures/github_readme.gif`;
  corridor v2, pillars and s-curve include frames from their evaluation rollout GIFs. Alignment and
  each UAV scene pair that evidence with a clean oblique geometry view. Quadrotor geometry is parsed
  from the scene MJCF; the alignment view uses the first recorded held-out context rather than an
  invented target pose.
- Added `plotting/prep/extract_env_frames.py`, the environment-frame registry in `sources.py`, the
  geometry builders in `builders/scenes.py`, and the small orthographic renderer `svg/scene3d.py`.
  Exact frames, crops and sources are recorded in
  `plotting/NOTEBOOK_20260915_figure_data_sources.md` and `data/prepared/ENV_FRAMES.json`.
- Added prose that explains how to read Figures 6.1 and 6.2. Corrected Figure 6.2's in-canvas label:
  the dashed diffusion series is five seeds by two episodes, a smaller mechanism sample, not DPCC's
  published five-by-ten protocol.
- Expanded Figure 6.3 with the available MeanFlow $K=2$ panel. The requested naïve flow-matching
  $K=1$ and $K=2$ panels remain unavailable: the checkout contains aggregate rows but no diagnostic
  images or saved plan states. Both exact cluster render requirements are registered as `PLANNED`;
  no visual was inferred from aggregate data.

## Verification

- Rebuilt the complete official figure store and exported all 18 figures referenced by v3.
- `python3.14 prep/extract_env_frames.py --check`: 5 of 5 current.
- `python3.14 prep/crop_vendored.py --check`: 3 of 3 current.
- `python3 tools/check.py`: all mechanical checks pass; 18 figures, no `\todofigure`; inherited
  Chapter 4's known wide `\multicolumn` warning remains. No TeX toolchain is installed, so the draft
  was not compiled locally.
- `python3 tools/sync_v2.py status`: v3 remains synchronized with v2.16; no inherited file moved.

