# v3.78 — 2026-09-23 · v2.27 synced (sanity check); step-budget wording aligned; bundle

**Asked (author):** "Sync the v2 (it is been updated/fix errors) and check cross note for v3 if need any update. if almost
nothing to update in v3, then this is just a sanity check and v2 version syncup verify. update bundle. then I will review."

## 1 · Sync v2.26 → v2.27

`sync_v2.py merge <file>` for the six moved files — `parts/00_preamble.tex`, `parts/01_frontmatter.tex`, Ch 1, Ch 2, Ch 3,
Ch 4 — all fast-forward, no conflict. v2 did not touch its Ch 5–8 bone this time, so no owned baseline needed advancing.
`status`: in sync at v2.27. `check.py`: passes (14 files, 284 labels, 38 figures, 53/53 citations); no v3 reference to the
removed `tab:hardflow-setup`; the reproduced HardFlow figure is unchanged (v3 copy identical).

What came down (v2.27, the ChatGPT/Codex audit corrections): the abstract and §1.4's six contribution sentences now carry
results; RQ1/RQ2 reworded; the average-velocity convention `(x_r, r, h)`, the consistency-interpolated loss, the sampling
counts (K sampling steps, endpoint NFE = K + n_guide, h = 0 endpoint query, distinct activation counts), one guiding step as
valid guidance, controller and selection conventions corrected in Ch 4; new labels `eq:method:engine:afloss`,
`eq:method:hf:nfe`, `tab:method-provenance`. The step-budget macro still renders K (comment changed only).

## 2 · The v2.27 → v3 note, checked

| v2.27 item | v3 |
| :-- | :-- |
| one guiding step is valid guidance; check Ch 5's reporting rule | consistent: Ch 5 reports only n_guide = 0 separately; Ch 6 already reads the corridor K = 3 endpoint cells. **Audit item 4 of v2.26 is settled by this rule** — nothing changes in v3 |
| K counts sampling steps; endpoint NFE = K + n_guide | **aligned:** `tab:eval` step-budget row "network evaluations per plan" → "sampling steps per plan"; Ch 5 step-budget paragraph: K is also the network evaluations only without endpoint projection (`%` comment); four phrases of the v3.76 Table 6.6 reading that called endpoint budgets "evaluations" → "budgets" |
| h = 0 endpoint query; distinct activation conventions | already in Ch 5 (v3.73) |
| no universal "endpoint σ = 1" | v3 states only the plain samplers' scales (v3.73) |
| `(x_r, r, h)`, temporal-consistency blocks, controller conventions | v3's chapters carry no average-velocity equation and only name the selection rules and the controller; nothing to align |
| contribution sentences from v3.75 | each checked against Ch 6 as it stands (v3.77): consistent, including the pillars sentence |

**Found and sent to v2** (`cross_draft/to_v2/FROM_v3_20260923_v3.78_v2.27_synced_abstract_endpoint_claim.md`): the abstract's
"Endpoint projection reduces planning time in the evaluated manipulation comparisons" is broader than Ch 6 — true on
D3IL-avoiding and on aligning at K = 10, not as a rule at the aligning operating point K = 20 (0.87–1.24 of per-step time
by model). §1.4's "evaluated avoiding comparisons" is exact. Not edited in v3 (v2 owns the abstract).

Inbox: the v2.27 → v3 row ✅ v3.78; new → v2 row.

## 3 · Bundle (tenth round)

`bundle/output/thesis_v3_20260923_201232_new.zip`, `…201233_new_clean.zip`, `…201233_new_nonotes.zip` (10 481–10 482 KB;
47/47 figures render; 9 inlined + 4 collapsed sources at v2.27; `--verify` byte-faithful on all three; no unresolved-reference
warning). Supersedes `164500`.

## Checks

`check.py` passes. Not compiled. Nothing committed.

Claude (Opus 5.5, Claude Code) · 2026-09-23 · not compiled · nothing committed.
