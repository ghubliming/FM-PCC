# v3.81 — 2026-09-24 · QA: Table 6.9 layout, Table 6.8's diffusion row; corridor step-cap evidence (no corridor edit)

**Asked (author):** "QA and fix. 1. Diffusion 20 — (not defined for the diffusion sampler) in Table 6.8, what is this?
lacking runs? 2. Table 6.9 has some overlapping issues, especially in the 'Model' column 3. 6.3.2.2 Projection cannot be
that bad???? → 'out of their 396 control steps before it;' that is bcs the step is too low for the new corridor env???
the Fig6.6 looks so good, not like all crashed, what is success criteria??? answer the QA1,3 fix the point 2." — then,
mid-pass: "check how many is end of control step and NOT in the goal line … how many should pass if enough step if keep
the trend, this is the key evidence. you can fix point1 and 2 for v3 thesis if need, but dont do any thing for corridor,
discuss with me now in chatbox". No bundle asked.

## 1 · Table 6.9 `tab:uav-pillars-raw` — the overlap (fixed)

Cause: eight `mean ± spread` columns side by side at `\tabcolsep` 2pt need ≈ 442pt against the 418pt line before the
X-type model column gets any width, so "CI-MeanFM" ran into its neighbours (a width estimate over every `tabularx` in
Ch 5, Ch 6 and the appendix, Palatino 9pt metrics, flags this table only). Fix: each configuration on **two rows**,
D3IL-avoiding above UAV-pillars, with an *Environment* column; seven columns, ≈ 100pt left for the model column. Every
number carried over unchanged (64 values checked); the selected configuration bold on both rows; caption "+ one row
each" (87 words); dataref records the change.

## 2 · Table 6.8 `tab:va-projection-models` — the diffusion endpoint row (clarified)

Not a missing run. Endpoint projection adds its control to the sampler's **velocity field** (Ch 4
`sec:method:hardflow`); the diffusion baseline is a DDPM reverse chain without one, and the evaluation refuses it by
design (`mix_visual_aligning/sampling/hardflow_projection.py:899` `resolve_engine_hf`; call site
`eval_mix_visual_aligning.py:3349–3353`). Row text "--- (not defined: no velocity field)"; caption says why (91 words);
dataref cites the code. The corridor guard's wording ("has never been run on the diffusion model") says the opposite
kind of thing — left for the author (corridor not touched in this pass).

## 3 · The corridor step cap — evidence only, nothing in the corridor section changed

Success on UAV-corridor: within **396 control steps** (`eval_mix_uav.py:195`, `SCENE_MAX_EPISODE_LENGTH['corridor']` =
1.2 × the slowest *level-corridor* demonstration, 10 s × 33 Hz) the vehicle comes within 0.30 m of the route's goal
point (2.8, y_route, z_trial) or crosses the vertical plane x = 2.8 through it, in controlled flight. Read from the R33
npz (first ten flights of all 136 cells), reproducing each trial's goal with the evaluation's own seeding
(`default_rng(10000 + i)`, route `LCR[i % 3]`):

- **1 360 flights, 222 without success — every one of them on the cap, short of the line.** No abort, no collision.
  180 = the flow models under per-step projection at K ≤ 2 over the hump (18 cells × 10); 42 = the baseline's projected
  flights (tilt r 8, c 10, t 7; hump r 7, c 7, t 3). No unprojected flight and no flow flight at K ≥ 3 hits the cap.
- **The 42 baseline flights are still moving toward the line** (none stalled): at their mean pace over the last 30
  steps (2–5 mm per step on the tilt; the unprojected pace is ≈ 20), 6 would cross within +10 % more steps, 37 within
  +25 %, all 42 within +50 % (windows of 10 and 60 steps agree: 32–41 within +25 %). They end 0.41–0.74 m (tilt) and 0.36–1.60 m
  (hump) from the goal point, most at y ≈ −0.5 (after the tilt they do not return to the route), so for those only the
  x = 2.8 plane is in reach.
- **If they kept that pace**, the baseline's S&C would read: hump r 3 → 10/10, c 3 → 10/10, t 6 → 9/10 at +25 %; tilt
  r 0 → 3/10, c 0 → 5/10 (+25 %) / 8/10 (+50 %), t 0 → 3/10 — i.e. the corridor's reading would change (the hump's
  "FM K20 endpoint 8/10 ahead of the baseline 6/10" would no longer hold on S&C).
- **The 180 flow flights are stalled** before the hump's apex (median movement 2.4 cm over the last 60 steps; median
  ≈ 14 700 more steps needed at that pace). Only the six wall-drift flights move, and they violate the wall.
- Side facts: 87 flow and 7 baseline projected successes crossed the x = 2.8 plane without entering the 0.30 m sphere,
  so they fly on to the cap (the evaluation stops early only inside the sphere).

Caveat: constant-pace extrapolation; only a re-flight with a longer budget proves it (the evaluation is deterministic
per configuration, so the first 396 steps should reproduce). **Decision with the author** — nothing applied.

## 3b · Follow-up (same day): R45 spec for the other agents — still no corridor text changed

Author: "can we just relax the goal line and do the DA again? … and a md for what is problem and should do the continue
steps run AND relax step to more in case later will run" · "the new goal line if possible direct at end of the corridor".
Written: [`data_status/PENDING_20260924_uav_corridor_step_cap_R45.md`](../../data_status/PENDING_20260924_uav_corridor_step_cap_R45.md)
(ledger row R45, INDEX line; preview scripts `Data_Analysis/DA_in_Paper/analysis/R45_preview/`).

- **R45a (DA, data only):** finish line at **the end of the corridor, x = 2.0 m** (walls, tilt and roof all end there;
  checked: past x = 2.0 the only violating steps, 25, belong to three flights that had already left the corridor
  sideways and are violating anyway; none past 2.31). Preview: baseline S&C tilt 0/0/0 → 3/8/3, hump 3/3/6 → 9/9/9;
  no flow cell changes in S&C; 2.31/2.36 differ by one hump flight. Reading if it holds: hump = trade-off (FM K20
  endpoint 8/10 at 291 ms vs baseline 9/10 at 623 ms), tilt = the baseline alone has S&C.
- **"Continue from step 396" is not possible:** the npz lacks attitude, body rates, controller state and the random
  state; torch is seeded once per job (`mix_uav/utils/setup.py:166`), so a longer budget re-samples every flight after
  the first capped one. **R45b** (optional) re-flies only the six baseline projected cells at 792 steps (≈ 2 h wall).
- **R45c:** `--max-episode-length 792` for any later corridor run; code fixes (stop at the finish line, per-trial
  seeding, resumable state) need the author's go-ahead.

## 4 · Records

The v3.80 records used table numbers from before `tab:uav-corridor-altitude` was inserted: the corridor tables are
6.11, 6.12, **6.13 (altitude)**, 6.14; the s-curve tables are **6.15, 6.16, 6.17**. Corrected in the v3.80 changelog,
CHANGELOG.md, the briefing, INBOX, the → v4 note and the three ledgers (the v3.75 spec text in the s-curve ledger keeps
its own numbers, now flagged there).

## Checks

`python3.14 tools/check.py` passes. Not compiled. No bundle. Nothing committed.

Claude (Opus 5.5, Claude Code) · 2026-09-24 · not compiled · nothing committed.
