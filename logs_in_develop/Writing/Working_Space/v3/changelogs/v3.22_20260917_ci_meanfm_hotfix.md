# v3.22 — 2026-09-17 · CI-MeanFM table-label hotfix

The author rejected `$\alpha$-MeanFM` because it resembled a branded derivative and overemphasised the
paper's interpolation parameter. The approved compact label is **CI-MeanFM**, expanded as
**consistency-interpolated MeanFM**. It retains the useful MeanFM-family relationship while naming the
mechanism instead of borrowing the published brand.

The full thesis prose name remains **consistency-interpolated average-velocity matching**. Only
space-constrained tables use **CI-MeanFM**. Section 5.3 defines the mapping at first mention.

Updated locations:

- Section 5.3 and the Chapter 5 training table;
- every affected Chapter 6 result-table cell and comparison header;
- the canonical translation table;
- the v3.21 summary and detailed layout changelog, so they do not present a rejected label as current.

The final consistency step ratio remains written separately as `$\alpha_{\mathrm{end}}=0.2$` or
`$\alpha_{\mathrm{end}}=0.05$`; **CI** does not replace that experimental parameter.

## Verification

- `python3 tools/check.py` passes: 14 files, 230 labels, 52 citations and 22 figures. The two inherited
  Chapter 4 width warnings are unchanged.
- New-sections-only bundle: `thesis_v3_20260917_105825_new.tex/.zip`; 22/22 figures rendered, zero
  placeholders, and all nine inlined plus four collapsed sources verify byte-faithful.
- Not compiled because this container has no TeX toolchain.
