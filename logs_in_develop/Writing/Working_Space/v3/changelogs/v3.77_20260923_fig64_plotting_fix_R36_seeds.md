# v3.77 — 2026-09-23 · Fig 6.4 drawn complete (a plotting fix); R36's K3 cells moved to seeds 7–10

**Asked (author):** "Fig 6.4 is where is wrong? need what kind fix? just writing/plotting/missing data? if just writing
plotting, plz just fix it and leave a comment in the tex. and the 1. The pending ledger, do we need update the scheduled
runs? modify it? if yes, just change it and remark from scheduled to updated (v3.XXX) and if need new runs, just add and
with the v3.XX"

## 1 · Fig 6.4 `fig:aligning-projected-tradeoff` — plotting only, no data missing

**What was wrong.** The figure drew five of the seven random-rule cells of Table 6.8. Both missing cells exist on disk:
- CI-MeanFM endpoint: the store read CI-MeanFM from the tagged run (`sources.ALIGNING_CELLS[('af', 20)]` →
  `_msgafon02_s6`), which has no endpoint arm; the endpoint cell is in the untagged `…_Eaf_EPlatest` run of the same
  checkpoint, in the same 15-09 corpus (10 rows, one per context — one run).
- diffusion per-step: the builder skipped `diffusion` by design (no tightened cell existed before R2); the R2fix cell is
  only in the 23-09 batch.
Neither reaches nine clean contexts, so both are hollow and the frontier is unchanged. Also found: the "box not moved"
label sat under the dashed line on the bottom frame, which struck it through — already so before this pass.

**What changed (store, author's go-ahead "just fix it"):**
- `plotting/sources.py`: corpus `visual_aligning_23_09` (`temp/23-09/batch_va2_20260923_210100`) and registry
  `ALIGNING_PROJECTED_EXTRA` — (diffusion, 20, `dpcc-r`) from the 23-09 batch, folder suffix `_msgR2fix`; (af, 20,
  `hardflow_sls-r`) from 15-09, suffix `_Eaf_EPlatest`.
- `plotting/builders/frontier.py::fig_aligning_projected_tradeoff`: reads those cells with the same geometry, variant
  filter and ten contexts; the baseline in near-black (`ENGINE_COLOUR_DISTINCT['diffusion']`) and in the legend; legend
  spaced by label length; "box not moved" moved above the line at the empty left end; docstring note.
- Verified: the two new points compute to Table 6.8's values (diffusion r 0.462 m, 809.2 ms, 4/10 clean; CI-MeanFM
  endpoint r 0.425 m, 294.2 ms, 8/10). Rebuilt, PNG at 3×, rendered and read, exported to `v3/figures/` (byte-identical).

**Chapter 6:** the sentence now says both cells are drawn hollow (keep at most eight contexts clean), with a `%` comment
recording the fix; the `\hole` is gone; caption back to "the cells of Table 6.8 under random selection" + "diffusion" in the
colour list (79 words); dataref names the two sources.

## 2 · The scheduled runs — one update, no new run

| run | verdict |
| :-- | :-- |
| R16a–c (Table 6.5) | unchanged — exactly the three pending rows, on the printed rows' checkpoints and folder keys |
| **R36a, R36b** (Table 6.3, CI-MeanFM K3, FM K3) | **🔄 updated v3.77:** seeds 6 → **7–10**, the seeds of the printed MeanFM K3 row (job 25444 does not contain seed 6), so the K3 block is paired on one seed set instead of mixing 4-seed and 1-seed rows |
| R36c (MeanFM K10) | unchanged — seed 6, the seed of the printed K10 rows |
| new runs | **none needed**: every open cell of the draft is covered by R16, R36, R33 or R44, and Fig 6.4 needed no run |

**Driver** `Slurm_Codes/temp_bash/pipeline_20260923_must_need.sh`: `SEEDS_K3` (default `7, 8, 9, 10`), `prune_yaml` takes a
per-job seed list (R36a/b `SEEDS_K3`, R36c `$SEED`); plan banner, protocol line, log checks and cost comment updated.
`bash -n` passes; the pruning was run on scratch copies of the three yamls (alphaflow and hardflow copies read
`seeds: [7, 8, 9, 10]`, meanflow `seeds: [6]`; nothing else changed). Not submitted from here.
**Runbook** `SLURM_RUNBOOK_20260922_all_lacking_runs.md` §6b (title, R36a/b rows, protocol paragraph, cost 2–5 h, log checks)
and §7 status row: 🗓 → 🔄 updated v3.77. **Ledger** NOW table: R36 row 🔄 updated v3.77; the v3.76 note's "one thing to
know, not to fix" replaced by the update. Table 6.3's dataref says which seeds the R36 cells will be on.

## Checks

`python3.14 tools/check.py` passes. The export notes one known item: `fig_hardflow_endpoint_generation` (v2's Ch 4 figure,
copied into `v3/figures/` at v3.73) is not registered in the store. Not compiled. No bundle (not asked). Nothing committed.

Claude (Opus 5.5, Claude Code) · 2026-09-23 · not compiled · nothing committed.
