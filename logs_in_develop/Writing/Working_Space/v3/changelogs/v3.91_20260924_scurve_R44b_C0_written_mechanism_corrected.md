# v3.91 — 2026-09-24 · UAV-s-curve: R44b and C0 written (Table 6.15 complete, Table 6.16 three of four rows, Table 6.17 on FM K1), the violation mechanism corrected; C1 and the figure left pending

**Asked (author):** "the rest some parts of s_curve data + DA are ready, write them. it lack some of the final pieces,
dont write that parts, only write the now ready parts."

**Source:**
- `Data_Analysis/DA_in_Paper/analysis/DA_20260924_scurve_R44bc_projection_controller.md` (raw result folders,
  md5-verified; B1 reproduces A7 flight for flight);
- the note `cross_draft/to_v3/FROM_DA_20260924_scurve_R44bc_ready.md`.

**Ready and written:** R44b (job 26195, per-step on FM K1) and C0 (job 26196, MuJoCo MPC on FM K1 unprojected).

**Not written, because it is not ready:**
- C1 (the projected plans under MuJoCo MPC): the fourth row of Table 6.16 stays "pending (R44c)".
- The rebuilt Figure 6.9: the pilot figure keeps its `\outdated`, and the pilot text stays.
- The final s-curve conclusion: still `\provisional`, now on the ready facts.
- UAV-s-curve in §6.4: the `\hole` now names what is missing.

## Changes (`chapters/06_results.tex`; the replaced texts in `withheld/20260924_v3.91_archive/`)

1. **🔴 Mechanism corrected (DA §6).** The reading after Table 6.14 no longer says the flights "follow their plans with a
   mean tracking error of 0.30–0.33 m while the route clears the boundaries by 0.121 m". It now says:
   - every commanded path cuts the second inside corner of the crossover, 8–19 cm inside its keep-out, where the route
     clears it by 0.121 m, and the violations lie at that corner;
   - the cascaded controller follows the command to within ~1 cm and never comes more than 1.4 cm closer to the corner;
   - the 0.30–0.33 m is the setpoint's lead along the path.

   It also says where the flights invert: at the second turn, 48 of 54 at x 0.2–1.2 m.

   The same fix is in the pilot sentence ("keep 0.29–0.34 m behind their commanded position, the lead … rather than a
   sideways error") and in the provisional conclusion.
2. **Table 6.15 `tab:uav-scurve-projection` is complete.** Per-step r / c / t: 5/10 each; 71.3 / 74.7 / 37.2 violating
   steps; aborts 3 / 3 / 5; 157.1 / 163.8 / 135.9 ms. "Pending" is removed from the caption. The `\hole` is replaced by
   the reading:
   - projection does not keep the nine crossings;
   - the projection takes 126–155 ms of the step time;
   - two floor drops each under r and c;
   - **the binding:** the projector binds the plan's measured positions, as DPCC does, not the commanded position,
     which UAV-corridor binds as well (`sec:setup:protocol:uav`). The projected commands still cut the corner by
     8–9 cm.
   - A `\guard` says the setpoint-bound variant was not flown here (DA §7, the author's call).
3. **Table 6.16 `tab:uav-controller`: three of four rows.**
   - MuJoCo MPC unprojected: 9/10, 41.5 ± 18.4, 0.297 m, 0/10.
   - Per-step cascaded: 5/10, 71.3 ± 66.6, 0.572 m, 3/10.
   - The caption names the rule: random, first of the tied rules by fewest aborts, then fewest violating steps.
   - The comparison paragraph now reads: MPC removes the inversion, and all 10 end within 0.3 m (0.297 against 0.571);
     9/10 succeed under both; MPC follows the command less closely (5 cm, up to 29 cm), its plans cut deeper, and it
     violates more (41.5 against 23.0). **Instability is the controller's limit; the violations are the plan's.**
   - The guard is now ten flights per cell on the selected configuration.
4. **Table 6.17 `tab:uav-controller-cost` is on FM K1, ten flights against ten (DA §8.3).**
   - Control loop 14.0 against 135.8 ms; planner 8.9 against 10.4; controller and simulator step 5.2 against 125.4.
   - The paragraph: ×24, 12× generating the plan, ~7× avoiding's entire action. The pilot split is kept in the text
     (6.5 against 125.8) and in the dataref. The caveat's closing sentence uses both.
5. **The s-curve conclusion (still `\provisional`)** now leads with the DA's verdict, the two limits:
   - stability at the second turn is the controller's (MPC removes it, at ×24);
   - the corner cut is the plan's; neither MPC nor per-step projection, as configured, removes it.

   Then the simple-data paragraph (MF/AF not ahead at K1), with a new `\guard`: FM's half-scale initial noise
   (`sec:setup:protocol:avoiding`) bears on that order, not on the controller comparison (DA §9).
6. **Pointers:** the §6.3 intro's "pending" sentence and the §6.4 `\hole` now name only what is missing.

## Records

- `INSIGHTS_20260924_results_per_environment.md`: S1 corrected, S2 moved to the selected configuration, S4 (the corner
  cut) and S5 (neither remedy) added.
- INBOX: the DA's R44b+C0 item is ✅ v3.91 (partial, C1 pending). There is a → v4 row with the correction, and the v3.80
  note to v4 carries a correction banner.
- `data_status/PENDING_20260923_uav_scurve_R44_raw_first.md`: a status line.

## Checks

`python3 tools/check.py` passes. Not compiled. No bundle (not asked). Nothing committed.

Claude (Opus 5.5, Claude Code) · 2026-09-24 · not compiled · nothing committed.
