# v3.74 — 2026-09-23 · one "Appendix" heading over the lettered appendices

**Asked (author, after the v3.73 bundle):** "why the Appendix main chapter is gone? now the ABC direct exposed outside? …
we should not change such things and major work will be done in the v4, but now I need the appendix big Chap back" —
then "just add it, if there is no".

**What was found first.** Nothing removed a wrapper: `chapters/09_appendix.tex` has been `\appendix{}` followed by three
`\chapter`s (Derivations, Extended Results, Reproducibility) in every commit since v3 was created (`cbefd343`, 2026-09-10);
v2's `thesis_v2.tex` and the TUM template (`Template_DONT_CHANGE/main.tex:51`) have the same shape, and every stored bundle
of 23-09 (`100734` … `160926`) prints A, B, C as top-level chapters. The v3.73 sync touched only comments and a
`\graphicspath` in the preamble and one abstract sentence. So there was no heading to restore; it is added.

**What changed.** `chapters/09_appendix.tex`: `\addpart{Appendix}` right after `\appendix{}` (the class is KOMA `scrbook`,
`oneside`). It gives an unnumbered "Appendix" part page, a part-level TOC entry and a PDF bookmark with A, B, C under it.
A, B, C stay chapters, so every `\autoref{app:…}` still reads "Appendix A/B/C" and no label or number moves. The back
matter that follows (`99_backmatter.tex`: Abbreviations, List of Figures, List of Tables, Bibliography) also sits after
that heading in the TOC and bookmarks, as it sits after `\appendix{}` in the template.

**Bundle (eighth round).** `bundle/output/thesis_v3_20260923_163243_new.zip`, `…163243_new_clean.zip`,
`…163244_new_nonotes.zip` (10 478 KB; 47/47 figures; `--verify` byte-faithful; each carries `\addpart{Appendix}` once).
Supersedes `160926`, which is otherwise identical.

`check.py` passes. Not compiled — no TeX toolchain here, so the part page has not been seen rendered. Nothing committed.
v4 told (inbox row): it owns any further restructuring of the appendix.

Claude (Opus 5.5, Claude Code) · 2026-09-23 · not compiled · nothing committed.
