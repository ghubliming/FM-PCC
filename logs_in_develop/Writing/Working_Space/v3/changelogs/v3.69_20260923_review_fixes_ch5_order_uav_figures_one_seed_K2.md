# v3.69 — 2026-09-23 · Review fixes: Ch 5 order (task → constraints), quadrotor figures rebuilt, one seed on pillars, ten corridor flights, diffusion K2 filled

Author's review of the v3.68 bundle (`204547`), twelve items; the bundle is **held** ("TBC, don't build").

## What changed, item by item

| # | asked | done |
| :-- | :-- | :-- |
| 1 | `[src: config/projection_eval.yaml …]` printed after Table 5.2's paragraph, and "(v3.67) … printed pending" in the running text | The grey `[src: …]` is the `\srcnote` drafting note: it prints in the **annotated** bundle only and is hidden by `_clean`/`_nonotes` (`make_bundle.py --clean-notes`). **The writing bug was real:** the sentence carried a draft version and drafting vocabulary. Now: "Untightened evaluations are therefore not reported. An unprojected plan is the same plan on either set, and its violations are scored against the tightened boundary." |
| 2 | Fig 5.3 (the task) before Fig 5.2 (the constraints), text alike | §5.2.1: the task paragraph ends at the goal line → `fig:env-avoiding` → the constraints paragraph → Table 5.2 → the keep-out/halfspace paragraph → `fig:constraints-avoiding-sets`. |
| 3 | Same for Figs 5.4/5.5, then Table 5.3 and Fig 5.6; adjust the UAV part if wrong | §5.2.2: intro → observation paragraph → `fig:env-aligning` → `fig:aligning-cameras` → the "why this task" paragraph → the constraint paragraph → Table 5.3 → the contexts paragraph → `fig:aligning-contexts`. §5.2.3: the scene list → `fig:env-uav` → the constraint-forms paragraph → Table 5.4 → `fig:constraints-uav`. Same rule everywhere: environment and task first, constraints second. |
| 4 | Fig 5.7 lower UAV-pillars view is mirrored against the render | Not a mirror in the projection (`Scene3D` is right-handed; checked) — the three orthographic views were drawn **from the finish end** (camera at +x, +y) while the renders look along +x from behind the start, which reads as a left–right flip. All three lower panels now look from behind the start (`fig_scene_uav_*`, azimuths 196°/204°/196°): pillars big pillar near and the finish line far, s-curve first passage near-right and second far-left, as in the renders. |
| 5 | Fig 5.7 caption is self-reasoning | Rewritten as facts: what each row is, what the green path is per scene, the finish line, the arena size, where the corridor constraints are drawn. |
| 6 | Fig 5.8 chaotic → a matrix, same-size panels, pillars 3 sets, corridor tilt + hump | `fig_constraints_uav` rebuilt as a 3 × 3 matrix: column 1 UAV-pillars = *top-left-hard*, *top-right-hard*, *both-hard* mapped at scale 36 (each with its own keep-out disk, translucent over the pillar it covers; pillars solid, no rings — they are obstacles, not constraints); column 2 UAV-corridor = **tilt** (top view at the launch altitude) and **hump** (side view, the roof); column 3 UAV-s-curve. Every panel 470 px wide at one aspect ratio (24/27), so all nine cells are the same box. Legend three rows of two. Paragraph and caption rewritten to the matrix. |
| 7 | §5.5.2: say only that the D3IL metric does not suit a different network / training target | One sentence: different network, different training target and output (a projected, tracked plan), so the published success rate does not transfer; the task is scored by final distance; the D3IL numbers describe how hard the task is. |
| 8 | Table 5.10 and anywhere: no "5 (1 for the closed-loop check) … the avoiding cell's episodes" — one seed, no detail | Table 5.10 UAV-pillars: **1 seed, 2 episodes per geometry, the three geometries**. §5.6.3.3 rewritten. `tab:eval` intro: "one training seed" (was "seed 6"). Ch 6: the three pillars captions say one training seed, two episodes per geometry; the live/closed-loop guard and its dataref sentence removed (the 22-09 gate check is a drafting note only). Appendix: every "seed 6" in captions/table cells → "one seed". |
| 9 | §5.6.3.3 reads as AI self-talk | Rewritten (see 8): one plain paragraph per scene, no "never pooled / on its own protocol". |
| 10 | Why 12 corridor flights? | **Not random, but not chosen for the thesis either:** `eval_mix_uav.py` assigns trial *i* to route `homotopies[i % 3]` (L, C, R), and the U16 corridor-v2 paper run (2026-09-13) fixed `n_trials = 12` = 4 per route so the three routes weigh equally; the 23-09 driver inherited `NTRIALS=12`. With the DA reading the first ten of each cell the split is 4/3/3 (trials 0–9). The thesis now says **ten flights per configuration, spread over the routes** (Table 5.10, §5.6.3.3, Ch 6 corridor captions ×5, `12/12` example in §5.4.4); the `\srcnote` records the 12-flown/10-read rule. |
| 11 | Recheck Ch 5/6 for the changed fundamentals | Grepped both chapters for `seed 6`, `twelve`, `12 flights`, `closed-loop check`, `planner in the loop`, `p23pv2live`, `(v3.xx)`: every remaining hit is inside a `\srcnote`/`\dataref`. **Not touched (🔒 §6.4):** Table 6.19's corridor rows still say `12/12` and "7 of 12" from the archived v2 corpus — to reconcile when the lock lifts and R33 lands. |
| 12 | Fill the diffusion K2 | Tables 6.1, 6.2, A.1, A.2 filled from `DA_20260923_diffusion_K2_five_seeds.md` §3 (five seeds × three geometries × two episodes; the batch reproduces the 19-09 corpus to the last digit). Bold on the K2 `r` and `c` rows by the table's own rule (they clear the baseline of record on all three columns). §6.1.1 paragraph rewritten: **the baseline's cheapest configuration that holds S&C 1.000 is two denoising steps under `c` — 61.2 steps, 211.4 ms, 38 % of the K20 cost**; the 30× claim is against the published configuration, 12× against the two-step baseline. |

## Left open

- **Figures 6.3/6.4 (k-ladder, trade-off) do not yet carry the diffusion K2 point.** The store's scripts read the 19-09 corpus; the K2 cell lives in `batch_avoiding_combined_20260923_110010`. Promoting it into `avoiding_unprojected_and_budget20.py` / `avoiding_rules_by_protocol.py` is a code edit in `analysis/` (the DA offers it) — needs the author's go. A `%` comment sits at Fig 6.3's caption.
- **`tab:target` (Ch 5) still pins the Target to K20 `c`** as the author decided; the DA's §4 notes that K2 `c` Pareto-dominates it. Author's call whether the Target statement mentions it.
- The DA's §6 open cost observation (the projector adds 193 ms on the diffusion arm at K2 against 8 ms on the flow arm) is **not** attributed in the text, as the DA recommends.
- §6.4 🔒 corridor rows (see item 11).

## Figure store

`plotting/builders/scenes.py`: the three `fig_scene_uav_*` cameras moved 180° (comment in code); `_uav_constraint_panel` gains `keepout` / `physical` disk flags; `_uav_side_panel(…, xlim, zlim, equal)`; `fig_constraints_uav` is the matrix. `plotting/sources.py`: `_pillars_v2_scene(name)` per geometry, `UAV_PILLARS_GEOMETRIES`; `UAV_CONSTRAINTS['scenes'][0]` stays the both-hard scene for the other consumers. Rebuilt and exported: `fig_scene_uav_{pillars,corridor,scurve}`, `fig_constraints_uav` (svg + png at scale 3).

## Files

`v3/chapters/05_setup.tex`, `06_results.tex`, `09_appendix.tex`; `Data_Analysis/DA_in_Paper/plotting/{sources.py, builders/scenes.py}` + `figures/env/*`, `v3/figures/*`; `data_status/PENDING_20260922_all_lacking_runs.md` (R33 note, R26 link); `cross_draft/INBOX.md` + `to_v4/FROM_v3_20260923_v3.69_one_seed_ten_flights_K2.md`; `v3/CHANGELOG.md`, `v3/BRIEFING_20260922_v3.60.md`.

`tools/check.py`: passes (hole 31, provisional 1, guard 19, srcnote 63, dataref 45, todofigure 4, flawed 2, outdated 1). **No bundle built (author: TBC).**

---

## Second round (same changelog, author 23-09): items 13–17, and the ledger check

| # | asked | done |
| :-- | :-- | :-- |
| 13 | §6.1.2.3 *The Executed Paths* feels random — appendix; the frontier right after the data table; say at the end of the avoiding part that more is in the appendix | Moved whole (text, `fig:avoiding-paths`, dataref, guard) to a new appendix section **`app:avoiding-paths`**; §6.1.2 now runs Table 6.2 → selection rules → the cost frontier. The §6.1 intro's twenty-episode sentence is gone; the avoiding conclusion ends with one sentence naming the three appendix sections (paths, every budget, twenty episodes). |
| 14 | Fig 6.4: the models do not share the budgets — need it? then full table + run what is lacking; else delete | **Kept as a question, replaced as a figure.** §6.1.2.5 keeps its label (`sec:res:fewstep`, cited by Ch 4 and Ch 8) and now prints **`tab:k-ladder`**: four models × $\nfe\in\{1,2,5,10,20\}$ at the operated rule, S&C / steps / ms per cell, every existing cell from Table A.2, the eight missing cells *pending* **R41** (ledger §28 + NOW table; runbook queued note): MeanFM K20, CI-MeanFM K5/10/20, FM K5/10 are evaluations of existing checkpoints (~3 h); diffusion K5 would be five trainings (optional). The figure block is archived in `withheld/20260923_v3.69_archive/`; `fig_avoiding_k_ladder` stays in the store unreferenced. The prose now uses the five-seed K2 baseline (211.4 ms, twelve times FM's 17.3) instead of the single-seed 225 ms. |
| 15 | §6.2.1.1's second paragraph reads like a message | Rewritten as results prose; the "printed pending rather than left blank" and "not a candidate operating point and is not evaluated" talk is gone. The grey `[data: …]` after it is a `\dataref` (annotated bundle only). |
| 16 | Tables 6.7/6.8 "Median [m]" — which metric? print both | The alignment metric is the final box-to-target distance per context; Table 6.5 already printed median and mean, 6.7/6.8 printed the median only. Both now carry **Median [m]** (with the share closed) **and Mean [m]** (mean ± sd over the ten contexts), computed from the corpus of record with `va_results.py`'s cell definitions (K2 block under the `mv1_Emf` tag, which reproduces the printed medians). Captions say so. Table 6.6 (all pending, R37) keeps its median columns until it has data. |
| 17 | Fig 6.7 to the appendix | Moved with its paragraph, dataref and guard to **`app:aligning-paths`**; the §6.2 conclusion points there. |
| + | Does the 22-09 ledger need an update? | Your corridor-v3 banner and R33 row stand. Changed: R39's NOW row no longer lists the two live cells (author: turbo only); **R41** added (NOW table + §28); the runbook's queued section carries R41. |

`check.py` passes (36 figures now referenced; 279 labels; no dangling reference). No bundle.

Claude (Fable 5.1, Claude Code) · 2026-09-23 · mechanical checks only. Not compiled.

## Bundle (author: "build the bundle")

`bundle/output/thesis_v3_20260923_100734_new.zip`, `…_new_clean.zip`, `…_new_nonotes.zip` (9682 KB; 46 figures
render, 0 placeholders; cross-references resolve; `--verify` byte-faithful). Supersedes the v3.68 set (`204547`).

---

## Third round (same changelog, author 23-09): Fig 5.8 / 5.11, pillars back to five seeds, aligning protocol, the K2 time

| # | asked | done |
| :-- | :-- | :-- |
| 1 | Fig 5.8: a second tilt panel in $z$; say which axis the hump is viewed along | Corridor column is now **tilt top view ($x$–$y$, at 1.11 m) · tilt side view ($x$–$z$, along the corridor) · hump side view ($x$–$z$)**; every subtitle names its plane; the grid stays 3 × 3 with all panels one size. Paragraph and caption follow. |
| 2 | Fig 5.11 corridor: tilt only? | It showed the tilt only (top view). Now **two corridor panels**: tilt from above with the three lanes, and the **hump from the side with the demonstrated altitude band 0.90–1.30 m** under the inflated roof — every lane passes under it. |
| 3 | Mark near Fig 5.11 that UAV-pillars' expert data is D3IL-avoiding's | A **UAV-pillars panel** is now the first panel of Fig 5.11: the 96 D3IL-avoiding demonstrations mapped into the arena at scale 36 over the both-hard set, 2/96 satisfying (the same test and count as Fig 5.9). Caption says these are the scene's demonstrations. |
| 4 | §5.5.3 Predictive Sampling: introduced in v2? | Yes — v2 §4 *Sampling-Based Predictive Control* (`sec:method:mjpc`) and the background (§2 quadrotor). Linked with `\autoref{sec:method:mjpc}`; nothing owed to v2. |
| 5 | Table 5.10 UAV-pillars back to 5 seeds × 2 episodes, aggregated as D3IL-avoiding | Table 5.10, §5.6.3.3 and the three Ch 6 pillars captions/datarefs back to the DPCC protocol (T5's endpoint cell: four seeds, as its source). |
| 6 | Table 5.10 aligning: diffusion 3 episodes per context? | Wrong statement, not a data problem: the corpus has the diffusion cell at **one rollout per context on 30 contexts, reduced to the ten** (checked with `va_results.py`); every model is 10 contexts × 1 episode. Row fixed; Ch 6 already read it that way. |
| 7 | The K2 diffusion data is wrong (time not 2× K1) | Investigated (ledger §29): identical on all five seeds; generation doubles as expected, the **projection half is ~3× per solve** — signature of CPU contention on the one job (26112) that ran it, which the flow cells of the batch never ran on. **Outcome cells stay (S&C, steps); the three ms cells are printed *pending (R42)*** in Tables 6.2, 6.5, A.2; §6.1.1 quotes the K10 row (18×) again for the cost claim; Table 6.2's caption says why. **R42** = re-time on a quiet node with one flow reference cell in the same job (~20 min). |

Figures rebuilt and exported: `fig_constraints_uav`, `fig_expert_uav` (`builders/scenes.py`, `builders/expert.py`: `_pillars_panel`, `_corridor_hump_panel`). `check.py` passes. No bundle (author: TBC).

Claude (Fable 5.1, Claude Code) · 2026-09-23 · mechanical checks only. Not compiled.

**Addendum (author's question, 23-09): can the K2 diffusion data still be used?** Yes. Outcomes (S&C, steps,
violations) and the unprojected time are sound and stay. Only the projected time is suspect, and only upward: the true
value is ≤ 211 ms and ≥ ~70 ms (two projections at the per-solve cost of every other budget). No ordering moves under
either bound; what moves is the factor against the *cheapest* diffusion cell — 12× at the measured value, ~4× at the
lower bound (30× against the baseline of record is unaffected). §6.1.1 now says exactly that instead of quoting the K10
row as the cheapest cell. R42 (~20 min) settles the factor.

## Bundle, third round (author: "build the bundle")

`bundle/output/thesis_v3_20260923_103745_new.zip`, `…103745_new_clean.zip`, `…103746_new_nonotes.zip` (10117 KB; 46 figures
render, 0 placeholders; cross-references resolve; `--verify` byte-faithful). Supersedes `100734`.

**Addendum 2 (author, 23-09): print the K2 time as measured.** The measured values (191.1 / 211.4 / 225.9 ms) are back in
Tables 6.2, 6.5 and A.2 — a reproducible measurement is not blanked for looking odd. Table 6.2's caption says it is
being re-measured and why; §6.1.1 and §6.1.2.5 quote 211.4 ms with the bound (12×, no less than ~4× after R42). R42
stays open as the re-timing.

**Addendum 3 (author: "could it be the threshold? check the logs; if 100 % sure, justify in the text").** Checked
(ledger §29 addendum): threshold 0.5 and fan 4 in the job log; gate projects t = 1 and t = 0 at K = 2; reproduced by two
jobs three days apart; the final-step-only variant of the same cell costs 28 ms, so the ≈185 ms is the projection of
the first denoising step, one step from noise, on which SLSQP runs far more iterations. **The number is real and
stays.** §6.1.1 gained one paragraph saying so; the "re-measured (R42)" phrases are gone from Table 6.2's caption,
§6.1.2.5 and `tab:k-ladder`; the factor against the cheapest diffusion cell is stated as 12×. R42 struck in the ledger.

**Addendum 4 (author: "this is a caveat to demo FM vs diffusion — the low smoothness costs solving time; add a caveat
subsection").** New **§6.1.2.6 *Caveat: What the Projector Is Handed*** (`sec:res:avoiding:caveat:noise`), placed after
Step Budget: the six-fold rise of the projected K2 time is the first-step projection on a plan one step from noise, which
only the baseline's inclusive gate ever performs (the flow window at K2 is the final step alone); per-solve costs from the
tables (final step 2–6 ms, first step ≈45 ms per candidate); reproducible, not hardware, not the threshold. The smoothness
conjecture is stated **as the author's conjecture and untested** — at equal noise level (K20, mid-schedule) both families
cost ≈7–8.5 ms per solve — with the two-sided test written as **R43 (optional)** in the ledger. The explanation paragraph
left §6.1.1 (one pointer remains); a guard says the benchmark's ordering does not rest on this cell. `check.py` passes.

**Addendum 5 (author, 23-09 — items 9–11).** (9) **Step Budget dropped**: the subsection and `tab:k-ladder` are archived
(`withheld/20260923_v3.69_archive/fewstep_tab_k_ladder_v3.69.tex`); `\label{sec:res:fewstep}` (cited by Ch 4 and Ch 8)
now sits on §6.1.2.1 *At the Protocol of DPCC*, whose Table 6.2 carries the same information; R41 struck. (10) §6.1.2.5
*Endpoint and Per-Step Projection* keeps its content and ends with a one-line verdict: on avoiding endpoint projection
has nothing left to win — K1/K2 per-step already reach 0.967–1.000 at ~18 ms and K3 buys no fewer steps at four times the
cost (dominated) — while on aligning, at K20, it matters and is the better projector. (11) Fig 6.4
(`fig_aligning_projected_tradeoff`): the direction key moved off the unprojected MeanFM ring to the empty lower-left;
the paragraph before it states the trade — no violation costs completion, allowing violation buys it. `check.py` passes.

**Addendum 6 (author, 23-09 — UAV-pillars is the avoiding replica, a plant check).** Ch 6 §6.3.1 rebuilt (old block in
`withheld/20260923_v3.69_archive/uav_pillars_block_v3.69b.tex`): intro says the scene tests the vehicle and its controller
on the configurations that win on the table; **one table** (`tab:uav-pillars-raw`) — MeanFM / CI-MeanFM × K1 / K2, before
and after per-step projection (t), table columns filled from Tables 6.1/6.2, air columns pending R39, five seeds × two
episodes per geometry; **one figure** (`fig:uav-pillars-paths`) — the arm's executed paths of `fig:avoiding-paths` over the
quadrotor's flown paths of the same seed/geometry/episode, unprojected and projected, two rows × four columns; the
endpoint sub-part (T5) and the diffusion/FM rows are dropped from the draft; conclusion hole reworded ("the same, or a
stated difference — either is a valid result"). Ch 5: the scene item, §5.3.3 and the demonstrations paragraph now say
*replica of D3IL-avoiding to test the vehicle and its controller*. 23-09 pending file §2.5 carries the new interface for
the run agent (four cells + paths; T3–T5 not read). `check.py` passes. No bundle.

## Bundle, fourth round (author: "build the bundle")

`bundle/output/thesis_v3_20260923_110037_new.zip`, `…110038_new_clean.zip`, `…110038_new_nonotes.zip` (10120 KB; 46 figures
render, 0 placeholders; cross-references resolve; `--verify` byte-faithful). Supersedes `103745`. Carries addenda 1–6:
K2 time as measured with the caveat subsection, Step Budget dropped, endpoint verdict, Fig 6.4 key moved, pillars rebuilt.

