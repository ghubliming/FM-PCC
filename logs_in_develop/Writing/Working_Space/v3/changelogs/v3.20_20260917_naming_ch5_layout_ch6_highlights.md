# v3.20 — 2026-09-17 · mechanism names, Chapter 5 layout, and obstacle-avoidance emphasis

## Scope

One pass combined the author-approved generative-objective names with the requested Chapter 5 and
§6.1 corrections. Chapters 1--4 and Chapter 7 were not edited; the Chapter 4 rename was sent to v2
through `cross_draft/`.

## Generative-objective names

The v3-owned thesis text now uses three mechanism names outside Related Work:

| artefact family | thesis name |
| :-- | :-- |
| `fm` | instantaneous-velocity matching |
| `mf` | analytic average-velocity matching |
| `af` | consistency-interpolated average-velocity matching |

The names were applied in Chapters 5, 6 and 8, the appendix name map, table rows, captions and the
Fig. 6.5 plan matrix. The §5.3 opening defines the learned quantity and target construction of each
objective with an in-place citation. `$\alpha_{\mathrm{end}}$` remains the final consistency-step
ratio; *floor* is not used. Published brands remain only in Related Work and explicit provenance or
artefact tokens.

The canonical translation table was updated to this decision. The previous v2.20 names are marked as
superseded in the v3 inbox, and the v2 note now requests the same three names plus the Chapter 4 title
`Consistency-Interpolated Average-Velocity Matching`, while preserving `sec:method:alphaflow`.
`.claude/` was not changed, as required by the repository instructions.

## Chapter 5

- §5.1 now states the complete simulator lineage: MuJoCo is the common physics engine; D3IL supplies
  the Panda task integration; the aerial platform is the Skydio X2 from MuJoCo Menagerie with the
  MuJoCo MPC quadrotor task patch.
- §5.2.1 now states that D3IL-avoiding is the exact DPCC benchmark—environment, platform, data,
  constraints, projection configuration and protocol—and identifies this fixed setup as the controlled
  validation of replacing DPCC's diffusion model.
- The avoiding, alignment and UAV figure groups are slightly smaller and separated by explicit float
  barriers, preventing a deferred figure from entering the next task subsection.
- Table 5.5 was transposed from five long metric rows under three paragraph-width headers to three
  protocol rows under compact metric columns. Its numerical content is unchanged.

## Chapter 6

- A v3-only `\mainconfig{}` macro renders selected main-configuration cells in red. §6.1 captions state
  what red means. The marked configurations are the available thesis configuration at DPCC's protocol,
  the five-seed analytic average-velocity operating point, and the lowest-cost observed
  consistency-interpolated configuration in the single-seed full-success comparison. The
  `$\alpha_{\mathrm{end}}=0.2$` rows are also marked in the per-geometry table.
- §6.1.3's conclusion now resolves the remembered advantage explicitly. At full success, the
  consistency-interpolated objective records 59.68 versus 60.17 control steps and 0.0286 versus
  0.2209 s/step against analytic average-velocity matching. The path difference is slight, and the
  time difference includes different budgets (2 versus 5 evaluations). This is a single-seed result
  with `$p=0.231$`; the analytic objective remains the five-seed operating point.

## Figure and build

- `Data_Analysis/DA_in_Paper/plotting/sources.py` and `builders/avoiding.py` now use the mechanism
  labels. The four-model unprojected bar chart was rebuilt as SVG and PNG with wrapped labels, visually
  checked, and exported into v3. `figures/EXPORTED.md` was regenerated.
- `python3 tools/check.py`: 14 files, 230 labels, 52 citations, 22 figures; no dangling references,
  missing keys, unbalanced environments or brace errors. Two inherited Chapter 4 width warnings remain
  and are already open for v2.
- Full bundle: `thesis_v3_20260917_102451_full.tex/.zip`, 22/22 figures rendered, 0 placeholders;
  `--verify` reports all 13 inlined sources byte-faithful. The draft was not compiled because this
  container has no TeX toolchain.
- New-sections-only bundle: `thesis_v3_20260917_103003_new.tex/.zip`, with the inherited v2 Chapters
  1--4 collapsed to heading stubs and all v3-owned sections included; 22/22 figures rendered and 0
  placeholders. This is the handoff artefact when only the v3 contribution is wanted.
