# v3.63 — 2026-09-22 · UAV-pillars falls back to `pillars_hg`: the enlarged campaign failed and is abandoned

Read after [`v3.62`](v3.62_20260922_broken_axis_full_ladder_closed_share_nine_paths.md). One author item —
*"Pillars runs FAILED. check the info box for v3, fall back to the old version of the pillars and fall back
Chapter 5 too, and mention it especially in the expert UAV-pillars data section"* — then the bundle.

---

## 0 · The info box

`cross_draft/INBOX.md` had one open row for v3, from the DA side on the author's decision:
[`FROM_DA_20260922_pillars_u17_abandoned_fallback_hg.md`](../../cross_draft/to_v3/FROM_DA_20260922_pillars_u17_abandoned_fallback_hg.md).
The `pillars_xl` campaign (R23, Gen15 U17, jobs 25970–25995) ran to completion and is **all zero under the
thesis metric**: S&C 0.00 and collision-free 0.00 in all 94 cells, unprojected rows included. The enlarged
keep-out (0.35 + 0.31 m) closes the channel between the pillar rows by about 6 cm per side, and every
projector in the study — DPCC per-step and HardFlow endpoint — routes the plan into that forbidden channel
instead of outward into the legal detour: contact-free, across the finish line, violating at every column.
Closure of record: `logs_in_develop/Gen15/U17/CLOSURE_20260922_U17_abandoned.md`. Row closed ✅ v3.63.

## 1 · Chapter 6 — the withheld `pillars_hg` section is back

`v3/withheld/20260918_uav_pillars_section.tex` (v3.40) is pasted back verbatim between UAV-corridor and
UAV-s-curve: the prose, `tab:uav-pillars` (per-step block, every evaluated budget), `tab:uav-pillars-endpoint`,
`tab:uav-pillars-best`, and `fig:uav-pillars-paths`. Its numbers are the 15-09 batch's and were
regression-checked by the DA side. Three adaptations: the v3.62 *success* wording (six places), the stale
`tab:state-models` label repointed to `tab:avoiding-raw-models`, and one parenthesis after the opening caveat
pointing at §5.3.3 for the abandoned attempt. **The caveat stays the section's first move**: the unprojected
flows are already feasible here (FM 10/10, MeanFM 9/10 clean), so the projected rows measure preservation, not
repair.

The v3.56 `pillars_xl` scaffolding is gone: the `\hole`s, the two pending templates (`tab:uav-pillars` xl
version, `tab:uav-pillars-projection`), the `\todofigure` `fig:uav-pillars-xl-paths`, and the `u7xlchk`
verification sentence. `\todofigure` is now 0 in the draft. No `pillars_xl` number appears anywhere.

Downstream in §6.3: the section intro, the cost-frontier paragraph (the scene is not drawn as a frontier;
`tab:uav-pillars-best` already places each model per block and budget), the **projection paragraph** of
§6.3.3 (endpoint projection is the method on pillars: 104 ms against 1611 ms in projection, 0/120 against
43/180 flights losing physical safety, 41 against 89 with a violation — **all six numbers reproduced from the
corpus today**, dataref says so), and the **UAV conclusion** (pillars ranks nothing among the flow models; the
one ordering that holds is against the baseline).

🔒 **Not done — §6.4 is locked** ("DO NOT EDIT, author, v3.60"). Its three summary tables have no pillars row
and its first sentence still says "each of the three environments that yield a result". The rows, in the
v3.62 wording, ready to paste when you unlock:

```latex
% tab:summary-models
UAV-pillars   & success on a collision-free flight & no single winner: FM ($1.00$ endpoint, mean $0.586$ per-step) and MeanFM ($0.90$, the shorter path) level; the plans are feasible before projection & CI-MeanFM, then Diffusion ($0.30$ at best) \\
% tab:summary-projection
UAV-pillars   & 5  & endpoint & holds the best configuration of each of FM and MeanFM at a fifteenth of the projection time; preservation, not repair \\
% tab:summary-combinations
UAV-pillars
  & none selected (the scene does not support one)
  & FM, $\nfe=5$, endpoint, single candidate: S\&C $1.00$ at $78$\,ms per step; MeanFM $0.90$ in a shorter path \\
```

## 2 · Chapter 5 — back to the 0.12 m pillar, and the attempt told where the data is described

- §5.2.3: the scene item no longer promises an enlarged-set comparison; it says the constraint set is the
  clearance the demonstrations were generated to keep, so the scene measures preservation.
- `tab:uav-scenes`: "six disks, radius 0.12 m". Its `\srcnote`: `pillars_hg` with `radius: 0.12`;
  `pillars_xl`/`pillars_xxl` named as kept behind an ABANDONED banner and never active.
- The sentence under `fig:constraints-uav` and both figure captions (`fig:constraints-uav`,
  `fig:expert-uav`) no longer mention a 0.35 m keep-out.
- `tab:protocol`: a UAV-pillars row (1 seed, 10 flights) is back.
- **§5.3.3, the expert-data section — the author's specific ask.** The `pillars_hg` paragraph is the first-kind
  one again (all four routes satisfy the set, tightening included). A **new paragraph** then records the attempt
  and why it is not reported: with the keep-out enlarged to 0.35 m none of the four demonstrated routes satisfied
  the constraint (each crossed by 0.15 m); in evaluation every projector routed into the physically open but
  forbidden channel between the rows, so the flights crossed the finish line — *success* under the Chapter 5
  criterion — while violating the keep-out at every column; no configuration was collision-free, S&C was 0.00
  throughout, unprojected rows included; a scene in which the projector finds the forbidden lane easier than
  the legal one measures the geometry and not the planner, so the enlargement was abandoned.

## 3 · Figures

`sources.UAV_CONSTRAINTS` pillars entry back to r = 0.12 (no `r_phys`; the enlarged drawing was 19–22 Sep only);
the planned `fig_uav_pillars_xl_paths` entry withdrawn (PLANNED is empty). `extract/expert_paths.py` re-run:
UAV-pillars **4/4 clean**, corridor 0/3, s-curve 1/1, as before v3.41. Rebuilt and inspected:
`fig_constraints_uav`, `fig_expert_uav`, `fig_uav_pillars_paths` (panel counts 9/10, 10/10, 9/10, 3/10 clean,
as the restored prose says). `export_to_draft.py v3`: 6 files copied, `fig_uav_pillars_paths` is back in
`v3/figures/`.

## 4 · Ledger and records

`PENDING_20260922` R23 → abandoned in both tables, §16 superseded banner, new §21; `INDEX.md` hook; INBOX row
✅; the withheld copy carries a RESTORED banner. The memory note and the U17 closure were already written on the
DA side.

---

# Second round, same changelog (author, 2026-09-22) — Fig 6.7 explained against Fig 6.6, the threshold study tabled, the pillars K=1 rows pending, the pillars success metric double-checked

## 5 · Fig 6.7 — "should mention this is after projection … what is different to Fig 6.6?"

Three things now say it. **On the page:** a heading *after projection, tightened constraints*, and the unprojected
MeanFM cells of Fig 6.6 at $\nfe=2$, $10$, $20$ drawn as faint dashed grey rings, so the cost of projecting is read
at the same budget (at $\nfe=20$: from 84 % of the distance closed at 190 ms to 61 % at 266 ms). **In the legend:**
"< 9/10 contexts violation-free" and "MeanFM before projection, same budget", on two rows. **In the prose and
caption:** every point is a plan after projection, dearer and further from the target than its unprojected twin;
what that buys is the constraint — a filled point keeps at least nine of ten contexts violation-free, this task's
counterpart of S&C, where the unprojected plan keeps one or two; only filled points enter the frontier.

## 6 · "Where is the threshold study in V_A?"

It is §6.2.2, the paragraph *Why the activation threshold is not left at its default* (`06_results.tex`, after
`sec:res:aligning:projection`): the $\nfe=100$ comparison of $\eta=0.5$ against $0.1$ — 15,218 ms against
1,195 ms per control step for 6 mm of median distance — and the rule that follows (a roughly constant *number* of
guiding steps: $\eta=0.4$ at 10, $0.2$ at 20, three guiding steps each). It was prose only. **New
`tab:va-threshold`** sets it out across the ladder — $\nfe$, $\eta$, solves $\lceil\eta\nfe\rceil$, guiding steps,
ms/step unprojected and projected, projected median, clean contexts — for MeanFM under `dpcc-r` on the untightened
set (the configuration of the $\nfe=100$ pair), every row recounted from the corpus today:

| $\nfe$ | $\eta$ | solves | guiding | ms unproj. | ms proj. | median | clean |
| --: | --: | --: | --: | --: | --: | --: | --: |
| 2 | 0.5 | 1 | 0 | 28 | 50 | 0.2395 | 3/10 |
| 10 | 0.4 | 4 | 3 | 99 | 383 | 0.0490 | 3/10 |
| 20 | 0.2 | 4 | 3 | 190 | 347 | 0.1245 | 3/10 |
| 100 | 0.1 | 10 | 9 | 925 | 1195 | 0.1876 | 2/10 |
| 100 | 0.5 | 50 | 49 | 892 | 15218 | 0.1816 | 2/10 |

A `\guard` says the table is read for the cost column and not as a budget ranking (the untightened projected
medians are not monotone). A chart was not added: five rows carry it, and the frontier figures already plot cost.

## 7 · Pillars `\hole` → *pending* rows and R32

`tab:uav-pillars` and `tab:uav-pillars-best` now print **MeanFM 1** and **FM 1** as *pending* rows; the `\hole` is
gone and its content is a `\guard` (the earlier `pillars` geometry's K=1 cells are a different constraint set).
**R32** in the ledger: the seven per-step configurations × 10 flights for each of the two models, ~2 h, no training.

## 8 · Table 6.10 — "double check if the success metric is wrong; MeanFM should always be better"

**Checked against the corpus, the analysis code and the evaluator. The printed numbers are right; the criterion is
what the expectation is about.** Every S&C value of the endpoint block reproduces from `per_rollout_detail.csv`
(`n_success_relaxed_and_constraints`: MeanFM 0.50/0.90/0.70/0.70, CI 0.20/0.40/0.40/0.30, FM 1.00/1.00/0.90/0.90),
and `pillars_grid.py` uses that column throughout. The evaluator carries **two** success criteria
(`eval_mix_uav.py` header): `success_relaxed = crossed_line and safe` — the thesis's *success*, the finish line —
and the strict `success = goal_reached and safe`, `GOAL_RADIUS = 0.30`. Under the strict one, with the constraint:

| $\nfe=5$ | S&C (finish line) | goal reached ($\le$ 0.30 m) |
| :-- | --: | --: |
| MeanFM endpoint $r$ | 0.90 | **0.90** |
| MeanFM per-step tight. $c$ | 0.90 | 0.80 |
| FM endpoint single | 1.00 | 0.40 |
| FM per-step $c$ | 1.00 | 0.40 |
| CI-MeanFM endpoint $c$ | 0.40 | 0.10 |
| Diffusion per-step tight. $t$ | 0.30 | 0.00 |

MeanFM's flights that cross the line also settle; six of FM's ten cross and never settle, ending 0.36–0.39 m from
the goal point — just outside the radius — which is what §6.3.1.2 already described flight by flight. So the
author's expectation holds under the strict criterion and not under the finish-line one, and the data is not wrong.
**Done:** the strict criterion is defined in Chapter 5 (`sec:setup:metrics:uav`, a new bullet), printed as a second
block in `tab:uav-pillars-endpoint` and as a *goal* column in both blocks of `tab:uav-pillars-best`, and read in a
new paragraph after the flight-by-flight one. The thesis criterion stays the finish line on every quadrotor scene;
which of the two the task should ask is the author's call, and the paragraph says so.

---

# Third round, same changelog (author, 2026-09-22) — the appendix duplicates, the bold rows, Table 6.5's diffusion rows, the ledger, and the bundle

## 9 · "The gigantic table and duplicate descriptions … Tables 6.1/6.2 — if you think it should be there, don't change anything"

**Kept.** The concise main tables with the every-budget copies in the appendix are the rule the author set at
v3.61 (max aligned budgets in the text, everything in the appendix), and the appendix tables are what the intermediate
budgets are cited from (§6.1.1, `tab:app:avoiding-raw-full`; §6.1.2, `tab:app:avoiding-dpcc-full`). What *was*
duplicated for no reason was the caption text: both appendix captions repeated the protocol, the column definitions
and the marks of their main table word for word. They now say what the copy adds — the intermediate budgets, which
exist for MeanFM and the baseline only, and the pending diffusion $\nfe=2$ cell — and point at the main caption for
the rest. Nothing in the tables changed.

## 10 · "The bold of the arrow and dot pointer rows vanished — bold the line"

`\mainconfig` is `\textbf`, but it was applied cell by cell and many marked rows had it on one cell or none: the
v3.61 restore from git brought back Tables 6.1/6.2 rows without it, the withheld pillars tables predate the
convention, and Table 6.8's rebuild kept it on one line of each pair. A mechanical pass now wraps **every cell of
every row that carries** `\selectedmark` **or** `\baselinemark`, in Chapter 6 and the appendix — 17 rows in the
chapter (Tables 6.1, 6.2, 6.5, 6.6, 6.7, 6.8, 6.10, 6.11, 6.12, 6.13, 6.14) and 6 in the appendix — leaving `---`
cells and `\multicolumn` wrappers intact. Two pillars rows that carried no mark at all get the baseline dot as well
(`tab:uav-pillars`, `tab:uav-pillars-endpoint`), so every table with the diffusion model marks it the same way.

## 11 · Table 6.5 — "delete the diffusion Ks, keep K=20 only"

The three *pending* diffusion rows ($\nfe=1$, $2$, $10$) **and** the diffusion $\nfe=100$ row are gone; the baseline
is printed at the budget it was trained with, as everywhere else in the chapter. Caption, dataref and two §6.2.1.1
paragraphs rewritten (the "all four models measured twice" paragraph is now about the three flow-based models at
$\nfe=100$; the frontier paragraph no longer quotes diffusion at 100). **Fig 6.6 follows the table:** the diffusion
$\nfe=100$ point is dropped from `sources.ALIGNING_REPORTED` (the cell stays registered), the figure rebuilt and
exported, the frontier unchanged, "four of the ten configurations". The $\nfe=100$ diffusion cell (0.1849 m,
1521.9 ms) is recorded in the dataref as existing and deliberately not printed. **R16 narrows** to the six
flow-model evaluations (~2 h); the three diffusion trainings are struck in the ledger.

## 12 · The 22-09 PENDING ledger — "see if it needs update; still dynamic, don't delete"

Updated, nothing removed: a **running addendum** under §0 (*Since the census, later on 2026-09-22*) lists what moved
since the v3.60 state of play — R23 abandoned, R31 opened and R29 superseded, R16 widened then narrowed, R32 opened,
the pillars metric check, the two K=100 cells drawn; R16's rows in §0 and §1 carry the strike-through of the diffusion
arm with the reason; §20 has a narrowing banner. The v3.60 state-of-play text above the addendum is left as written.

## Checks

`python3.14 tools/check.py` (after the third round): 14 files, 6 831 lines, 273 labels, 53 citations, 40 figures —
**all mechanical checks pass**; drafting macros: 16 `\hole`, 3 `\provisional`, 20 `\guard`, 64 `\srcnote`,
53 `\dataref`, **0 `\todofigure`**. Second round: `fig_aligning_projected_tradeoff` rebuilt, inspected, exported.
Third round: `fig_aligning_tradeoff` rebuilt and exported. **Not compiled** — no TeX toolchain here.

**Bundle** (built 2026-09-22 10:19 UTC, closing the update): `export_to_draft.py v3` reported 0 copied, 61 current.
`thesis_v3_20260922_101932_new.zip` (annotated), `thesis_v3_20260922_101932_new_clean.zip` (source notes hidden),
`thesis_v3_20260922_101933_new_nonotes.zip` (source, data and guard notes hidden), 8995 KB each; Chapters 1–4
collapsed at v2.24, cross-references all resolve, **44/44** packaged figures renderable (the pillars path figure
is the 44th), 0 placeholders. `make_bundle.py --verify`: 9 inlined + 4 collapsed sources byte-faithful. These
supersede the v3.62 `094334/094335` bundles.

**Bundle, third round** (built 2026-09-22 10:54 UTC, closing v3.63): `thesis_v3_20260922_105444_new.zip`
(annotated), `thesis_v3_20260922_105445_new_clean.zip` (source notes hidden),
`thesis_v3_20260922_105445_new_nonotes.zip` (source, data and guard notes hidden), 9020 KB each; 44/44 figures
renderable, 0 placeholders, cross-references all resolve, `--verify` byte-faithful. **These supersede the
`101932/101933` set**, which predates the second and third rounds.

## Files

- `v3/chapters/05_setup.tex`, `v3/chapters/06_results.tex`, `v3/withheld/20260918_uav_pillars_section.tex` (banner).
- `DA_in_Paper/plotting/sources.py`; `DA_in_Paper/data/expert_paths.json`; `DA_in_Paper/figures/env/fig_constraints_uav`,
  `fig_expert_uav`; `figures/da/fig_uav_pillars_paths`; `figures/MANIFEST.md`; `v3/figures/` copies, `EXPORTED.md`.
- `data_status/PENDING_20260922_all_lacking_runs.md`, `data_status/INDEX.md`, `cross_draft/INBOX.md`,
  `v3/CHANGELOG.md`, `v3/BRIEFING_20260922_v3.60.md`.

Claude (Fable 5.1, Claude Code) · 2026-09-22 · mechanical checks only. Not compiled.
