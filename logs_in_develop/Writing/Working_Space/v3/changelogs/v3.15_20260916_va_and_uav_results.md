# v3.15 — 2026-09-16 · §6.2 alignment and §6.3 quadrotor rewritten from the 15-09 data

Plan: [`plans/PLAN_20260916_VA_and_UAV_results.md`](../plans/PLAN_20260916_VA_and_UAV_results.md) (first a Codex handover; reassigned).

## How
- Two stdlib scripts in the official store recompute **every number** quoted in §6.2–6.3 from the
  committed 15-09 batches: `DA_in_Paper/analysis/va_results.py`, `uav_results.py`. Both reproduce the
  analyses of record exactly (Gen14 CLOSURE_20260907; Gen15 DA_20260914 corridor v2, CLOSURE_20260910,
  DA_20260912, DA_20260909_T5). `analysis/INDEX.md` rows replaced.
- 15-09 alignment data = the same 14,102 rollouts the closure used: the pending tightened-diffusion run
  has not landed.

## §6.2 Alignment (seed 6, 10 contexts)
- New `tab:va-models`. MeanFlow ahead of diffusion (−0.3744 m, 10/10, p = 0.0020) and of flow matching
  (0.233 m, 9/10, sign p = 0.0215); **flow matching ≈ diffusion** (p = 1.0). Stated on untightened
  geometry — the only one with a diffusion cell.
- New §6.2.3 consistency training: level with MeanFlow at K2 (p = 1.0), behind at K20 (perm. p = 0.0234;
  floor 0.05: 9/10, p = 0.0215), flat in K; mechanism sentence kept.
- `\guard`: the ten contexts are **training** contexts, not held-out — also added to §5.5.2.
- 🔴 Corrected: "the diffusion baseline reaches the same freedom from violations at 2157.9 ms" — false;
  it is violation-free in 0.10 of episodes, 130 violating steps, 21/30 unmoved (untightened only).
- Orientation `\hole` kept and made specific: the angle columns differ between runs for identical contexts.

## §6.3 Quadrotor (seed 6)
- Corridor: new `tab:uav-corridor`; MeanFlow and consistency training 12/12 collision-free and across the
  line, flow matching ≤ 2/12 collision-free, projected diffusion 0/36 across the line. Endpoint projection
  now compared at K5 (two guiding steps); K3 has one.
- Pillars: new `tab:uav-pillars`, 11 full-constraint configurations: MeanFlow 0.618 > flow matching 0.327 >
  consistency training 0.218; MeanFlow ahead in 9/11. The earlier means (0.635/0.359/0.235) included 8
  geo-free ablations. Diffusion reference 0.00 at the goal point.
- 🔴 Corrected: "none of 210 endpoint flights left the constraint set" measured **physical** safety (no
  contact, airborne), and included ablations. Now: physically unsafe 0/120 vs 43/180; flights with
  violations 41/120 vs 89/180.
- New §6.3.3 consistency training: level on corridor, **last on pillars, behind flow matching**.
- S-curve: `tab:uav-scurve` listing; discussion on the tracking controller (same plan: MuJoCo MPC 3/3 vs
  brake-to-rest 0/10, inversion at altitude on 10/10). Endpoint-projection s-curve rows excluded (pre-fix).

## §6.4 and Ch. 8
- 🔴 "MF > FM > diffusion holds … on alignment" — false (FM ≈ diffusion). Now: MeanFlow > {FM, diffusion}
  everywhere; full order on obstacle avoidance and pillars.
- 🔴 "All endpoint-projection results have ≥ 2 guiding steps" — false for corridor K3; table moved to K5.
- `tab:summary-models`, `tab:summary-projection`, RQ1, RQ4 updated to match.

## Checks
`tools/check.py` pass (known v2 Ch. 4 warning only). Bundle `thesis_v3_20260916_100127_new.zip`:
cross-references resolve, 18/18 figures render. Not compiled locally.

## Open — needs the author
- **`d3il_baseline`** in the alignment data = our run of D3IL's image DDPM policy (test split, 60 contexts):
  median final distance 0.44 m from 0.45 m, box never moved in 44 % of 3,884 episodes. §5.3.2 says no
  baseline exists; decide whether to report it.
- Tightened diffusion K20 on alignment (no constraint comparison vs baseline until run).
- Seeds beyond 6 for alignment and quadrotor; held-out alignment contexts.
- The ~119 ms/step MPC overhead is quoted from T5; the committed timing column does not show it.
