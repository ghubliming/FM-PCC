# v3.27 — 2026-09-17 · Chapter 5: platform facts, constraint figure, incentive, metric definition

All twelve Chapter 5 key points of the 2026-09-17 review. Chapter 6 untouched (awaiting its own list).

## Key points

1. **`tab:platforms` execution claim, checked in the code.** The claim was right but overlong: the
   env uses `CartPosQuatImpedenceController` (`IKControllers.py:134–270`) — damped-least-squares
   differential IK with a nullspace term, joint targets into the joint-space tracking controller. The
   row now reads *"inverse kinematics (IK) turns each Cartesian setpoint into joint commands"*, defining the
   abbreviation. Table widened (first column 2.9 cm, `\footnotesize`, tighter padding), so
   "Franka Emika Panda" no longer breaks across lines. v2 asked to add **IK** to the acronym list.
2. **Removed the self-attacking clause** from the X2 row ("intervals of simulated time, not real-time
   rates") — the manipulator row makes no such remark and a platform table is not the place. The row keeps
   the plan interval (every third physics step, 0.03 s) and the 0.01 s controller step; the lock-step fact
   stays in the source note.
3. **Robot name and end effector corrected.** D3IL: *"7DoF Franka Emika Panda"*, and for this task family
   *"the robot is equipped with a cylindrical end effector"* (Sec. 3.3). Both environments load
   `panda_rod_invisible.xml` (rod radius 0.01 m, length 0.30 m). The previous "parallel two-finger gripper"
   was the gripper model these tasks do not use. Sent to v2.
4. **§5.2.1** now cites `\parencite[Sec.~6.1]{romer2025diffusion}` where it says the geometries are DPCC's.
5. **§5.2.2 incentive added.** Obstacle avoidance is close to solved (0.95 from the state in D3IL's own
   table); this environment raises the plan to three dimensions, replaces the state by two camera views, and
   is a task D3IL's best image policy finishes in roughly a third of episodes — so the projection is tested
   where only some episodes finish. Points forward to §5.4.2.
6. **Fig 5.5 reframed.** The in-hand tile cut the box at the corner. All three recorded expert rollouts were
   ranked by the box's distance from the tile centre and its clearance from the border; `expert_rollout_1.gif`
   frame 163 is the only instant where the box is complete and centred. Both tiles now come from it (same
   instant preserved), caption and source note updated.
7. **`tab:uav-scenes` is now a constraint table**, in the two forms D3IL-avoiding uses: halfspaces and
   circular keep-out regions. The workspace box is gone from the table and the text (a recording guard, not
   a task constraint); the last row states the 0.31 m inflation and the 0.025 m tightening.
8. **New Fig 5.7 `fig:constraints-uav`** — the three aerial constraint sets from above, the companion of
   `fig:constraints-avoiding`: obstacle, the band added by the vehicle radius, dashed tightened boundary,
   and walls acting only over their own span of $x$. Built from
   `sources.UAV_CONSTRAINTS` (transcribed from `config/uav_projection.yaml`) by
   `builders/scenes.py::fig_constraints_uav`; SVG + PNG, exported to v3, inspected.
9. **§5.4.2 carries D3IL's own numbers**: from images 0.359 / 0.316 / 0.278, from the state 0.891, and
   obstacle avoidance from the state 0.950 (Table 3). `tab:va-vs-d3il` gains a *Reported result* row and a
   `\FloatBarrier` so it stays inside §5.4.2.
10. **Deleted** the stray "As on D3IL-aligning, the baseline is therefore …" sentence from §5.4.3; the UAV
    baseline is stated in §5.4 and §5.6.3.
11. **§5.5.1 metric defined, caveat removed.** The added metric is *the time to compute one action*, the
    quantity DPCC states for its own method; it covers generating the $B$ candidate plans, projecting them
    and selecting one (the timer wraps the policy call, `scripts/eval.py:348–350`), and excludes the
    simulator step and the low-level execution. No real-time wording. The caveat went to v4 for Ch 7.
12. **§5.6.4 shared-node paragraph moved to the appendix** (`app:repro`), leaving one sentence pointing
    there. v4 notified.

## Verification
- `tools/check.py` passes: 23 figures now referenced, no dangling references or missing figure targets.
- New figure rendered and inspected; `export_to_draft.py v3` copied `fig_constraints_uav` and the two
  refreshed camera tiles. `MANIFEST.md` and `EXPORTED.md` regenerated by the tools.
- Bundles rebuilt (annotated and `--clean-notes`). **Not compiled** — no TeX toolchain in this container.

## Left open
- `\hole`s untouched by this pass: plan smoothness, alignment orientation, DPCC-protocol rows, GPU-hours.
