# v3.25 — 2026-09-17 · thesis-style visuals, Chapter 5 audit, and clean review bundle

## Previous-work audit

- Confirmed that v3-owned prose uses the approved mechanism names, while every figure and table uses
  **Diffusion**, **FM**, **MeanFM** and **CI-MeanFM**. The canonical translation table retains the strict
  rule that the published brands appear only in Related Work; artefact names remain only in explicit
  provenance/source notes.
- Confirmed the earlier compact-float work and the wrapped Chapter 6 tables pass the mechanical width
  checker. Its two remaining width warnings are in inherited Chapter 4 and are already assigned to v2.
- Replaced one residual Chapter 6 phrase, “consistency training,” with “the consistency-interpolated
  objective.”

## Chapter 5

- Rewrote the opening to define the two evaluation phases locally instead of referring backwards to
  Section 1.2. It also maps the descriptive task titles to **D3IL-avoiding**, **D3IL-aligning**,
  **UAV-corridor**, **UAV-pillars**, and **UAV-s-curve** once at chapter entry.
- Kept MuJoCo explicit as the common simulator, D3IL as the Panda task integration, and MuJoCo
  Menagerie/MuJoCo MPC as the quadrotor model/task lineage.
- Filled the alignment workspace box from `config/visual_aligning_eval.yaml`: nominal
  $x\in[0.20,0.80]$ m, $y\in[-0.45,0.45]$ m, $z\in[0.02,0.50]$ m; after the configured 0.03 m
  inward tightening, $x\in[0.23,0.77]$ m, $y\in[-0.42,0.42]$ m, $z\in[0.05,0.47]$ m.
- Made the quadrotor-demonstration text state that Chapter 4 owns the construction and mathematics,
  while Table 5.4 owns only the numerical and scene-specific parameters. Audited v2.17 and confirmed
  that its method already gives the waypoint/arc construction, cosine profile and derivatives,
  controller execution, rejection rule, and recorded observation/action pair.
- Put Table 5.7 on a dedicated float page and relaxed its row spacing so its multi-line cells remain
  readable instead of being compressed into the earlier layout.

## Chapter 6 visuals and emphasis

- Rebuilt Figures 6.1, 6.2, 6.4 and 6.5 from the canonical `DA_in_Paper` plotting sources. Removed
  the presentation-style explanatory sidebars and secondary legend prose; the plots now contain only
  the data, axes, compact model/rule labels and necessary data labels. The removed interpretation is
  retained in the captions and surrounding thesis prose.
- Replaced the bright model palette with a restrained dark-slate family. The projection comparison
  uses the same monochrome/dark constraint vocabulary as the environment diagrams.
- Strengthened the main-configuration cue in Tables 6.1–6.4 without relying on colour: selected rows
  now carry a bold black triangle in addition to bold text/numbers, and each caption defines the mark.

## Cross-draft and bundle support

- Added `cross_draft/to_v2/FROM_v3_20260917_v3.25_ch5_backrefs_and_uav_method.md`, dated and tagged
  v3.25. It asks Section 1.2 to point forward to Chapter 5 and records that no new UAV-method prose is
  needed in v2. Added the matching dated/versioned inbox row.
- Added `bundle/make_bundle.py --clean-notes`. The normal bundle remains annotated; clean mode injects
  the existing supported `\submissiontrue` switch before `\begin{document}`, hiding only `\srcnote`.
  Holes and other drafting diagnostics remain visible. The bundle README documents both the command
  and the equivalent manual Overleaf switch.

## Verification and outputs

- Rebuilt and visually inspected the four avoiding figures, then exported the canonical figure store
  to v3. All 22 figures have renderable assets.
- `python3 tools/check.py` passes: 14 files, 4,389 lines, 230 labels, 52 citations and 22 figures. The
  two inherited Chapter 4 width warnings remain. The draft still contains drafting macros by design.
- Annotated v3-only bundle:
  `thesis_v3_20260917_144454_new.tex/.zip`.
- Clean-source-note v3-only bundle:
  `thesis_v3_20260917_144454_new_clean.tex/.zip`.
- Both bundles contain 22/22 rendered figures, resolve all retained cross-references, and verify
  byte-faithful for all nine inlined plus four collapsed sources. The clean bundle alone contains an
  active `\submissiontrue` line.
- Not compiled because this container has no TeX toolchain; the checks above are structural and the
  charts were inspected as generated PNG previews.
