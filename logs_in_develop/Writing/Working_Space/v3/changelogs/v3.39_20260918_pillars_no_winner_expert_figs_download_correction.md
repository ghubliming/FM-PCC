# v3.39 — 2026-09-18 · UAV-pillars loses its winner, the expert figures move together, and a wrong claim about Fig 6.3 is withdrawn

Four items. Two of them correct something I got wrong.

---

## 1. The three expert-versus-constraint figures now sit together

`fig:expert-aligning` and `fig:expert-uav` were added to the datasets section in v3.36, but the
equivalent for the benchmark — the 96 demonstrations over the three geometries — was still back in the
task section, where it had been since v3.9. It is now the first of the three, immediately under the
sentence that counts them (0, 1 and 2 of 96 satisfy the geometries), so the three environments make the
same statement in the same place: **Figure 5.9** (avoiding), **5.10** (alignment), **5.11** (quadrotor).
Its caption now names the other two as its companions and ends on why the count matters — satisfying
those constraints can only be the projection's doing. The geometry table stays in the task section and
the figure is referenced forward from there.

## 2. `fig:expert-uav`: the vehicle put where it says something

Two fixes to where the X2 is drawn.

**The s-curve vehicle was at the arena edge.** The whole straight run down the first passage ties at
$0.45$\,m clearance, so the plain argmin landed on the first sample of the route. Ties are now broken
**toward the obstacle**, and the vehicle sits at $(-0.50,\,-0.80)$ — the corner where the route leaves
the passage and starts the crossing, which is the moment worth seeing. The tie is exact there: the wall
and the corner cap are both $0.45$\,m away.

**The three corridor vehicles were one blob.** All three lanes are tightest at the same $x\approx2.0$.
Each vehicle now goes to the tightest moment **of its own route that is clear of the vehicles already
drawn** (by 3.4 vehicle radii), so they spread along the stretch where every lane is inside the slide.
A violating route is never moved to a moment where it does not violate, so a red path never carries a
green vehicle. The extract keeps the full clearance ordering (`tight_order`) for this.

## 3. Fig 6.3 — I was wrong: four of the five panels are a download

The previous hole said all five missing panels needed cluster evaluations and "cannot be recovered from
any artefact on disk". That confused *not on this container* with *never run*. The batch's own
`candidates_detailed.csv` lists the evaluations:

| panel | run folder | seeds |
| :-- | :-- | :-- |
| FM $\nfe=1$ | `…/flow_matching_v3_ode_selectable/H8_Dmodels.diffusion.FlowMatchingODE_a1.5_b1.0_aw10/H8_K1_Meuler_T0.5_…_msg20trials` | 6–10 |
| FM $\nfe=2$ | same model folder, `H8_K2_…_msg20trials` | 6–10 |
| CI-MeanFM $\nfe=1$ | `…AlphaFlowODE_msgafon02_s6`, K1 | 6 |
| CI-MeanFM $\nfe=2$ | same, K2 | 6 |

Those four are a **fetch** of the dashboard the eval writes as it runs, at the crop the existing three
use. The fifth, **diffusion at $\nfe=2$, cannot exist**: every `GaussianDiffusion` folder in the batch is
`K20`, because the step count of a diffusion model is fixed when its noise schedule is discretised at
training time. That cell stays empty rather than mixing training budgets across a row.

Corrected in three places: the `\hole` and caption of `fig:raw-plans`, a correction block at the top of
`REQUEST_20260916_cluster_fm_plan_panels.md` with the folder paths, and ledger row R5.

## 4. UAV-pillars: no winner is claimed, and the baseline is compared on equal terms

v3.36 split the two projection blocks in `tab:uav-pillars` and `tab:uav-pillars-endpoint`, but left
`tab:uav-pillars-best` as it was: **one best-of-eleven row per model, with a triangle on FM at $1.00$**.
That table was the flaw. The diffusion baseline has **no endpoint row on this scene**, so a best-of-eleven
column compared three models on eleven configurations against a baseline that had five — and then marked
the winner of that comparison.

`tab:uav-pillars-best` is rebuilt as **the best configuration within each block, at every budget each
model was evaluated at** (`analysis/pillars_grid.py::best_per_block`, new this pass):

| model | $\nfe$ | best per-step | S&C | best endpoint | S&C |
| :-- | --: | :-- | --: | :-- | --: |
| MeanFM | 2 | $r$ | 0.70 | — | — |
| MeanFM | 5 | tight., $c$ | 0.90 | $r$ | 0.90 |
| CI-MeanFM | 1 | tight., $t$ | 0.70 | — | — |
| CI-MeanFM | 2 | $t$ | 0.50 | — | — |
| CI-MeanFM | 5 | tight., $t$ | 0.90 | $c$ | 0.40 |
| FM | 2 | $r$ | 0.50 | — | — |
| FM | 5 | $c$ | 1.00 | single | 1.00 |
| Diffusion | 20 | tight., $t$ | 0.30 | — | — |

**No configuration is marked as selected any more, and the text says the scene supports none.** What it
supports: all three flow models beat the baseline in every cell; at $\nfe=5$ FM reaches $1.00$ in both
blocks and MeanFM $0.90$ in both; over the seven per-step configurations the two share they are level at
$0.586$; at $\nfe=2$ MeanFM is ahead, $0.70$ against $0.50$. CI-MeanFM is the one model whose endpoint
block is much worse than its per-step block, $0.40$ against $0.90$.

Carried through the rest of the chapter: the UAV conclusion now says UAV-pillars "carries no selected
model–projection combination, and none is claimed for it"; `tab:summary-models` reads *not ordered*;
`tab:summary-combinations` reads *none selected*; and the projection summary says endpoint projection
matches the per-step result of FM and MeanFM at a fifteenth of the time **and that CI-MeanFM is the
exception**. `fig:uav-pillars-paths`'s caption says which of the two bests each panel is drawn at.

**On the geometry question.** `pillars_hg` is the right constraint set and the only active one
(`config/uav_projection.yaml`: `active_geo_variants`, and the `_hg` entries are the corrected "honest
geometry" of U7 — walls limited to the x-range they occupy, the invented workspace box widened, the
arbitrary 2 cm planner pad dropped). The older `pillars` entry has more budgets, which is why more
candidates exist under it, but it is a different constraint set kept for rollback; pooling the two would
invent a budget ladder that was never flown against these constraints. MeanFM and FM at $\nfe=1$ exist
only there, which is why this table has no $\nfe=1$ row for them and says so.

## Checks

`python3 tools/check.py`: 13 files, 245 labels, 53 citations, 30 figures; hole 21, guard 12,
todofigure 5. All mechanical checks pass. PNG companions refreshed and the export's staleness guard
clean. **Not compiled — there is no TeX toolchain in this container.** Nothing committed.
