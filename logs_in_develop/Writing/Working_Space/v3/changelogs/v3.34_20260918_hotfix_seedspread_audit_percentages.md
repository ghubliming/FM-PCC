# v3.34 — 2026-09-18 · hotfix: the seed table moved to the results, a cell-by-cell data audit, mean percentages

## Key points

1. **`tab:seed-spread` left the evaluation-parameter section.** Seed-to-seed means and standard deviations
   are not evaluation parameters. The table and its two sentences now sit in §6.1.1.2, next to the comparison
   they qualify, where Chapter 6 already referenced them. §5.6.3 keeps one plain sentence: D3IL-avoiding is
   evaluated over five seeds, so a result there carries the across-seed variation; how much it varies is
   reported with the results.
2. **A cell-by-cell audit of every committed batch**, written up as
   `data_status/PENDING_20260918_verified_data_audit.md`. It records what was checked (the three 15-09
   batches enumerated by model × budget × backbone × projection variant, the `temp/` tree, the curated
   archive, the Slurm log directories), **what turned out to exist after all** (the four recoveries of
   v3.28--v3.32, so they are never re-requested), and **what is confirmed absent**. It also records that the
   newest data is the 15-09 checkpoint and the newest log directory `2026-09-15`, so **every open row of the
   earlier ledgers is still genuinely open** — nothing is sitting unread. Three ledger rows were added,
   **R20--R22**, for the quadrotor gaps below.
3. **`tab:va-models` carries the percentage for the mean as well as the median**, both as the share of the
   initial $0.4530$\,m closed. MeanFM at $\nfe=20$ now reads $0.0741$ (84\,\%) and $0.0933$ (79\,\%) against
   the baseline's $0.4140$ (9\,\%) and $0.3901$ (14\,\%).
4. **The quadrotor endpoint-projection matrix is the largest real gap** (answer to the fourth question,
   also in the audit file): UAV-corridor has endpoint projection only as a single candidate and under
   temporal consistency, and only at $\nfe=3,5$; UAV-pillars has all four endpoint configurations at
   $\nfe=5$ only; UAV-s-curve's endpoint rows predate the switched-wall fix and are excluded; and the
   diffusion baseline has **no endpoint row on any scene** and no per-step random-selection row on pillars.

## Verification
- `check.py` passes; the moved table keeps its label, and both references to it still resolve.
- Bundles rebuilt. **Not compiled** — no TeX toolchain here.
