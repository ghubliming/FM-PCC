# v3.58 — 2026-09-21 · Chapter 5 protocol, tensor layouts and sampling laws

## Changes

- Table 5.10 keeps the measured twelve UAV-corridor flights: the evaluation used four on each of three routes, so taking the first ten would unbalance the routes and require recomputing Chapter 6. The main obstacle-avoidance row now states only DPCC's five-seed, two-episode protocol; the twenty-episode extension remains in the appendix and is linked from Chapter 5 prose.
- Figure 5.6's caption says the lower plan views show only $x$ and $y$ and omit altitude $z$; the upper MuJoCo renders remain three-dimensional.
- Table 5.8 and its adjacent paragraph now give the actual input and output tensor shapes for all three environments. UAV plan width is corrected from 9 to 12, consistent with Chapter 4's `tab:embodiments` and the curated dataset schema. The two visual inputs each have one RGB frame per camera; the model's visual latent is 128-wide.
- Table 5.9 and adjacent prose distinguish the projected position coordinates: 2D $x,y$ in D3IL-avoiding, 3D $x,y,z$ in D3IL-aligning and the UAV scenes.
- Appendix `app:training-time-laws` gives the mathematical definitions of discrete diffusion-index sampling, FM's **reflected** Beta draw, and MeanFM/CI-MeanFM's two-draw logit-normal pair with the half-batch zero-length interval. Table 5.8 points to it. The earlier v3.47-to-v2 note's unreflected Beta density was wrong; a correction note supersedes it. A v4 note records the author's instruction to retain the appendix section.
- Two v3-only PDF cross-references to the collapsed Chapter 4 equation `eq:method:dep:vdes` now use its surviving section label; the local reference to `eq:method:degen:ngen` likewise uses its section. The newly added plan-layout link also uses the Method section label rather than its collapsed table label. These equation references were the source of the PDF's `??`, not editorial placeholders.

## Evidence

- UAV-corridor: `logs_in_develop/Gen15/U16/DA_20260914_corridor_v2_paper_full.md` §1 records seed 6, twelve trials per cell, four per route and the verified `n_trials=12` logs.
- Tensor layouts: `mix_visual_aligning/datasets/sequence.py:122--138`, `mix_visual_aligning/models/visual_fm_diffusion.py:58--64`, `mix_visual_aligning/models/visual_unet.py:23--46,106--118`, and `uav_expert_data_collect/curate_dataset.py:60`. Chapter 4 `tab:embodiments` already lists the channel widths.
- Time draws: `mix_visual_aligning/models/diffusion.py:299--301`, `fm_diffusion.py:300--307`, `mf_diffusion.py:353--368,423--428`, `af_diffusion.py:486--505,691--696`; selected parameters in `config/avoiding-d3il.py:895--897,969,1026--1028` and analogous visual/UAV configs.
- The projected coordinate dimensions follow `v2/thesis_v2.tex`, `tab:embodiments`, and the per-environment projection configurations. No evaluation number was changed.

## Checks and handover

- `python3 tools/check.py`: 14 files, 5981 lines, 264 labels, 39 figures; all mechanical checks pass. `git diff --check` found no whitespace errors. No TeX toolchain is available; **not compiled**.
- v2 handover: `cross_draft/to_v2/FROM_v3_20260921_v3.58_shapes_and_reflected_time.md` — Chapter 4's $(H+1)d$ plan notation disagrees with the implemented $(N,H,d)$ windows; add the exact tensor layouts and use the corrected time-axis law.
- v4 handover: `cross_draft/to_v4/FROM_v3_20260921_v3.58_training_time_appendix.md` — preserve `app:training-time-laws` when the appendix moves to v4.
- Final v3-only bundles: `thesis_v3_20260921_150750_new.zip` (annotated), `thesis_v3_20260921_150753_new_clean.zip` (source notes hidden), and `thesis_v3_20260921_150756_new_nonotes.zip` (source, data and guard notes hidden). All report zero unresolved cross-references and 42 renderable figures. `make_bundle.py --verify` confirms the last bundle byte-faithful (9 inlined + 4 collapsed sources). Preliminary bundles from this pass are superseded.

**Written by:** Codex GPT-6 Astra · 2026-09-21. **Not compiled.**
