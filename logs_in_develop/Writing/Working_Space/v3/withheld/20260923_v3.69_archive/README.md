# withheld / 20260923_v3.69_archive

- `fewstep_k_ladder_v3.68.tex` — §6.1.2.5 *Step Budget* as it stood at v3.68 with `fig_avoiding_k_ladder`
  (the K ladder drawn on ragged budgets: MeanFM {1,2,5,10}, CI-MeanFM {1,2}, FM {1,2,20}, diffusion
  {1,2,10,20}). Withdrawn by the author on 2026-09-23 ("a figure must be accurate; show the full table and
  run what is lacking"). Replaced by `tab:k-ladder` (full grid, pending cells = R41). The figure itself
  stays in the store (`figures/da/fig_avoiding_k_ladder.svg`) and is not referenced by the draft.
- §6.1.2.3 *The Executed Paths* and Fig 6.7 *D3IL-aligning executed paths* were **moved**, not archived:
  appendix `app:avoiding-paths` and `app:aligning-paths` (v3.69).
