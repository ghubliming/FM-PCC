# v5.8 — 2026-09-25 · ROUND2 FIX: the twenty DO items of the round-2 answer applied in v5 (Orchestra job O023)

**Source:** the second reading of v5.7 — `RELEASE/output/20260925_212528_thesis_release_ORCH_v5.7_DATA_FIX/feedback/ROUND2_FEEDBACK_TO_RESPONSE_AND_V5.7_20260925.md`
(§4 the closing pass, §6 the deeper recheck) — as answered in `feedback/CLAUDE_ANSWER_to_ROUND2_20260925_2157_v5.7.md`, list R2-A. **Kind: ROUND2 FIX** —
factual and consistency corrections; six undo regressions of v5.5–v5.7; no number of record, table cell or figure changed.

**Asked (author, 2026-09-25):** "ship. and v5 changelog and realse".

## The twenty items

| item | file | what the text says now |
| :-- | :-- | :-- |
| 6A (mine, v5.6) | `05` §5.5.3 | "On UAV-corridor and UAV-s-curve the cascaded geometric controller receives the accumulated position setpoint, with zero velocity and acceleration feed-forward and a zero yaw reference, in every evaluation." — the reported flights ran the `pid_stopgo` tracker (`v_des = 0`); Ch 4 §4.3.5 was right |
| 4.2a (mine, v5.5) | `05` §5.5 | "so that the vehicle's per-axis reach, not only its centre point, clears it (the directional limit is stated in §4.6.3)" |
| 4.2b | `08` §8.1 | "On the manipulator the guarantee is conditional: the tightening absorbs the mismatch only while the one-step change of the tracking error stays within the configured margin, which was not calibrated here (§5.5)" |
| 4.3 | `06` §6.2.2; `07` ×2 | "Including the same guarded context preserves the ordering of violation-free counts; the reported distances and timings also include that context."; "one of them held by the guard"; "all ten (one held by the guard) against nine" |
| 4.4a (mine, v5.6) | `06` §6.2.2 | "because the selected model is run at its $\nfe=20$ operating point and the threshold study has already priced the other budgets" |
| 4.4b (mine, v5.6) | `08` §8.3 | "the constraints that are in the program at every replan earlier; the span-switched ones — the walls and the hump's two halves — enter by the vehicle's current position, …" |
| 4.4c | `06` ×6 | "$\nfe=20$" / "sampling steps" in the six endpoint sentences that said "twenty evaluations" |
| 4.5 | `06` §6.2.1 | "a task that rewards more evaluations would expose it, and a task already saturated at one evaluation would not" |
| 6B | `06` §6.3.3 | "a tenth of the whole loop unprojected." (the projected share deleted) |
| 6C | `05` §5.5 | "the goal line crossed, the box delivered — or when the episode limit is reached; on the quadrotor scenes crossing the finish line is latched and the flight continues to its goal point, the limit or the abort (§5.4.3)" |
| 6D-1 | `06` §6.1.2 | "every flow-based row at its operating budget is cheaper than every diffusion row at equal success with constraint satisfaction" |
| 6D-2 | `07` RQ1 | "where the diffusion model trained and run at one denoising step loses it" |
| 6D-3 | `06` §6.2.1; `07` | "and it moves the box in every context" / "moves the box in every context and reduces …" |
| 6D-4 | `07` RQ1 | "…the consistency-interpolated model on six and the analytic one on three." |
| 6E | `05` §5.4.1, §5.4.3; `06` Table 6.11 | "The steps are the length of an episode in control steps, averaged over every episode of the cell whatever its outcome; an episode ended by a collision or by the limit contributes the step at which it ended, and UAV-pillars follows the same convention."; "on UAV-corridor the steps to the end of the corridor, over the flights that reach it"; caption "averaged over the flights that reach it (---: none does)" |
| 6F-1 (Ch 7 clause mine, v5.6) | `06` §6.1.3; `07` | "non-dominated when no other configuration in the comparison is at least as fast per action and shorter in control steps; dominance refers to the reported means on the evaluated configurations"; "equal success with constraint satisfaction, fewer control steps and less time per action, the sense of §6.1.3" |
| 6F-2 | `05` §5.6.4 | "for $\eta > 0$, as described in §4.5.4, whose (guiding-step count) is the general form" |
| 6F-3 | `06` §6.1.1 | "None of them arrives legally as a rule." |
| 6F-4 (mine, v5.5) | `06` Table 6.14's caption | "among the rules with the fewest aborts random selection has the fewest violating steps" |
| D4 | `06` §6.1.2 | "Endpoint non-convergence was uncommon: …" |

## Every edit of Chapters 7 and 8 (before → after; the Chapter 5 and 6 edits are the sentences above)

| tag | before | after |
| :-- | :-- | :-- |
| 4.3 guard (Ch 7 Summary) | constraint, keeping every context violation-free at the price of per-step projection. | constraint, keeping every context violation-free, one of them held by the guard, at the price of per-step projection. |
| 4.3 guard (RQ2) | average-velocity matching under random selection all ten against nine at $275$ against | average-velocity matching under random selection all ten (one held by the guard) against nine at $275$ against |
| 6D-2 separately trained checkpoint | where the diffusion model, ⏎     run away from its trained budget, loses it | where the diffusion model ⏎     trained and run at one denoising step loses it |
| 6D-3 "only" removed (Ch 7) | analytic average-velocity matching is the only model that moves the box in ⏎ every context, and it reduces the median final distance | analytic average-velocity matching moves the box in ⏎ every context and reduces the median final distance |
| 6D-4 dangling phrase deleted | the analytic one on three, with the ⏎     fewest violating steps. | the analytic one on three. |
| 6F-1 Ch 7 gloss (the v5.6 clause replaced) | Pareto-dominates --- ahead on every reported axis at once, the sense of ⏎ \autoref{sec:res:avoiding:pareto} --- its | Pareto-dominates --- equal success with constraint satisfaction, fewer control steps and less time per action, the ⏎ sense of \autoref{sec:res:avoiding:pareto}  |
| 4.2b conditional guarantee (Ch 8) | On the manipulator this holds, because the end effector ⏎ tracks its setpoint within the margin; | On the manipulator the guarantee is conditional: the tightening ⏎ absorbs the mismatch only while the one-step change of the tracking error stays within the con |
| 4.4b horizon item (the v5.6 list replaced) | projector the always-active constraints earlier --- the disks, the box, the tilt and the hump; the switched ⏎     walls of \eqref{eq:method:env:switched} enter  | projector the constraints that are in the program at every replan earlier; the span-switched ones --- the walls ⏎     and the hump's two halves, \eqref{eq:metho |

## Checked

- `tools/check.py`: 17 files, 7802 lines, 277 labels, 53 of 53 citations, 41 figures — all mechanical checks pass.
- `tools/make_release_v5.py --dry-run`: 19 + 48 files, 9 holes, 1 finding (`\getDoctype`), 4 residue hits.
- 6A against the campaign drivers (`Slurm_Codes/temp_bash/eval_20260923_p23_corridor_v3.sh:246`, `fetch_20260924_p23cv3_corridor_paths.sh:51–58`: run
  folders `…_pid_stopgo_…`), the evaluation (`FM_v3_uav_test/eval_fm_uav.py:1161–1162`, `mix_uav_test/eval_mix_uav.py:1895`: `pid_stopgo → v_des = 0`)
  and the controller (`uav_env_test/flight_controller.py:79–91`: `a_des`, `yaw_des` zero by default). 6E against
  `FM_v3_meanflow_test/eval_flow_matching_v3_meanflow.py:745–748`, `DA_in_Paper/analysis/avoiding_rules_by_protocol.py:90–95` and
  `plotting/extract/corridor_v3_frontier.py:15, 44–57`. 6F-1 against `plotting/builders/frontier.py:77–83`.
- **Not compiled.** Nothing committed.

## Release

`RELEASE/output/20260925_220823_thesis_release_ORCH_v5.8_ROUND2_FIX/` — job O024, tag `ROUND2_FIX`, two notes in `RELEASE/CHANGELOG.md`. Page estimate ~179 (152-215). `diff -rq` against the v5.7 build: 4 file(s) differ — `chapters/05_setup.tex`, `chapters/06_results.tex`, `chapters/07_conclusion.tex`, `chapters/08_discussion.tex`; no file only on one side.

## Left open

- R2-B (the abstract, the corridor wording, the two optional cuts) and R2-C (the `10/10` cells) — the author's word each; the ⛔ rows and 🟡
  remainders of the round-one answer's C; the held B-i rows.

Signed: Orchestra (Claude Fable 5.1, Claude Code), job O023 · 2026-09-25 · not compiled · nothing committed.
