# v5.6 — 2026-09-25 · FACTUAL CHANGE: the conservative C pass of the review answer applied in v5 (Orchestra job O017)

**Source:** the first-reading audit of the golden release (`RELEASE/output/20260925_115125_…_GOLDEN_TEMPLATE/feedback/THESIS_FIRST_READING_REVIEW.md`,
§1 C1–C12, §2 M1–M9), sorted, risk-tagged and given conservative verdicts in `feedback/CLAUDE_ANSWER_…md` §9 C (jobs O009, O016).
**Kind: FACTUAL CHANGE** — what the thesis states changes in the places below; every change is a correction against the code or the thesis's
own tables and rules, or a qualifier; no number of record, table cell or figure changed.

**Asked (author, 2026-09-25):** "Go new changelog for V5 and release, mark as Factual Change" · "remember new column mark the Part C done/not".

## Row by row

| row | done | what changed | not done |
| :-- | :-- | :-- | :-- |
| Ci-1 | ✅ | §4.3.4: the residual is not $w_t$; $w_t$ on the measured channels is the change of the tracking error over one step, $e_t - e_{t+1}$ (the setpoint accumulates the increments) | — |
| Ci-2 | ✅ | §4.4.3: at $r=\ftime$ the *target* reduces to the instantaneous-velocity target of the FM loss; the loss keeps its two heads and the reweighting | — |
| Ci-3 | ✅ | §4.6: the keep-out row on the horizontal components $\vect{p}_i^{xy}$, a vertical cylinder | — |
| Ci-4 | ✅ | §6.2.1 and §6.2.3: "does not improve" scoped to two–twenty evaluations, "only at a hundred does it" (Table 6.5); "Raising the budget to twenty …" | — |
| Ci-5 | ✅ | §6.2.1: the seven / two counts (no evidence of record) deleted; "The level result at $\nfe=2$ is in distance only" stays | the recount (D1) |
| Ci-6 | ✅ | §6.2.2: "the model that comes closest to the target does so at twenty" for "solves the task" | — |
| Ci-7 | ✅ minimum | §6.2.1, §6.2.3, §6.4 finding 2: the stop-gradient mechanism is "an account … consistent with the ladder and not tested here"; B-i-4 with it | moving it to Ch 8 |
| Cii-1 | ✅ in part | Ch 7 Summary and RQ1: "on ten training-set contexts"; Ch 8 Limitations: the contexts are drawn from the training set, no held-out context evaluated; §1.4 carries no result narrative since v5.5 | the abstract |
| Cii-2 | ✅ | §5.3: the held-out tenth is cut with stride one, shares demonstrations, selects the checkpoint only | — |
| Cii-3 | ✅ in part | §5.5.2: the context-7 guard stated before the tables; the captions of Tables 6.8, B.4a, B.4b say "nine attempted" | x/9 in the rows; the recount (D2) |
| Cii-4 | ✅ | four places: "controller-and-simulator cost" | — |
| Cii-5 | ✅ in part | §4.6.3: the reach is a per-axis bound, exact on the walls, less on a diagonal approach to a disk; Fig. A.1's caption: the enclosing radius ≈ 0.36 m from the drawn dimensions, the radial reach UAV-pillars uses | the §5.6.3 "4.8 cm past a nominal disk" sentence |
| Cii-6 | ✅ | §4.2 overview, §4.5.2, §5.5: $\gamma$ is DPCC's estimate taken over as a configured margin; 0.025 m DPCC's, 0.03 m chosen; neither calibrated here | — |
| Cii-7 | ✅ | §4.6.3: the measured velocity is a predicted channel; no dynamics row constrains it; the mismatch ball is a ball in position | — |
| Cii-8 | ✅ | §8.3: a longer horizon helps the always-active constraints; the switched walls need a rule on the predicted position | — |
| Cii-9 | ✅ in part | §5.4.4: "the larger of the two spreads … therefore the conservative one" deleted | the "fronts of observed means" re-wording |
| Cii-10 | ✅ | §5.5: the best rule is chosen on the evaluated grid for every model alike; a selected configuration is the best evaluated cell | — |
| Cii-11 | ⛔ | — | the goal column of Table 6.12 |
| Cii-12 | ✅ one phrase | Ch 7 Summary: "the objectives cannot be told apart and the budget decides" | the causal rewrite |
| Cii-13 | ✅ | RQ1: "and each objective trained as Table 5.8 states it" | — |
| Cii-14 | ⛔ | — | the K / NFE sweep |
| Cii-15 | ✅ | §4.5.1: SLSQP, at most 1000 iterations, function tolerance $10^{-6}$; §5.5.3: zero acceleration feed-forward and zero yaw reference on UAV-corridor / UAV-s-curve | — |
| Cii-16 | ✅ Ch 2 | §2.2: "fixed when training ends in the inherited implementation" | the abstract |
| Cii-17 | ⛔ | — | the Ch 8 paragraph, the Table C.1 row |

## Every edit (before → after)

| tag | before | after |
| :-- | :-- | :-- |
| Cii-16 inherited implementation | so it is fixed when training ends; fewer steps require | so it is fixed when training ends in the inherited implementation; fewer steps require |
| Ci-1 M2 residual -> change of the residual | times per step, so the arm does not exactly reach $\vect{p}\sidx{des}$ within one step --- the residual ⏎ is precisely the $\vect{w}_{t}$ that \eqref{eq:method:dpcc:euler… | times per step, so the arm does not exactly reach $\vect{p}\sidx{des}$ within one step. Because the setpoint ⏎ accumulates the planned increments, the mismatch $\vect{w}_… |
| Ci-2 M8 target, not objective | reduces to $\afield\sidx{tgt} = \vect{v}$ and the objective ⏎     becomes \eqref{eq:bg:fm:loss}. These anchor the field. | reduces to $\afield\sidx{tgt} = \vect{v}$ and the target ⏎     is the instantaneous-velocity target of \eqref{eq:bg:fm:loss}, while the loss keeps its two heads and the ⏎… |
| Ci-3 M5 cylinder | \begin{equation}\label{eq:method:env:disk} ⏎   \left\\|\vect{p}_{i}-\vect{o}\right\\|_{2}^{2} \ge \rho^{2}, ⏎ \end{equation} ⏎ which makes the feasible set non-convex. | \begin{equation}\label{eq:method:env:disk} ⏎   \left\\|\vect{p}_{i}^{xy}-\vect{o}\right\\|_{2}^{2} \ge \rho^{2}, ⏎ \end{equation} ⏎ with $\vect{p}_{i}^{xy}$ the horizonta… |
| Cii-6 M3 overview | mismatch $\vect{w}_{t}$, whose bound $\gamma$ is estimated from unconstrained rollouts, and the | mismatch $\vect{w}_{t}$, whose bound $\gamma$ \ac{DPCC} estimates from unconstrained rollouts and this ⏎     thesis takes over as a configured margin (\autoref{sec:setup:… |
| Cii-6 M3 tightening section | Here $\gamma$ is estimated from unconstrained rollouts. | In \ac{DPCC} $\gamma$ is estimated from unconstrained rollouts; here it is not re-estimated, and the margins ⏎ actually configured are stated in \autoref{sec:setup:protoc… |
| Cii-15 M9 solver settings | The implementation uses a numerical local solve and can ⏎ return its last iterate on non-convergence, | The implementation uses a numerical local solve --- \ac{SLSQP} with at most $1000$ iterations and a function ⏎ tolerance of $10^{-6}$ --- and can return its last iterate … |
| Cii-7 M4 channels | as in \eqref{eq:method:dep:varows}. The corridor is two parallel walls; | as in \eqref{eq:method:dep:varows}. The measured velocity is carried in the plan as a predicted quantity only: ⏎ no dynamics row constrains it and the geometric rows act … |
| Cii-5 M1 per-axis reach (§4.6.3) | that a plan for the vehicle's centre is a plan for the vehicle under that assumption. The offset is | that a plan for the vehicle's centre is a plan for the vehicle under that assumption. The reach is a per-axis ⏎ bound: a surface met head-on along a body axis, as the cor… |
| Cii-2 C3 windows overlap | In all three environments, a random tenth of the training windows is held out and used only to select ⏎ the checkpoint (\autoref{tab:train}). | In all three environments, a random tenth of the training windows is held out and used only to select ⏎ the checkpoint (\autoref{tab:train}); the windows are cut with a s… |
| Cii-9 C9 non-argument deleted | It is also the larger of the two spreads available and ⏎     therefore the conservative one. Each seed | Each seed |
| Cii-3 C4 guard in the protocol | contexts in which one configuration is ahead, together with the median final distance over the ten. | contexts in which one configuration is ahead, together with the median final distance over the ten. ⏎ On the tightened set the enlarged keep-out region overlaps the box o… |
| Cii-6 M3 margins + Cii-10 C10 selection | clears it --- the manipulator's end effector is constrained as a point and carries none ⏎ (\autoref{sec:method:env:uav}). | clears it --- the manipulator's end effector is constrained as a point and carries none ⏎ (\autoref{sec:method:env:uav}). Neither margin was calibrated on rollouts of thi… |
| Cii-15 M9 controller references | than $1$\,m/s and without velocity feed-forward, and a flight that touches a pillar ends there. UAV-corridor is evaluated with ten flights per configuration, spread | than $1$\,m/s and without velocity feed-forward, and a flight that touches a pillar ends there. On UAV-corridor ⏎ and UAV-s-curve the tracking controller receives the pla… |
| Ci-4 C6 scope (§6.2.1) | on this task: it does ⏎ not improve when it is given a larger budget. At two network evaluations | on this task: from two to twenty ⏎ evaluations it does not improve when it is given a larger budget, and only at a hundred does it ⏎ (\autoref{tab:va-models}). At two net… |
| Ci-4 C6 scope (buys nothing) | Raising the budget spends time and buys | Raising the budget to twenty spends time and buys |
| Ci-7 C6 account as hypothesis (§6.2.1) | The mechanism is in the training target. It is built from a stop-gradient copy of the network itself, so ⏎ part of the network's own error enters the field it learns, and… | One account of this is the training target; it is consistent with the ladder and not tested here. The target is ⏎ built from a stop-gradient copy of the network itself, s… |
| Ci-5 C1 unsupported counts deleted | \guard{The level result at $\nfe=2$ is in distance only. Counting the contexts in which any ⏎ configuration reaches the target, analytic average-velocity matching succeed… | \guard{The level result at $\nfe=2$ is in distance only.} |
| Ci-6 C1 solves -> comes closest | here, because the model that solves the task does so at twenty | here, because the model that comes closest to the target does so at twenty |
| Cii-3 C4 caption Table 6.8 | constraints, the same ten contexts in every row, one training seed. Columns as in | constraints, the same ten contexts in every row (nine attempted: context~7 is held by the box--obstacle guard ⏎   on the tightened set and counts as unmoved, \autoref{sec… |
| Ci-4 + Ci-7 (§6.2.3) | The consistency-interpolated model does not improve with the budget ⏎ and reaches $60\,\%$ only at a hundred evaluations: its training target is built from a stop-gradien… | The consistency-interpolated model does not improve with the budget below a hundred evaluations ⏎ and reaches $60\,\%$ only there; the account consistent with this, not t… |
| Cii-4 C12 (two places) | at a twenty-fourth of the controller's cost per control step | at a twenty-fourth of the controller-and-simulator cost per control step |
| Cii-4 C12 (MuJoCo MPC) | at a twenty-fourth of MuJoCo MPC's controller cost per control step | at a twenty-fourth of MuJoCo MPC's controller-and-simulator cost per control step |
| Cii-4 C12 (§6.4 list) | at twenty-four times the controller's cost per step. | at twenty-four times the controller-and-simulator cost per step. |
| B-i-4 / Ci-7 (§6.4 finding 2) | The self-referential target of the consistency-interpolated model, invisible on D3IL-avoiding, stops it ⏎     improving on D3IL-aligning. | The self-referential target of the consistency-interpolated model is the account consistent with its ⏎     plateau on D3IL-aligning, invisible on D3IL-avoiding; it was no… |
| Cii-1 C3 scope (Summary) | On D3IL-aligning the dominance repeats where the task separates the models: without projection, at | On D3IL-aligning, on ten training-set contexts, the dominance repeats where the task separates the models: without projection, at |
| Cii-1 C3 scope (RQ1) | and differ in cost. On D3IL-aligning without projection analytic average-velocity matching | and differ in cost. On D3IL-aligning without projection, on ten training-set contexts, analytic average-velocity matching |
| Cii-12 C7 one phrase | on straight-line demonstrations the generative model does not ⏎ matter, the budget does; | on straight-line demonstrations the objectives cannot be told ⏎ apart and the budget decides; |
| Cii-13 C8 as trained | Yes, with a margin, on the benchmark of \ac{DPCC}. With the ⏎     backbone, the data and the evaluation held fixed, consistency-interpolated | Yes, with a margin, on the benchmark of \ac{DPCC}. With the ⏎     backbone, the data and the evaluation held fixed, and each objective trained as \autoref{tab:train} stat… |
| Cii-1 C3 scope (Limitations) | D3IL-aligning, UAV-corridor and UAV-s-curve are evaluated on one training seed, D3IL-avoiding and ⏎ UAV-pillars on five (\autoref{tab:protocol}). | D3IL-aligning, UAV-corridor and UAV-s-curve are evaluated on one training seed, D3IL-avoiding and ⏎ UAV-pillars on five (\autoref{tab:protocol}); the ten alignment contex… |
| Cii-8 M7 horizon | A longer horizon would hand the ⏎     projector its constraints earlier and the endpoint solve a farther endpoint, at a price: the | A longer horizon would hand the ⏎     projector the always-active constraints earlier --- the disks, the box, the tilt and the hump; the switched ⏎     walls of \eqref{eq… |
| Cii-3 C4 caption Table B (threshold) | $\eta$ on D3IL-aligning: MeanFM under random selection, tightened constraints, ten contexts, one ⏎   training seed, under both projectors. | $\eta$ on D3IL-aligning: MeanFM under random selection, tightened constraints, ten contexts (nine attempted: ⏎   context~7 is held by the box--obstacle guard on the tight… |
| Cii-3 C4 caption Table B (projection) | projection method, tightened constraints, ten contexts, one training seed. \emph{Violation-free}: | projection method, tightened constraints, ten contexts (nine attempted, as in \autoref{tab:va-threshold}), one ⏎   training seed. \emph{Violation-free}: |
| Cii-5 M1 Fig A.1 caption | The dashed circle is the $0.31$\,m rotor reach, the configured inflation of the projection constraints on UAV-corridor and UAV-s-curve.} | The dashed circle is the $0.31$\,m rotor reach, the configured inflation of the projection constraints on UAV-corridor and UAV-s-curve: the per-axis reach, which the encl… |

## Checked

- `tools/check.py`: 17 files, 7793 lines, 277 labels, 53 of 53 citations, 41 figures — all mechanical checks pass.
- `tools/make_release_v5.py --dry-run`: 19 + 48 files, 9 holes, 1 finding (`\getDoctype`), 4 residue hits.
- Ci-1 re-derived before writing: with $p_{des,t+1} = p_{des,t} + \Delta p_t$ and $e_t = p_{des,t} - p_t$, the Euler row's mismatch on the measured
  channel is $w_t = p_{t+1} - p_t - \Delta p_t = e_t - e_{t+1}$. Ci-3 against `config/uav_projection.yaml` (`sphere_outside`, `dimensions: ['x','y']`).
  Cii-5's 0.36 m: $\sqrt{0.14^2 + 0.18^2} + 0.13 = 0.358$ from Fig. A.1's own dimensions (footprint $0.54 \times 0.62$ m, rotor radius 0.13 m).
- **Not compiled.** Nothing committed.

## Release

`RELEASE/output/20260925_212026_thesis_release_ORCH_v5.6_FACTUAL_CHANGE/` — job O018, tag `FACTUAL_CHANGE`, two notes in `RELEASE/CHANGELOG.md` naming the audit and the kind. Page estimate ~179 (152-214). `diff -rq` against the v5.5 build: 7 file(s) differ — `chapters/02_background.tex`, `chapters/04_method.tex`, `chapters/05_setup.tex`, `chapters/06_results.tex`, `chapters/07_conclusion.tex`, `chapters/08_discussion.tex`, `chapters/09_appendix.tex`; no file only on one side.

## Left open

- The ⛔ rows and the 🟡 remainders (your word each); the held B-i rows; group D.

Signed: Orchestra (Claude Fable 5.1, Claude Code), job O017 · 2026-09-25 · not compiled · nothing committed.
