# v5.3 — 2026-09-25 15:20 · Tier A leftovers fixed at the source: Figures 5.10 / 5.11 (clipped text, A6) and 6.4 (palette, A9), rebuilt in the DA store and re-exported (Orchestra job O007)

**Source:** the first-reading audit of the golden release — `RELEASE/output/20260925_115125_thesis_release_v2.28_v3.100b_v4.2_GOLDEN_TEMPLATE/feedback/THESIS_FIRST_READING_REVIEW.md` (a reviewing agent, 2026-09-25, PDF only), answered and sorted in `feedback/CLAUDE_ANSWER_to_THESIS_FIRST_READING_REVIEW_20260925_1310_…md` §9 (group A, items A6 and A9). **Kind: PURE BUG FIX** — form, format and consistency only; no claim, no number of record, no figure content changed.

**Asked (author, 2026-09-25):** "is the still open A pure bugs and zero chance of other issues? if pure bug, just fix and back to source"

**Answer, then the work.** Of the tier-A items left open after v5.2, two are pure bugs and were fixed where the figures are made
(`Data_Analysis/DA_in_Paper`), then re-exported into v5: **A6** (text clipped inside two exported figures) and **A9** (one frontier
figure coloured the models differently from the other three). Nothing else was a bug: Figure 6.7's empty sixth panel is a layout
choice (the page fit is v5.2's height cap), A11 is the author's front-matter metadata, and the two RELEASE-tool items are code
hardening, not defects in the thesis. **No `.tex` of v5 changed in this pass; no number, no data, no marker or axis moved** — only
the position of text and the colour of markers.

## The fixes at the source

| # | figure | what was wrong | fix (file:line) | result |
| :-- | :-- | :-- | :-- | :-- |
| A6 | 5.10 `fig_expert_aligning` | the one-line subtitle ("120 recorded contexts · 106 direct pushes cross the keep-out region, none the halfspace") ran past the 620 px canvas; the PNG cut it at "…none the l" | `expert.py:263` top margin 50 → 64 × fs; `:275`–`277` the subtitle split — `frame()` draws "120 recorded contexts", a second `text()` line carries the counts | PNG 1860×2817 → 1860×2874 (+2 % height; included at 0.62 linewidth) |
| A6 | 5.11 `fig_expert_uav` | the third legend key ("the vehicle to scale, for size only (the 0.31 m reach is in the surfaces)") ran past the 948 px strip at font 18 × 1.55; the PNG cut it at "…is in the surfa" | `expert.py:76`, `:79` `_legend` wraps a key at `\n` (`LEGEND_LINE` = 32 px, rows below move down; keys without `\n` render byte-identically); `:237` the key split in two | PNG 2844×3402 → 2844×3498; the v5.2 cap `height=0.70\textheight` absorbs it |
| A9 | 6.4 `fig_aligning_projected_tradeoff` | its own palette `#1F4E79 / #8B3F71 / #C45B24` — CI-MeanFM purple here, teal in Figures 6.2 / 6.3 / 6.6 (`ENGINE_COLOUR_DISTINCT`); v3's changelog records no decision for it | `frontier.py:298` `colours = dict(S.ENGINE_COLOUR_DISTINCT)` | SVG diff = colour strings only; PNG 2280×1500 |

Rebuilt with `python3 plotting/make_figs.py fig_expert` and `… fig_aligning_projected_tradeoff`; `figures/MANIFEST.md` restored from
git afterwards (a matched run rewrites it with only the matched rows). PNGs rendered with `python3.14 plotting/svg/preview_png.py
<svg> <png> --scale 3` — **the store's own PNGs are exactly that** (a re-render of two unchanged figures came out pixel-identical),
so these are the pipeline's PNGs, not previews. Exported with `plotting/export_to_draft.py <v5>`: 6 files copied, 55 already
current, `v5/figures/EXPORTED.md` regenerated (41 figures; v2's `fig_hardflow_endpoint_generation` is reported as not in the store —
it never was; it lives in the draft).

## Verified

- The three new PNGs read by eye: the subtitle's two lines and the wrapped key sit inside their canvases; Figure 6.4's legend and
  markers show blue / teal / orange / near-black as Figure 6.2 does.
- Content unchanged: the expert figures read the same `data/expert_paths.json`; the frontier the same corpus; the diff of the
  projected figure's SVG is colour attributes only; the expert SVGs differ in the text elements and the canvas height.
- v5 copies = store copies (SHA-256 of all six files equal).

## Checked

- `tools/check.py`: 17 files, 7 833 lines, 275 labels, 53 of 53 citations, 41 figures — all mechanical checks pass (as v5.2).
- `tools/make_release_v5.py --dry-run`: 19 text + 48 binary files; 9 holes, 1 finding (`\getDoctype`), 4 residue hits — as v5.2.
- **Not compiled.** Nothing committed: the DA working tree carries the two builders and six figure files modified, for the author to
  commit with the rest.

## Release

`RELEASE/output/20260925_152307_thesis_release_ORCH_v5.3_BUGFIX_A/` — built 2026-09-25 15:23 (job O008) on the author's word
"release it. (mark from v5.2/bug fix A)": the PURE BUG FIX build carrying v5.2 (twenty text fixes) and v5.3 (three figures fixed at the
source); tag `BUGFIX_A`, two notes in `RELEASE/CHANGELOG.md` name the audit and the kind. 19 text + 48 binary files, Overleaf zip
7 841 KB, page estimate ~179 (153–215), 9 holes, 1 finding, 4 residue hits. Against the golden release's `latex/` only the six files
v5.2 / v5.3 changed differ (`04_method`, `05_setup`, `06_results`, the three PNGs). To compile on Overleaf; first look: pp. 29–31
(the former `??`), 61, 67, 122, 131 (the float fixes).

## Left open

- Groups B, C, D of the review answer (the author's ticks).
- A11 (metadata) and the two tooling items (a double-`\label` warning, a residue pattern) — code, on the author's go.
- Figure 6.7's relayout, if the author wants the sixth slot used (a 3 × 2 grid changes the caption's "left / right").

Signed: Orchestra (Claude Fable 5.1, Claude Code), job O007 · 2026-09-25 15:20 · not compiled · nothing committed.
