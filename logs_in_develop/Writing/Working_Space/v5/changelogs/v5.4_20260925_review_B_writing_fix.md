# v5.4 — 2026-09-25 15:52 · B WRITING FIX: the low-risk wording items of the review answer's group B-i applied in v5 (Orchestra job O010)

**Source:** the first-reading audit of the golden release — `RELEASE/output/20260925_115125_thesis_release_v2.28_v3.100b_v4.2_GOLDEN_TEMPLATE/feedback/THESIS_FIRST_READING_REVIEW.md`
(§4 W1–W3, W6; §5; §6), sorted in `feedback/CLAUDE_ANSWER_…md` §9 as **group B-i** (low risk, minor, a named sentence). **Kind: B WRITING FIX** — wording only: no claim,
no number of record, no table cell, no figure changed; the drafting macros untouched.

**Asked (author, 2026-09-25):** "start on B … we may no risk fix B-i first" → "Go. do B-i." — and, on the record's form: "in B update, we build a new
changelog and mark as B writing fix".

## The 27 edits (v5.4 line numbers; before → after)

| row | where | before | after |
| :-- | :-- | :-- | :-- |
| B-i-1 | `01_introduction.tex:5` | A robot that plans with a generative model buys expressiveness with computation. Diffusion | A robot that plans with a generative model gains expressiveness at a cost in computation. Diffusion |
| B-i-2 | `01_introduction.tex:12` | This cost is not incidental but \emph{structural}, and the structure is what makes it ⏎ addressable. In a diffusion model the number of steps is a | This cost is not incidental but \emph{structural}, and it can be addressed where it arises. In a ⏎ diffusion model the number of steps is a |
| B-i-11 | `02_background.tex:131` | A fully actuated manipulator in position control holds its configuration through joint feedback; a ⏎ planned sequence of end-effector positions becomes joint commands through \ac{IK}. A quadrotor is ⏎ different. Its four rotors produce a thrust along one body axis and three moments, so it controls four ⏎ of its six degrees of fr | A fully actuated manipulator in position control holds its configuration through joint feedback: a ⏎ planned sequence of end-effector positions becomes joint commands through \ac{IK}, and the arm holds ⏎ its last setpoint even though tracking error and collisions still depend on the commanded motion. A ⏎ quadrotor is different.  |
| B-i-7 | `04_method.tex:206` | \parencite[Sec.~6.1]{romer2025diffusion}. Three things follow, and all three are inherited by every ⏎ environment in this thesis. | \parencite[Sec.~6.1]{romer2025diffusion}. Each of the following is inherited by every ⏎ environment in this thesis. |
| B-i-7 | `04_method.tex:278` | observation through the map in \autoref{sec:method:env:uav}. Two properties follow. | observation through the map in \autoref{sec:method:env:uav}. |
| B-i-7 | `04_method.tex:368` | Two consequences matter for the thesis. \eqref{eq:method:dep:dls} is a \emph{local} solve run three | \eqref{eq:method:dep:dls} is a \emph{local} solve run three |
| B-i-7 | `04_method.tex:494` | as published; only the task is ours. Three pieces define it. | as published; only the task is ours. |
| B-i-7 | `04_method.tex:708` | the experiment settings in \autoref{sec:setup:protocol}. Two consequences follow. | the experiment settings in \autoref{sec:setup:protocol}. |
| B-i-7 | `04_method.tex:811` | Three properties of \eqref{eq:bg:mf:loss} are used later in this thesis. | The following properties of \eqref{eq:bg:mf:loss} are used later in this thesis. |
| B-i-7 | `04_method.tex:866` | Four properties of \eqref{eq:method:engine:mftgt}--\eqref{eq:method:engine:mfloss} are choices of ⏎ this implementation: | The following properties of \eqref{eq:method:engine:mftgt}--\eqref{eq:method:engine:mfloss} are choices of ⏎ this implementation: |
| B-i-8 | `06_results.tex:140` | Read down the success column first. Every flow-based model reaches the goal in every one of its thirty | Every flow-based model reaches the goal in every one of its thirty |
| B-i-8 | `06_results.tex:144` | Read the next column and none of them arrives legally. The highest success with constraint satisfaction | None of them arrives legally. The highest success with constraint satisfaction |
| B-i-8 | `06_results.tex:156` | The last column prices generation alone. One network evaluation of a flow-based model costs $9.0$ to | The last column is the cost of generation alone: one network evaluation of a flow-based model costs $9.0$ to |
| B-i-10 | `06_results.tex:802` | from a starting distance of $0.4530$\,m --- it closes $84\,\%$ of the distance --- and it is the only model | from a mean starting distance of $0.4530$\,m --- the median reduced by $84\,\%$ of it --- and it is the only model |
| B-i-10 | `06_results.tex:804` | flow-based models close $10$--$14\,\%$ of the distance, while the diffusion median is $2\,\%$ farther ⏎ away than the box started; each leaves it untouched in some contexts. | flow-based models reduce the median by $10$--$14\,\%$, while the diffusion median is $2\,\%$ farther ⏎ away than the mean starting distance; each leaves the box untouched in some contexts. |
| B-i-10 | `06_results.tex:883` | instantaneous-velocity matching closes $6$ to $10\,\%$ of the distance at every budget from two to a hundred, ⏎ and the consistency-interpolated model $17$, $6$, $14$ and $60\,\%$, so at ten evaluations it is at the | instantaneous-velocity matching reduces the median by $6$ to $10\,\%$ of the mean starting distance at every budget from two to a hundred, ⏎ and the consistency-interpolated model by $17$, $6$, $14$ and $60\,\%$, so at ten evaluations it is at the |
| B-i-10 | `06_results.tex:888` | budget it was trained with. The ladder starts at two evaluations: the analytic model closes $39\,\%$ of ⏎ the distance there against $84\,\%$ at twenty, so a smaller budget is not an operating point on this | budget it was trained with. The ladder starts at two evaluations: the analytic model's reduction is $39\,\%$ ⏎ there against $84\,\%$ at twenty, so a smaller budget is not an operating point on this |
| B-i-9 | `06_results.tex:1195` | Endpoint projection is therefore the better projector on the constraint on this task, at the budget the | Endpoint projection is therefore the better projector on the constraint --- more contexts free of violations --- on this task, at the budget the |
| B-i-10 | `06_results.tex:1365` | non-dominated configuration of the task is analytic average-velocity matching: at twenty evaluations it closes ⏎ $84\,\%$ of the distance, against $14$, $10$ and $-2\,\%$ for the consistency-interpolated model, | non-dominated configuration of the task is analytic average-velocity matching: at twenty evaluations it reduces ⏎ the median final distance by $84\,\%$ of the mean starting distance, against $14$, $10$ and $-2\,\%$ for the consistency-interpolated model, |
| B-i-10 | `06_results.tex:1368` | cost, it has already closed two fifths. | cost, the reduction is already two fifths. |
| B-i-10 | `06_results.tex:2452` |     Without projection every non-dominated configuration is analytic average-velocity matching, which closes ⏎     $84\,\%$ of the distance at twenty evaluations against $14$, $10$ and $-2\,\%$ for the others. |     Without projection every non-dominated configuration is analytic average-velocity matching, which reduces ⏎     the median final distance by $84\,\%$ of the mean starting distance at twenty evaluations against $14$, $10$ and $-2\,\%$ for the others. |
| B-i-9 | `06_results.tex:2475` | Endpoint projection is the better projector on the constraint where the task needs a budget that gives it | Endpoint projection is the better projector on the constraint --- more contexts kept free of violations --- where the task needs a budget that gives it |
| B-i-9 | `06_results.tex:2525` | better projector on the constraint. The quadrotor scenes show the role of the demonstrations: generated for | better projector on the constraint, keeping more contexts free of violations. The quadrotor scenes show the role of the demonstrations: generated for |
| B-i-3 | `06_results.tex:2541` |   \item \textbf{The price of a projection is set by what the projector is handed.} A plan close to the |   \item \textbf{The time a projection takes depends on how far from feasible the plan it receives is.} A plan close to the |
| B-i-14 | `06_results.tex:2176` | training seed. Columns as in \autoref{tab:uav-scurve}; \emph{ms/step} includes the projection. Per-step | …\emph{ms/step} includes the projection; \emph{---} under \emph{Selection rule}: no rule, a single arm. … |
| B-i-5 | `07_conclusion.tex:48` | The quadrotor scenes carry the framework to a plant that holds nothing by itself. UAV-pillars flies | The quadrotor scenes carry the framework to a plant that needs feedback even to hover. UAV-pillars flies |
| B-i-6 | `08_discussion.tex:24` | The results stop at three things: what the framework does not do, what a deployment has to add to it, ⏎ and what has not been tested. | This chapter states what the framework does not do, what a deployment has to add to it, and what has ⏎ not been tested. |

Per row: **B-i-1, B-i-2** the two Introduction aphorisms made literal · **B-i-3** the §6.4 slogan "the price of a projection is set by what the
projector is handed" → the literal statement the item's body already makes · **B-i-5** "a plant that holds nothing by itself" → "a plant that needs
feedback even to hover" · **B-i-6** Chapter 8's opener "The results stop at three things" → a plain statement of the chapter's three parts (kept as a
lead, not deleted) · **B-i-7** the seven counting openers of Chapter 4: the bare counts deleted, the lead-ins that carry content kept without the
number ("Each of the following is inherited …", "The following properties …") · **B-i-8** §6.1.1's three reading instructions: two deleted, the third
("prices generation alone") turned into what the column is · **B-i-9** "the better projector on the constraint" names its metric (more contexts free of
violations) in the three Chapter 6 places where it stood alone; the other three in Chapter 6 and both in Chapter 7 already named it; the abstract's
instance is held · **B-i-10** the shorthand "closes N % of the distance" → the defined quantity (the reduction of the median final distance relative
to the mean starting distance) in five places plus "closed two fifths"; the per-context sentence in §6.4 ("in six more it closes 40 to …") is a true
per-context statement and stays · **B-i-11** the arm / quadrotor contrast said twice in §2.4 → one paragraph, every fact kept · **B-i-14** Table 6.14's
caption defines the dash of its rule column; every other dash was already defined (6.6, 6.7, 6.8, 6.12 in caption or cell).

## Held — not minor after all, or not v5's

- **B-i-13** (`1.000 ± 0.000` → `10/10`, Tables 6.11, 6.12, B.1–B.4, about 150 cells): a **decision**. Your v3.83 instruction printed the corridor tables
  as rates with ± ("the avoiding in chap6 result is clearly using the 0–1 and the 0.9X ± range"), your Chapter 5 rule (§5.4, "Counts are printed as
  counts … gets no ±") says the opposite for ten-flight cells. Pick one; if counts, it is one mechanical pass.
- **B-i-17** (the appendix figure captions B.1–B.3): they carry the protocol by your v3.49 rule (protocol in the caption); shortening them means
  moving protocol — a decision.
- **B-i-4** (the second §6.4 slogan): the echo of the CI-MeanFM account, tied to Ci-7 (🟠).
- **B-i-9, the abstract's instance**: your text. **B-i-12** (the Discussion's candidate-selection paragraph): prunes numbers you placed.
- **B-i-15, B-i-16**: DA figure work (line styles beside red / green; label thinning, the boxed arrows, tick sizes) — a DA pass on your go.

## Checked

- `tools/check.py`: 17 files, 7 835 lines, 275 labels, 53 of 53 citations, 41 figures — all mechanical checks pass; drafting macros unchanged.
- `tools/make_release_v5.py --dry-run`: 19 + 48 files, 9 holes, 1 finding (`\getDoctype`), 4 residue hits — as v5.3. **Not compiled.**
- Every edit re-read in place after applying; no `\label`, `\eqref`, `\autoref` or number touched.

## Left open

- The held B-i rows above; groups B-ii (rebuilds, each a decision then a job), B-iii (no target), C (🔴 6 · 🟠 9 · 🟢 9) and D of the answer.
- **Release (temporary review marker):** `RELEASE/output/20260925_201059_thesis_release_ORCH_v5.4_TEMP_REVIEW_Bi/` — built on the author's word
  ("release it (as a temp marker, I will use for review now for the B-i changes in PDF)", job O011): a marker to read the 27 edits in the
  PDF against `…_ORCH_v5.3_BUGFIX_A`, not a milestone. 19 + 48 files, ~180 pages estimated, 9 holes, 1 finding, 4 residue hits; only the six
  chapter files of this revision differ from the v5.3 build.

Signed: Orchestra (Claude Fable 5.1, Claude Code), job O010 · 2026-09-25 15:52 · not compiled · nothing committed.
