# v5.1 — 2026-09-25 · The seven INBOX rows open for v2 / v3 / v4 resolved in v5 (Orchestra job O004)

**Asked (author, 2026-09-25):** "First job is to resolve any the INBOX for v2,v3,4. to v5.1."

**What v5.1 is:** the revision at which every cross-draft item that was open on 2026-09-25 is settled *in the aggregate* —
absorbed by construction (v5 already holds the revision the note asked for), answered (the text already says what the note
asks), or not applicable (it concerned the legacy sync machinery v5 does not have). **No thesis text changed in this pass**;
the INBOX rows are marked `🔀 v5.1` with the resolution, the legacy drafts' own files are untouched, and each resolution is
recorded here with its evidence so that a later reader can check it against the files.

## The rows

| # | → | from | what the row asked | resolution in v5 | evidence |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 1 | v2 | Orchestra O001 · 2026-09-25 | FYI: the Orchestra chat and its protocol for edits inside the drafts | **superseded** by the O003 note (the Advance flow: the thesis lives in v5). The legacy protocol still holds for a job inside a draft | `cross_draft/to_v2/FROM_Orchestra_20260925_O003_advance_orchestra_v5.md`; `Orchestra/README.md` |
| 2 | v3 | Orchestra O001 · 2026-09-25 | the same FYI | the same | `…/to_v3/FROM_Orchestra_20260925_O003_…` |
| 3 | v3 | v4.2 · 2026-09-25 | (a) "at twenty evaluations" as the endpoint sampler's budget is an unresolved K / NFE terminology follow-up (audit of v4.1a, F02 / §12.3); (b) §6.1.2.2's "from 18.1 to about 11.7 ms" is analytic average-velocity matching's own candidate-study record — name the model if wanted | **(a) no change — the convention is stated.** Ch 5, *Evaluation* (`sec:setup:protocol:eval`, `chapters/05_setup.tex:1262–1269`): the step budget counts the sampling steps; without endpoint projection it is also the number of network evaluations; under endpoint projection each guiding step adds one evaluation, so a plan costs $\nfe + n_{\mathrm{guide}}$ evaluations; "the tables name the sampling steps $\nfe$ throughout". Ch 6 §6.1.1 (`06_results.tex:83–87`) restates what $\nfe$ counts in each family. Under that convention "at twenty evaluations" in Ch 6 names $\nfe = 20$; Ch 7 writes $\nfe$ (v4.2). A sweep of Ch 6's prose to "steps" is the audit's item and stays parked with the audit ("DONT care the Audit for now", author). **(b) no change — the sentence already names the model:** "would bring analytic average-velocity matching at $\nfe=1$ from $18.1$ to about $11.7$ ms per control step" (`06_results.tex:437–439`) | the three passages named |
| 4 | v3 | v2.28 · 2026-09-24 | v2.28 is ready to sync (abstract, §1.4, outline, "MuJoCo MPC", `\acro{PD}`, §3.4, §4.6.3); merge `02_background`, `04_method`, `99_backmatter` together | **absorbed by construction at v5.0:** v5's `parts/00_preamble.tex`, `01_frontmatter.tex` (abstract), `99_backmatter.tex` (acronyms), `chapters/01–04` are the split of `v2/thesis_v2.tex` **at v2.28** (`inherited/MANIFEST.md`, SHA `6863f3a6866c…`). The three files the note wants merged together are all v2.28: `\acro{MJPC}` is gone from the list and no `\ac{MJPC}` remains in any chapter (the only "MJPC" is a file name inside a `\dataref` at `06_results.tex:2353`, removed in a release); `\acro{PD}` is declared (`parts/99_backmatter.tex:17`) and used (`chapters/04_method.tex:188`). v3 need not sync for a release | `inherited/INIT_STATE.json`; `tools/check.py` (all pass); the golden-release equivalence of v5.0 (the golden build already read v2.28 live) |
| 5 | v4 | Orchestra O001 · 2026-09-25 | the same FYI | superseded by O003 | `…/to_v4/FROM_Orchestra_20260925_O003_…` |
| 6 | v4 | v3.100b · 2026-09-25 | v3 archived its copies of Ch 7–9; retire the four rows of `v4/tools/sync_v3.py`; tell v3 before renaming a Ch 7–9 label (its `check.py` checks 13 references into them) | **not applicable in v5:** v5 has no inherited copies and no sync tool; every chapter is in one tree, so a Ch 5/6 reference into Ch 7–9 is checked by `v5/tools/check.py` like any other (276 labels, 0 dangling). `v4/tools/sync_v3.py` is untouched — a legacy tool, v4's call if a legacy bundle is ever built again | `tools/check.py`; `v5/README.md` rule 6 |
| 7 | v4 | v2.28 · 2026-09-24 | v4's v4.0 / v4.1 items are closed in v2.28; they reach v4 through v3's and v4's syncs | **absorbed by construction at v5.0:** v5 holds v2.28 directly (row 4); no sync needed for a release | as row 4 |

The three `⏳ (FYI — nothing asked of … now)` rows written by O003 stay open: they are the notification of the Advance flow
itself, for the owner chats to read at their next session start (the author may close them any time).

## Checked

- `tools/check.py`: 17 files, 7 824 lines, 276 labels, 53 of 53 citations, 41 figures — all mechanical checks pass (unchanged: no text edited).
- `tools/make_release_v5.py --dry-run`: 19 text + 48 binary files, 9 holes, 1 finding (the standing `\getDoctype` patch), 4 residue hits — unchanged.
- `tools/absorb.py status`: nothing to absorb; v2.28 · v3.100b · v4.2 unchanged since init.
- `cross_draft/INBOX.md`: 7 rows `🔀 v5.1`, 3 rows `⏳` (the O003 FYIs), `## → v5` empty.
- **Not compiled** (no TeX toolchain here). Nothing committed. No file of v2 / v3 / v4 touched.

## Left open (not this job's)

- The K / NFE wording sweep in Ch 6 (row 3a) and everything else in the parked review answer (job O002).
- The author's decisions listed in `changelogs/v5.0_…` (the long-data web link and switch, the glossary, raster figures, `\getDoctype`).

Signed: Orchestra (Claude Fable 5.1, Claude Code), job O004 · 2026-09-25 · not compiled · nothing committed.
