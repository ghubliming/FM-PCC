# v5.2 — 2026-09-25 15:01 · Tier A of the first-reading review applied in v5: the format and consistency bugs A1–A20 (Orchestra job O006)

**Source:** the first-reading audit of the golden release — `RELEASE/output/20260925_115125_thesis_release_v2.28_v3.100b_v4.2_GOLDEN_TEMPLATE/feedback/THESIS_FIRST_READING_REVIEW.md` (a reviewing agent, 2026-09-25, PDF only), answered and sorted in `feedback/CLAUDE_ANSWER_to_THESIS_FIRST_READING_REVIEW_20260925_1310_…md` §9 (group A, items A1–A20). **Kind: PURE BUG FIX** — form, format and consistency only; no claim, no number of record, no figure content changed.

**Asked (author, 2026-09-25):** "Init a Changlog and update in v5 for the tier A. and no need to realse. after fix also mark in the
Answer md saying been fixed."

**What v5.2 is:** the revision at which group A of the review answer
(`RELEASE/output/20260925_115125_thesis_release_v2.28_v3.100b_v4.2_GOLDEN_TEMPLATE/feedback/CLAUDE_ANSWER_to_THESIS_FIRST_READING_REVIEW_20260925_1310_v2.28_v3.100b_v4.2.md`,
§9 A) is in the text: the things that were simply broken in the golden release's PDF (a lost equation label, an audit identifier in print,
four floats over the page, two captions that misdescribe their figure / table, one leftover of a retired wording) and the nine sentences
that contradicted the table beside them. **Nothing of record changed**: no table cell, no figure file, no drafting macro; every corrected
number is the value the cited table already printed. Line numbers below are v5.2's.

## The edits

| # | where (v5.2) | before → after | verified against |
| :-- | :-- | :-- | :-- |
| A1 | `04_method.tex:687`, `04_method.tex:876` | `\label{eq:bg:fm:loss}\label{eq:method:engine:fm}` on one `equation` → one label; the single `\eqref{eq:method:engine:fm}` → `\eqref{eq:bg:fm:loss}` (a source comment explains) | amsmath's `Multiple \label's` rule: the last label wins, the first is dropped, so `\eqref{eq:bg:fm:loss}` (three places) printed `??` on pp. 29–31; no other file used the dropped name |
| A2 | `06_results.tex:961` | "… in two (CLOSURE\_20260907 R.4)." → "… in two." | the identifier is dev residue; its provenance remains in the `\dataref` two lines above. The sentence itself is Ci-5 of the answer (the author's call) |
| A3 | `06_results.tex:1751` | Table 6.12's caption: the tie-break parenthetical → "(the order and every rule in App B.4)"; the goal-point sentence dropped | page image p. 122: the last row sat on the page number, about one line over; the order is App B.4's first sentence, the goal-point counts are in the text three lines above the table |
| A4 | `06_results.tex:2145` | Figure 6.7: `width=\linewidth` → `width=\linewidth,height=0.70\textheight,keepaspectratio` | p. 131: the caption printed below the footer; the PNG's aspect 1.408 against a text block of about 1.465 makes the full-width figure 0.96 of the text height. The DA relayout (2×3 / 3×2, the empty sixth slot) stays open |
| A5 | `05_setup.tex:514`, `05_setup.tex:705` | Figures 5.8 and 5.11: the same `height=0.70\textheight,keepaspectratio` cap | pp. 61, 67: captions on / over the footer rule; aspects 1.131 and 1.196 → 0.77 and 0.82 of the text height at full width plus 7–8 caption lines |
| A7 | `06_results.tex:484` | Figure 6.2's caption: "triangles temporal consistency" → "squares …"; "a hollow point is more than $0.05$ below its panel's best success with constraint satisfaction and stays out of the comparison" added | the PNG's legend (circles / squares); the band rule at §6.1.3 ("within $0.05$ of the best value in its panel") |
| A8 | `06_results.tex:1031` | Table 6.6's caption: "ms/step: mean ± sample standard deviation over the contexts" → "mean over the contexts" | the rows carry plain means ($24.6$, $46.2$, …) |
| A10 | `06_results.tex:911` | "close to the line at which the box was not moved at all" → "close to the $0\,\%$ line" | §5.5.3's definition: $0\,\%$ is a median final distance equal to the mean initial one, "not a box left unmoved"; the same paragraph (eight lines above) already defines the dashed line so |
| A12 | `05_setup.tex:1112` | "Every model is trained once per training seed, and the same checkpoint … every step budget …" → "Every flow-based model …; the diffusion baseline's budget is fixed with its noise schedule at training (§5.6), so each of its budgets is a separately trained checkpoint. Otherwise only the evaluation parameters vary" | Ch 6 §6.1.1: "every diffusion budget is a separately trained checkpoint and every flow-based budget is the same checkpoint read again"; Fig 6.1's text: "two models, one trained at each budget" |
| A13 | `06_results.tex:68` | "… ten episodes per geometry (§5.6.1)." → "… — except where a table states otherwise: Table 6.3 runs on four training seeds at $\nfe=3$ and on one at $\nfe=10$, one of its cells on twenty episodes." | Table 6.3's Seeds column (4 / 1) and caption (CI-MeanFM $\nfe=10$: twenty episodes per geometry); the text beside both blocks already says "four training seeds" / "one training seed" |
| A14 | `06_results.tex:96` | "no unprojected row exceeds $0.100$ S\&C" → $0.133$ | Table 6.1: FM $\nfe=20$ S\&C $0.133 \pm 0.075$; the text of §6.1.1 itself: "The highest … is $0.133$" |
| A15 | `06_results.tex:148` | "Between $15.5$ and $19.0$ control steps … violate a constraint" → "$15.5$ and $20.4$" | Table 6.1's violating-steps column: $15.5$ (MeanFM) to $20.4$ (Diffusion $\nfe=2$) |
| A16 | `06_results.tex:162` | "one at the flow-based operating budget and ten at the baseline's" → "… and eleven at the baseline's, whose gate includes the threshold step" | §5.6.4 (guiding steps): "eleven rather than ten at $\nfe=20$ and $\eta=0.5$" |
| A17 | `06_results.tex:908` | "Four of the ten configurations are on it" → "Four of the thirteen" | Figure 6.3 plots every cell of Table 6.5 but the two $\alpha_{\mathrm{end}}=0.05$ rows: 15 − 2 = 13 |
| A18 | `06_results.tex:966` | "comes closest to the target at $\nfe=20$" → "comes closest to the target, and $\nfe=20$ is its operating budget — the $\nfe=100$ point ends only $0.0068$\,m closer at $4.7$ times the cost" | Table 6.5: $0.0741$\,m at $190.5$\,ms ($\nfe=20$) against $0.0673$\,m at $892.4$\,ms ($\nfe=100$); §6.2.1 says the same two lines above Figure 6.3 |
| A19 | `06_results.tex:1267`, `06_results.tex:1323` | "do not move the box on this task at $\nfe=20$" → "barely move the box … — medians $14$ and $10\,\%$ of the distance closed, the box unmoved in two and four of ten contexts"; the second instance ("the two models that do not move the box") → "barely move" | Table 6.5 at $\nfe=20$: CI-MeanFM $0.3897$ ($14\,\%$), unmoved 2/10; FM $0.4085$ ($10\,\%$), unmoved 4/10 |
| A20 | `06_results.tex:351` | "This is the one place in the thesis where a flow-based model is ahead of the baseline on every reported axis at once …" → "Here a flow-based model is ahead of the baseline on every reported axis at once …" | §6.2.2: on D3IL-aligning the selected model also Pareto-dominates the baseline's best row (nine against four contexts, $0.179$ against $0.462$\,m, $266$ against $809$\,ms), so the uniqueness claim was false |

## Not in this pass (not v5's to make)

- **A6** — Figure 5.10's subtitle and Figure 5.11's third legend line are cut inside the image: DA re-exports (`fig_expert_aligning`, `fig_expert_uav`).
- **A9** — CI-MeanFM is purple in Figure 6.4 and teal in 6.2 / 6.3 / 6.6: DA, one colour per model.
- **A4's relayout** — Figure 6.7's empty sixth slot (2×3 or 3×2): DA. The height cap above makes the page fit meanwhile.
- **A11** — the front-matter `TODO: Author / Supervisor / Advisor / Submission date`: the author's metadata (`parts/00_preamble.tex`, the `\get…` block).
- **Tooling** — a double-`\label` warning and a `CLOSURE_` / `R\d+` residue pattern in the RELEASE tool: code, on the author's go.

## Checked

- `tools/check.py`: 17 files, 7 833 lines, **275 labels** (v5.1: 276 — the dropped duplicate), 53 of 53 citations, 41 figures — all mechanical
  checks pass. Drafting macros unchanged: hole 1, provisional 1, guard 19, srcnote 78, dataref 60, flawed 2, longdata 1, outdated 0, todofigure 0.
- `tools/make_release_v5.py --dry-run`: 19 text + 48 binary files; 9 holes, 1 finding (the standing `\getDoctype` patch), 4 residue hits — as v5.1.
- Changed lines against the inherited base (`inherited/materials/`, `diff`): `04_method.tex` 7, `05_setup.tex` 10, `06_results.tex` 54.
- The float fixes are sized, not compiled: page images `feedback/_review_work/page_069/075/130/139.png` and the PNG aspect ratios; the first
  compile of a v5.2 build is where they are confirmed (the caps can be loosened if the figures come out smaller than needed).
- **Not compiled** (no TeX toolchain here). Nothing committed. No file of v2 / v3 / v4 touched; no figure file touched.

## Left open

- Groups B (tone), C (content) and D (runs / data) of the review answer — the author ticks; C is applied item by item, each with its
  "touches" cell as the limit.
- The DA items and the metadata above.
- No release built (the author: "no need to release"); `tools/make_release_v5.py --job O### --note "…"` when asked.

Signed: Orchestra (Claude Fable 5.1, Claude Code), job O006 · 2026-09-25 15:01 · not compiled · nothing committed.
