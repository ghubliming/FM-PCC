# Claude's answer to `ROUND2_FEEDBACK_TO_RESPONSE_AND_V5.7_20260925.md`

**Written:** 2026-09-25 21:57 · **By:** the Orchestra chat (Claude Fable 5.1, Claude Code), job O022 · **Answers:** `feedback/ROUND2_FEEDBACK_TO_RESPONSE_AND_V5.7_20260925.md` (254 lines; the reviewer read the round-one answer and the v5.7 LaTeX, not the code or the data)
**Release answered:** `20260925_212528_thesis_release_ORCH_v5.7_DATA_FIX` · **v5 revision:** v5.7 · **Status:** answered, then shipped — **Applied:** R2-A (the twenty ✅ DO items) → **v5.8, ROUND2 FIX** (job O023); **Released:** `RELEASE/output/20260925_220823_thesis_release_ORCH_v5.8_ROUND2_FIX/` (job O024), page estimate ~179 (152-215). R2-B (the abstract, the corridor wording, the two cuts) and R2-C (the `10/10` cells) still wait for the author's word. Every item below was checked against the v5.7 release copy (line numbers are its), and the two that needed it against the code; the author ticks §3.

## 0. The short version

**The reviewer is right on the substance, and six of the defects it lists were introduced by my own passes v5.5–v5.7.** Each is named below with its evidence; all six are small and go in the next pass. In one case (§6A) the round-one answer misread the evaluation call: the reported quadrotor flights use the stop-and-go tracker with **zero** velocity feed-forward, as Chapter 4 says, and the sentence I added to Chapter 5 in v5.6 says the opposite. That sentence is wrong and must go.

**Tally over the 27 points.** ✅ DO 20 (each a sentence or a clause, none a rebuild; two of them narrow a claim the tables contradict) · 🟡 the author's call 5 (the abstract, two storyline wordings, two optional cuts) · ⛔ NOT 1 (the `10/10` cells, still your decision from B-i-13) · production 1 (compile and inspect). Nothing here reopens D, which stays closed by your ruling.

**What the reviewer gets right in principle.** "Declined" is not "refuted": the round-one answer's ❌ did mix the two, though its C-iii list already separates "wrong on the facts" from "against a decision you took". I keep that separation below. Stopping D is defensible only with the claim boundaries the reviewer lists in its §3, and each of those boundaries is now in the text (§2 below says where).

**Regressions I introduced (v5.5–v5.7), all ✅ DO:** the Chapter 5 controller sentence (§6A); "the vehicle, not only its centre point, clears it" in §5.5 (§4.2a); "comes closest … at twenty" (§4.4a); "always-active … the tilt and the hump" (§4.4b); the Chapter 7 gloss "ahead on every reported axis" (§6F-1); the merged caption's "violates least" (§6F-4).

## 1. The verdicts

Legend: ✅ DO — a fix the tables, the code or the thesis's own definitions require, sized · 🟡 the author's call · ⛔ NOT · risk 🟢 wording / 🟠 a claim narrows or a specification changes.

### §4 — the closing pass the reviewer asks for

| # | the reviewer says | checked (v5.7) | verdict · smallest fix · risk |
| :-- | :-- | :-- | :-- |
| 4.1 abstract | unchanged; scope the alignment result (ten training-set contexts, simulation, no strict completion), the diffusion sentence (inherited sampler), the UAV ordering (observed) | `pages/abstract.tex:5, 20–25` unchanged since v2.28. The proposed replacement is **true on the tables**: median 0.0741 against 0.4619 m (Table 6.5); the selected endpoint configuration 10/10 violation-free with context 7 held by the guard (Table 6.8); no context within 1.8 cm (§6.4). | 🟡 **yours** — the abstract has been held throughout as your text. The wording is checked; say the word and it goes in as three sentences: the reviewer's alignment sentence, "fixed during training in the inherited implementation", and "on the simpler generated demonstrations instantaneous-velocity matching was enough" (observed, not caused). 🟠 |
| 4.2a | Setup still says inflation makes "the vehicle, not only its centre point" clear the surface | `05:1003–1005` — **my v5.5 sentence** (the prose moved out of Table 5.9); §4.6.3 now states the per-axis limit (`04:1481–1487`) | ✅ **DO**: "so that the vehicle's per-axis reach, not only its centre point, clears it — the directional limit is stated in §4.6.3". 🟢 |
| 4.2b | Discussion says the manipulator bound holds "because the end effector tracks its setpoint within the margin" while Setup says the margins were not calibrated | `08:12–14` as quoted; `05:1005–1007` says neither margin was calibrated; `04:368–372` (v5.6) says the mismatch is the one-step change of the tracking error, not the tracking error | ✅ **DO**: "On the manipulator the guarantee is conditional: the tightening absorbs the mismatch only while the one-step change of the tracking error stays within the configured margin, which was not calibrated here (§5.5); on the quadrotor …". 🟠 (a claim narrows) |
| 4.3 | "Comparisons between rows are unaffected" survives; Ch 7 says "every context" / "all ten" without the guard | `06:943` as quoted; `07:29–30` "keeping every context violation-free"; `07:85` "all ten against nine" | ✅ **DO**: the reviewer's sentence at `06:943` ("Including the same guarded context preserves the ordering of violation-free counts; the reported distances and timings also include that context."); "(one of them held by the guard)" at `07:30` and `07:85`. 🟢 |
| 4.4a | "comes closest to the target does so at twenty" is still false: K = 100 is closer | `06:862` — **my v5.6 wording** (Ci-6); Table 6.5: 0.0673 m at K = 100 | ✅ **DO**: "because the selected model is run at its $\nfe=20$ operating point and the threshold study …". 🟢 |
| 4.4b | "always-active … the tilt and the hump" conflicts with Method: the hump's halves are span-switched | `08:112–113` — **my v5.6 list** (Cii-8); `04:1476–1478`: "each over its own span of $x$, in the form of (switched)" | ✅ **DO**: "the constraints that are in the program at every replan earlier; the span-switched ones — the walls and the hump's two halves, \eqref{eq:method:env:switched} — enter by the vehicle's current position, …" (no list of the always-active ones). 🟢 |
| 4.4c | "twenty evaluations, endpoint projection" — at K = 20, η = 0.2 that is 23 evaluations | endpoint sentences that say "twenty evaluations": `06:1031, 1502, 1512, 1557, 1862, 1904` (six); the corridor and aligning endpoint summaries | ✅ **DO, bounded** to those six: "$\nfe=20$" / "twenty sampling steps". Not the chapter-wide sweep (Cii-14 stays NOT). 🟢 |
| 4.5 | "cannot be told apart" still suggests equivalence; "as this one does" returns to certainty; define dominance as observed means | `07:37–38`, `06:1835–1836`, `07:71–72`, `07:99–101` "cannot be told apart"; `06:753–754` "as this one does"; Ch 7 dominance gloss `07:13–14` | 🟡 **yours** for the corridor wording — the checked replacement, if you want it: "the observed results did not separate the objectives at the shared budgets" (observed, no equivalence), same at the four places. ✅ **DO** the two small ones: drop ", as this one does" and ", as D3IL-avoiding is," at `06:753–755` (🟢); the dominance definition is in 6F-1. |

### §6 — the deeper recheck

| # | the reviewer says | checked | verdict · smallest fix · risk |
| :-- | :-- | :-- | :-- |
| 6A | Method: zero velocity feed-forward; Setup: "receives the planned position and velocity" — which is executed? | **Method is right.** The corridor v3 and s-curve campaigns ran the `pid_stopgo` tracker: run folders `E<engine>_K<k>_mpc4_pid_stopgo_T0.5_<tag>` (`Slurm_Codes/temp_bash/eval_20260923_p23_corridor_v3.sh:246`, `fetch_20260924_p23cv3_corridor_paths.sh:51–58`), the thesis's own source notes ("Tracker of record `pid_stopgo` (CascadedPID, `v_des` = 0"), and the code: `pid_stopgo → v_des = zeros(3)` (`FM_v3_uav_test/eval_fm_uav.py:1161–1162`, `mix_uav_test/eval_mix_uav.py:1895`), `a_des` defaults to zero and `yaw_des = 0.0` (`uav_env_test/flight_controller.py:79–86`). The Chapter 5 sentence is **mine (v5.6, Cii-15) and wrong**: the round-one answer saw `compute(p, q, v, om, p_des, v_des)` and did not follow `v_des` to its value. UAV-pillars is a separate plant with `feedforward=False`, consistent. | ✅ **DO**: `05:1128–1130` → "the cascaded geometric controller receives the accumulated position setpoint, with zero velocity and acceleration feed-forward and a zero yaw reference" (the reviewer's sentence). 🟠 (a specification is corrected) |
| 6B | "half of it projected" has no printed measurement | `06:1770–1771`; the projected loop times (159.4 / 348.6 ms) exist only in the source's data note, where the planner's share "does not split cleanly" | ✅ **DO**: delete "half of it projected"; keep "a tenth of the whole loop unprojected". 🟢 |
| 6C | the generic episode definition ends a run at the finish line; the metrics and the s-curve protocol latch the crossing and fly on | `05:1026–1029` ("the finish line of the scene flown through") against `05:631–633` and `06:1575–1578` | ✅ **DO**: in the generic definition, "the goal line crossed, the box delivered — or when the episode limit is reached; on the quadrotor scenes crossing the finish line is latched and the flight continues to its goal point, the limit or the abort (§5.4.3)". 🟢 |
| 6D-1 | "every flow-based row is cheaper than every diffusion row at equal S&C" | `06:407–408`; Table 6.2: FM K = 20 $c$ 1.000 at 476.7 ms against Diffusion K = 2 $c$ 1.000 at 211.4 ms — false as stated; **true at the operating budgets** (17–26 ms against ≥ 191 ms) | ✅ **DO**: "every flow-based row at its operating budget is cheaper than every diffusion row at equal success with constraint satisfaction". 🟠 (a claim narrows) |
| 6D-2 | "run away from its trained budget, loses it" — each diffusion budget is its own checkpoint | `07:58–59` against `06:86–87` (v5.2, A12) | ✅ **DO**: "where the diffusion model trained and run at one denoising step loses it". 🟢 |
| 6D-3 | "the only model that moves the box in every context" — CI-MeanFM α = 0.05 also 0/10 unmoved at K = 20 | `07:20–21`, `06:620–621`; Table 6.5 row `06:659`: 0/10 | ✅ **DO**: delete "only" in both places ("and it moves the box in every context"). 🟢 |
| 6D-4 | "with the fewest violating steps" dangles after three crossing counts | `07:74–75`; Table 6.13: FM 23.0, MeanFM 18.5 (7/10 aborted), CI-MeanFM 23.3 | ✅ **DO**: delete the phrase. 🟢 |
| 6E | which episodes the Steps column averages | **Avoiding / UAV-pillars:** the evaluation records the control step at which the episode ended for every episode — success, termination or the limit (`FM_v3_meanflow_test/eval_flow_matching_v3_meanflow.py:745–748`) — and the DA averages all episodes of the cell (`DA_in_Paper/analysis/avoiding_rules_by_protocol.py:90–95`); so Steps is the episode length over all episodes. **Corridor:** steps to the finish line over the flights that reach it, `---` when none does (`plotting/extract/corridor_v3_frontier.py:15, 44–57`). The s-curve tables print no Steps column. | ✅ **DO**: one sentence in §5.4.1 ("Steps is the length of an episode in control steps, averaged over every episode of the cell whatever its outcome; a terminated or capped episode contributes the step at which it ended") and one clause in §5.4.3 / Table 6.11's caption ("over the flights that reach the end; --- when none does"). 🟠 (a specification is stated) |
| 6F-1 | dominance is described with both costs strictly better; Ch 7 glosses it as "ahead on every reported axis" although S&C ties; the 0.05 band | `06:331–332`; the figure computes with `frontier.py::_frontier`: a point is dominated when another has a strictly better outcome (fewer control steps) at no more time per action; `07:13–14` is **my v5.6 clause** (B-iii-3) | ✅ **DO**: the definition as computed ("no other configuration in the band is at least as fast per action and takes fewer control steps"); the Ch 7 gloss → "equal success with constraint satisfaction, fewer control steps and less time per action, the sense of §6.1.3"; one clause "dominance refers to the reported means on the evaluated configurations". 🟢 |
| 6F-2 | ⌈ηK⌉ − 1 gives −1 at η = 0 | `05:1153` against `04:1295` (the max form) | ✅ **DO**: "for $\eta>0$; the general form is \eqref{eq:method:degen:ngen}". 🟢 |
| 6F-3 | "None of them arrives legally" against S&C up to 0.133 | `06:83` | ✅ **DO**: "None of them arrives legally as a rule:". 🟢 |
| 6F-4 | "random aborts least and violates least of those" is ambiguous after the merge | `06:1712–1713` — **my v5.5 caption**; the old caption limited the comparison to the rules that abort least | ✅ **DO**: "among the rules with the fewest aborts, random selection has the fewest violating steps". 🟢 |
| 6G-1 | the raw-plan explanation says the image analogy twice and coaches the reader | `06:106–108` and `124–125` | 🟡 **optional cut**, ≈ 5 lines: drop the second analogy sentence and the repeated "not the executed path". 🟢 |
| 6G-2 | the controller closing paragraph and the Conclusion restate the same counts | `06:1765–1773` against `1777–1787` | 🟡 **optional cut**, ≈ 0.3 page: delete the paragraph, keep the Conclusion with 6B's fix. 🟠 |
| 6G tables | `1.000 ± 0.000` etc. for ten flights; the merged table's width; figure legibility | Tables 6.11 / 6.12 / B.4; `06:1717–1727`; `09:339` | ⛔ the `10/10` cells stay **your decision** (B-i-13: v3.83 against the Ch 5 rule; the reviewer sides with the rule); the table and the figures are the compile's to judge |
| D4 | "The endpoint solves also converge" overstates | `06:467` | ✅ **DO**: "Endpoint non-convergence was uncommon:". 🟢 |

### §1, §3, §5 — remarks that need no edit

- **"Declined" and "wrong" should not share a verdict.** Agreed; the round-one answer's C-iii already separates the two ("the reviewer is wrong on the facts" against "against a decision you took"), and the status columns record what was done, declined or held. No document is added.
- **Stopping D.** Agreed on the reviewer's terms. Each boundary it lists is now stated: the ten scored contexts include the guarded one (§5.5.2, the three captions, and 4.3 above); results concern the declared 0.31 m convention (§4.6.3, Fig. A.1); the margins are inherited or chosen and uncalibrated (§5.5, §4.5.2); conclusions concern the evaluated contexts and trained configurations (Ch 7, Ch 8); timing is controller-and-simulator on the unprojected pair (§6.3.3, Ch 7).
- **Length.** The round-one answer's own record already says ≈ 3 pages off the PDF and ≈ 5 out of the body, not 11–14. The release tool estimates 179 pages for v5.7; only a compile settles it.
- **The compiled PDF.** None is in the release folder because nothing is compiled here; the build is compiled on Overleaf by the author. The reviewer's positive source scan (631 reference targets, none undefined, none duplicated) agrees with `check.py`.

## 2. What the code settles

1. **The velocity reference (6A).** Three sources agree: the campaign drivers name the tracker in every run folder (`…_pid_stopgo_…`), the thesis's source notes call it the tracker of record with `v_des = 0`, and the evaluation sets `v_des = np.zeros(3)` for that tracker while the default `pid` would use `action / dt`. The controller adds `a_des = 0` and `yaw_des = 0` by default. So the executed loop is: accumulated position setpoint, zero velocity and acceleration feed-forward, zero yaw — Chapter 4's description. The Chapter 5 sentence of v5.6 contradicts it and is withdrawn in the next pass.
2. **The Steps population (6E).** Avoiding and UAV-pillars: every episode contributes its length, whatever its outcome. Corridor: only the flights that reach the end contribute their steps to it; a cell with no such flight prints `---`. The two conventions differ and neither is stated in the text; both will be.

## 3. The list for the author (job O022)

**R2-A · Fixes the tables, the code or the thesis's own definitions require — ✅ DONE in v5.8 (ROUND2 FIX), no number of record changed:**
6A (the controller sentence, mine) · 4.2a (the per-axis clause, mine) · 4.2b (the conditional guarantee in Ch 8) · 4.3 (the guard in three places) · 4.4a (mine) · 4.4b (mine) · 4.4c (six endpoint sentences) · 4.5 the two small clauses · 6B · 6C · 6D-1 (the claim narrowed to the operating budgets) · 6D-2 · 6D-3 · 6D-4 · 6E (two definitions) · 6F-1 (the definition, my gloss, one clause) · 6F-2 · 6F-3 · 6F-4 (mine) · D4's verb.

**R2-B · Yours — say the word, the wording is checked:** the abstract (4.1, three sentences) · the corridor wording "did not separate the objectives at the shared budgets" (4.5, four places) · the two optional cuts (6G-1, 6G-2).

**R2-C · Still your decision from before:** the `10/10` cells (B-i-13).

**R2-D · Production, not the Orchestra's:** compile the next build on Overleaf and inspect pp. 29–31, the moved floats, the merged Table in §6.3.3 and the appendix figures; remove the metadata `TODO`s and the Appendix B flag at submission.

Nothing applied, nothing compiled, nothing committed; no thesis file touched by this answer. Signed: Orchestra (Claude Fable 5.1, Claude Code), job O022 · 2026-09-25 21:57.
