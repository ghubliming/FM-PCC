# v4.1 — 2026-09-24 · The Conclusion is Chapter 7, the Discussion Chapter 8; Interpretation, Negative Results and Threats dropped; Isaac Sim named

**Author's instruction (2026-09-24, eight points):** (1) the 8.4 hole is Isaac Sim; (2) Ch 7 is too long
and duplicates Ch 6, §7.1 Interpretation is not needed; (3) §7.2 Negative and Inconclusive Results and
§7.3 Threats to Validity came from the template bone, are not a must, "suicide" — chop them; (4) §7.4
Limitations is good; (5) §8.3 Towards Deployment and §8.4 Future Work go into the Discussion, the
Conclusion keeps 8.1/8.2 only; (6) the Conclusion becomes Chapter 7 and the adjusted Discussion Chapter
8; (7) add what is needed, keep it short; (8) archive the deleted sections.

## 1 · What changed

| file | change |
| :-- | :-- |
| `chapters/07_conclusion.tex` | **new name and position**: `\chapter{Conclusion}` with §7.1 Summary and §7.2 Answers to the Research Questions — the v4.0 text unchanged (`ch:conclusion`, `sec:conc:summary`, `sec:conc:rqs` kept). 101 lines. |
| `chapters/08_discussion.tex` | **new name and position**: `\chapter{Discussion}` with a three-line lead and §8.1 Limitations (v4.0 §7.4, plus one scope sentence on the seeds: one training seed on D3IL-aligning, UAV-corridor, UAV-s-curve; five on D3IL-avoiding and UAV-pillars), §8.2 Towards Deployment (v4.0 §8.3, label `sec:disc:practice`, **plus one paragraph on the candidate machinery** — the -r/-t/-c discussion the author's notes asked for, compressed from v4.0 §7.1.4: one candidate → identical rules; four → projection ×3.2–4.9; single-candidate endpoint 291 vs 422–425 ms; the rule by model; the parallel-solve prediction), §8.3 Future Work (v4.0 §8.4, label `sec:disc:future`; **the simulator is NVIDIA Isaac Sim**, the hole is gone). 145 lines. |
| `chapters/07_discussion.tex`, `chapters/08_conclusion.tex` | removed from the tree; the v4.0 files are verbatim in `withheld/20260924_v4.1_archive/` with a README. **Dropped for good:** §7.1 Interpretation (six subsections), §7.2 Negative and Inconclusive Results, §7.3 Threats to Validity. |
| `chapters/09_appendix.tex` | the Reproducibility `\srcnote` pointed at the dropped `sec:disc:threats`; it now points at `sec:disc:practice` (the paragraph on what the time per action contains). |
| `thesis_v4.tex` | input order `07_conclusion`, `08_discussion`; header comment. |
| `tools/sync_v3.py` | policy **`watch`** for v3's frozen `07_discussion.tex` / `08_conclusion.tex` (drift in v3 is reported, nothing merged, no v4 file); the renamed v4 files are `own`. |
| `inherited/MANIFEST.md`, `README.md`, `CHANGELOG.md`, `notes/OPEN_…`, `../DRAFT_OWNERSHIP.md` | the new chapter map. |

Labels gone with the dropped sections: `sec:disc:interpretation`, `sec:disc:negative`, `sec:disc:threats`
and the v4.0 subsection labels. **Checked: no file of Chapters 1–6, the parts or the appendix references
them** (the v3.98 handover's remark that Chapter 4 references two of them does not hold at v2.27).

## 2 · What the endings now say, in five pages

- **Ch 7 Conclusion** (~2.5 pages): the three steps and the general conclusion; RQ1–3 with counts.
- **Ch 8 Discussion** (~2.5 pages): what the framework does not do (positions not actuator commands; a
  constraint is what is declared; endpoint projection needs a velocity field; plan quality read through
  violating steps and drawings; scope of the transfer, seeds); what a deployment adds (constraints as a
  runtime input, the budget as a deployment choice, positions any controller follows; the two bounds;
  planner time vs execution time, no real-time claim; the candidate machinery it can drop); future work
  (Isaac Sim demonstrations, the controller, real-time execution, constraints from perception,
  re-tasking, the terminal cost, the FM sampler prior, seeds).

Nothing was added beyond the seeds sentence and the candidate paragraph (author, point 7: "if you think
now is fine, also good").

## 3 · Checks and bundle

`tools/check.py`: 20 files, 7969 lines, 285 labels, 53 of 53 citations, 41 figures; hole **1** (the web
link), provisional 1, guard 20, srcnote 78, dataref 59, flawed 2, outdated 0, longdata 4; **all
mechanical checks pass**. `sync_v3.py status`: in sync with v3.98a / v2.27, the two frozen v3 files
watched. Bundles `thesis_v4_<stamp>_new` (v2 collapsed) and `_full_clean` rebuilt, both `--verify`
byte-faithful. **Not compiled.** Nothing committed.

## 4 · Cross-draft

- **→ v2:** Ch 1's outline sentence (`01_introduction.tex:156–157`) now prints "Chapter 8 interprets the
  findings and their limits, and Chapter 7 concludes with the research questions and future work" —
  order and content need v2's rewording (suggested text in the note). INBOX row.
- **→ v3:** the v4.0 note's pointer target for the Ch 5 `app:repro` sentence is now `sec:disc:practice`
  (was `sec:disc:threats`); note and INBOX row amended.

## 5 · Same changelog, second round (author's points 9–11): the appendix

| point | change |
| :-- | :-- |
| 9 — "A. Derivations" is a misnomer | Appendix A is **Supplementary Material** (`app:derivations` and new `app:supplementary` on the chapter). §A.1 Training-Time Sampling Laws is unchanged (author's keep, v3.58; label `app:training-time-laws`, still "Section A.1"). |
| 10 — the quadrotor dimensions are not reproducibility | The X2 figure (`fig:platform-x2-dimensions`, caption unchanged) is **§A.2 Quadrotor Dimensions**, with one sentence of text. Appendix C is the **Compute Environment** alone and titled so (labels `app:repro` kept, `app:compute` added; the starred section is gone). **Index changes:** Figure C.1 → Figure A.1; Table C.1 stays; Chapter 5 says the figure is "in `app:repro`" three times (05_setup.tex:54, :79, :87) — v3 notified, nothing of v3's edited. |
| 11 — Appendix B keeps five sections | Kept, in this order: B.1 D3IL-avoiding executed paths, B.2 D3IL-aligning executed paths, B.3 UAV-corridor flights along the corridor, B.4 UAV-corridor under every selection rule (**the `\longdata` marker and the `\ifappendixfull` switch stay**), B.5 D3IL-avoiding at twenty episodes per seed (input as is; alias `app:avoiding-twenty-episode`). **Dropped and archived** (`withheld/20260924_v4.1_archive/appendix_B_dropped_sections.tex` + `app_long/`): D3IL-avoiding at every evaluated budget, the single-candidate endpoint ladder, UAV-s-curve under every selection rule, UAV-s-curve at the budgets on disk. The chapter's lead paragraph lists the five. |

**Dangling references bridged, v3 notified.** Chapter 6 (v3-owned) still references four labels of the
dropped sections: `tab:app:avoiding-raw-full` (06_results.tex:107, :144), `tab:app:avoiding-dpcc-full`
(:335, :367), `app:avoiding-budgets` (:760), `app:uav-scurve-rules` (:2142, :2159, :2172),
`app:uav-scurve-budgets` (:2058). They are kept as **alias labels on the Extended Results chapter
heading** (a commented block) so that every `\autoref` resolves and the build stays clean; the sentences
now print "Appendix B" for tables that are no longer there, so v3 rewrites or drops them
(`cross_draft/to_v3/FROM_v4_20260924_v4.1_appendix_index_changes.md`), and the alias block goes.

**Checks after this round:** `tools/check.py` 17 files, 7634 lines, 282 labels, 53 of 53 citations, 41
figures; hole 1, provisional 1, guard 19, srcnote 78, dataref 53, flawed 2, outdated 0, **longdata 1**;
all pass, in both switch states. Bundles `thesis_v4_20260924_212229_new` / `_full_clean` rebuilt and
byte-faithful. Not compiled. The v4.1a appendix (before this round) is
`withheld/20260924_v4.1_archive/09_appendix_v4.1a_before_B_trim.tex`.

## 6 · Point 12, abbreviations: no change (author)

The Abbreviations page of the **working bundle** (`_new`, Ch 1–4 collapsed) shows DPCC alone because the
`acronym` package prints only entries marked used by `\ac{}` (`printonlyused`) and ten of the eleven
`\ac{}` calls are in the collapsed chapters. The **full build** prints all eleven (DPCC, NFE, ODE, JVP,
MPC, MJPC, IK, FiLM, SLSQP, NLP, UAV) by itself. Author: a bundle-building artefact, no aggregation
needed; nothing was changed. FYI only: abbreviations Ch 5–9 use bare after defining them (S\&C, FM,
MeanFM, CI-MeanFM; PD, RGB, GPU, CPU) are never `\ac{}`-ed and so never reach the list in any build —
`notes/AUDIT_20260924_abbreviations_v2_v3.md`, v2/v3's call.

**Signed:** Claude (Fable 5.1, Claude Code), v4 · 2026-09-24 · not compiled.
