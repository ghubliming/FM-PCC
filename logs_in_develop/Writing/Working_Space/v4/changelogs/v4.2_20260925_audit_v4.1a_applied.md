# v4.2 — 2026-09-25 · The ChatGPT audit of v4.1a applied (§15 of the audit MD) on v3.100; the author's two notes (the long-data / link flag in the Extended Results heading; the planning-horizon item); figures re-exported; bundles and release rebuilt

**Asked (author, 2026-09-25):** "Ship the v4 update with changelog", with two notes — (1) write the LONG DATA
and the web link / repository code link into the Appendix heading *B. Extended Results* as a flag, so it shows
in the release build, keeping every existing flag and warning ("just an adding job, into the heading, no more");
(2) mention in the Discussion that a long horizon would, in theory, cost more compute and give better foresight
in receding-horizon control, as a potential next research direction, if not already written and if it makes
sense. "Go. update v4."

## Absorbed

- **v3.100** (`tools/sync_v3.py merge`, fast-forward: `05_setup.tex`, `06_results.tex`); the stamp is v3.100,
  which carries **v2.27** (v3 has not merged v2.28). What v3.100 changed is
  `v3/changelogs/v3.100_20260925_audit_v3.98_applied.md`: 76 passages — violations scored on the nominal set,
  the latched finish-line crossing, the alignment percentage as the reduction of the median relative to the mean
  initial distance, CI-MeanFM Pareto-dominates and MeanFM trades one episode in thirty, "the same planner
  configuration", the halfspace in the demonstration check, and the K = 2 bit-identical run named as FM.
- **Figures:** `python3.14 Data_Analysis/DA_in_Paper/plotting/export_to_draft.py v4` copied six files
  (`fig_aligning_tradeoff`, `fig_aligning_projected_tradeoff`, `fig_expert_aligning`; svg and png), 55 current.
  Its line "MISSING from the store: fig_hardflow_endpoint_generation" concerns v2's figure
  (`04_method.tex:1316`), which is in `v4/figures/` as v3 copied it from v2 (v3 CHANGELOG, the v2.26 sync);
  not a v4 matter.

## The change list applied — audit MD §15, in full

Every "from" text was verified verbatim against the live files before the pass; the 33 replacements were made by
script (`apply_v42.py`, scratch), each "from" asserted to occur exactly once. The auditor's §13 refinements are
inside every item; three corrections of my own §11.3 are recorded in §15.7 of the audit MD.

**Ch 7 `chapters/07_conclusion.tex` (C1–C8)** — C1 CI-MeanFM Pareto-dominates the diffusion model of DPCC,
MeanFM matches the saving at one episode in thirty (v3.100 B7/B30). C2 the aligning sentence scoped to the
unprojected comparison at twenty steps: the only model that moves the box in every context, 84 % of the mean
initial distance ($0.0741$ against $0.4530$ m), the medians' trend (MeanFM falls at every budget, CI-MeanFM
non-monotonic to twenty, substantially better at a hundred, still behind), no context within the 1.8 cm test
under the selected endpoint configuration or unprojected. C3 "twenty evaluations" → $\nfe = 20$. C4 the s-curve
sentence: the same planner configuration in closed loop; a twenty-fourth of the controller's cost per control
step, a tenth of the whole loop on the unprojected pair. C5 the storyline sentence: corridor = cannot be told
apart where the flights traverse it at a jointly tested budget; s-curve = instantaneous-velocity matching leads.
C6 RQ1 rewritten: CI-MeanFM and FM named for the one-evaluation result; the AV-first order scoped to the tasks
with human demonstrations; CI-MeanFM ahead by one episode on D3IL-avoiding and on UAV-pillars, behind on
D3IL-aligning; the universal "leads or holds in every environment" dropped; s-curve FM 9/10, CI-MeanFM 6/10,
MeanFM 3/10 with the fewest violating steps. C7 RQ2 rewritten: the guiding-step count by reference to
`sec:method:degenerate`, the $2.45\times$ claim gone (v3.100 B6), the bit-identical run named as FM,
"comparable planning time" ($275$ against $266$ ms), $\nfe$ throughout, the K = 3 avoiding result with its
per-step counterparts ($0.917$, $0.958$, about half the time). C8 RQ3: the tie named for the corridor and its
traversing flights. C9 (Summary trim): no further cut — the auditor called it editorial; the author's call.

**Ch 8 `chapters/08_discussion.tex` (D1–D11)** — D1 opening without the section list. D2 the altitude
sentence: the demonstrations of the other two scenes lie in 0.90–1.30 m, the projected hump flights climb above
that range. D3 the budget sentence (the selected budget per task; the diffusion model of DPCC with the inherited
sampler needs one checkpoint per budget). D4 the lead (0.49–0.61 m at the first violating step of the projected
flow-based corridor flights; about 0.3 m on the s-curve) and the cross-track (about a centimetre on average,
unprojected flow-based s-curve flights, cascaded controller). D5 the controller costs: 5 and 125 ms are the
controller and the simulator step together, unprojected pair, two execution environments. D6 the candidate
paragraph rewritten: one candidate is a tested option, named as FM, endpoint, $\nfe = 20$, over the hump
(291 against 422–425 ms, 0.2 against 0.4–0.7 violating steps); rule identity at one candidate is a fact about the
rules; the 18.1 → about 11.7 ms estimate named as the candidate study's record for analytic average-velocity
matching, "an idealised prediction from two measured points" (no generation/projection split is printed in
Ch 6, so none is quoted). D7 human demonstrations with varied routes as a collection design to test (Isaac Sim
stays). D8 "The controller and the commanded path": corridor lead vs s-curve corner cut, each with its test.
D9 the alignment target as a test of a task-aware terminal cost that keeps the reported constraint outcomes.
D10 the sampler item: "would test the one sampling difference identified". D11 seeds: D3IL-aligning,
UAV-corridor and UAV-s-curve.

**Appendix `chapters/09_appendix.tex`, `chapters/app_long/uav_corridor_rules.tex` (E1–E7)** — E1 the
0.31 m rotor reach inflates the constraint surfaces of UAV-corridor and UAV-s-curve; UAV-pillars carries the
extent through the scale of the scene (prose and caption). E2 the B.1 guard draws one of the thirty episodes
(episode 2 of each cell). E3 B.2: context 7 is the context the tightened evaluation never attempts (the
tightening overlaps the box, the evaluation's box–obstacle guard holds the box, as Ch 6's guard says); the
violations counted on the nominal set, the dashed boundary being the projector's margin (prose and caption).
E4 the B.2 guard split into a `\guard` (why the end effector is drawn) and a `\dataref` (the re-run that
drawing the box would need). E5 B.4: the summary on cells whose flights all traverse the corridor (2.1
violating steps and 21.4 control steps at most, recomputed from the four tables), the K = 1 / K = 2 hump rules
per rule (temporal consistency 1/10, 2/10, 0/10 at K = 2; the zero violating steps of the other rules are flights
that stall short of the apex until the step cap), the baseline's cumulative-cost rule traversing on nine flights
of ten; the rules file's intro: the crossing is latched and the controlled-flight checks are taken over the whole
flight (v3.100 A8) — the only edit inside that data record, noted in its header comment. E6 C: the two MuJoCo MPC
cells ran in a second environment (MuJoCo 3.x with MJX, the controller's predictive simulations on the GPU);
caption and MuJoCo row say so; no exact minor version, no new `\hole` (auditor §13.6). E7 after the twenty-episode
`\input`, before the Compute Environment chapter: a `\guard` that the reported agreement concerns aggregate
outcomes and one seed–geometry cell differs, with the seed-7 / top-right-hard `\dataref` (D01; the chapter keeps
29/30; the curated file itself untouched).

**Maintenance (M1–M3)** — README banner "Ch 7 Conclusion, Ch 8 Discussion"; `notes/OPEN_…` item 12 (the endings
≈ 5 pages since v4.1); this changelog, the CHANGELOG entry, the INBOX rows and the note to v3 (below).

## The author's two notes

1. **Heading flag.** `\chapter{Extended Results (long data: web link and repository code link)}` — no short
   TOC title, so the flag shows in the table of contents, at the heading and in the release build. The
   `\hole` (web link, author's URL), the `\longdata` banner on B.4 and every `\guard` stay as they were.
2. **The planning horizon** — a Future Work item after *Real-time execution*, not previously written (no
   "horizon" in Ch 7/8 before this pass). Facts it restates: every plan is eight steps long
   (`04_method.tex:94`, `:978`), one step is executed before the next replan (`05_setup.tex:1241`,
   `sec:setup:protocol:eval`), a wall the horizon does not yet reach enters at the replan that reaches it
   (`04_method.tex:1571`), the projection program carries one block of variables and one inequality per obstacle
   per horizon step (`04_method.tex:1089`, `:1109`), and the temporal U-Net convolves along the horizon
   (`:975–978`). The item states the trade — earlier constraints and a farther endpoint against a longer network
   sequence and a larger program — and ends "untested"; the Future Work `\srcnote` names the sources.

## Checked

- `tools/check.py`: 17 files, 7788 lines, 276 labels, 53 of 53 citations, 41 figures; **all mechanical checks
  pass** (hole 1, provisional 1, guard 19, srcnote 78, dataref 60, flawed 2, longdata 1). **Not compiled.**
- Bundles `bundle/output/thesis_v4_20260925_110246_new` (annotated, Ch 1–4 collapsed at v2.27; 13 files,
  5623 → 5792 lines) and `thesis_v4_20260925_110246_full_clean` (17 files, 7805 → 7913 lines); headers read
  v3.100 / v2.27; both `--verify` byte-faithful; the six re-exported figures travel in the zips.
- Release `RELEASE/output/20260925_110504_thesis_release_v2.28_v3.100_v4.2` (+ zip, 7829 KB), built from the
  live files (v2 v2.28, v3 v3.100, v4 v4.2): 20 text + 48 binary files, ~163 pages estimated (138–195), 10 holes
  (the template metadata TODOs, the v3 provisional, the web-link `\hole`, the `\longdata` banner, the empty
  acknowledgments page, raster-only figures), 1 bug (v2's `\getDoctype`, as in the previous release), 4 residue
  hits (the template's TODO metadata lines) — none from v4.2. The heading flag, the planning-horizon item and the
  B.5 guard are in the release's Ch 8/appendix. A compiled PDF dated 10:56 today sits in `RELEASE/output/`
  (the author's, from the previous release); `make_release.py --attach-pdf` records a page count if wanted.

## Cross-draft

- INBOX: the v3.100 row addressed to v4 closed (✅ v4.2); the v2.28 row stays ⏳ until v3 merges it.
- → v3 (FYI, no change requested): `cross_draft/to_v3/FROM_v4_20260925_v4.2_audit_applied_two_ch6_fyi.md` —
  the v3.100 note is closed; "evaluations" as the endpoint sampler's budget in Ch 6 is an unresolved K/NFE
  terminology follow-up (audit F02, §12.3); §6.1.2.2's 18.1 → 11.7 ms is analytic average-velocity matching's
  candidate-study record (D03).
- → v2: nothing.

## Open

- **D01** (MeanFM seed 7 / top-right-hard, 1/2 against 20/20): a data-provenance watch item, the author's cluster
  download; the guard after B.5 states the aggregate scope; the chapter keeps 29/30.
- **v2.28** reaches v4 only after v3's `sync_v2.py merge`; then `sync_v3.py merge` and rebuild.
- The scope of the ordering sentences (the tasks with human demonstrations) was put to the author in the review
  message and taken as confirmed by the go.
- The release's one recorded BUG is v2's `\getDoctype` metadata (the release sets it to "Master's Thesis"
  itself); v2's to fix for the template build.

Signed: Claude (Fable 5.1, Claude Code), v4 · 2026-09-25 · figures exported with local `python3.14`, no cluster
job · **not compiled** · nothing committed · nothing under `v2/` or `v3/` touched.
