% =============================================================================
%  DATA RECORD -- input by chapters/09_appendix.tex inside \ifappendixfull ... \fi
%  Extracted byte-for-byte from the v3.98 handover (FROM_v3_v3.98_20260924_201920/
%  chapters/09_appendix.tex, section "Endpoint projection across the step budget on D3IL-avoiding"); nothing inside was edited at v4.0.
%  The section heading, its label and its reading stay in 09_appendix.tex.
% =============================================================================
The single-candidate ladder of endpoint projection, archived from \autoref{sec:res:avoiding:projectors} at v3.66 (\texttt{withheld/20260922\_v3.66\_archive}), reduced this evaluation to how many of the three selection rules of per-step
projection endpoint projection is ahead of. The numbers behind those counts are below. Every entry is
analytic average-velocity matching at activation threshold $0.5$, on \textbf{five training seeds (6--10)
$\times$ three geometries $\times$ two episodes} --- 15 seed--geometry cells and 30 episodes --- with
mean $\pm$ the sample standard deviation over the seeds.

\begin{table}[htpb]
  \caption[Endpoint against per-step projection, per budget]{Endpoint projection against the three
  selection rules of per-step projection on D3IL-avoiding, per step budget. Endpoint projection is shown
  under temporal consistency, the rule it is operated with. \textbf{The time per control step is not
  printed here and the archived counts did not use it}: in this evaluation endpoint
  projection was given one candidate plan and per-step projection four, so the two are not priced on the
  same work. The matched comparison, with four candidates on both sides, is
  \autoref{tab:avoiding-projectors}.}
  \label{tab:app:hf-ladder-detail}
  \centering
  \footnotesize
  \begin{tabularx}{\linewidth}{@{}r L rr@{}}
    \toprule
    $\nfe$ & Projection and rule & S\&C & Steps \\
    \midrule
    1  & per-step, $r$        & $0.900 \pm 0.149$ & $64.3 \pm 2.6$ \\
       & per-step, $c$        & $0.933 \pm 0.091$ & $72.4 \pm 8.4$ \\
       & per-step, $t$        & $0.967 \pm 0.075$ & $58.6 \pm 2.2$ \\
       & endpoint, $t$        & $1.000 \pm 0.000$ & $63.8 \pm 3.3$ \\
    \midrule
    2  & per-step, $r$        & $0.967 \pm 0.075$ & $65.1 \pm 2.6$ \\
       & per-step, $c$        & $0.933 \pm 0.091$ & $97.2 \pm 4.8$ \\
       & per-step, $t$        & $0.967 \pm 0.075$ & $59.4 \pm 2.0$ \\
       & endpoint, $t$        & $0.933 \pm 0.091$ & $64.8 \pm 1.3$ \\
    \midrule
    5  & per-step, $r$        & $0.867 \pm 0.139$ & $69.2 \pm 4.8$ \\
       & per-step, $c$        & $0.833 \pm 0.167$ & $61.5 \pm 3.6$ \\
       & per-step, $t$        & $0.933 \pm 0.149$ & $60.6 \pm 2.4$ \\
       & endpoint, $t$        & $0.933 \pm 0.149$ & $66.6 \pm 1.5$ \\
    \midrule
    10 & per-step, $r$        & $0.833 \pm 0.236$ & $73.9 \pm 2.9$ \\
       & per-step, $c$        & $0.833 \pm 0.118$ & $59.4 \pm 1.6$ \\
       & per-step, $t$        & $0.933 \pm 0.149$ & $63.6 \pm 1.9$ \\
       & endpoint, $t$        & $0.833 \pm 0.167$ & $66.4 \pm 2.7$ \\
    \bottomrule
  \end{tabularx}
\end{table}

\dataref{temp/1108/Revised\_2/batch\_avoiding\_combined\_20260811\_221322, analytic average-velocity
matching on the temporal U-Net (\texttt{H8\_K*\_Meuler\_T0.5\_A0.5\_B1\_\ldots MeanFlowODE}, \texttt{bbunet});
per-step rows \texttt{dpcc-\{r,c,t\}}, endpoint rows \texttt{hardflow\_new-t}; seed-mean over the
geometries, then mean and sample standard deviation over the five seeds, the convention of
\autoref{sec:setup:metrics:spread}. Recomputed 2026-09-21; the per-step cells reproduce the
corresponding rows of \autoref{tab:avoiding-dpcc-protocol} exactly.}
