# ⚠ DEPRECATED — fix_2_Config (2026-06-28)
#
# This file has been split into two files that match the avoiding-d3il.py pattern:
#
#   config/uav_projection.yaml          ← projection-only params (variants, constraints, geometry)
#   config/uav.py plan_flow_matching_v3_uav block  ← eval control params (batch_size, control_hz,
#                                                     behavior_log, reanchor_alpha, lead_gain, etc.)
#
# eval_fm_uav.py no longer reads this file. It calls load_pcc_config(scene, seed) which reads
# from both new sources above.
#
# This stub is kept so that any Slurm job that references it explicitly gets a clear error
# rather than a silent wrong-path. Safe to delete once all cluster jobs have been updated.
