# Briefing: FM Train & Eval Scripts Creation

## Overview
This briefing documents the creation of Flow Matching (FM) training and evaluation scripts that mirror the original diffusion-based scripts, but use the FM implementation in `flow_matcher`.

## What Was Done
- **Created FM_test/train_FM.py**: A full-featured training script for FM, adapted from `scripts/train.py`.
  - All `diffuser` imports and logic replaced with `flow_matcher` equivalents.
  - Uses `flow_matcher`'s `GaussianDiffusion`, `Trainer`, and config utilities.
  - Supports the same CLI arguments, seed management, and W&B logging as the original.
- **Created FM_test/eval_FM.py**: An evaluation script for FM, adapted from `scripts/eval.py`.
  - All `diffuser` imports and logic replaced with `flow_matcher` equivalents.
  - Uses FM models, policies, and projectors for evaluation and visualization.
  - Maintains compatibility with the original evaluation workflow and config files.

## Usage
- To train an FM model: `python FM_test/train_FM.py [args]`
- To evaluate an FM model: `python FM_test/eval_FM.py [args]`
- Both scripts are drop-in replacements for their diffusion counterparts, but operate on FM logic.

## Rationale
- Ensures a one-to-one mapping between diffusion and FM workflows for easy benchmarking and comparison.
- Keeps the original codebase untouched; all FM work is isolated in new scripts and the `flow_matcher` package.
- Enables rapid testing, training, and evaluation of FM models with minimal changes to user workflow.

## Next Steps
- Update documentation and experiment configs as needed.
- Use these scripts for all FM-based experiments and benchmarking.

---
*Generated by GitHub Copilot, March 2026.*